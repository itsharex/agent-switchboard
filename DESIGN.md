# Agent Switchboard 设计系统

**版本：** 0.1
**状态：** 实施前已批准的视觉基线
**方向：** `Frosted Relay` — 面向双客户端配置控制台的 Apple 式层级与克制的压纹实体表面。

## 1. 产品与视觉主张

**主题：** 一个安全管理 `Codex` 与 `Claude Code` 提供商配置的本地桌面应用（`Windows` / `macOS` / `Linux`）。

**受众：** 一位需要了解当前启用内容、即将变更内容及撤销方式的技术型用户。

**唯一任务：** 让当前启用配置和切换后果能够立即被理解。

这不是营销页面，也不是通用 `SaaS` 仪表盘。主视觉对象不应是横幅图，而应是真实路由状态：当前客户端及其启用的供应商。主窗口切换入口在供应商页的变更预览，托盘提供显式快捷切换；二者共用同一执行器，当前连接双卡只展示状态与诊断入口。

## 2. 文字是信息，而非装饰

界面文字必须配得上所占空间。产品管理的是配置，因此空白区域往往比一叠用来强调区域重要性的标签更合适。

- 每个工作区域最多只有一个任务标题。仅当辅助行会改变用户下一步决策，或解释不明显的风险时才添加。
- 不得只为让面板显得经过设计而添加眉题、“功能”副标题、营销短语或配置主题填充文案。
- 徽标或标签只可说明真实的当前属性：启用状态、校验结果、警告、筛选条件或客户端身份。它不是视觉分隔符，也不能代替间距。
- 不得在标题下放置成组的状态类标签。唯一有意义的状态应紧邻它修饰的控件或值。
- 字段标签、校验信息和配置路径注释仍是必要的功能性文字。保持简短，并紧邻其说明对象。
- 采用删除测试：移除一处文字后，若设置、操作、状态、警告或决策并未变得更难理解，就删除它。

当前连接双卡与配置内容提供层级。其余视觉工作必须由字体、留白、细分隔线和材质深度完成。

## 3. 参考解读

`本项目` 提供了有价值的实现纪律：

- 有文档记录的设计系统；
- 一个令牌所有者，而非组件局部的视觉常量；
- 中文界面文字使用 `MiSans`，代码使用专用字体；
- 按层级使用实体表面、细边和内高光；
- 低阴影、高可读性的配置表面。

`Agent Switchboard` **不**继承 `本项目` 的蓝粉品牌渐变、面向儿童的图像、大型展示区或全局装饰性粒子效果。本产品处理配置和凭据，因此平静与可审计性比表现力强的颜色更重要。

### 界面来源

`Frosted Relay` 的布局、令牌、组件、交互设计、图标与界面文案均由本文件为 `Agent Switchboard` 创建。

## 4. 当前连接与状态归属

供应商是默认工作区。标题与快捷操作下方、客户端选择器上方并排展示 Codex / Claude 两张当前连接卡，使用实体内容面、客户端身份细边与清晰的文字层级。卡片同时展示两端供应商、用户级模型、服务地址主机名与接入方式；事实来自配置状态，不从列表选择或编辑草稿推测，也不把语法正确等同于配置已生效。读取失败、配置缺失、语法错误、外部修改、档案待应用、范围警告及写入锁各自显示真实原因。

- 双卡只在供应商列表显示；编辑、运行参数、用量查询、导入及其他工作区卸载双卡和动画，返回列表重新读取已有状态。下方客户端选择器只筛选档案，不影响双卡。配置异常在所属卡片内的实体信息面展示，并保留诊断入口；官方 Codex 连接提供额度详情入口。删除原单客户端摘要条，不保留重复展示。
- 双卡始终使用安静实体面；可读连接以 Codex 行动蓝或 Claude 紫色细边和对应客户端文字标识，读取失败或配置不可用时保持中性边框。卡片不绘制粒子、光晕或渐变，避免装饰层遮挡配置事实。
- 「配置与环境」或「查看诊断」直达设置中的配置诊断；使用本机网关的档案另有「网关诊断」，Codex 官方连接另有「官方额度详情」。所有切换仍从供应商预览经确认进入执行器。
- 「设置 → 诊断 → 配置与环境」呈现两端配置文件、匹配状态、上次切换、范围警告与写入锁；遗留锁的恢复入口紧邻所属客户端。其后的运行环境只读取应用版本、构建模式、平台、传输响应和应用数据目录，长路径可换行。桌面协议与浏览器开发桥接明确区分。
- 「用量 → 额度与重置」首先展示本机官方登录的账号限额与重置检测，再展示 Codex Runway 的非官方公开信号。两者各自标注来源，打开仅加载已有缓存；用户显式刷新才请求远端，失败保留最近成功数据。
- 官方重置检测只比较连续成功读数中的七天窗口，用量下降或声明重置时间改变才记录事后重置；账号变化清空旧基线，不将公开消息当作账号额度。基线继续由 `state/codex-quota-baseline.json` 持有，供应商卡片的定时查询与该账号级手动刷新相互独立。
- 公开信号合并 feed 的 `events`、`fulfilledSchedules` 与 `manualCompletions`，区分全局重置、重置卡发放和其他完成信号，保留公开来源链接与缓存时间。它不代表当前账号额度，也不声称提供完整推文时间线。

## 5. 令牌计划

运行时令牌值最终将集中在一份样式表中，拟定为 `src/styles/tokens.css`。组件只能消费语义令牌。

### 核心色板

| 令牌 | 名称 | 值 | 作用 |
| --- | --- | ---: | --- |
| `--asb-ice` | 极光冰白 | `#F6F7FB` | 浅色画布与高光 |
| `--asb-ink` | 板岩墨黑 | `#14161B` | 主文字与高对比度内容 |
| `--asb-current` | 深流蓝 | `#071725` | 深色画布与环境背景 |
| `--asb-action` | 行动蓝 | `#0A84FF` | 按钮、焦点与 `Codex` 身份 |
| `--asb-claude` | `Claude` 紫 | `#8B7CFF` | `Claude` 身份与非操作性标签 |
| `--asb-safe` | 安全绿 | `#30D158` | 已验证与已完成状态 |

警告与错误仅使用 Apple 式系统语义色。它们不得成为第二套品牌色板。

### 字体

| 角色 | 字体栈 | 用途 |
| --- | --- | --- |
| 展示文字 | `var(--asb-font-user)`, `Segoe UI Variable Display`, `MiSans`, `system-ui` | 页面标题与当前提供商 |
| 界面文字 | `var(--asb-font-user)`, `Segoe UI Variable`, `MiSans`, `system-ui` | 标签、表单与导航 |
| 代码 | `Maple Mono`, `Cascadia Mono`, `ui-monospace` | `TOML`、`JSON`、路径和配置差异 |

- 界面默认字体为 `Noto Sans SC`（2026-08-31 用户指令）：`OFL` 许可，经 `@fontsource/noto-sans-sc` 以 400 / 600 / 700 三个字重随应用打包（`main.tsx` 引入，子集按 `unicode-range` 懒加载），任何机器上都可真实渲染，不依赖系统安装。字体名到 `CSS` 值的引号转换唯一所有者是 `src/lib/font-family.ts`。
- `--asb-font-user` 的运行时值由设置「界面字体」覆写（所有者 `useAppSettings`，与 `data-theme` 同一应用点）；代码与配置预览不随界面字体变化，保持等宽栈。
- 未作出单独的许可证决定前，不得打包 `SF Pro`。
- 标题使用 `600` 字重，字距紧凑但易读。
- **字阶是封闭集合，五档封顶**（2026-09-11 用户指令「排版规则」）。层级只由「档位 + 字重 + 弱化色」表达；新增第六种尺寸是设计变更，不是组件决定。每档都是 `font` 简写，消费方无法从容器继承到意外的字号或行高。

  | 档 | 令牌 | 值 | 唯一用途 |
  | --- | --- | --- | --- |
  | 页标题 | `--asb-text-title` | `600 18px/1.25` | 每页唯一（`.asb-panel-title`）与弹窗标题（`.asb-dialog-title`） |
  | 小节 | `--asb-text-section` | `600 15px/1.4` | 模块 / 分组标题（`.asb-section-title`） |
  | 正文 | `--asb-text-body` | `400 15px/1.55` | 说明与值 |
  | 标签 | `--asb-text-label` | `400 13px/1.4` | 字段标签、行元数据、行内注记 |
  | 注脚 | `--asb-text-caption` | `400 12px/1.4` | 计数、时间注、来源注 |
  | 代码 | `--asb-text-code` | `400 13px/1.6` | `TOML` / `JSON` / 路径——它是字族，不是新的一档 |

- 展示数值唯一令牌是 `--asb-text-signal`（`700 72px`）：它**不是字阶**，只用于单个大数字（用量占比、重置回答），永不用于文案、标题或标签。
- 标题只有三个类，各自对应字阶的上三档，全部由 `styles/base/panels.css` 唯一持有：`.asb-panel-title`（页标题 / 弹窗标题）、`.asb-section-title`（模块与分组标题）、`.asb-group-title`（卡片内的字段簇标题，取标签档 `600`）。**不得再造标题类，也不得以元素选择器局部设权**（`.asb-card h4` 一律禁止）。
- `root.css` 全局重置 `h1`–`h6` 的 `margin` 与 `font`（`font: inherit`）：未被任何类认领的标题绝不回落成浏览器默认的 `1.17em` 粗体，从而悄悄压过字阶。落到元素选择器或没有类的标题都会让层级静默失效，因此由守卫测试强制「每个标题都带类」。
- 一切可纵向比较的数字带 `tabular-nums`。唯一车架是 `.asb-num`（属性值来自令牌 `--asb-num-font-variant`）：新写或改写标记时一律加类，不再逐处重述该属性；只有当选择器在结构上无法挂类时才允许在组件规则里直接声明——现存例外仅有 `.asb-chart-tooltip` 的 `nth-child` 组合选择器与托盘窗口的 `.tray-detail` / `.tray-time`。
- 正文使用 `15–16px` 与 `400` 字重。
- 配置数据使用等宽数字与代码字体。
- 展示字体必须克制：工作界面中不得出现超大的营销式标题。

### 形状与间距

| 令牌 | 值 | 用途 |
| --- | ---: | --- |
| `--asb-radius-window` | `20px` | 主外壳与大型面板 |
| `--asb-radius-panel` | `16px` | 主面板 |
| `--asb-radius-control` | `10px` | 输入框与辅助控件 |
| `--asb-radius-action` | `8px` | 主操作和破坏性操作按钮 |
| `--asb-radius-check` | `5px` | 复选框等小型勾选控件 |
| `--asb-space-unit` | `4px` | 原子间距单位 |

结构间距使用 `8`、`12`、`16`、`24`、`32` 和 `48px`。操作控件不自动采用药丸形；完整药丸形仅保留给紧凑状态控件和启用的提供商节点。

**控件高度是封闭集合，三档封顶**（2026-09-11 用户指令）：

| 档 | 令牌 | 值 | 用途 |
| --- | --- | ---: | --- |
| 标准 | `--asb-control-height` | `40px` | 输入框、下拉、按钮——`DESIGN.md §10` 的最小交互目标 |
| 紧凑 | `--asb-control-height-compact` | `32px` | 分段控件（`asb-segments` / `asb-tabs`）、行内图标钮、批量胶囊 |
| 微钮 | `--asb-control-height-micro` | `28px` | 仅装饰性字形钮（如搜索清除），**命中区仍投影回 `40px`** |

任何组件不得挑选第四档高度：同一屏上堆叠 `28/30/32/40` 正是「乱」的直接来源。

间距节奏与尺寸的守卫（2026-09-11）：`src/styles/tokens.test.ts` 禁止在 `padding*` / `margin*` / `gap*` / `inset*` / `top` / `right` / `bottom` / `left` / `width` / `height` / `min-*` / `max-*` / `flex-basis` / `border-radius` 里写裸 `px`。白名单仅四种：`styles/tokens.css`（唯一所有者）、`styles/theme.css` 与 `styles/boardui.css`（图表层隔离区）、值为 `1px` 的发丝线、以及选择器含 `icon`/`logo`/`svg`/`img`/`glyph` 的图标尺寸。**一个数字要么来自比例尺，要么在声明处拥有名字**——组件内部固有几何允许声明本文件域的 `--asb-<component>-<thing>` 常量（如 `--asb-slider-thumb-size`），禁止把字面量直接写进声明。同一测试同时禁止消费未定义的 `--asb-*` 令牌（历史上 `--asb-text-caption` / `--asb-text-primary` 的幽灵引用让文字静默继承了父级字体），并要求每个标题都带类。

## 6. 材质系统

所有表面直接绘制自身的颜色与渐变，不采样或重绘其后的像素。

| 材质 | 用途 | 表面 |
| --- | --- | --- |
| 环境背景 | 应用背景 | 直接绘制的柔和蓝色径向流场 |
| 轨道面 | 顶栏与路由栏 | `--asb-surface-rail`、细分隔线与内高光 |
| 菜单面 | 菜单、工具提示与通知 | `--asb-surface-menu`、模态投影 |
| 抽屉面 | 差异确认与模态流程 | `--asb-surface-sheet`、细分隔线与内高光 |
| 内容面 | 表单、表格、代码预览、差异主体 | `--asb-content` 或 `--asb-content-muted`，高对比度文字 |

实现规则：

- 将环境背景渲染为真实的、带 `aria-hidden` 的 DOM 层。用少量大型径向光源直接绘制；不得出现可见的伪元素装饰。
- 表面使用 `1px` 白色或中性色细分隔线，以及一道轻微的内嵌高光。
- 阴影仅用于模态框或抬升的菜单。卡片默认不得悬浮。
- 主窗口只有这一套实体表面实现；禁止 CSS `filter`、`backdrop-filter` 和浏览器能力后备分支。独立托盘窗口使用操作系统提供的单层材质（见系统托盘交互规则），不在内容内叠加模糊。
- 尊重系统浅色和深色模式。浅色模式使用极光冰白；深色模式使用深流蓝，绝不使用纯黑。

滚动条（2026-08-28 用户指令，自有设计）：全局以「墨线滚动条」替换 WebView2 默认滚动条——`10px` 槽位、轨道与按钮全透明（无凹槽底色），滑钮经透明描边内缩为 `4px` 墨色药丸线并保持全槽拖拽命中区，悬停加深、拖拽最强，最短 `32px`；深浅模式各用一套基于正文墨色的 tint，绝不引入第二强调色。规则唯一所有者是 `base.css` 的 `::-webkit-scrollbar` 全局块，值由令牌（`--asb-scrollbar-*`、`--asb-radius-pill`）提供，覆盖工作区、配置预览与文本域等一切滚动表面。不得混用标准 `scrollbar-width` / `scrollbar-color`（Chromium 121+ 下会令自定义伪元素样式失效）。

## 7. 布局

桌面端为顶部导航与单一工作区。主导航唯一保留「供应商、扩展、会话、用量、设置」，默认进入供应商，不设概览或主导航更多菜单。当前页由 `aria-current="page"` 表达：普通项弱化 `500` 字重，悬停转主文字，当前项主文字 `700` 字重；不使用填充底、描边或下划线。窄窗口允许顶栏换行，五个入口始终直接可达。

~~~text
┌─────────────────────────────────────────────────────────────┐
│ Agent Switchboard   供应商  扩展  会话  用量  设置             │
├─────────────────────────────────────────────────────────────┤
│ 供应商                         客户端偏好 · 切换历史          │
│ ┌ Codex 当前连接 ───────┐ ┌ Claude 当前连接 ───────┐       │
│ │ 当前供应商 · 模型     │ │ 当前供应商 · 模型      │       │
│ │ 地址 · 接入方式 · 诊断│ │ 地址 · 接入方式 · 诊断 │       │
│ └──────────────────────┘ └───────────────────────┘       │
│ Codex / Claude                           导入 · 新建供应商   │
│ 供应商列表                                                  │
│ 显式请求的变更预览                              确认切换      │
└─────────────────────────────────────────────────────────────┘
~~~

- 「供应商」含列表、编辑与运行参数、用量查询、导入子界面。两个客户端的列表界面完全一致（2026-09-11 统一）：`ProviderWorkspaceShell` 唯一持有面板骨架（「供应商」页标题 + 偏好设置/切换历史右簇、`DualRelay` 路由卡、客户端选择与 导入/新建 工具栏），`SortableProviderRows` 与 `ProviderRowShell` 唯一持有行布局（拖拽手柄、头像、名称与元信息行、启用主操作、使用中徽章、图标操作簇、行内展开）；Claude 行的用量/测试/官方额度展开与 Codex 行的行内切换预览都作为各自内容放进同一壳；Codex 的官方登录是列表顶部固定、不可排序的一条官方行（启用/使用中/重新登录/订阅额度/编辑/删除），第三方的「新建供应商」编辑器用「接入方式」单选选择第三方服务或官方登录，切换即替换会话；删除两端统一走同一张确认单。导入按客户端分流（2026-09-11 一键式，取代同日早先的种子补全式）：扫描响应只含本应用可行动的行（Claude 与 Codex；gemini / claude-desktop / grok 等外来客户端行不可见），全部行统一复选、底部「导入所选」一键导入——Claude 行与 Codex 官方登录行（无自定义端点的官方路由，落到通用存储的官方档案，凭据始终由原生客户端持有）走通用导入判重，Codex 第三方行在后端由唯一补全所有者 `CodexImportSeed::completion_draft` 以声明默认能力 + 源端真实目录事实（源明示值 > 官方发布限速 > 通用默认）实体化成严格档案草稿、经与编辑器保存相同的校验后建档案（源端用量脚本随建随存），路由已存在的行在本地档案缺用量脚本时富集该脚本（`enrich_codex_route_usage_query`，与 Claude 行同一判重语义：真重复跳过、缺脚本则补充），导入凭证全程不过 IPC；用量脚本对双端一致支持自定义源码与内建模板——空 `code` 的 `general`/`balance`（DeepSeek、StepFun、SiliconFlow、OpenRouter、Novita 五家官方余额端点，按档案地址在查询时选择）与 `token_plan` 全部六家数据面供应商（智谱个人/团队——团队版烘焙源端组织/项目 ID 并走 `?type=2` 专用头、Kimi、MiniMax、ZenMux、OpenCode Go）在导入时合成为原生 request/extract 程序（vendored 自钉定参考基线，`ccswitch/templates/`），`newapi`/`github_copilot`/`volcengine` 等依赖档案之外独立凭据（站点令牌、GitHub 登录、控制面 AccessKey）的模板给出行级精确未导入理由；导入成功且没有未处理结果时返回列表并选中导入档案，失败或部分导入留在原位展示明细。本机配置导入仅 Claude。
- 「扩展」含 Skills、MCP、全局指令。资源的发现、导入、部署和历史保持既有预览与确认流程。
- 「用量」含消耗统计、额度与重置，两种数据分区呈现，不相加或互相换算。
- 「设置」左侧分类为应用偏好、客户端偏好、备份与恢复、诊断、关于与更新；窄窗口改为内容上方分类。设置页唯一 `h2` 页标题是侧栏「设置」，内容区各分类面板标题为 `h3` 小节令牌 `.asb-section-title`（2026-09-11 降级，不再与页标题同级同尺寸）。诊断内分配置与环境、本机网关、运行日志。供应商快捷入口携带客户端，并提供返回供应商按钮；顶栏更新入口直接定位关于与更新。
- 工作区与子分类位置保存在当前应用会话。供应商编辑的目标、文件版本与两层草稿独立于列表客户端；客户端偏好和全局指令草稿按客户端分别保存。离开供应商列表卸载用量查询卡，离开网关诊断停止轮询，消耗统计仅在可见时安排刷新。

- 顶栏即自绘标题栏（2026-08-27 用户指令，参考微软电脑管家的融合方式；`decorations: false`）：左侧宝石应用图标 + 加粗品牌名为拖拽区，右侧水平导航项 + 置顶图钉快捷钮（契约见设置节「窗口始终置顶」行，与设置页复选框同一设置两个视图）+ 最小化 / 最大化 / 关闭三钮（关闭悬停转系统红）；实体轨道材质，底部细分隔线。激活导航项遵循本节的纯文字当前页样式。最大化钮镜像实时窗口状态（2026-08-28）：窗口态显示单方块「最大化」，最大化态显示双方块「还原」，随窗口 resize 事件同步。2026-08-31 曾按当日指令短暂切换纯原生标题栏，用户对照微软电脑管家后当日拍板回退本方案——真原生（系统绘制、样式不可改）与「融入页头」互斥，wry 不支持 WebView2 WCO（wry#1650），最终形态即本条。
- 系统托盘（2026-09-05）：保留系统图标，删除原生菜单。左右键均开关同一个独立 `tray` 窗口，Esc 或点击外部隐藏；鼠标移出不关闭。面板宽 360 逻辑像素，外轮廓只由原生窗口裁切；内容铺满客户区，不另画圆角、描边或外侧留白。关闭会给无装饰窗口添加白边的 Tauri shadow；Windows DWM 请求无描边圆角，macOS Popover 材质半径由窗口配置拥有。系统不支持圆角时保持同一无框矩形表面，不另建后备窗口。使用单层系统材质与高对比度文字；不套用主界面导航，不加载主界面业务树。Windows 使用 Acrylic，macOS 使用 Popover 材质；Linux 通过 StatusNotifierItem 激活同一自定义界面，不提供 DBusMenu。Wayland 的全局定位由合成器控制，不承诺贴合图标。
- 托盘按 Codex / Claude Code 分组，显示供应商名、模型和已有缓存额度及时间；真实生效档案勾选且不可重复切换。点击明确的「切换」行先生成既有预览，再携带哈希进入同一原子切换执行器；托盘不构造配置、不传递 API 密钥，也不另起用量轮询。头部左侧为应用 logo、右侧为「打开主界面」图标+文字钮（2026-09-07 用户指令：品牌文字删除由应用图标替代，主界面入口带可见文字，面板根 `aria-label` 是唯一可及名来源）；底部为「管理供应商 / 退出」。托盘面板的文字与布局唯一所有者是 `src/tray/tray.css`，全部消费本系统令牌，不借用图表层工具类（2026-09-11 随全局统一指令清除）。读取失败保留可用内容并显示原因；操作失败保持面板以便重试。
- 托盘定位与显示状态由 Rust 唯一拥有：按图标位置选择显示器，以该显示器缩放比例统一到物理像素，优先向可用空间展开，再限制在工作区内；覆盖负坐标、副屏、任务栏方向及高 DPI。内容准备好、尺寸及位置设置完成后才显示，内容增高超过工作区时只滚动列表。主题、字体和减少动态效果与主窗口消费同一外观函数。只有托盘入口可用时关闭策略才允许隐藏主窗口；不保留原生恢复菜单。
- 应用外壳必须始终可恢复（2026-08-28 用户指令）：顶栏与窗口控制不包入页面渲染树；工作区由错误边界隔离，任一业务视图渲染异常时仅替换为「界面未能加载」恢复面，托盘和关闭控制仍保留。
- 工作区占满顶栏以下的全部宽度；顶部路由卡不再与侧边栏分享横向空间。
- 页面框架唯一契约（2026-09-08 用户指令「各个界面的 UI 设计诸如按钮布局全部对齐，无兼容无过渡无技术债」；2026-09-11 布局统一指令续）：每个顶层页面与子工作区以同一页头行开场，`.asb-panel-heading`（styles/base/panels.css）是唯一所有者——左簇 `.asb-panel-heading-main` 按「返回图标按钮 → 页面标题 → 分类页签」顺序排列，右侧为页面级操作或控件组，窄宽度整体换行。页头右簇唯一容器是 `.asb-panel-actions`：会话、日志等页面的搜索、筛选与刷新一律放入该容器，页面特有尺寸经 `.asb-<page> .asb-panel-actions` 作用域补充，不再自建工具条类（`asb-session-toolbar`、`asb-runtime-log-controls` 已随本轮删除）；日志页头副标题同步删除，页头左簇回到标题（＋页签）唯一形态。单选筛选（客户端、日志级别、导入来源、Skill 来源类型）唯一形态是 `asb-segments` radiogroup（`RadioOption`），`TabButton` 只作为 `Tabs` 页签的内部按钮存在，绝不用于筛选。子工作区（供应商编辑、导入、用量查询、扩展来源浏览）的返回一律是标题左侧的方形 `back` 图标按钮（"←"，可及名说明返回目标），不再以文字返回按钮占用页头右侧；「设置」侧栏的「返回供应商」是页面级侧栏入口，保留文字按钮。分类页签唯一形态是分段胶囊 `asb-tabs`，tablist 语义与键盘（方向键 / Home / End、roving tabindex）由共享 `Tabs` 组件（`src/components/Tabs.tsx`）唯一拥有；`asb-edit-header`、`asb-usage-workspace-head`、`asb-provider-import-header`、`asb-diagnostics-heading`、`asb-ext-heading`、下划线式 `asb-workspace-tabs`与「查询方式」局部页签皮肤全部删除，不留兼容分支。顶层页头标题与主导航名一致：「用量」页头行为标题＋分类页签，「会话」页头行为标题＋右侧搜索、客户端筛选与刷新。
- 会话管理为主从双栏：页头行为「会话」标题，右侧是搜索、客户端筛选与「刷新会话」。左栏是筛选后的会话列表，右栏只在选中后加载详情与对话历史。正文不使用装饰性彩卡；正文位于近实心内容面，长记录在详情内滚动。窄屏时上下堆叠，列表保留独立的固定高度滚动区。
- 日志页是应用自身运行事件的只读账本：顶部「记录级别」经应用设置的唯一保存路径选择「调试 / 信息 / 警告 / 错误 / 静默」阈值；「静默」停止后续写入，绝不作为表格事件。已记录事件可按「全部 / 调试 / 信息 / 警告 / 错误」筛选，并可手动刷新或显式打开应用日志文件夹；主体为时间、级别、闭合事件名与失败时的稳定错误码表格。打开操作由后端解析并交给系统文件管理器，路径不进入渲染层。它只读取本应用受限日志文件中的结构化记录，不读取配置写入历史、客户端配置、文件路径、密钥、请求正文、响应正文、异常消息或调用栈；窄屏时记录级别、筛选、刷新与打开控件纵向排列，表格在自身容器内横向滚动。
- 变更预览没有独立检查器栏（2026-08-28 用户指令）：在显式请求后于供应商列表正下方、同一面板内展开，不再作为右侧栏或页面底部面板。配置工作区是稳定的中心区域，不得把差异做成次级页面。
- 窄屏（低于 `900px`）顶栏允许换行；表单和差异中不得出现水平滚动。
- 操作横幅是浮层，不在文档流内（2026-08-28 布局位移修复；同日修正；2026-08-31 职责收窄）：横幅层绝对定位于工作区顶部、`pointer-events: none`（横幅表面不拦截内容点击）、近实心底 + 模态投影；横幅内的操作控件（如旧档案清空入口）单独恢复指针事件，文字区域仍可穿透。横幅只承载需要用户决策的持续错误——目前唯一形态是 `profile-store-unsupported`（供应商存储格式不受支持，附「清空旧档案并重新开始」入口）；一次性操作反馈（失败、警告、切换 / 恢复 / 撤回结果）一律走 §8 全局通知，不再占用横幅。横幅的出现 / 消失绝不改变页面几何，滚动锚定不参与补偿。滚动容器（`.asb-main`、配置预览体）一律 `scrollbar-gutter: stable`，滚动条出现不得引起重排；配置预览体高度固定 `320px`，文件内容增减行不得改变模块外几何。

## 8. 组件语言

- Tooltip 只解释当前可聚焦或可悬停的控件；触发器收到鼠标或键盘点击后，提示立即关闭，并在指针离开或焦点移走前不得因同一动作重开。统一使用紧凑提示面（最大宽度 `16rem`、上下 `4px` 内边距），该交互与尺寸只由全局 `Tooltip` 组件及其样式拥有，业务按钮不得各自维护 Tooltip 状态（2026-09-02 用户指令）。
- 数据表格只由全局 `Table` 组件渲染（2026-09-02）：`columns / rows / rowKey` 列契约是唯一表格形态，视觉复刻 `本项目` 数据表（2026-09-02 用户指令；令牌为本系统自有）——通栏弱化表头带、其下是无框大间距行、悬停行提亮，所有列一律左对齐（2026-09-02 用户指令）；表格自持 `1px` 细边框与控制圆角的 Apple 式圆角框（2026-09-02 用户指令），表头带与末行悬停面裁切到自身圆角，日志滚动区与工作台结果容器不再叠加第二层框；表头默认保持文档流，只有拥有表格滚动区的模块在自身样式中固定表头，滚动区仍归各模块自身容器。用量模块的显示全部接入：备份历史、运行日志、外部配置导入扫描、供应商用量读数与查询工作台结果（共享 `UsageReadingsTable`），Codex 官方订阅额度与官方额度重置窗口（共享 `QuotaWindowsTable`）；卡片与主从列表不并入表格。

### 四个全局原语（2026-09-11 用户指令「排版规则」）

同一件事只允许有一种形态。这四个原语的唯一所有者是 `styles/base/overlays.css` 与 `styles/base/panels.css`，业务模块不得再各自发明。

- **弹窗骨架唯一**：`.asb-dialog-backdrop`（全屏遮罩；`.is-inline` 变体作用于工作区而非视口，供保留顶部导航的确认单使用）＋ `.asb-dialog`（表面，含 `1px` 细边、面板圆角、模态投影与顶缘高光）＋ `.asb-dialog-surface`（内部弹性列）＋ `.asb-dialog-heading` / `.asb-dialog-body` / `.asb-dialog-footer`（上下各一道发丝线，标题唯一 `h2` 且用 `.asb-dialog-title`，正文区自持滚动）。宽度只有三档：`--asb-dialog-width-narrow`（`440px` 确认单）/ `--asb-dialog-width-standard`（`620px` 默认）/ `--asb-dialog-width-wide`（`1000px` 来源浏览与计划确认）。**不存在第二套弹窗解剖**，因此弹窗无法再长出各自的间距节奏或第四种宽度。
- **空态唯一**：`.asb-empty-state`＝字形 ＋ `.asb-section-title` 标题 ＋ `.asb-empty-state-detail` 一句说明 ＋ 至多一个动作。标题与动作必须说清是「尚无内容」「筛选无结果」还是「加载失败」三者中的哪一种。
- **行内注记**：`.asb-empty` 是「正在读取…」「无变更」这类**表面已有内容时**的弱化单行，取标签档；它绝不作为某个分区的唯一内容——那属于空态。
- **事实行唯一**：`.asb-fact-row`（`<dl>`，标签列固定 `--asb-fact-label-width` 后接值），取代各地自行发明的键值行。
- **客户端筛选唯一**：`components/ClientFilter.tsx` 的分段单选（`role="radiogroup"` ＋ `asb-segments`）是唯一惯用法。同一问题不得一处用 `tablist`、一处用单选组：`tablist` 承诺的是面板切换语义，筛选没有该语义。
- **客户端名称唯一所有者**是 `lib/client-name.ts`：`clientName`（控件内的短标签「Claude」）与 `clientFullName`（正文中的产品名「Claude Code」）必须来自同一处，不得由模块再推导。

### 当前连接双卡

`DualRelay` 是供应商列表双客户端当前连接的唯一展示所有者。复用原渐变、星光与内部实体字段面，完整诊断归配置与环境；卡片只展示实际配置与相关入口，不承载另一套切换流程。宽度不足 760px 时上下排列，名称和字段允许换行，异常说明不得覆盖星光上的白色标题。

### 用量工作台

- 「用量 → 消耗统计」只读取本机 Codex / Claude Code 会话记录，并按「汇总指标 → 模型构成 → 本地 token 日趋势 → 精确明细」排列；图表不与供应商余额或官方额度并置、更不互相换算。四项汇总指标、模型构成、日趋势和精确明细都是有明确间距的独立模块，不共享边缘或连接式外框；模型构成以左侧环形总量与右侧模型、占比、token 值账本对应，日趋势为独立通栏分析面。没有可信时间戳的 token 仍计入汇总和明细，但明确不进入日趋势。
- 用量页按时间范围从后端持久、无凭据的本地会话快照读取最近成功报告；再次进入同一范围先展示标明时间的快照。后端是快照有效期的唯一所有者，返回每项 `refreshAfter`；页面仅在可见时据此安排后台重扫。手动「刷新」始终绕过该范围的快照；失败时保留已显示结果并说明读取失败。快照写入独立 `model-usage-cache.json`，损坏时重新汇总并原子替换；它不属于供应商余额、官方额度或 token 历史账本。
- 本地模型 token 的数值格式由 `src/lib/token-format.ts` 唯一拥有（2026-09-04）：汇总卡、趋势卡和模型构成使用明确带 `tokens` 单位的 K / M / B 紧凑值（如 `225.7M tokens`）；明细表的 token 列标题标注 `tokens`，单元格保留完整千分位数，供核对真实计数。不得在组件内各自缩写或省略单位。
- 供应商用量与官方额度的通用数值 / 单位格式由 `src/lib/usage-format.ts` 唯一拥有（2026-09-05）：表格、趋势标题与比例轨对同一读数保留至多两位小数并附其来源单位；`UsageTrendChart` 的调用方必须以 `valueKind` 显式声明 `generic`、`local-token` 或 `percentage`，本地模型 token 仍遵循上条专属格式，不能仅因字符串单位相同而混用展示语义。
- 图表层采用受控组件基础层（2026-09-04 用户指令取代先前的「仅自建图表」约束）：图表样式接线入口按 Vite 方式加载 Tailwind v4（`@tailwindcss/vite`），**不引入 preflight**，以 `.bui-scope` 承载组件所需复位；暗色从应用 `data-theme` 桥接，`--color-chart-*` 序列色重映射到 `Frosted Relay` 序列色（行动蓝 / Claude 紫 / 安全绿 / 警告橙），图表令牌不属于第二个视觉语言。KPI 没有真实对比数据时不渲染涨幅徽章；基础令牌层是令牌所有权测试豁免的唯一例外。图表层 vendor 组件（`StatCards`、`Chip`、`Tooltip` 及其基础层）唯一栖身 `components/charts/`（2026-09-11 迁移）；`tokens.test.ts` 守卫业务 `TSX` 禁止消费其颜色 / 字号 / 圆角工具类（`text-body-*`、`bg-background-*`、`rounded-2xl` 等），第二套工具类语言只允许存在于 `components/charts/**` 与 `.bui-scope` 子树内。
- 全局 `UsageTrendChart`、`ModelUsageDistributionChart` 与 `UsageRatioMeter` 是唯一图表组件（趋势与构成以 Recharts 按 图表组件层 卡片语言绘制，`UsageRatioMeter` 仍为表格内比例轨）：趋势按同一单位分组并保留可读图例，时间戳取全部真实读数的并集、缺口不插值，单一真实读数渲染为可见数据点；趋势卡大标题只聚合同一时间戳上的序列，只有 `local-token` 语义的同日桶会在每条序列都有该时间点时汇总，独立套餐或额度窗口即使同单位也逐系列展示；静止态显示最新真实读数，绝不跨时间求和；官方额度百分比使用明确的 0–100 纵轴；构成只汇总前五项，其余归为「其他」，图例保留精确数值与百分比；比例轨只显示可计算的真实比例。空数据、单点、长标签、单位冲突与减少动态效果都必须保持稳定布局。
- `state/usage-history.json` 是供应商余额与 Codex 官方额度历史的唯一所有者：仅在真实读取成功后记录数值，保留最近 365 天且每条序列至多 720 点，读取时同样过滤超过保留期的快照。账本不得保存 API key、服务地址、查询脚本、原始响应或账号标识；供应商历史按当前查询摘要隔离，档案变更或删除时清理；官方额度仅在前后账号标记都存在且相同的情况下追加，标记变化或不可比较时清理旧趋势后记录。图表只读账本，不发起网络请求。

### 提供商行

通用 `ProviderProfile` 供应商契约同时承载 Claude Code 的档案与**两端的官方登录档案**；Codex 的**第三方**供应商由专用 `CodexProviderProfile` 拥有，绝不出现在通用存储。`CodexProvidersPage` 是 Codex 工作区的唯一所有者：列表顶部是固定的官方登录行（不可排序，来自通用存储，提供启用/重新登录/订阅额度/编辑/删除），其下是专用第三方档案的拖拽排序列表。每行展示名称与默认模型（官方行展示「官方登录」），提供启用、预览、编辑与删除；重排一次提交全部当前 Codex 第三方档案的文件版本。官方登录的切换与第三方走同一条预览加确认路径，且从不读写认证存储。

以下行结构适用于 Claude Code 的通用供应商档案（2026-08-27 用户指令 1:1 复刻；配色仍用本系统令牌）：

- 首字母头像方块（44px，中性底）、提供商名称，下方明细分两行（2026-08-28 用户指令）：首行为服务地址链接——显示主机名、行动蓝、悬停下划线，点击经 `opener` 插件在系统浏览器打开（wry 会拦截网页内新窗口，纯锚点无效；权限作用域仅允许 `http/https`），无地址时该行显示路由模式文案；次行为模型，仅有值时显示；
- 行首为拖拽把手（2026-08-28 用户指令，把手拖动排序；视觉为本系统令牌）：把手是行内唯一拖拽目标，指针移动超过 8px 才启动拖拽，行主体仍是点选；键盘以 Space 抬起、方向键移动、Space 落下完成排序。列表顺序由每个客户端目录中供应商文件的顺序字段唯一拥有；重排必须提交该客户端全部档案及各自版本，不引入聚合档案数组。拖拽中的行加强为行动蓝描边与投影，不缩放。
- Claude 当前供应商身份由后端 `activeProfileId` 唯一判定：路由、端点及自定义凭据匹配，模型、运行参数和客户端偏好不参与；官方路由标记不代表登录令牌有效性。相同连接档案优先按最近切换记录消歧，否则不任意选择。完整配置的 `matchStatus` 独立展示，不用于推断供应商身份。当前行使用实体选中面与行动蓝细边，动作簇左侧同时显示「使用中」状态胶囊（文字承载状态，绝不只靠颜色）；不使用投影或彩色装饰侧边条；Codex 当前第三方则只由专用激活记录与本机 capability 投影共同判定。
- 变更预览在显式请求前不占位；预览按钮是眼睛开关（2026-08-28 用户指令）——展开后图标变闭眼并带安静蓝色调激活底，再点收回，图标与 `aria-expanded` 共同承载状态，绝不只靠颜色。展开的预览内容渲染在**该行卡片内部、行线下方**（2026-08-28 用户指令，同日由面板底部移入行内）：卡片为纵向两段（行线＋展开区），展开区以细分隔线与行线分界、复用同一卡片表面；「变更预览」标题与「确认切换」主按钮位于展开区头部，其他行不受影响；
- 行尾为图标动作簇（编辑 / 预览或收起预览 / 供应商测试 / 用量 / 更多操作五个内联线性图标，装入胶囊形容器）：点击直接作用于该行、不改变点选流程；动作簇默认隐藏，随行悬浮或键盘驱动的 `:focus-within` 显现，后者以 `data-focus-source="key"` 守卫输入模态（2026-09-08 用户报告：指针点击同样聚焦行主体，不守卫会把操作栏钉成常驻、点别处才消失）；供应商测试在卡片内展开共用模块，默认选择连通性测试，点击「开始检测」才检测地址；另一选项为真实请求。行尾不单独展示真实请求按钮。点选卡片只更新后续预览、编辑和切换所需的内部选择，不产生持续高亮、星光、描边或阴影；只有当前生效行保留持续可见的状态面与「使用中」文字；
- 「启用」主按钮（Play 图标 + 文字，2026-08-28 用户指令，主按钮形式；令牌为本系统自有）在所有非生效行渲染但默认不展示，仅在行被鼠标悬浮时于动作簇左侧短暂出现（2026-09-01 用户指令）。`aria-selected` 继续只表达选择语义，不参与该按钮显隐；键盘操作经动作簇内的同一预览图标进入相同的预览流程（动作簇随键盘聚焦显现，见上）。点击启用拉起变更预览，写入仍需「确认切换」确认弹窗，启用按钮绝不直接写配置。生效行不渲染启用按钮，状态由「使用中」胶囊表达。变更预览面板头部提供「确认切换」主按钮，进入唯一的切换确认弹窗。
- Claude 官方登录行在「启用」按钮同位（动作簇左侧、悬浮显现）渲染「重新登录」次级文字按钮（2026-09-02）：展开该行内 `OfficialLoginPanel`（与编辑视图同一组件、同一状态机），`aria-expanded` 承载展开态；官方行与自定义行共用同一动作簇，编辑图标同样渲染并进入编辑视图。Codex 不复用这个行或登录面板：其官方凭据始终由原生 Codex 管理，应用只观察登录状态并切换模型地址覆盖。
- 卡片常带细分隔边框，当前、选中与悬停转为实体选中面并强化行动蓝细边；预览展开的行除外——展开卡是白色阅读表面，悬浮不染色（2026-09-01 用户指令）；
- 客户端切换是分段胶囊控件（2026-08-28 用户指令移至页面标题行；2026-09-02 用户指令改为标题行下方一栏：左侧分段胶囊、右侧新建主按钮同行对齐）：共享一个容器底座，外框统一为 `--asb-control-height`，激活段为白色凸起卡片，未激活段平贴透底；选择 Codex 时进入其专用档案页，选择 Claude 时进入通用供应商行；段内容为 OpenAI / Claude 品牌标（官方渠道获取：OpenAI 官方矢量、Claude App Store 官方应用图，商标归属各自公司，仅用于标识集成对象）；
- 新建供应商是客户端切换行右侧的主按钮（2026-09-02 用户指令改为图标＋文字、与 tab 行对齐等高）：全圆角胶囊，`+` 线性图标与「新建供应商」文字并排；高度统一为 `--asb-control-height`，使用统一行动蓝实体面与白字，不使用专属渐变、光晕或投影；Codex 进入专用档案表单，Claude 进入通用档案表单；无障碍名称由可见文字承载；
- 变更预览只在显式请求后渲染——选中或切换筛选不得自动拉取差异；候选预览换选即失效，保证安全切换永远对应可见差异。
- 不得添加装饰性眉题、标语，或“安全 / 本地 / 快速”一类的标签云；
- 不得使用装饰性图标方块或脱离列表状态语义的彩色渐变卡片；

### 供应商测试（2026-09-08）

- 本节的应用内供应商测试属于 Claude 通用档案。Codex 的协议、网关和第三方连通性由专用隔离回归与实机验收覆盖，不能把 Claude 的直达测试冒充 Codex 网关验收。
- 自定义供应商只保留动作簇内的「测试供应商」入口，在卡片内展开「连通性测试 / 真实请求」分段选择。编辑器在模型与协议选项之后、备注之前提供「连接测试」分区，复用同一模块；主模型旁只保留模型获取与选择。官方登录不提供连接测试。
- 展开区使用现有字体、实体内容面和中性细边框：标题下显示后端准备的完整请求地址与 API 格式；主体左右对应测试模型、固定短提示与模型回复，小屏上下排列。协议名称复用全局所有者，不另建局部名称表。
- 展开模块不访问供应商，选择真实请求后准备目标。列表使用已保存档案；编辑器使用当前未保存的地址、密钥、API 格式、Responses 选项与默认模型，不要求先保存或填写名称。后端将目标保存为内存中的一次性请求凭证，仅返回不含凭据的地址、默认模型与固定提示「请只回复：连接成功。」；点击「发送请求」后才发送一次非流式请求。已保存档案执行前重新核对文件版本，草稿按准备时的连接快照执行，不生成临时档案或落盘。测试模型可临时填写，也可点击「获取模型」按需拉取被测连接的模型列表，再用与编辑器同款、按供应商分组的可搜索下拉选择要真实连通的模型。未保存草稿的连接（含密钥）只经「准备」命令进入后端一次；此后「获取模型」「发送请求」与取消都只传后端持有的准备令牌 `requestId`，其调用参数、响应与日志不携带密钥。列表获取不消费令牌、不生成一次性凭证、不修改档案或客户端配置，由后端读取密钥并只随模型请求头传输。目标变化后旧列表与迟到响应作废；获取期间获取按钮、下拉、模型输入与发送操作一并禁用，旧列表不可选择；档案变化、凭证过期和无效输入必须显式拒绝，不能静默换目标。
- 结果只来自实际请求：成功需要有效的模型文本回复；HTTP 200、HTML 页面、空内容、上游错误和不完整回复不能冒充成功。显示实际 HTTP 状态、耗时、时间及供应商返回的模型 ID；供应商未返回模型 ID 时不补造。失败直接消费 `ProviderDiagnostic`：区分 DNS、网络、TLS、超时、WebSocket 不支持、endpoint、认证、参数、模型不存在、限流、Provider 错误与响应 / SSE 解析；展示 endpoint、transport、HTTP 状态、request id 和纯文本脱敏正文，超过 16 KiB 标明截断。非 2xx 的正文读取失败或非 UTF-8 编码不改变 HTTP 失败类别；诊断正文只存在本次结果中，不写日志或历史。没有演示选择器或协议降级重试。
- 「取消请求」中止桌面端异步请求；确认取消后可重新发送，旧请求的迟到结果不能覆盖新结果。取消失败显示原因并允许重试取消；已经结束的请求以真实回执为准。收起、切换测试类型、离开页面或连接字段变化同样清理待发送凭证并取消运行中的请求，收起后归位焦点。
- 费用说明紧邻发送操作，取消后说明供应商可能已产生用量。验证仅覆盖直达供应商 API 的本次模型请求，不代表客户端切换或本机协议转换已验证；结果不写入连通性、启用状态或用量历史。

### 编辑视图

Claude 详情页的信息组织（2026-09-08）：资料、连接和模型用平面分组、细分隔与留白表达层级，名称与网站在宽屏并列，窄屏逐项排列；备注、Claude 模型映射和高级连接选项按需展开，已有非默认值初始可见。关键连接字段常驻，地址说明紧邻输入框；实际请求 URL 由 `resolve_provider_endpoints` 纯解析命令返回，界面不复制拼接规则、不发探测请求，地址快速变更时旧解析结果失效。统一保存底栏仍调用通用供应商事务，运行参数二级页仍共用唯一草稿。

Claude 新建或编辑供应商时切换到独立界面：页面内容替换为「返回按钮 + 标题 + 表单卡」，表单卡铺满工作区宽度，路由条与列表、变更预览全部隐藏，保存 / 取消仍在表单尾部。它通过显式路由模式区分自定义 API 中继和官方登录；官方入口不保存服务地址、密钥、模型覆盖或自定义用量查询。

Codex 新建或编辑第三方时使用独立的 `CodexProviderProfile` 表单（2026-09-11 对齐 Claude 编辑视图）：同一套分区结构（基本资料、连接配置、模型、Responses 能力、供应商能力、连接测试、备注）与二级运行参数页、同一「配置运行参数」往返、同一统一保存底栏与端点解析、密钥显隐、连接测试模块；编辑器跨页常挂载以保留草稿，编辑器内切换客户端通过替换会话交接，Claude 与 Codex 草稿绝不互相转换。表单不提供 `routeMode`、官方档案、`OfficialLoginPanel` 或通用 `ProviderProfile` 草稿。模型目录用结构化行编辑（模型标识、上下文窗口、输出上限、逐项能力勾选、默认与支持推理档位），「获取模型」按已声明能力生成可编辑目录行——能力声明本身绝不由模型列表推断；模型映射为「客户端模型 → 上游模型」成对行；能力声明用勾选组与 Chat 推理参数结构化控件（仅 Chat Completions 上游且启用推理时可配）。前端镜像后端契约逐项校验并以中文问题清单阻止无效保存，不提供原始 JSON 编辑。表单一次性保存名称、显式 API 根、API 密钥、默认模型、上游协议、`standard` / `minimal` 请求模式、模型目录、模型映射和能力声明。新建立即写入专用档案；编辑已激活档案时，后端先准备完整客户端文件预览，用户在全局确认单确认「保存并应用」后才提交；未激活档案编辑可直接保存。官方按钮只切回原生官方直连，切换、回滚、恢复和改端口均不写 `auth.json`，官方 OAuth 令牌不进入档案、界面或日志。

Claude 自定义档案明确选择 API 格式：Anthropic Messages（服务根后追加 `/v1/messages`）、Chat Completions（API 根后追加 `/chat/completions`）或 Responses（API 根后追加 `/responses`）。Codex 档案的 `endpoint` 始终是已验证的完整 API 根，`upstream` 决定具体操作的上游协议；两个路径都保留用户填写的 `/v1`、`/v2`、`/openai` 等 API 前缀，不自动补版本。校验和网关统一使用 `asb_core::endpoint`，完整请求 endpoint 不作为根地址别名接受。认证方式由上游协议决定：Responses 与 Chat Completions 用 Bearer，Anthropic Messages 用 x-api-key。

Codex 的 `requestMode` 是专用档案字段，只允许 `standard` 或 `minimal`；通用 `responsesOptions`、`modelOptions` 和 WebSocket 开关不再描述 Codex 上游。Codex 客户端标识固定为小写 `openai`：第三方投影为带稳定本机 capability 的 `openai_base_url`，官方切换移除此覆盖。预览与诊断遮蔽 capability，OAuth 与上游 API key 不出现在切换差异中。

Codex 官方订阅直连，所有第三方通过本机网关；Claude 原生 Anthropic 保持直连。Codex 到本机支持 HTTP/SSE 与 WebSocket，本机到第三方统一 HTTP/SSE。最小模式先处理可解析的 WebSocket 上下文再移除可选扩展；未知引用、显式 store=true 和不透明跨后端内容明确失败。只有跨协议组合关闭无法承载的服务端网页搜索，不能把“经过网关”一概视为“需要协议转换”。网关必须支持多轮、历史压缩和空闲连接关闭；退出说明第三方依赖，不隐式切回官方。

### 表单与配置预览

- 标签应命名用户能控制的对象：“基础 URL”“模型”“API 密钥”“运行参数”。
- 下拉选择的唯一所有者是 `Select` 组件（2026-08-28 用户指令复刻 本项目 的 `Radix Select` 结构，视觉为本系统令牌）：字段形触发器与 `asb-input` 同语言（细分隔边框、控制圆角、悬停转行动蓝），右侧弱化 chevron，未选中时显示占位文案；浮层为实体菜单面、模态投影与顶缘高光，打开即时呈现；选项行 `40px` 触控目标、键盘高亮为安静蓝调表面、当前项右置勾选指示，方向键与 `Escape` 可达。一切下拉选择消费该组件，不再使用原生 `<select>`。
- 文本输入框的唯一所有者是 `Input`（单行）与 `Textarea`（多行）组件（2026-09-07 用户指令「输入框做全局组件并全界面统一接入」）：字段外观（细分隔边框、控制圆角、画布底色、占位文案、悬停转行动蓝、禁用态）由 `styles/base.css` 的 `.asb-input` / `.asb-textarea` 独占，业务组件不得手写 `<input>` / `<textarea>` 或携带覆写外观的类；等宽变体由 `code` 属性承载。鼠标点击不额外绘制视觉态，键盘 `Tab` 进入时仅将既有单层边框转为行动蓝，不添加外圈、阴影或偏移。字体选择器与模型选择器菜单的搜索框同样经 `Input` 接入，其外层已有边框遵循同一规则。`Checkbox`、`Switch`、`RadioOption`、`Slider` 与分段选择（`asb-seg-opt`）内部的原生控件不属于文本输入框，维持各自组件所有。
- 避免使用重复可见字段标签或只是在叙述表单的卡片级副标题。
- 路径、`TOML`、`JSON` 和差异使用代码字体与接近实心的背景。
- 配置文件预览逐行换行显示：行号列固定，长行（如超长服务地址）在文本列内折行，任何情况下不得出现横向滚动（2026-08-28 用户指令）。
- 预览渲染各有一个唯一所有者（2026-08-28 用户指令统一化、同日拆分）：`CodePreview`（文件视图：行号 + 令牌着色 + 换行）与 `DiffView`（差异视图：键 + 旧值 → 新值）。一切客户端文件预览（供应商变更预览、备份差异）与客户端偏好片段预览都消费这两个组件，不得另行重建标记或着色逻辑；客户端偏好片段不是客户端文件候选。
- 差异视图为红绿语义（2026-08-28 用户指令）：旧值红色文字配浅红 tint 芯片（`--asb-danger` + `--asb-diff-remove-bg`），新值绿色文字配浅绿 tint 芯片（`--asb-diff-add-text` + `--asb-diff-add-bg`，明暗两套令牌），「移除」红色加粗；箭头与「移除」文字保证状态不只靠颜色可读（§10）。旧值缺失（`（无）`）保持中性弱化——无旧值即无移除，不得染红。芯片为近实心底上的着色小块，绝不引入背景模糊。
- 优质打印候选全文归一切预览表面（2026-08-28 用户指令接入供应商变更预览）：`FilePreview.content` 是唯一 UI 来源——执行器只把**按 TOML / JSON 键路径脱敏后的展示副本**交给界面；原始候选文本仍只在执行器内用于 `rendered_hash`、语法校验与原子写入。因此确认仍绑定原始候选哈希，界面绝不显示 API key 或任何宿主既有密钥。供应商变更预览不再平铺「保持不变的设置」键列表：被保留的宿主键在候选全文中原位可见；面板顶部路径标题由文件视图自带的文件条承担，避免重复。`SwitchPreview.preserved` 旧字段已随迁移删除——任何预览表面不得重建宿主键平铺列表，一切配置文本一律经 `CodePreview` 渲染。
- 除供应商编辑器内用户主动点击“查看密钥”的受控输入外，密钥必须渲染为稳定的脱敏令牌，绝不显示部分可见的值。查看状态只在当前表单内生效，切换档案时恢复隐藏；预览、差异、日志、错误和诊断始终脱敏。
- 错误应说明确切字段和恢复操作；不应道歉。
- 时间展示的唯一所有者是 `Time` 组件（2026-08-28 用户指令统一化）：格式 `YYYY年MM月DD日 HH：MM` 本地时区（2026-09-02 用户指令改为中国常用格式，无秒、全角冒号），**不带时区偏移后缀**（时钟读数本就是本地时间，`（UTC+8）` 标注已移除）；`timeLabel` 只是格式化纯函数，扫描测试锁定组件外无任何直接消费者（配置诊断匹配态与上次切换、备份历史、端点验证、撤销/恢复确认全部经组件接入）。
- 端点连通检测统一位于供应商测试模块：列表与编辑器复用相同的检测流程和结果呈现，点击「开始检测」才访问当前地址。检测不携带密钥、不发送模型请求；任何 HTTP 响应仅代表地址可达，网络错误才报告无法连通，结果不代表模型或密钥可用。
- 「当前用户级配置模型」是供应商列表、编辑与预览的固定上下文：命中真实配置的供应商卡显示此模型而非档案保存值；编辑器在主模型输入框下方以弱化说明行显示它（客户端使用默认模型时不显示该行），变更预览顶部以键值行显示同一事实（空值显示「默认模型」）。三处均消费应用快照中该客户端 `ConfigFileStatus.route.model`，绝不重新推导或读取草稿值。它只描述已读取的用户级配置文件，绝不声称是运行中会话的模型；`ConfigFileStatus.route.scopeWarnings` 非空时，编辑器与预览必须显示范围警告。
- 快捷模型选择的唯一所有者是 `ModelPicker` 组件（2026-09-03 用户指令移植 `外部配置` 的模型下拉）：获取模型后，主模型与 `Claude` 三档映射（Haiku / Sonnet / Opus）的输入框旁各出现一个字段形 chevron 图标按钮，点开为可搜索浮层菜单——顶部搜索框按模型 `id` 与厂商名过滤，模型按 `owned_by` 厂商分组（后端 `/v1/models` 契约唯一所有者 `probe::ProviderModel`），无厂商归属的归「其他」并固定在末尾（不随用户环境排序变化）；组名作小号弱化标题，当前值右置勾选，交互契约（方向键、`Escape`、外点关闭、回焦）与字体选择器一致，浮层与搜索框共享同一菜单材质。菜单只负责选中，清除模型一律直接编辑输入框；旧的「（从获取列表选择）」`Radix Select` 选择路径已随本轮删除。
- `1M` 启用勾选内联在其模型行的右侧（2026-09-03 用户指令），不换行独占一行：可见文案固定为紧凑的「`1M`」，完整语义（如「主模型启用 1M 上下文」）经复选框原生的 `aria-label` 保留为无障碍名（`Checkbox` 组件的唯一扩展点）；主模型的 `1M` 位于快捷下拉与端点操作之间，三档映射的 `1M` 位于各自输入框行内，`Codex` 的上下文窗口勾选同位显示，独立的「模型运行参数」字段集随内联化删除。
- 用量查询位于供应商卡片右侧的操作按钮区，不在供应商编辑器中出现。它是无文字的单图标按钮，文字只在悬浮提示与无障碍名称中保留；未配置的卡片点击后直接进入独立的完整工作区且不占额外空间，已配置的卡片默认显示并读取账本读数，图标用于收起或再次展开。收起选择按档案持久化（2026-09-02 用户指令）：收起档案的集合由应用设置的 `collapsedUsageIds` 唯一拥有并经同一保存路径落盘，重启后保持；未收录的档案一律默认展开。配置为选填，并在「字段提取」和「自编脚本」两种模式间二选一：字段提取沿用一次 `GET` 请求、`{{baseUrl}}` / `{{apiKey}}` 占位符与余额 / 已用 / 总量路径；脚本是受限 JavaScript，只能生成一次 `GET` / `POST` 请求并从 JSON 响应提取一组或多组具名数值，网络请求仍由应用执行。独立工作区自身拥有草稿和「保存查询」动作；保存后回到供应商列表，不与编辑供应商草稿耦合。已保存供应商的刷新由后端仅凭 `profileId` 解析档案、执行查询；成功结果同时更新当前进程的托盘摘要缓存和应用自有历史账本，渲染器不再传递档案密钥。历史曲线按当前查询摘要读取，配置被编辑、删除或重置后旧曲线随之清理。读数在卡片内以「用量 / 更新时间」标题行、真实余额趋势和全局用量读数表组成（2026-09-02 用户指令，唯一所有者 `UsageReadingsTable`；同日用户指令移除标题行的查询类型徽标——字段提取 / 脚本的区别只在配置工作区呈现）：列为「额度 / 余额 / 已用 / 总量 / 占比」，空值统一显示「—」，每组套餐一行；只有可计算比例时占比列才显示细进度轨，标题行右侧保留「编辑查询」与「刷新」；查询工作台的「本次结果」以同一张读数表呈现。密钥只随受管请求传输，绝不出现在结果、错误或诊断里；配置仅存于应用档案存储，绝不写入任何客户端配置文件。每个已配置的查询自带「自动刷新间隔」（`refreshIntervalMinutes`，2026-09-03 用户指令，随档案保存而非应用设置：字段提取与自编脚本两种模式共用，工作台内以数字输入编辑，任意 0–1440 整数分钟、`0` 为仅手动，越界输入回退不落盘，校验唯一所有者 `asb-core validate_usage_query`；字段为严格必填；缺失或未知形态一律拒绝，绝不迁移、补默认值或改写原文件；外部配置导入的查询同样从手动开始，其自身的 autoQueryInterval 归入未导入键警告）。已配置的供应商卡片在列表挂载期间按该间隔查询，无论详情展开还是收起；收放不额外查询或重置计时，间隔为 0 时仅首次读取及手动刷新。卡片通过 `useProviderUsage` 持有唯一查询与历史状态，详情和收起摘要共用结果，读取状态机仍由 `useAutoQuery` 负责。收起时模型信息栏显示各套餐的剩余百分比及余额；无有效总量时只显示真实余额或已用量，不推算百分比、不合并不同套餐或单位；更新失败保留上次读数并注明失败，查询配置变化后清除旧读数并重新查询。离开供应商列表、切换客户端或进入编辑界面时对应卡片卸载，查询停止。
- Codex 官方登录状态另有原生「订阅额度」表，不复用、也不能配置为上述第三方查询；它是账户观察结果，不是官方供应商档案。后端临时读取 Codex 既有 OAuth 身份，向服务端查询限额窗口；返回值严格收敛为「窗口名称、已用百分比、重置时间、查询状态」，令牌和账号标识不进入渲染器、档案、缓存、日志、预览或错误。用量工作区的「Codex 官方额度重置」面板保持账号级手动刷新不变。窗口经唯一所有者 `QuotaWindowsTable` 以「窗口 / 已用 / 重置 / 占比」表格行显示，趋势图只展示成功读取后写入的同一窗口百分比序列。未登录、身份过期与服务端拒绝分别给出可恢复状态，界面不声称该非公开服务端接口是稳定的官方 API。

### 供应商运行参数与客户端偏好

- 顶层导航不再提供「通用设置」。供应商编辑页标题旁的「配置运行参数」按钮进入二级子界面，主表单只显示连接、模型和档案信息；参数控件不在主表单中堆叠展开。每一层只有一个标题与返回按钮：「运行参数」返回供应商编辑，「新建 / 编辑供应商」返回列表。
- 参数子界面和供应商主表单共用唯一草稿。返回保留两层所有编辑，取消供应商同时丢弃；子界面没有独立保存命令。最终「保存供应商」统一经过准备和提交，正在启用的档案若有效参数改变，仍须展示完整候选并确认保存和应用。目录加载失败保留草稿并提供重试；新草稿的参数尚未初始化前不能保存。
- `ProviderProfile` / `ProviderDraft` / `ProviderFile` 的参数契约只属于 Claude。Codex 的模型和路由事实只由专用档案中的目录、映射、能力和请求模式拥有，不能回填到通用 `responsesOptions` 或 `modelOptions`；新 Codex 档案取得参数目录的默认值，已有档案保留自身已校验的参数。
- 参数取值为 `自动` 或合法显式值。「自动」在切换时移除该受管键，让客户端与当前模型决定默认行为；切换 B 不能残留 A 的参数。修改 A 不会改动 B 的档案。跨协议路由的网页搜索限制只影响本次候选，不改供应商保存的意图。
- 「设置 → 偏好设置」保留 Codex / Claude 选择，「官方设置目录」为按需展开的帮助入口，全局指令位于「扩展 → 全局指令」；通知、终端交互、历史、审批与沙箱等偏好在所有同客户端供应商间共享，并只保存于 `configuration/client-settings/{client}.json`。
- 客户端偏好的保存、草稿与只读片段预览独立于供应商草稿；保存不直接写客户端配置，仍由供应商应用事务统一投影。页尾提供恢复默认、保存状态和保存按钮，之后仅一个可展开 / 收起的客户端配置预览。片段只含显式客户端偏好，不能冒充完整供应商候选。失败保留草稿，外部变更要求重新加载。
- Codex 的默认子 agent 模型 `agents.default_subagent_model` 与默认推理强度 `agents.default_subagent_reasoning_effort` 属于供应商运行参数：模型通过该供应商的模型列表选择，推理强度使用滑块，并在切换供应商时一起投影。这样非 OpenAI 上游接入 Codex harness 时不会残留上一家供应商的模型标识；「自动」移除对应键。偏好设置中的「子 agent 运行」仍是独立直写资源，只拥有 `agents.enabled`、`agents.max_concurrent_threads_per_session` 与 `agents.interrupt_message` 三个全局运行标量。每次修改都先读取真实用户级 `config.toml`、生成脱敏的完整候选文件、二次确认，再经锁、外改哈希、备份、语法校验、原子替换与恢复事务写入。角色表、角色 TOML、未知 `[agents]` 成员和其他宿主配置均原样保留；已退役 `agents.max_threads` 绝不读取、映射或写入，用户指定最大并发前必须自行删除它。
- 「保存并预览应用」先保存偏好并刷新状态，再为当前客户端已启用的供应商生成全新完整预览并跳回列表；旧预览立即失效。取消预览不写客户端文件，最终确认才调用原切换执行器。没有已启用档案时提供前往供应商入口；供应商或用量查询编辑尚未结束时禁止该跨页操作，保留草稿。确认应用后显示后端观察到的匹配结果。
- 控件、合法值、默认自动值、分组和所有权的唯一来源为 `asb-core::ownership` 目录。`SettingOwner::Provider` 与 `Client` 区分持久化归属，两类编辑器复用 `SettingsFields`；界面不得硬编码第二套字段列表。校验器拒绝跨归属、缺失和未知字段。
- 官方设置目录显示字段的实际编辑入口：供应商参数、客户端偏好、独立模块或保留不写入；目录不提供原始 TOML / JSON 编辑器。Codex 的子 agent 默认模型与推理强度只通过供应商参数投影，全局运行策略只通过独立事务写入，不形成第二条路径。项目、本地、受管来源、结构化权限与沙箱表、宿主内部状态仍由原所有者管理。
- 「全局指令」直接编辑客户端真实的 `AGENTS.md` / `CLAUDE.md`，跟随客户端选择；文档本身是唯一真相，不随供应商复制。保存继续执行锁、外部版本比对、隔离备份、原子替换与写后逐字校验；失败恢复紧邻备份。
- 全局指令编辑器只显示文件名，保持高对比度代码面、高度上限与内容内滚动；「重新读取」「放弃草稿」「保存」各自独立，未保存草稿时禁止重新读取。提示词备份不混入客户端配置备份列表。
- 推理强度滑块使用本产品的胶囊轨道、能量渐变填充（`--asb-gradient-energy`，安全绿→行动蓝→Claude 紫）、填充内九颗微光粒子、刻度与白色滑钮；唯一所有者是 `Slider` 组件（2026-09-03 用户指令补齐 `本项目` 契约）：入口校验有限数值 / max>min / step>0 / 值在域内（违反即抛错），量程超过 13 档时刻度压缩为 7 个采样点；按住原生输入时动效时长切至 `--asb-motion-press` 且滑钮 `0.96` 按压，松手回到常规时长；动效随 `prefers-reduced-motion` 关闭。

### 设置（2026-08-28 用户指令）

- 「设置 → 应用偏好」管理应用自身的外观、窗口与托盘、性能；客户端偏好边界见上一节。软件更新与版本信息位于「关于与更新」，备份与恢复、诊断各有独立分类。
- 「关于与更新」以产品行开场（2026-09-09 用户指令：该分类界面必须存在 logo）：左侧为 40px 应用 logo，右侧为产品名与一句话定位。logo 与顶栏、托盘消费同一 `src/assets/app-icon.png` 宝石应用图标，作为本产品身份标识的唯一图像来源，不新增第二套品牌图；图像 `alt` 为空、纯装饰，含义由相邻产品名承载。logo 保持裸图，不加装饰性图标方块、渐变底、描边或光晕（§供应商列表的装饰禁令同样适用于此处），窄宽度下不换行成两行标题。
- 「界面主题」为 `跟随系统 / 浅色 / 深色` 分段单选：显式浅色压过系统深色、显式深色压过系统浅色，跟随系统维持媒体查询；「动态效果」为 `跟随系统 / 减少动态效果`，后者停止滑块和控件强调动画。
- 「界面字体」（2026-08-31 用户指令，交互参考 `ZiChe` 的字体选择器、视觉为本系统令牌）为 `FontPicker` 下拉：字段形触发器以当前字体渲染字体名；菜单为实体菜单面，顶部搜索框过滤，选项 = 随应用打包的 `Noto Sans SC` + `list_system_fonts` 枚举的已安装字族（去重），每个选项以该字体本身渲染「字体名 + 中文 Aa 012」示例——浏览列表即是预览；每行预留固定宽度的右置勾选槽位（当前项显示勾选，样本列不随勾选位移），方向键 / `Home` / `End` 在列表内移动焦点，`Escape` 与外部点击关闭。选择即经完整设置对象保存并立即应用于展示与界面文字；代码保持等宽字体。字族枚举唯一所有者是 `src-tauri/src/fonts.rs`（`GDI` `EnumFontFamiliesExW`，跳过 `@` 竖排字族，排序去重），枚举失败时仅保留打包默认项，不阻塞设置。
- 「点击关闭按钮时」为分段单选：`最小化到托盘`（默认，关闭后继续运行并由托盘恢复）或 `退出应用`（关闭即结束进程）；「窗口始终置顶」与「开机自动启动」为即时开关（与「启用硬件加速」同款）。置顶先调用跨平台 native 窗口接口；开机自动启动先通过 Tauri autostart 插件实际注册或取消系统登录启动项；任一步失败都会恢复先前的桌面状态且不落盘。顶栏置顶快捷钮与该开关是同一设置的两个视图：图钉钮位于导航项与窗口三钮之间，未置顶灰色图钉 / 置顶行动蓝并携带 `aria-pressed`，提示与可及名「置顶窗口 / 取消置顶」；点击经同一 `set_app_settings` 完整对象路径持久化（先 native 后落盘），设置未加载前不渲染，保存失败保持原视觉状态。
- 设置页布尔行为即时开关（2026-08-31 用户指令复刻，同日按操作逻辑定稿为文字在左、开关在右）：行结构与分段 / 字体行同构——标签与说明在左列，`Switch` 控件在行右缘。`Switch` 组件（`role="switch"`，可及名经 `aria-label`，隐藏原生 input 保留键盘与读屏行为）只绘制控件本体：44×26 药丸轨道 + 18px 白滑钮（`--asb-slider-thumb-bg`）滑动 220ms；开启轨道换装移植的 本项目 主按钮渐变（青→蓝→粉 88° 五停 `--asb-gradient-button`）加内嵌蓝光晕，悬停白光增强（`--asb-gradient-button-glow(-hover)`），与供应商行「启用」主按钮同一渐变身份；关闭态为画布底 + 发丝边的中性轨道，悬停边框转行动蓝。按下轨道 `0.98` 缩放；圆环仅键盘导航时显示；减少动态效果时滑动与换装瞬时完成。`Checkbox` 继续拥有导入等列表的复选框语义，二者不混用。
- 「启用硬件加速」控制 WebView2（Windows）的 GPU 渲染；该设置分组仅在 Windows 渲染（`navigator.userAgent` 判定），其他平台没有对应的 WebView 创建参数可暴露，`AppSettings` 字段仍保留在契约内。因浏览器参数仅在 WebView 创建时读取，用户在当前设置页打开或关闭该开关后，性能分组才紧邻显示「重启以应用硬件加速 / 重启应用」操作行，由用户自主决定是否立即完整重启。点击调用后端唯一 `restart_application` 命令重启整个桌面进程，不以页面刷新或最小化到托盘替代。关闭时在 Wry 默认浏览器参数上追加 `--disable-gpu`；若窗口已有显式参数则保留它们并追加该开关。
- 主窗口在原生创建阶段以 `center: true` 居中，避免首次打开落在屏幕下方或先显示后跳动；窗口运行中由用户拖拽后的坐标不被页面代码干预。
- 「软件更新」在应用挂载后静默检查一次 GitHub `latest.json`，不显示全局忙碌遮罩，也不因网络失败打断启动。Tauri Updater 以应用内置 Minisign 公钥校验清单与载荷签名；发现新版本时，顶栏导航与置顶钮之间出现行动蓝「更新」按钮与绿色状态点（提示与可及名「发现新版本 {version}」），点击直达「设置 → 关于与更新」。设置分组始终保留「检查更新」次级按钮；检查中显示「正在检查新版本」，检查后原位展示「已是最新版本」或「发现新版本 {version}」并附当前版本与检查时间（本地时区）。发现新版本时同一行追加「下载并安装」主按钮，展示已知总大小的百分比或未知总大小的已下载 KB；下载、验签与安装只由用户点击触发。Windows NSIS 成功后由更新器退出、安装并自动重启；macOS / Linux 安装完成后调用同一应用重启命令，若该命令失败则显示「更新已安装，需要重启」与「重新启动」按钮，绝不再次安装同一载荷。每次检查替换、安装结束或组件卸载均释放对应更新资源；仅用户主动检查、下载、安装或重启失败走全局错误通知。
- 设置值以完整严格对象原子保存到应用数据 `state/settings.json`：`closeBehavior`、`theme`、`motion`、`alwaysOnTop`、`launchAtLogin`、`hardwareAcceleration`、`interfaceFont`、`runtimeLogLevel` 全部必填；`collapsedUsageIds`（2026-09-02 用户指令，供应商用量面板的收起档案集合）是唯一例外字段——旧文件缺省该字段时确定性地读作空集（全部展开），其余偏好不受影响，一经写入即与必填字段同等严格；`interfaceFont` 必须是非空、无首尾空格、不含引号 / 反斜杠 / 控制字符且不超过 64 字符的字体名；除此以外的任何旧形态、不完整形态、未知字段和非法枚举值都明确拒绝，不存在旧设置迁移、双读或默认字段补写。绝不写入客户端配置文件、供应商档案或客户端偏好文件。
- 设置组使用紧凑实心信息行，与运行参数的分段语言一致；不会显示虚构的配置代码行或候选文件预览。设置行不携带说明性副文（2026-08-31 用户指令移除全部描述行）——标签与控件即一行设置的全部内容；唯一例外是检查后的版本 / 时间结果与失败态的恢复指引，二者是状态而非描述。分组之间的 `24px` 结构间距唯一所有者是 `.asb-app-settings`（设置页只有这一个容器根，表单、失败行与软件更新组都是它的直接子项；§5 结构间距令牌供值）。
- 设置读取失败（2026-08-31 用户指令；2026-09-03 全局横幅）时设置页固定显示失败态而非停留「加载中」：同款紧凑实心信息行携带 `role="alert"`——标签行「设置加载失败：{原因}」、说明行「读取失败期间，外观与关闭行为使用默认值」与「重试」次级按钮（重新拉取，成功后原位替换为表单）；加载成功前顶栏置顶钮维持不渲染。全局错误通知照常发出，页面内文字是持久来源。同一失败同时以工作区顶部的持久错误横幅出现在**每一页**（2026-09-03 用户指令）：横幅为「应用设置不可用：{原因}」加次级「一键修复」按钮，直接调用同一 `repair_app_settings`——设置类操作（用量收起、置顶、日志阈值）在设置不可读期间静默等待，横幅让阻塞原因与恢复入口在任何发生处可达；横幅与设置页失败行是同一修复动作的两个视图（与顶栏置顶钮 / 设置页置顶复选框同款双视图关系），修复成功或重试成功后横幅随加载状态消失。

### 会话管理（2026-08-28 用户指令）

- 顶层「会话」页只覆盖 `Codex` 与 `Claude Code`，与产品支持范围一致。列表支持关键词与客户端筛选，默认由最近活跃时间排列。
- 选中一条会话才加载其对话历史；详情显示客户端、会话 ID、最近活跃、已记录工作目录和恢复命令。会话源路径不在界面或前端契约中暴露。消息目录只列用户的真实提问并逐项定位到原文：`Codex` 以用户角色记录的非对话载荷（`AGENTS.md` 指令转储、环境上下文、`IDE` 上下文包装）不进入目录；`IDE` 包装内嵌的请求段（最后一个匹配标题的负载）作为该条预览，提不出请求段的整条隐藏。`Claude Code` 会话不做此清洗。
- 行操作包括「在命令提示符中恢复」、复制恢复命令和（可得时）复制工作目录。「在命令提示符中恢复」以新窗口启动受控的客户端恢复命令，工作目录可用时直接作为进程目录传入；不提供删除会话、修改历史、会话数据库写入或任意命令执行入口。
- 来源缺失或不可读是行内提示，不替代其它客户端的可读结果，也不伪造空数据。
- 会话列表与对话内容为运行期内存缓存，缓存期为 30 秒：期内切换页面或重新选中最近查看的会话直接显示缓存、不发请求；过期后先显示缓存再后台刷新，列表不回到扫描占位。对话内容缓存至多保留最近查看的 16 条，超出时淘汰最旧条目。「刷新会话」始终强制重扫。缓存不落盘，应用重启后首次进入照常扫描（2026-08-31 移植自 `外部配置` 的查询缓存模式，由前端重新实现）。

### 操作

所有操作按钮的唯一所有者是全局 `Button` 组件（2026-09-03 用户指令复刻 `本项目` 按钮体系，`src/components/Button.tsx`）：组件拥有几何契约，总是输出 `.asb-btn` 基座加恰好一个变体表面类——基座持有 `--asb-control-height` 控件高、`--asb-button-min-width` 最小宽、行动圆角、`6px` 图标间距、居中 `inline-flex` 与默认 `type="button"`（提交按钮显式传 `type="submit"`）；变体（primary 行动蓝 / secondary 中性描边 / danger 红色 / icon 方形触达 / plus 行动胶囊 / back 方形返回）只在 `base.css` 持有表面与几何差量，业务文件不得再手写 `asb-btn-*` 类名。所有输入框、下拉触发器与分段控件同样使用 `--asb-control-height`，Tab 内段使用 `calc(var(--asb-control-height) - 8px)`，不得为页面另设高度。

| 操作 | 处理方式 |
| --- | --- |
| 确认切换 | 行动蓝填充，白色标签，位于变更预览标题行，必须确认 |
| 取消切换 | 中性描边操作，位于变更预览标题行「确认切换」左侧，点击收回变更预览、不写入任何配置（2026-09-02 用户指令） |
| 查看差异 | 安静实体面 / 实心次级操作 |
| 恢复备份 | 中性描边操作，必须确认 |
| 打开备份文件夹 | 中性描边操作，直接唤起系统资源管理器，无确认 |
| 撤回上一次切换 | 系统红色填充，白色标签，必须确认（2026-08-28 用户指令） |
| 删除提供商 | 系统红色用于入口与确认流程 |

### 备份（2026-08-31 用户指令）

- 备份页使用「本地备份」与「加密云端备份」双 `Tab`；默认显示本地历史。本地页签独占撤回与打开备份文件夹操作，云端页签独占 Supabase 表单；切换页签不会写入或清除本地档案。`Tab` 复用既有分段控件的近实心底座、白色激活面与行动蓝描边，不增加导航或设置页入口。
- 云端页签不是嵌套材质卡片，复用本页的近实心内容面与既有表单控件。
- 云端页签在表单前放置「从零配置 Supabase」的有序教程，默认收起。标题行只有一个次级「展开配置教程 / 收起配置教程」按钮，以 `aria-expanded` 和 `aria-controls` 连接教程内容；展开后依次说明创建项目、只复制 `Project URL` 与 publishable key、在 `Integrations → Data API` 确认 `Enable Data API` 已开启后执行应用给出的初始化 SQL、在 Dashboard 直接创建项目 Auth 用户、填写并测试、保存后以备份密码上传或恢复。教程指定 `Authentication → Users → Add user → Create new user` 并保持 `Auto Confirm User`，避免无网页回调的桌面应用误走邮件邀请及默认 `localhost` 回跳。它必须明确区分项目 Auth 密码、Dashboard / GitHub 登录密码与备份密码，并明确禁止使用 Account Access Token、secret / service-role key 或数据库密码；教程仍是紧邻字段的文字序列，不做第二张卡。
- 本地页签填满工作区可用高度：撤回、打开文件夹与上次操作说明固定在模块内顶部，长备份历史仅在自身表格容器滚动；不得以外层页面滚动来查看备份行。
- 撤回确认弹窗在确认前展示“当前配置 → 将恢复备份”的受管键差异（复用 `DiffView`）；差异仍在读取或读取失败时，“确认撤回”禁用，用户只能等待或取消。差异较长时只在弹窗明细区滚动，确认 / 取消始终可见。
- 用户填写自己 Supabase 项目的 HTTPS 地址、publishable key 和项目 Auth 登录邮箱；「显示初始化 SQL」原位展示唯一的表结构与 RLS 策略，用户在自己的 Supabase SQL Editor 执行。应用绝不请求或保存 Supabase secret/service key。
- 「测试连接」使用当前草稿与临时项目 Auth 登录密码验证登录和当前用户对备份表的读取权限；它不要求先保存草稿，不创建或替换云端备份，成功后保留登录密码在当前窗口内存以便随即备份。
- 项目 Auth 登录密码与自行设置的备份密码只在当前操作输入框中暂存，上传或恢复成功后立即清空，且永不持久化。远端只保存经 Argon2id 派生 AES-GCM 密钥加密的完整应用配置目录：两个客户端的全部供应商档案（含端点、模型、API 格式、最大输出 token、独立运行参数和 API 密钥；认证请求头由 API 格式推导）、客户端偏好与切换记录；不上传 Codex / Claude Code 原始配置文件或本地文件备份。
- 「备份到云端」与「从云端恢复」均先经确认弹窗。恢复是红色确认：替换本机供应商档案（含运行参数）、客户端偏好和切换记录，但不修改当前实际客户端配置；恢复后必须重新预览才可写入客户端。恢复直接前一版 schema 3 加密快照时，先将共享参数按当前目录分配给各供应商并转换为 schema 4，再重新加密并覆盖同一云端记录，远端升级成功后才替换本机档案；未知或版本不受支持的快照明确拒绝。

### 扩展工作区（2026-09-07，沿用本项目视觉体系）

「扩展」工作区包含 Skills / MCP / 全局指令三个页签。资源管理流程、信息层级与客户端操作逻辑按同类工具的组织方式设计；视觉继续由本项目 Frosted Relay 拥有，沿用蓝紫客户端身份色、实体表面、字体、圆角、间距与全局控件，不复制外部实现的视觉皮肤。对照的源码基线固定于 `389dd96cb8567f41d05ba4aaebce8a73dc524040`（`UnifiedSkillsPanel`、`UnifiedMcpPanel`、`SkillsPage`、`McpFormModal`）。

- 页签与入口：页头行左簇为「扩展」标题＋ Skills / MCP / 全局指令分段页签（tablist 与方向键 / Home / End 由共享 `Tabs` 组件唯一拥有）；右侧为「从本机发现」与当前类型的主入口（「发现 Skills」或「添加 MCP」）。进入来源浏览后主入口让位于标题左侧的方形返回钮（可及名「返回扩展库」），页头其余结构不变。检查更新、新建本地 Skill、导入便携包、注册项目和操作历史收进「更多扩展操作」。移除重复的底部添加入口与常驻长表单。
- 列表：**列表之上只有一条工具带**（2026-09-11 用户指令）。左侧是视图控件——本地搜索与客户端过滤；右侧是整库动作——两个客户端的批量部署胶囊与「全部更新」。二者由容器间距分隔，不画分隔线、不叠第三条工具行：旧布局把数量条、搜索行与结果行堆在一起，让同一屏出现三种控件高度，是「乱」的直接来源。客户端过滤使用与「会话」页相同的分段单选（`ClientFilter`），不再是 `tablist`。列表总数降为列表下方的一行注脚（`.asb-ext-result-count`，注脚档），仅在需要时附加「显示 N 项」。行内显示名称、Skill 描述或 MCP 命令摘要、来源/传输类型、两个客户端开关与更新/编辑/删除操作；行操作在悬停和键盘聚焦时可见，触摸屏始终可见。搜索只读已返回的非敏感名称、描述、命令与传输类型，不搜索凭据。客户端图标有固定浅色底，在深浅主题中均可辨识。控件高度只用两档：搜索框与按钮 `40px`，分段与胶囊 `32px`。
- 来源发现：Skill 来源浏览是独立工作视图，可切换 GitHub 仓库和本地目录；明确点击解析/扫描后才读取，候选以卡片呈现，可本地筛选并选择客户端适用范围。本地目录既可手动输入，也可经「浏览…」打开系统目录选择对话框（`pick_directory` 命令，桌面与浏览器开发同源）选取，选中即填入并立即扫描，取消则保持原值。加入一个候选后保留结果列表，已入库内容显示禁用的「已在扩展库」；改变来源字段清空旧候选，读取失败保留上次成功结果并说明原因。
- 详情与编辑：点击条目打开扩展详情弹窗，行内编辑直接打开相应编辑器。Skill 保留 manifest、兼容诊断、依赖、文件编辑与版本管理；MCP 保留字段级编辑、凭据脱敏和连接检测。弹窗用同一个 ExtensionDialog 管理焦点隔离与关闭后的焦点归位，Esc 只关闭最上层；操作执行中禁用关闭。下拉菜单挂载到所属弹窗内，保持键盘与读屏可用。已有安装从新增目标列表排除；仍有安装的定义删除时引导先管理安装。
- 目标与意图：行内客户端开关按该条目的现有作用域聚合，表达未启用 / 部分启用 / 全部启用，并以可访问名称说明实际文件状态；部分启用再次点击会启用剩余绑定，无任何绑定时才新建用户级安装。数量条批量操作覆盖当前类型的整个库，包括搜索隐藏的条目，跳过不支持该客户端的内容；开关、计数与操作请求由 deployment-state 唯一计算。所有操作先生成同一份预览，确认后才更新状态。Claude 项目共享 Skill 的停用先明确个人/共享设置范围。项目安装仍通过已注册项目和多选目标完成。
- 更新：从更多操作检查整个 Skills 库，行内显示可更新或失败状态；可逐项更新，也可从数量条执行「全部更新」。成功入库的报告及时清除，失败不抹掉其他结果；只有存在启用绑定的条目进入统一部署预览，未部署内容完成入库后直接报告结果。更新、编辑和版本恢复继续使用现有不可变内容版本与事务执行器，不自动查询远端。
- 版本固定（2026-09-07）：Skill 绑定提供「固定版本」（仅内容与库当前版本一致时可用），行内显示「已固定 <摘要 12 位>」徽标；固定后库与来源更新不再影响该目标，更新预览以零步骤警告目标说明跳过原因；「解除固定」恢复跟随库当前版本。MCP 绑定没有内容版本，不提供固定。
- 预览与确认：任何客户端写入都先 prepare 出脱敏计划，再进入 ExtensionPlanSheet；它与详情、删除、停用范围和管理现有安装共用 ExtensionDialog。计划逐目标列出键级 before/after、部署/移除文件和警告，敏感连接数据继续明确提示。确认按钮为「确认应用」，取消不写客户端；同步操作锁在忙碌状态渲染前阻止重复提交。
- 连接检测：MCP 详情提供显式「连接检测」。等待结果期间展示有界的阶段进度条（连接 → initialize → 读取目录 → 完成），当前阶段以行动蓝高亮；完成后以文字徽标报告结果（通过含协议版本与目录数量、部分、失败含分类、需要客户端验证），并始终提示「在原生 /mcp 确认」。检测可取消。
- 能力徽标：客户端能力表中未核实的条目显示「未核实」徽标与核实条件文字；被能力拒绝的操作在目标选择器中禁用并说明原因。
- 历史与恢复：「更多扩展操作 → 操作历史」打开独立弹窗，展示时间和逐目标结果，提供恢复入口；恢复仍走相同预览与确认。没有记录时显示明确空状态。
- 空状态与失败：空状态按当前类型判断，区分尚无 Skills/MCP、筛选无结果与加载失败；提供对应的添加、清除筛选或重试入口。扫描失败保留上次结果；启动恢复告警阻断写入，保留读取与关闭能力。
- 统一诊断契约与一键修复（2026-09-07）：发现扫描一次返回完整快照——`scanId`、`scannedAt`、观察行与结构化诊断；每条诊断包含唯一 `id`、机器码 `code`、客户端、类型化对象引用（发现行 / 托管绑定 / 渲染器安全的扫描位置标签）、展示文案与 `remediation`（可自动修复 / 需人工处理 / 信息提示，均带原因）。扫描级与条目级警告汇入同一集合，按「客户端＋对象＋问题类型」去重，绝不按文案合并不同对象；观察行不再携带自由文本警告，清单问题三态（缺 `SKILL.md` / 缺 frontmatter / frontmatter 无效）与 MCP 集合类型错误、传输字段缺失或冲突均有独立编码，未知字段只作信息提示。警告面板收起时为一行「发现 N 条警告 · M 条可修复」，摘要行（含折叠箭头）本身是唯一展开 / 收起控件，行尾只保留「一键修复」按钮（摘要可省略、按钮不换行；2026-09-07 用户指令「保留一个即可」，不再另设「展开」按钮）；信息提示在详情中保留，但不计入警告或可修复数量。每次成功扫描后回到收起态；条目行只显示紧凑警告数，点击定位到对应诊断。一键修复仅收集可自动修复项：`prepare_extension_repair(scanId, diagnosticIds)` 校验扫描为最新、逐项重查修复资格，恢复最后一次有效部署状态（Skill 目录按基线版本整体恢复或补齐缺失文件、MCP 条目按基线值原样恢复），保持版本固定与启停意图、不推进 `lastAppliedRevision`；MCP 修复以准备时读取的同一份文档内容建立哈希前置条件，并在写入前确认目标服务槽位仍为空，绝不覆盖扫描后的外部新值。修复产生与其它写入同型的脱敏预览，走同一条预览→确认→事务路径（锁、备份、写后校验、回滚、历史恢复），整批一起应用或一起回滚；执行成功后刷新扩展库并重新扫描，以新诊断判定消失项，刷新或扫描失败如实显示「修复已执行，验证未完成」。文档整体缺失或损坏、内容被外部修改、链接与权限异常、缺少清单内容等一律归为人工处理，绝不以删除、重置或虚构内容消除警告。前端由 `useDiscoverScan` 唯一持有扫描快照、修复准备与接管确认状态。
- 本机发现弹窗跟随当前 Tab 与过滤条（2026-09-07）：后端一次扫描 Codex、Claude 与已登记项目，切换弹窗内 Skills / MCP 页签、客户端与搜索复用同一快照；扫描时间和失败标记继续显示。项目通过已登记 projectId 解析为名称；复制或管理成功后刷新库与发现结果。
- 观察行动作资格由后端按内容判定（2026-09-07 续）：每条发现行携带 `actions` —— `import`（可复制 / `inLibrary` 已有等价内容 / 原因）与 `takeover`（可管理 / 原因）及 `managedDefinitionId`。Skill 以不可变内容摘要判等，MCP 以完整可表达的候选定义判等（未建模字段、传输缺失或冲突则不可复制也不可管理）；历史 / 托管来源只读不可管理。矩阵：未入库未托管 → 「复制到扩展库」+「管理现有安装」；已有相同库内容 → 禁用「已在扩展库」+「管理现有安装」；已托管 → 仅「查看管理详情」；内容无效 → 禁用并在行内说明原因，不让用户点击后被拒。
- 文案契约（2026-09-07 续）：「导入 / 导入到库」改为「复制到扩展库」（保存一份副本，原安装保持独立），「接管」改为「管理现有安装」（把当前位置纳入管理并记录原始状态）；列表提示「管理现有安装已包含加入扩展库，无需先复制」；确认面板只讲三件事——管理对象（客户端 · 作用域）、现有文件保持原样、解除管理时恢复管理开始时记录的原始状态；管理发现等价定义时直接复用，不新建副本。
- 列与警告范围（2026-09-07 续）：发现列表列为「名称（含 MCP 传输 / Skill 摘要 12 位）｜客户端｜作用域／项目｜管理状态｜警告｜操作」，同名的不同客户端或项目各自成行；警告摘要为「当前结果有 N 条警告，M 条可修复」+「修复这 M 项」，摘要行即展开入口，数量随 Tab、客户端与搜索变化，无法归属到单条结果的目录 / 文档级错误按 resourceKind 与客户端归入对应 Tab、不被搜索隐藏；修复按钮只收集当前视图内的可修复项，确认面板以资源名列出对象，不静默修复其它 Tab 的项目；没有可自动修复项时显示原因与展开入口，不以重新扫描代替修复。

### 设置中的网关诊断与端口管理（2026-09-07 用户指令「完整实现：固定默认端口、允许用户修改、修改后持久化、冲突时明确恢复」）

网关端口是产品级契约：新安装默认 `127.0.0.1:47821`（后端常量唯一维护），已安装用户升级保留原端口，端口修改后持久复用；绝不自动递增、随机换号或静默重建。控制器的运行状态（待命 / 运行中 / 端口冲突 / 绑定被拒绝 / 需要修复 / 需要恢复）与端口事实（`configuredPort` 已保存、`listeningPort` 实际监听、`baseUrl` 仅在监听时给出）由后端快照唯一拥有，界面绝不把已保存端口渲染成正在监听。网关启动失败不阻断应用：窗口照常打开，供应商管理、日志与切换到直连 / 官方登录保持可用；依赖网关的切换在任何客户端写入之前拒绝，托盘与退出判断同时检查持久化依赖和实际客户端配置，而不是只看内存路由。

- 监听信息行：面板标题右侧为状态文字徽标（六态同名文字，失败态用错误色文字承载，绝不只靠颜色）；其下三行——「监听地址」展示监听中的回环地址并附「复制」按钮，未监听时显示「未在监听」；「监听端口」展示已保存端口并附「修改」按钮（需要恢复时禁用）；「使用客户端」固定为 Codex、Claude Code。瓦片「监听端口」改为「实际监听」，未监听显示「未监听」。
- 失败横幅（`role="alert"`）：完整展示结构化错误文案——端口被占用时含占用进程名与 PID（平台可可靠识别时）或「占用进程未知」，系统拒绝时含系统错误码；横幅提供「重试」（重新绑定已配置端口）与「修改端口」两个动作。需要修复态说明状态损坏或客户端网关身份无法与当前路由精确匹配，状态损坏时已保留诊断副本；引导到供应商页重新应用（预览将展示新地址与令牌变化）。需要恢复态展示原目标端口、具体原因，说明已保留恢复记录与备份且不覆盖当前配置，提供「保留当前配置并清除恢复记录」的两步确认；恢复未决期间经网关的切换与新的端口修改一律暂停。
- 修改端口弹层（复用 `asb-sheet` 材质）：第一步输入 1024–65535 的整数（不接受 0），准备阶段在后端校验并占用新端口套接字，冲突或被拒时给出结构化原因且不改动任何文件；第二步预览「监听端口 X → Y」与每个受影响客户端的完整服务地址 before/after（Codex 含 `/v1`），声明上游地址、API 密钥、模型、运行参数、客户端偏好与本机能力令牌不变；关闭弹层立即释放预留套接字，未确认预览自动过期。「确认修改并应用」执行一笔可恢复事务（等待在途请求至多 30 秒、超时取消且服务不中断，客户端配置写入走同一执行器边界的备份、校验与审计，只改端点字段），完成后提示「请重新启动相关客户端或会话」——配置更新不代表运行中的客户端已重新读取。预览后被外部修改、Codex 登录缓存或供应商路由参数变更，或端口已变更的旧计划一律拒绝并要求重新预览。
- 端口崩溃恢复的呈现：提交点之前中断由应用启动时确定性回滚；回滚目标被外部修改则进入需要恢复态（见上），绝不静默覆盖或静默重建；重启时目标端口再被占用按端口冲突呈现，由用户重试或改端口，不再自动换号。
- 监听信息与遥测的布局细化（2026-09-07 用户指令「好好布局优化」；2026-09-11 去 Tailwind 化）：三行监听信息与端口修改说明并入同一内嵌信息面（`--asb-content-muted` 底、`--asb-radius-control` 圆角，与统计瓦片同材质），失败 / 修复 / 恢复横幅仍以全宽横幅紧随其后；「近 60 分钟请求」趋势在窗口内零请求时以文字空态（`role="status"`）替代空坐标轴，有数据时 X 轴固定每 10 分钟一刻度，绝不因图表宽度出现自动密排刻度。修改端口弹层的端口输入框经全局 `Input` 组件接入（同日输入框统一化指令）。网关页全部表面、文字与错误色由本系统令牌拥有（`styles/base/gateway.css` 唯一持有样式，2026-09-11 用户指令清除图表层工具类）：信息面 / 统计瓦片用 `--asb-content-muted`，正文 `--asb-text-body`，错误文字 `--asb-danger`；趋势图表卡是本页唯一的图表层表面，由 `components/charts/GatewayTrendChart.tsx` 的 `.bui-scope` 拥有。

### 全局通知（2026-08-31 用户指令，复刻 本项目）

结构复刻 本项目 的 toast 系统，视觉为本系统令牌。`use-toast.ts` 是唯一契约所有者（模块级 store，任意层可直接调用 `toast()`，无需 context）；`Toaster` 在应用根挂载一次。

- 栈：视口左上角悬浮（2026-08-31 用户指令，避开顶部居中的导航），最多同屏 2 条，超出淘汰最旧；错误置顶且绝不因新通知被挤出。栈本身 `pointer-events: none`，通知表面恢复指针事件。
- 种类与图标：info / success / warning / error 四种手绘线性图标，色调分别为行动蓝 / 安全绿 / 警告橙 / 系统红；error 用 `role="alert"`，其余 `role="status"`。
- 自动关闭：info 4.5s、success 3.2s、warning 6s、error 10s（2026-08-31 用户指令设定错误关闭时长，比警告更长以留足阅读后端错误信息的时间）；`durationMs: 0` 可对单条强制手动关闭。
- 暂停契约：悬停、聚焦或页面隐藏时暂停计时并保留剩余时长，离开后继续。
- 材质：实体菜单面（`--asb-surface-menu`）、模态投影、顶缘高光；标题半粗、副文转弱（仅有副文时保持实色）；右上关闭钮 40px 触控目标。
- 呈现：通知新增与移除均即时完成，不建立退出状态或动画计时器。
- 层级：`z-index: 70`，位于表单确认页与工具提示之上。
- 与操作横幅的分工（2026-09-01 收敛）：横幅（§7）只承载带决策入口的持续错误；toast 是全部一次性操作反馈的唯一通道——写操作失败（error，含 message）、切换 / 恢复备份 / 撤回的结果（成功为 success「已切换到「X」/ 已恢复备份 / 已撤回上一次切换」，有警告时合并为一条 warning，标题带警告条数、每条警告一行）。客户端偏好保存成功以页面状态呈现，失败保留草稿并显示错误；导入用 toast 提示结果；部分失败时留在导入子界面展示未导入明细，全部成功后返回对应客户端的供应商列表。

## 9. 动效与状态

- 按下：只改变背景、边框或文字颜色，不缩放控件。
- 路由节点移动：`220ms`、标准缓动，仅在确认后发生。
- 面板进入：即时呈现，不做位移或缩放入场。
- 悬停：背景和细分隔线变化；不得使用卡片抬升动画。
- 焦点不使用外圈、阴影或偏移；文本输入框只在键盘导航时将既有单层边框转为行动蓝，其他控件不绘制焦点视觉态。
- 减少动态效果：移除路由移动；状态变化保持即时且可见。

## 10. 无障碍与性能

- 最小交互目标为 `40px`（`--asb-control-height`）；紧凑档 `32px` 与微钮档 `28px` 只用于 `§5` 列出的场合，且微钮仍须把命中区投影回 `40px`，仅图标按钮为 `44px`。
- 所有文字均达到面向 `WCAG` 的对比度，包括紫色身份标签。
- 绝不能只用颜色表示启用状态：还需包含文字、圆点或轨道位置。
- 运行时不得使用 `filter` 或 `backdrop-filter` 读取、模糊或重绘背景像素。
- 低功耗条件和请求减少动态效果时，禁用高成本的环境背景移动。
- 提供键盘导航，并使用 `Escape` 关闭面板和对话框。

## 11. 应当与禁止

**应当**

- 让配置数据保持视觉焦点。
- 用留白、细分隔线和材质深度建立层级。
- 让简短标题与实际设置 / 值共同说明面板。
- 让下一项不可逆操作清晰无误。
- 在相关页面始终显示启用的路由。

**禁止**

- 在当前连接双卡以外使用彩虹渐变、动态粒子、霓虹光晕或通用指标卡片。
- 模糊代码块、差异文字或表单文字；主界面不采样背景像素，托盘只采用本文件明确规定的操作系统材质。
- 用半透明采样材质代替信息层级。
- 将标签堆、眉题、通用副标题或解释性填充文案用作视觉装饰。
- 在组件中增加第二种操作色或新的圆角尺度。
- 用装饰动画替代路由事实或配置状态。

## 12. 设计评审说明

对于“Apple 式层级”，最初直觉往往是全屏磨砂玻璃。这会让配置管理器变得含糊且令人疲劳。本系统有意只把视觉强调用在一个有意义的元素——当前连接——其余配置与审计界面则以直接绘制的实体面、细边和内高光保持平静、清晰。同样的克制也适用于文字：移除装饰性标签和副标题文案后，信息层级会更清楚，而非更弱。

## 13. 安装体验与三端打包

打包契约按平台拆分：`tauri.windows.conf.json`、`tauri.macos.conf.json`、`tauri.linux.conf.json` 分别拥有内部 NSIS 部署引擎与 WebView2Loader 资源、`["dmg", "app"]`、`["deb", "appimage"]` 三份 bundle 契约。`npm run tauri:build:<平台>` 是唯一构建入口；Windows 通过 `scripts/build-windows-installer.ps1` 编译引擎后嵌入自绘引导，再对最终引导签名，发布阶段只读取 `bundle/installer`，不发布内部引擎。macOS 的 `app` 目标负责生成签名 `.app.tar.gz` 更新载荷，`dmg` 继续作为人工下载安装包。

Windows 安装器是独立的全自绘 WPF 窗口，不依赖应用或 WebView2 已安装。它只完成安装这一件事：说明正在安装什么、选择位置、展示真实安装状态，并在完成后让用户决定是否启动应用。macOS 出 Apple Silicon 与 Intel 两个 dmg；Linux 出 deb（声明 `libwebkit2gtk-4.1-0` 运行依赖）与 AppImage。CI 矩阵构建四个产物组，逐产物扫描；标签发布仅在整个矩阵及所有签名载荷检查通过后执行。

- 引导使用 Agent Switchboard 自有双半宝石标识、圆角自绘控件、清晰留白与单一行动按钮，不显示第三方商标或传统向导侧栏。`installer/Theme.xaml` 唯一拥有安装器颜色、字体与控件模板；不保留旧 NSIS 页头/侧栏位图。
- 安装引导与托盘遵循同一单层外轮廓规则：DWM 拥有窗口圆角并关闭系统描边，WPF 根 Grid 铺满客户区，不绘制第二层圆角 Border；WindowChrome 不增加圆角或缩放边框。内部表单控件的边框不受影响。
- 安装引导顶部 60 逻辑像素为系统标题拖动区域；最小化与关闭按钮单独参与点击命中，安装进行中仍可最小化。窗口不允许缩放。构建必须先构建当前应用部署引擎再嵌入引导，不允许仅凭相同版本号复用旧引擎。
- 安装引导只有深色单一视觉设定，不做深浅色转换（2026-09-11 用户指令）。背景唯一来源是嵌入资源 `installer/assets/installer-background.wdp`（WPF 原生解码的 JPEG XR，深流蓝底＋行动蓝与 Claude 紫双光晕），`ImageBrush` 以 `Stretch.Fill` 铺满客户区，资源缺失时回落 `Surface` 实色；不再请求 DWM 半透明材质，DWM 仅拥有窗口圆角与无描边轮廓。标题栏、安装、进度与完成界面全部由 WPF 绘制；系统权限提示和文件夹选择器仍由操作系统提供。
- 引导默认仅为当前用户安装，已有安装位置从原安装登记读取。内部 NSIS 始终静默执行，负责文件部署、升级、快捷方式和卸载登记；WebView2 检查及缺失时的静默下载安装由引导唯一拥有。安装进行时禁止强制中断部署，没有假百分比；使用真实活动状态，减少动态效果时静止显示。
- `/P` 自动进入自绘进度，`/S` 全程无窗口，`/UPDATE` 与 `/NS` 传递给部署引擎。`/R` 和 `/ARGS` 由引导在部署成功后启动应用，避免二次 NSIS 参数解析；手动 `/D=` 必须是未加引号的行尾路径，`/ARGS` 后的内容全部视为应用启动参数。安装成功与启动失败是独立状态，不把启动失败误报为安装失败。
- 完成后启动默认不勾选。简体中文与英语随系统语言选择；错误显示可操作原因，失败可重试。安装器图标与引擎卸载器图标继续由 `src-tauri/icons/icon.ico` 提供。MSIX 不使用此引导，由 Store 自身管理。
