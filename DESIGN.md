# Agent Switchboard 设计契约

## 产品目标

Agent Switchboard 是 Codex 与 Claude Code 的本地配置控制台。界面的首要任务是让用户在写入前理解将发生的变更，在写入后确认实际状态，并在失败时获得可操作的恢复路径。

设计优先级：

1. 配置事实与执行状态优先于装饰。
2. 每项写入都有唯一执行入口、预览、备份、校验与恢复语义。
3. 同一概念在不同工作区使用同一控件、术语和视觉层级；Codex 与 Claude 的界面修改同步实现：一端新增或调整的 UI 能力，另一端在同一变更中对齐，仅当能力本身不适用于该客户端时才允许单端存在。
4. 不以颜色、动画或隐藏交互代替明确文字和可访问语义。

## 视觉语言

- 使用安静、紧凑的本地工具界面：实体内容面、细分隔线、有限圆角与高信息密度。
- 浅色和深色模式保持同一层级关系；除供应商概览的顶部连接卡外，不使用纯黑背景、大面积渐变、毛玻璃或装饰性光晕。
- 供应商概览顶部连接卡的背景为动态渐变：激活卡使用蓝青主底、浅蓝过渡与右侧黄橙/淡粉紫柔光叠加的横向渐变，空闲卡在中性底上叠合同源蓝紫双色的浅淡晕染；渐变以两倍卡宽图层匀速滑过一个图层宽度，颜色无缝循环流动且循环点不出现接缝，流速缓慢克制，切换客户端或进出列表视图时相位连续不跳变，减弱动效时静止呈现。正文与状态仍以文字表达，不以颜色代替状态语义。
- Codex 使用行动蓝，Claude 使用紫色，只用于客户端身份和需要强调的状态。
- 应用标记由无底色的 Codex 蓝色与 Claude 紫色双路径交叉构成，表达本地双客户端切换；不拼贴第三方品牌图形，不用渐变、背景块、中心徽章或细线。唯一母版为 `src/assets/app-icon.svg`，原生图标通过 `npm run icons:sync` 生成。
- 成功、警告、失败分别使用安全绿、警告橙和错误红；所有状态同时提供文字说明。
- 路径、配置、JSON、TOML、摘要和差异使用等宽字体与接近实心的内容面。

## 字体、间距与控件

- 正文使用 `Noto Sans SC`；代码和路径使用系统等宽字体。
- 页面标题、模块标题、标签、正文、说明和代码各自遵守固定字阶，不以元素默认样式决定层级。
- 间距来自全局令牌；模块间使用容器 `gap`，不叠加随意的外边距。
- 所有矩形表面、控件、弹窗、表格和卡片共享 `--asb-radius: 12px`；仅胶囊状态和圆形标记使用各自的语义形状。
- 标准交互控件高度为 40px；紧凑行内控件与图标操作为 32px。
- 视图内部分组靠留白与细分隔线，不靠装饰性盒层：盒与边框只属于输入控件、行内展开内容和代码/差异预览；组间距至少为组内间距的两倍。
- 表单内容有行宽上限 `--asb-measure-form`，长文本行宽不超过 `--asb-measure-prose`；字段宽度暗示内容长度，数字与短代码用窄档、名称类值用中档（`.asb-field.is-narrow` / `.is-medium`），其余字段保持流式。
- 字段帮助与错误文本共享所属字段的左基线，外观由 forms.css 唯一拥有，模块不得按页面重设样式；数据表格整表使用 tabular 数字。
- 所有图标按钮必须有可访问名称；仅在悬停可见的动作也必须在键盘聚焦和触摸环境中可用。

## Windows 安装引导

- Windows `.exe` 使用单页自绘安装引导，不复刻多步“下一步”向导，不展示组件、许可协议或无关选项。
- 首屏以居中的品牌标记、产品动作标题、一句说明和低权重版本号建立层级；安装位置收在唯一的紧凑材料卡中，底部只保留取消和一个明确主操作。
- 排版采用克制的 Apple 式层级：显示标题 32px、600 字重、紧凑字距；说明使用 15px / 23px 行高；标签、版本与诊断退到 12–14px 的低对比层。单一行动蓝承载主按钮、焦点与进度，品牌紫仅用于标记本身。
- 手动安装允许选择当前 Windows 帐户下的安装位置；应用内更新的目标位置为只读事实，不能在更新过程中改写为另一份安装。
- 引导只有准备、安装中、完成和可恢复失败四种状态。安装中锁定写入目标并阻止关闭；失败显示可执行的恢复方向与必要的引擎返回代码，完成时仅提供打开应用这一后续动作，且“打开应用”默认勾选，不勾选则仅完成关闭。
- 不用装饰性渐变、卡片阴影或持续性动画制造层级；背景、细线、有限圆角与留白服务于安装事实和唯一下一步。
## 页面结构

主导航包含供应商切换、客户端配置、扩展、会话记录、用量监控和设置。

- **供应商切换**：展示两端当前连接、档案列表、编辑、预览和切换。
- **客户端配置**：管理当前客户端的通用配置、Codex 子 agent 运行设置和全局指令；全局指令与客户端选择器保持同步，直接编辑对应的用户级文件。
- **扩展**：管理 Skills 和 MCP。Skills/MCP 使用统一表格；主点击进入编辑，高级管理独立承载部署和诊断。
- **会话记录**：只读浏览本机会话并提供明确的恢复或删除动作。
- **用量监控**：区分消耗统计与额度/重置，不将不同来源的数据相加或推断。页面头部遵循工作区标题契约：消耗统计的时间范围与刷新、额度页签的「刷新官方额度」「刷新重置信号」作为页面动作挂在分类导航右侧（仅所属页签渲染），快照与读取新鲜度为次级状态行；「模型消耗」「Codex 官方额度重置」「Codex 重置信号」模块卡只保留标题与内容。
- **降智雷达**：题目、次数、开始/取消与「历史记录」入口由用量页头部拥有；模块展示自定义题与当前/最近一次检测的当时配置、进度、汇总和明细。检测历史保存在应用数据目录的本地数据库（state/codex/probes.sqlite3，批次 + 单次运行两层），是唯一持久化来源：打开与刷新页面只读记录，点击开始才产生真实调用；刷新或重启后经后端查询当前批次重连进度，前端不记忆批次 ID；异常退出的批次在下次启动标记「已中断」，保留已完成结果并按已保存会话 ID 补读可确认用量，不自动续跑、不补判；在调用前、调用期间和判分前校验客户端文件、模型目录与实际网关路由；监视文件变化并记录路由激活代次，配置变化时终止当前调用且不判分，停止后续调用并将批次标记「配置已变化」。批次记录题目全文、期望答案、判分规则版本、CLI 版本与配置快照（档案 ID/名称、模型、推理强度、脱敏连接身份、指纹）；档案改名或删除后历史仍显示检测时快照，无法识别时显示「未关联档案」；会话 ID 在 CLI 返回后立即落盘，不保存凭据与原始认证配置。只按成功完成调用的最终非负整数答案判分，运行失败、取消与中断一律记「未判定」，与回答错误分开；模型与 tokens 必须归属 CLI 返回的同一会话 ID，缺失用量保留未知，部分汇总显示「已记录消耗」和覆盖次数，不能以零代替未知。最后一条结果与批次终态在同一事务提交；保存失败停止后续调用并展示「结果尚未保存」与重试入口，待保存结果独占检测槽，重试成功前不能开始新检测，当前页与历史详情一致展示待保存数据，不显示已归档成功；正常退出须先结束进程并保存结果，失败则取消退出并保留重试入口；异常退出由进程生命周期所有权终止检测进程组；数据库损坏时明确报错并保留原文件。历史为全页编辑框架：时间/档案/状态筛选、按时间倒序分页读取（分页行用全局共享分页控件）、详情含逐次回答与错误、「使用此题重新检测」将历史题干与期望答案回填为自定义题，保留原次数并以当前配置执行，不重新引用现有题库；历史重开、检测状态变化和删除后刷新列表与档案筛选，过期详情响应不能覆盖当前详情；查看历史期间仍可见运行中批次入口；删除需确认数量与时间范围，仅删除雷达自身记录，运行中的批次不可删除。启动、状态读取、取消和删除失败均提供具体原因；隐藏页签暂停状态轮询，重新进入时读取仍在执行的检测。
- **设置**：集中偏好设置、客户端工具、本机网关、备份恢复、诊断和关于；本机网关和客户端工具都是设置一级分类，不嵌套在诊断内。设置分类使用图标与文字并排的侧栏，当前项以浅行动蓝底与文字强调；偏好设置沿用用户字体，组内为左侧名称与说明、右侧控件的细线行，组间以更大留白分隔，不为每一项嵌套卡片。诊断头部遵循工作区标题契约：刷新状态挂在配置与环境页签的导航行右侧，记录级别、级别筛选、刷新与打开日志文件夹作为日志页签的视图控制行；「配置状态」与「日志」模块卡只保留标题与内容。配置与环境按客户端组织事实区块：客户端名与状态胶囊同在区块头行，区块之间以细分隔线分隔，事实行复用全局键值行契约，不在模块卡内嵌套盒层；配置状态与运行环境的加载骨架按最终事实行的行盒占位，供应商导入的扫描结果沿用同一区块解剖。配置文件与应用数据的路径值本身可点击：点击在系统文件管理器中打开——文件定位并选中，文件夹直接打开，路径解析由应用后端拥有，打开失败以错误红文本行说明。日志页签以共享数据表格呈现：级别列为文字胶囊标记，读取与打开日志文件夹失败以错误红文本行说明，空态不引入盒层；表格随工作区滚动面滚动，自身只保留横向溢出。日志数据分页呈现：分页行是全局共享分页控件，挂在表格下方，仅一页时不出现；切换级别筛选回到第一页，加载骨架按整页行数占位。
- **应用偏好**：主界面缩放提供 90%、100%、110%、125%，文字与控件同比即时缩放，原生最小窗口尺寸同步调整；屏幕可用区域不足时明确拒绝该比例。全局唤起快捷键使用单一录入控件，可取消或清除，录入期间不执行唤起；注册冲突保留原快捷键，保存及回滚失败就地显示原因。启动页面提供供应商切换或上次访问页面，只持久化顶层页面标识，启动时重新读取状态，不恢复客户端选择、子分类、草稿、编辑器或待确认写入；恢复结果不得覆盖用户启动后主动选择的页面。应用偏好以严格完整契约读写，不补全旧格式字段、不自动覆盖旧文件；读取失败保留显式修复入口。
- **客户端工具**：一个面板直接展示没有其他工作区归属的客户端专属功能：Codex 工作场景或 Claude 客户端集成。Codex 工作场景只组合并恢复已有供应商、扩展与指令状态，不创建第二套配置，也不重复资源管理入口。客户端标签带对应品牌标记，不增加摘要卡、快捷入口、二级导航或嵌套工作面；不得展示连接状态或任何已由其他工作区拥有的内容。
- **本机网关**：页面以单一拓扑主图组织：客户端、网关与供应商画在同一张图上，视觉权重严格对应三个问题：活着没、谁在经过、跑得好不好。网关节点是全图唯一视觉锚点：状态灯与状态词承载监听状态并附带「N 秒前更新」，紧凑等宽地址牌承载完整回环访问地址与固定宽度的复制图标，事实行只列配置端口与实际监听，修改端口是其常驻动作；监听失败时节点整体转为警示材质并内嵌失败信息与恢复动作。图以左右两条母线为接线背板：客户端节点固定在左列首末行，供应商节点按路由逐行排在右列；每条路由的连线从母线水平引出，协议芯片坐在连线上——客户端连线标注客户端原生协议，供应商连线标注上游协议，协议转换用行动蓝连线与标签、本机转发用中性连线与标签，供应商节点明确标注「协议转换」或「本机转发」；每条网关路由与请求都显示客户端协议 → 上游协议；连线上的流量计数只统计近 60 分钟匹配活动路由的内存遥测样本，零流量不显示计数，直连流量不伪造为网关遥测；无路由时供应商列以虚线空位说明直连状态。修复指引与恢复阻塞是主图的内嵌子条，附着在图下方，不另起平级告警卡。图表带以等宽大数字与图表呈现累计请求（60 分钟分钟柱趋势，柱高以窗口内最大计数满高并随轮询平滑过渡）、失败请求、p50/p95 耗时（条宽线性对应 p95 满宽，只统计已完成请求）与上游协议分布，不带卡片框线或内部分隔线，仅靠间距与字阶分列；「最近请求」明细不做折叠，默认常驻展示最近 20 条。拓扑与最近请求共用页面实体底面，以留白和细线分区；仅中央网关节点使用浅中性材质强调。响应式断点由网关内容宽度决定：中等宽度保留客户端、网关、供应商三列，协议芯片附着所属节点；窄窗口下图退化为纵向堆叠，母线与连线隐藏，协议芯片保留。端口变更仍须先预览并确认，不能将保存端口误示为正在监听。网关状态灯保持静止：待命为中性、运行中为安全绿、失败为错误红，并始终伴随状态文字。柱高与条宽过渡只发生在轮询刷新时；加载骨架脉冲只在读取期间存在，两者都遵守减少动态效果偏好。

界面骨架分两级：主界面是一级工作区骨架，供应商的新建/编辑等二级功能页是二级编辑器骨架。一级骨架由 `WorkspaceHeader` 拥有：标题行、主导航行（左侧分类页签、右侧页面动作）和可选的次级视图控制行保持一致结构，页面动作与视图控制一律上收头部，内容模块卡只保留标题与内容；页面不自行拼装同类标题结构。

二级编辑器骨架由 `EditorFrame` 拥有，供应商编辑/新建、运行参数、用量查询、供应商导入与导出、扩展发现与来源管理、降智雷达检测历史等二级功能页共用：进入时帧替换所在工作区的内容流，返回控件回到入口列表。帧内只有模块区滚动；底部动作栏由页面组合，无常驻提交的页面可省略——内容动作（如「查询用量」）留在模块区，提交动作（如「保存查询」「保存供应商」）进固定动作栏。二级页挂载期间草稿保活，`hidden` 只切换可见性，轮询等副作用只在可见视图运行。两级骨架互不混用：一级页面不在页内再嵌全页帧，二级帧不回头组头部语法；判断一个界面的改动只需先认领它属于哪一级骨架。

## 组件所有权

供应商余额与 Codex 官方额度的刷新周期由后端拥有，不依赖客户端页签、列表或详情面板的挂载。列表和托盘订阅同一缓存更新通知并读取同源结果，不直接展示查询请求的返回值。脚本用量缓存以 `UsageSnapshot { summary, error }` 保存最近一次完成状态与保留的成功读数；首次失败无余额，刷新失败保留读数并明确标记「上次读数」。进入页面只读缓存；刷新间隔为 0 时只允许手动查询，尚无缓存时显示空态而非持续加载。查询进行中与读取失败在所属行反馈。

- `Button`：全局按钮尺寸与变体。
- `Input` / `Textarea` / `Select` / `Checkbox`：字段语义与基础外观。
- `Table`：数据表格的唯一 DOM 与样式契约。
- `WorkspaceHeader`：页面和模块标题行。
- `EditorFrame`：全页编辑框架的唯一所有者——固定顶部（标题与方形箭头返回控件，目的地由返回控件的可访问名称说明）、可滚动的模块区、可选固定底部操作栏。
- `ProviderEditorFrame`：在共享编辑框架上组合供应商写入栏（取消与「保存供应商」，保存可经 `formId` 关联外置表单）。
- `ExtensionDialog`：扩展相关弹窗的焦点隔离、标题和关闭行为。
- `CodePreview`、`EditableCodePreview`、`DiffView`：只读配置、可编辑片段和键级差异的唯一渲染所有者。
- `Time`：界面时间语义和本地格式化。

模块只能在全局原语上添加业务布局，不复制基础控件结构或创造平行组件体系。

## 写入与恢复

- 客户端配置应用、恢复原生默认值和清空额外配置将真实文件与 ASB 已保存设置作为同一恢复单元；仅设置意图变化也要创建备份，文件不存在且无需变化时保持不存在。备份恢复同时还原两者，并保存恢复前的配套快照以支持撤销；缺少或损坏的配套快照明确拒绝恢复，不猜测历史设置。备份差异包含完整文件字段与 ASB 设置意图差异。备份历史数据分页呈现：复用全局共享分页控件，挂在表格下方，仅一页时不出现；换页收起已展开的差异行，恢复新增备份导致列表前移时页码收敛。
- 原生默认值恢复直接移除标准覆盖，不重放已保存的额外配置。高级恢复保留 Codex Hooks、Plugins、Apps、MCP、Skills 及通用片段所声明路径的当前值，保留 Claude 扩展与清单管理的额外字段；子 agent 三项运行设置的删除必须出现在确认预览中。高级删除集合直接遵循官方设置目录的“仅保留”边界，保留角色、权限、信任状态和环境等宿主结构。路径匹配使用原始键段，预览对含点号、括号或引号的键明确引用，不能与嵌套路径或数组索引混淆；URL 中的用户名和密码统一脱敏。预览与确认重置时均执行现有备份恢复校验，不放宽供应商格式、路由、模型目录或认证契约。

- UI 只能请求类型化预览和应用操作，不能直接写入 Codex 或 Claude Code 配置。
- 切换和扩展执行器是配置写入的唯一所有者。
- 写入前读取实际文件状态，写入中创建备份，写入后验证候选内容；失败时回滚或报告明确恢复状态。
- 受控配置只变更当前档案或扩展拥有的字段；不属于它的键必须保留。当前受控字段始终由界面状态归一化：显式值写入、自动值删除。历史受控字段在同一预览、备份、校验和恢复事务中删除，不保留别名、双读、双写或手动清理路径。
- 扩展计划对每个配置文档至多渲染一次写入：批量内所有条目补丁按序叠加到同一份穿线文本，由首个变更该文档的目标携带这次唯一写入。
- 扩展部署到已有未托管原生条目的键位即收编该条目：基线记录接管前原值作为逐字恢复点，替换必须经展示前后差异的计划确认；已托管键位在同一文档与命名空间内互斥，冲突拒绝。Skill 目录的外来内容不适用收编，仍要求经发现页显式接管。
- 敏感连接数据的写入、删除仍有安装的扩展，以及影响共享项目设置的操作必须明确确认。
- 外部文件变化、前置条件冲突、恢复失败和不可读取配置必须可见，不能静默接受过期输入。配置语法异常时只生成可证明不丢失语义的自动修复候选（例如 BOM、JSON 注释或尾随逗号归一化）；无法确定的异常必须拒绝覆盖。

## 供应商界面

- 官方额度读取与刷新只由 Codex 供应商行拥有，详情面板只消费行状态；Claude 列表不保留 Codex 额度入口。托盘快照的 `usage` 是唯一用量字段，以 `kind: script | official` 与 `reading` 明确数据来源；script 的 reading 为统一用量快照，official 为账号额度；没有缓存时为 `null`，不保留并行字段或旧格式读取分支。

- Codex 官方登录的供应商行在模型主数据轨下方展示各额度窗口的剩余百分比（100% 减已用百分比）和上游声明的重置倒计时，与订阅额度详情共用一次读取；收起详情不停止已配置的自动刷新。托盘也展示对应原生账号或绑定账号的缓存余额，以 `5h`、`7d` 等紧凑窗口标签保留单行布局；不存在读数时不虚构余额，失败后保留的读数明确标记「上次读数」。官方额度缓存只按当前账号匹配，托盘不触发网络查询或凭据刷新；Claude 官方登录尚无对应订阅额度读取契约，不显示推测值。

- 当前连接卡只展示从真实配置读取的状态，不从草稿或列表选中项推断。
- 档案行以名称、模型、地址和启用状态为核心；按身份、模型、连接和固定宽度操作轨稳定分区；数据列随可用宽度同比收缩，原生窗口最小宽度必须保证这一单行数据轨仍可读，模型文本始终单行横向排列，绝不换行、将模型栏移到新行或为档案列表加入横向滚动；不显示列标题，任何一行的动作数量不得移动模型栏的起点。模型显示等宽纯值，各行模型名同以模型列左缘为起点，保持单行与固定行高；已应用状态以行动蓝小圆点与低权重文字锚定模型单元格尾缘，与模型值至少相隔 16px，不使用胶囊底色、边框或内边距。长模型名只在自己的区域省略，状态不收缩、不挤动模型名起点。档案行使用弱化的中性细边框；悬停与键盘焦点以浅中性底反馈，实际应用行保持浅蓝底，切换确认展开时使用中性阅读底面。鼠标环境下行内动作只在悬停或键盘焦点时显示，触摸环境保持可见。脚本用量摘要按各读数独立分组，全部常显：余额型突出余额，已用与总量为次级事实；百分比型使用来源明确的 `%` 单位，突出剩余或实际存在的已用值，不由名称或总量猜测类型，不虚构缺失值。重置时间、失效原因与补充说明由脚本明确返回，摘要完整显示；摘要置于主数据轨下方的全宽摘要轨，可独立换行，绝不裁切、覆盖操作区或横向滚动。更新时间与刷新按钮由摘要唯一拥有，刷新保留读数，失败显示原因和重试入口。完整数值、准确重置时间与有效状态保留在按需展开的详情表格中。相对时间与倒计时每 30 秒更新，页面隐藏时暂停计时；到达声明的重置时间只提示「已到重置时间」，不本地重置额度。主数据轨与模型槽使用较舒展的固定高度；摘要轨可以独立增高，但任何动作、摘要或收起的用量面板都不得挤压或拓高模型栏；用量详情只在展开时挂载，未配置或不存在脚本用量时摘要区域为空，不显示占位符。Codex 与 Claude 的第三方档案行拥有同一组行内动作：编辑、测试连通性、配置用量和删除；测试连通性在行内展开供应商测试面板，配置用量在未配置时打开用量查询工作区、已配置时展开用量读数面板，详情默认收起，应用设置仅持久化 `expandedUsageIds` 中明确展开的档案；不保留旧折叠集合字段、双读或转换分支。两个行内面板的展开与收起使用与编辑器一致的高度、位移与透明度过渡，内容保持挂载至收起动画结束，并遵守减少动态效果偏好。列表不提供独立配置预览；启用只生成切换候选，确认区在候选所属行内展开并展示写入差异，不使用弹窗，确认前绝不写入。列表不以持久选择状态高亮任何行，只有真实使用中、悬停、键盘焦点和展开的切换确认可改变行状态。操作轨以右缘为唯一扫描锚点：文字与图标动作一律右对齐，各行动作数量随行能力增减（第三方行编辑、测试连通性、用量、删除，Codex 官方行编辑、订阅额度、删除，Claude 官方行编辑、删除），同一动作不要求跨行同列；删除在包括已应用行在内的所有行可用，删除已应用档案的防护由删除流程的确认与校验承担，行内不因已应用状态隐藏删除。操作图标直接作用于当前行。
- 托盘只承载切换供应商与查看缓存用量。每个供应商固定单行：「当前勾选—名称—用量—右侧切换槽」，名称超长省略；正常逻辑宽度为 480px，仍受屏幕可用区域约束。用量按主界面同一口径，显示来源实际存在的余额（缺失时只展示已有的已用或总量）或独立窗口剩余百分比；上游明确的窗口标签压缩为 `5h`、`7d`。最多并排显示两个读数，按实际宽度测量收敛，放不下的读数以 `+N` 标明；名称可以省略，数字与单位不裁切，用量不侵占固定切换槽。查询失败无缓存时显示「查询失败」，保留成功读数时显示「上次读数」，失效账号或套餐以文字说明；不虚构零值。左侧勾选轨与右侧切换槽始终保留；切换文字在悬停、键盘聚焦和触摸环境中可见，切换中常显，状态变化不移动列宽。托盘只读后端同源缓存，不触发查询、凭据刷新，不展示模型、地址、完整表格或详情提示。面板高度保持上限，超量时纵向滚动；页眉只放标记与软件名，页脚为「打开主界面」和「退出」。
- 编辑器是供应商列表内的独立全页工作面：不使用居中的窄栏，而是铺满可用工作区。`WorkspaceHeader` 位于固定顶部，返回控件为方形箭头按钮，不附可见文字装饰；取消与“保存供应商”位于固定底部。只有中间的配置模块区域滚动，接入方式、基本资料、连接、连接测试、模型和高级设置仍各自以有间距的独立模块呈现；每个模块内部使用稳定的资料标签列和有界字段列。
- 供应商编辑采用克制的 Apple 式排版：保留用户选择的界面字体，使用紧凑字距、600/400 的明确字重对比、实体内容面、细边框和单一行动蓝；独立模块不使用阴影或渐变制造层级。高级设置是唯一的嵌套例外：展开后，Responses 能力、供应商能力、运行参数、订阅刷新和备注等各自呈现为独立的浅层子模块，不能连成连续字段列表。
- 新建流程先选择接入方式，再依次完成基本资料、连接、连接测试和模型；每一步独立呈现，协议细节、供应商能力、运行参数和备注收在“高级设置”模块中。编辑态将客户端与接入方式显示为静态事实，不使用禁用选择控件伪装为可编辑字段。编辑器内的折叠模块和连接测试面板使用同一组高度、位移与透明度过渡；内容在收起动画结束前保持挂载，切换图标同步旋转，并遵守减少动态效果偏好。
- Codex 官方登录的接入方式、名称和官网地址属于同一个“基本资料”区块；不得出现重复的“基本资料”标题。编辑时接入方式为静态事实。高级设置包含运行参数、订阅额度刷新和备注；运行参数复用供应商参数目录与字段控件，编辑从已保存档案初始化，仅新建从目录默认值初始化，返回编辑保留草稿，恢复默认值也仅修改草稿，统一保存沿用既有预览确认流程。官方登录不展示第三方连接、协议能力、模型目录或跨供应商子 agent 路由。
- 新建或编辑供应商中的运行参数使用与客户端配置一致的行结构：名称、保存含义、当前已保存设置和选择控件构成默认视图；变更时仅显示一行待保存摘要。参数归供应商档案所有，不展示客户端文件代码或把参数误示为已应用。分组、参数行与重置操作栏以留白和细分隔线组织，不使用模块底色盒层。
- 运行参数中“默认子 agent 模型”是跨档案路由引用，只有一种形态：自动，或指定“目标档案 + 目标模型”。选择器数据源唯一为各档案已保存的模型目录，不做实时拉取；默认收起的“跨供应商选择（高级）”开关控制候选集——关闭时只列本档案目录，开启后按档案名列出其他档案。帮助文案如实说明：子代理请求由本机网关转发到目标档案、失败不回退主模型、配置路由后本档案强制经网关路由。引用不可解析时以警告文案就地提示，后端校验仍是权威。
- 客户端配置的每个设置与子 agent 运行模块使用相同的行结构：名称、写入含义、当前配置摘要和选择控件构成默认视图；有修改时只显示一行待应用摘要。当前代码和 `-` / `+` 差异收在本项的按需展开区，作为审阅证据而非日常编辑内容；自动值明确说明字段未设置。展开区的展开与收起使用与编辑器折叠模块一致的高度、位移与透明度过渡，箭头同步旋转，并遵守减少动态效果偏好；展开区与选择控件之间不加分隔线，仅以行距衔接。
- 客户端配置的设置行、分组、目录、全局指令和子 agent 运行靠留白与细分隔线组织，不使用模块盒层或行底色。「恢复为客户端原生默认值」位于页面头部导航行，与官方设置目录、通用配置文件、全局指令并排（danger 变体），默认范围只移除 ASB 管理的标准通用配置覆盖及 Codex 的三项子 agent 全局运行设置；高级范围在此基础上只移除未管理且未在官方设置目录标记为“仅保留”的字段。两个范围都不触及供应商参数、全局指令、登录凭据、扩展配置或额外通用配置；底部保存操作只保留保存和预览应用操作及其状态。状态色只保留给语义状态。
- 子 agent 运行是 Codex `config.toml` 的 `[agents]` 分区：它以独立视觉模块组织三项运行设置，但不创建第二个配置文件或写入事务；与其余客户端配置合并为同一候选文件后一起预览、备份、校验和写入。
- “官方设置目录”与“通用配置文件”并列位于客户端选择器右侧；该入口以单一模块读取并展示脱敏后的当前机器真实配置，模块右上角提供编辑图标，点击后同一模块内原地转为编辑，不再分为上方只读预览与下方编辑器。手动编辑只允许修改界面未拥有的字段；标准通用设置只在页面表单中编辑；Claude 的 ASB 管理额外通用配置拥有独立编辑器；真实配置无法安全解析时该模块作为自动修复入口且不可直接编辑。敏感标记由后端按原路径复原。所有写入均经预览、确认、备份、校验和恢复执行器。
- “全局指令”与“官方设置目录”“通用配置文件”“恢复为客户端原生默认值”并列位于客户端选择器右侧；四者互斥展开。全局指令在工具栏下方编辑当前客户端的用户级指令文件，编辑器拥有固定高度和本地滚动，输入内容不应改变页面宽度；展开和收起使用同一组高度、位移和透明度过渡，并遵守减少动态效果偏好。
- 模型发现使用连接的显式模型列表 URL，且模型列表 URL 只属于发现、不改变实际请求路由。完整请求 URL 不能推导模型列表地址：未配置模型列表 URL 时，必须禁用模型拉取并说明原因；不得回退尝试或猜测其他端点。
- 模型目录解析只属于后端唯一契约所有者一处：按上游真实目录格式接受 OpenAI/Anthropic 的 `data[].id`、Codex 目录（ChatGPT backend、智谱 Responses）的 `models[].slug` 与 Copilot 的 `vendor`；无效条目剔除、重复合并、空目录报错。不为单一供应商保留第二份解析，也不做端点或格式的回退尝试。
- 模型发现只采用上游显式返回的图片输入事实：支持、不支持和未确认必须区分。未确认模型在编辑器中显示 mixed 状态，默认不将图片写入 `input_modalities`；不得从模型名称或供应商名称推断多模态能力。
- 配置预览展示脱敏后的候选文件与差异；长行在文本列内换行，不以横向滚动隐藏内容。「恢复为客户端原生默认值」位于页面头部导航行并与其余入口并排，确认前先生成并展示标准设置恢复预览，再在同一展开区确认写入；不另起确认弹窗。恢复展开区内提供“同时移除界面外字段”高级开关：开启后以高级范围原地重新生成合并预览，标准设置与可移除界面外字段的移除项完整列出，最终确认按钮标注双重范围。若 Claude 存在由 ASB 接管的额外通用配置，则在标准恢复预览下方以独立的“清空 ASB 管理的额外通用配置”操作呈现，二者不能合并确认；高级范围恢复与额外配置清空的最终确认使用危险色。移除项在各自预览内完整展示为无卡片嵌套的细分隔列表。存在未保存的客户端或子 agent 草稿时，所有恢复与清空操作均不可用。

## 扩展界面

- 头部按工作区标题契约组织：标题行之下依次为导航行（左侧 Skills/MCP 数量条，右侧与客户端配置样板一致的统一文字动作组，检查更新、操作历史等低频操作收进「更多」菜单）和搜索行（搜索框与客户端部署概览）；内容列顺序固定为表格和结果注脚。
- 「从本机发现」与「发现 Skills」是挂在共享编辑框架上的全页工作面：固定顶部为标题与返回扩展库的箭头控件，中间内容滚动，固定底部操作栏承载主操作——发现面为全选与导入所选，来源浏览面为按来源提交的扫描/搜索；不再以页内面板或弹窗承载。来源浏览面内的 Skill 仓库管理以模块头组织的独立模块呈现：标题与上下文行、右侧「添加仓库」与「返回发现」模块动作，不再另设页中页或自带返回控件。
- 表格列为扩展、客户端、操作。扩展列显示名称、摘要和来源/传输；客户端列显示启停；操作列提供更新、部署与诊断、删除。
- MCP 行摘要以圆形状态点标注最近一次连接检测结果：通过、部分通过、失败分别为安全绿、警告橙、错误红，未检测与无结论不着色；状态点始终伴随文字说明，只反映最近一次由用户触发的检测，不代表实时连接状态。
- 主点击直接打开对应编辑器。保存或关闭后返回扩展库，不在管理和编辑间往返。
- 本地路径字段提供原生目录/文件选择按钮，选择结果回填输入框；手动输入保留，确认仍由表单提交动作完成。
- 部署与诊断是独立高级管理面，包含当前安装、项目目标、连接检测、能力审计、版本更新和删除。
- 次要信息默认使用披露区，避免长按钮文案或常驻诊断拉长弹窗。

## 滚动与弹窗

- `#root` 不滚动；工作区、表格、编辑器和弹窗主体各自拥有明确滚动面。
- 滚动面预留稳定滚动条槽位，避免滚动条出现或消失造成布局晃动。
- 弹窗使用单一骨架：标题固定、主体可滚动、底部动作固定。不要在弹窗内引入无边界的第二个主滚动容器。
- 宽窗口优先使用分列或表格；窄窗口允许自然换行或局部横向滚动，但不应压缩关键操作至不可读。

## 状态与反馈

- 加载、空状态、成功、警告、失败和恢复状态都有明确文本。
- 加载占位与最终内容同尺寸（`.asb-skeleton`），不以单行文字代替表格、图表或表单的加载面，避免数据到达时页面重排；骨架脉冲只在读取期间存在。
- 破坏性动作出现在其作用对象之后，永不作为阅读流的首项；与安全动作分行并以细分隔线隔离，始终使用 danger 变体，不借用 primary/secondary 样式表达破坏性。
- Toast 报告瞬时结果；需要后续处理的失败必须保留可定位入口。
- 动效只用于状态过渡和内容层级，不使用循环装饰动画。尊重 `prefers-reduced-motion`。

## 可访问性与性能

- 所有可操作元素可通过键盘访问，焦点在弹窗打开和关闭时可预测地移动与恢复。
- 图标、颜色、进度和状态都必须有文本替代或可访问名称。
- 交互优先使用语义 HTML 和原生输入控件；复杂菜单和弹窗使用受控的可访问组件。
- 视觉样式不使用 `filter` 或 `backdrop-filter`。优先使用实体面、边框和轻量过渡，避免不必要的重绘。

## 禁止项

- 不创建平行的按钮、表格、弹窗、时间或配置预览组件。
- 不保留废弃字段、双读双写、兼容分支或第二个事实来源。
- 不让 UI 直接修改真实客户端配置。
- 不用模糊错误、假成功、隐式默认值或自动覆盖掩盖恢复问题。
- 不将本地控制台扩展为云服务、公开网关或自动化代理。
