# 更新日志

本文件是 GitHub Release 与应用内更新说明的唯一来源。每次发布在顶部新增一个与发布标签匹配的版本段；每段仅列出实际包含的“新功能”“改进”“修复”分类，只有发生不兼容调整时才加入“重要变更”。

## [0.2.8] - 2026-09-21

### 重要变更

- 桌面偏好设置采用完整的当前格式，必须包含界面缩放、全局快捷键与启动页字段。旧格式或损坏文件会明确提示无法读取，需在界面中确认修复；修复只重置应用偏好，不影响供应商档案和客户端配置。

### 新功能

- 偏好设置新增 90% / 100% / 110% / 125% 界面缩放、全局快捷键录制与启动页选择；快捷键可显示或隐藏主窗口，启动页支持供应商列表或上次访问的顶层页面。
- 降智雷达新增本地检测历史：每次检测以「批次 + 单次运行」两层结构保存到本机数据库（state/codex/probes.sqlite3，含题目全文、期望答案、判分规则版本、CLI 版本与当时配置快照，不保存凭据）；刷新或重启后从历史重连当前批次，异常退出的批次标记「已中断」并保留已完成结果、按已保存会话 ID 补读用量，重启不自动续跑；每次调用前校验配置指纹，变化即停止后续调用；运行失败、取消与中断记「未判定」，与回答错误分开。历史入口位于雷达头部，支持时间/档案/状态筛选、分页、详情查看、「使用此题重新检测」与显式删除（仅删除雷达自身记录，运行中的批次不可删除）；保存失败停止后续调用并提供重试入口。
- 偏好设置新增「运行事件记录级别」：以调试/信息/警告/错误/静默五档控制应用运行事件写入日志文件的阈值，保存后立即生效；该阈值此前已持久化但无界面暴露。
- 偏好设置新增「启动时最小化到托盘」：主窗口改为启动时创建为隐藏、由启动流程按设置决定是否显示，开启后应用启动即驻留系统托盘；设置文件读取失败时主窗口照常显示，恢复路径始终可达。
- 本机网关页重构为单一拓扑主图：客户端、网关与供应商画在同一张图上，以母线、连线与协议芯片表达每条路由的客户端协议 → 上游协议并标注「协议转换」或「本机转发」；连线流量计数只统计近 60 分钟匹配活动路由的内存遥测样本，零流量不显示计数，直连流量不伪造为网关遥测；无路由时供应商列以虚线空位说明直连状态。图表带以等宽大数字与图表呈现累计请求（60 分钟分钟柱趋势，柱高随窗口内最大计数满高并随轮询平滑过渡）、失败请求、p50/p95 耗时条与上游协议分布。

### 改进

- 客户端配置每行设置下方的「查看当前配置代码 / 待应用代码差异」按需展开区新增展开与收起过渡：高度、位移与透明度与编辑器折叠模块一致，箭头同步旋转，并遵守减少动态效果偏好；同时移除选择控件与展开区之间的细分隔线，改以行距衔接。
- 供应商档案行的两个行内面板（供应商测试、用量读数）新增展开与收起过渡：高度、位移与透明度与编辑器折叠模块一致，内容保持挂载至收起动画结束，并遵守减少动态效果偏好。
- 供应商概览顶部连接卡的渐变流动在切换客户端或进出列表视图时相位连续，不再从固定相位重启造成跳变。
- 设置 → 诊断 → 配置与环境按客户端组织事实区块：客户端名与状态胶囊同在区块头行，区块之间以细分隔线分隔，事实行复用全局键值行契约，模块卡内不再嵌套盒层；配置状态与运行环境的加载骨架按最终事实行的行盒占位（此前骨架数量与实际客户端数不符，数据到达时页面重排）；状态胶囊改用胶囊语义形状；供应商导入的扫描结果沿用同一区块解剖，可读取状态着安全绿。
- 诊断 → 日志页签整理：级别标记改为文字胶囊，读取与打开日志文件夹失败以错误红文本行呈现，空态去除嵌套盒层并换用记录图标；清除从未生效的表格内部滚动声明，表格随页面滚动面滚动、仅保留横向溢出。

- 供应商余额和 Codex 官方额度改由后端按档案间隔刷新，列表与托盘共用缓存；页面切换不额外查询，间隔为 0 时仅手动刷新。
- 重写中英文 README，补齐下载安装、首次使用、缓存与历史、备份范围及各平台开发构建指引。

### 修复

- 加固降智雷达执行与保存：调用前、执行中及判分前检查配置与有效路由，变化时终止调用且不判分；按实际 CLI 会话读取用量，缺失数据显示未知。结果与批次结束状态原子保存，保存失败保留结果并阻止新检测和正常退出；应用异常退出时回收检测进程。
- 修复 Codex 跨档案子代理模型的选择、准入与用量归属：排除账号绑定目标，使用目标档案的认证、协议、映射和运行参数；HTTP、WebSocket 与 compaction 共用目标解析，失败不回退主模型。
- 保存子代理目标档案时同步更新受影响的活动模型目录；分别记录被编辑档案与活动配置所有者，失败回滚和中断恢复使用正确的文件与路由。
- 补齐客户端设置备份与恢复，修正配置路径、字段归属和脱敏处理，保留不属于当前操作的配置内容。
- 修复本地 Windows 发布构建中检测配置快照的 Rust 所有权错误，并移除未使用导出。

## [0.2.5] - 2026-09-20

### 新功能

- 用量页「Codex 重置信号」重构为与来源站点一致的预告卡：大字结论按置信度分桶显示（如「≥70% 是」）、到账窗口（日期级预告按公告日起 24 小时窗口）与倒计时/相对时间、右栏事实列表（最近已完成重置、最近检查、预计下次），底部附 53 周 UTC 重置热力图（未来占位日隐藏，色块含无障碍名称）。
- 重置预告不再依赖 `resetTimeline.nextSchedule` 单一指针：指针缺失时回退取最新未被完成记录或抑制清单消费的 `reset_scheduled` 公告事件，与站点口径一致。

### 改进

- 公开 feed 解析收紧为单一严格契约：热力图数据必填并校验时区（仅 UTC）、周数、日期与等级（0–4），不满足即报错而非静默钳制；不符合新结构的旧缓存直接判无效并刷新重建；事件 post id 只认纯数字 `source.postId`。
- 官网截图组件记录各截图真实像素尺寸，图片加载完成前布局不再跳动。

## [0.2.4] - 2026-09-20

### 重要变更

- Codex 默认子 agent 模型改为跨档案路由引用（`subagentRoute`），不再接受档案内模型字符串参数：`agents.default_subagent_model` 参数拼写已退役，存量档案如在该参数位置存过值，加载时会明确报错并指引在运行参数中重新配置（不做自动迁移）。携带子代理路由的档案一律经本机网关激活。

### 新功能

- Codex 供应商档案的默认子 agent 模型支持跨供应商路由：以「目标档案 + 目标模型」的唯一形态保存，可指定任意已保存档案目录中的模型（含本档案，默认收起的「跨供应商选择（高级）」开关控制候选集）；子代理请求由本机网关按请求解析并转发到目标档案（`asb:` 前缀路由 id），失败不回退主模型、不做跨供应商故障转移，用量归因到目标档案，被引用模型同步合入客户端模型目录与 `/v1/models`。引用不可解析时在保存、切换、请求三处显式报错，被引用档案在路由存在期间不可删除。
- 用量页新增「降智雷达」：通过本机真实调用 Codex CLI 批量实测当前激活配置（内置三道验证题或自定义题目，3/5 次可选，随时取消），展示每次通过结果、reasoning tokens、实测消耗与耗时；检测会话如实计入「消耗统计」，结论定位为参考信号而非模型判定。
- Windows 安装包新增 MSI 引擎版本：自定义安装引导界面可内嵌 MSI 引擎（`tauri:build:windows:msi`），按本机所有用户安装到 Program Files、安装时由 Windows 请求管理员权限，与原有 NSIS（当前帐户、自选目录）两种产物并存。
- 配置状态面板与启动流程新增「恢复阻塞」提示：存在未完成配置事务时明确告知配置写入暂不可用，并指引自「设置 → 备份恢复」完成恢复。
- 本机网关的 WebSocket 通道接入与 HTTP 完全一致的 Codex 转发策略：相同准入、多端点重试、账号路由解析与图片不受支持时降级重试，失败时正确下发 `response.failed` / `response.incomplete` 终止事件。

### 改进

- 客户端偏好设置、恢复默认值、配置修复、手动应用与 Claude 集成标记写入统一纳入与供应商切换相同的持久事务日志：写入中途异常后可在备份恢复中找回；Codex 内部自动切换改用与界面完全相同的完整预览校验（含认证文件哈希）。
- Anthropic→Responses 流式转换加固：上游离散索引重编号为连续 `output_index`，重复 block 索引、stop 后继续 delta、未知块类型均按错误处理；自定义工具补发增量事件；终结快照复用流式阶段已发出的相同 item 标识；JSON 与 SSE 共享渲染抽取去重约千行。
- 备份历史列表为各备份原因补上中文说明（如「供应商切换前备份」「恢复默认值并移除界面外字段前备份」）。
- 供应商连接卡的动态渐变重做为两倍卡宽图层匀速循环流动：循环点在几何上无缝、不再依赖缓动漂移，双卡呈反相流动，减弱动效时静止呈现。

### 修复

- Claude「恢复默认值并移除界面外字段」深度重置不再误删技能覆盖（skillOverrides）与凭据助手（apiKeyHelper、otelHeadersHelper）。
- 恢复旧版 Codex 供应商格式的备份不再被静默迁移改写：直接明确报错并指引重新应用供应商，避免路由信息在迁移中丢失。
- 修复损坏配置前先完整备份原始损坏内容（备份不再要求语法合法，仅替换内容需合法）；渲染写入改为先落恢复日志、再写文件、最后提交应用状态，失败时目标文件保持原样且恢复记录保留。
- 状态为 incomplete（截断）的响应不再进入 Codex 续写历史。
- 启动时待恢复事务处理失败不再阻断应用启动，改为记录日志并保留事务与备份。
- Claude 目标配置文件缺失时按空 JSON 对象处理，保证哈希与解析一致。

## [0.2.3] - 2026-09-18

### 新功能

- 供应商导入与导出支持完整配置迁移：「导出 SQL」将全部供应商（Claude、Codex 第三方与官方登录记录）的存储档案原样写入单一 SQL 文件，模型路由、能力位、原生用量脚本与排序位置零损失；新增「导入 SQL」页签，在其他设备选择导出文件即可预览并导入，已存在的档案可勾选覆盖更新（官方登录记录每客户端仅保留一条，与他机记录冲突时跳过并说明），全程无需命令行。文件包含 API 密钥。
- 供应商导入导出接入原生文件夹选择：「本机数据库」可扫描任意包含 cc-switch.db 的文件夹（默认仍是主目录位置，导入沿用同一文件夹保证预览新鲜度，扫描结果显示实际数据库路径）；「导出 SQL」选择目标文件夹后自动填入默认文件名并保留已输入的文件名。

### 改进

- 供应商列表工具栏的「导入」入口改名为「导入 / 导出」，与二级页承载的导入、导出职责一致（Claude 与 Codex 供应商页共用同一入口）。
- 设置页左侧分类导航按使用频率重排为偏好设置、客户端工具、本机网关、备份恢复、诊断、关于，并重写词条消除歧义：「应用偏好」改为「偏好设置」（避免"应用"的动词误读）、「客户端管理」改为「客户端工具」（与主导航「客户端配置」页区分）、「关于与更新」改为「关于」；备份面板标题与 DESIGN.md、README.md 同步。
- 导入供应商页来源切换接入全局分段 tab 契约：轨道高度恒定、切换不再拉伸位移，键盘方向键导航随契约生效。
- 移除已被官方登录命令取代的预留账户管理与台账查询死代码（约 2700 行），Rust 侧编译零警告；会话用量回放同步保留待接入。

### 修复

- 修正云端备份恢复确认弹窗中关于旧格式备份“先升级再保存”的失实说明，并移除恒不触发的升级重传路径：应用从不升级旧格式备份，快照版本不符时直接拒绝恢复；恢复成功提示同步精简。
- 修复供应商编辑/新建界面模型下拉的浮层错位与遮挡：菜单改为固定锚定在触发器上（空间不足向上翻、随滚动跟随、不再被编辑器容器裁剪），并自适应加宽以完整显示长模型 ID；Codex 运行参数与测试请求的模型下拉同步受益。
- 修复托盘供应商行宽度被按钮基础样式覆盖导致的布局压缩：行改用无样式按钮变体，由自身样式完整接管布局与悬停态。
- 修复已启用档案行「已应用」徽标随名称长度漂移且底色与行高亮同色导致的悬空错位：徽标固定在模型列行首、与各行模型名同起点对齐，并在高亮行上以加深底色与描边呈现。

## [0.2.2] - 2026-09-18

### 新功能

- 「恢复为客户端原生默认值」新增高级范围：确认前可开启「同时移除界面外字段」，应用会重生成合并预览并完整列出标准设置与界面外字段的移除项，最终确认按钮标注双重范围。
- 扩展部署命中已有未托管原生条目的键位时直接收编该条目：基线记录接管前原值作为逐字恢复点，替换须经前后差异计划确认，已托管键位冲突仍拒绝。
- 模型列表解析统一支持 Codex 风格目录的 `models[].slug`（ChatGPT backend、智谱 Responses）与 Copilot 的 `vendor` 字段：无效条目剔除、重复合并、空目录报错。

### 改进

- 切换供应商不再使用确认弹窗：确认区直接在所属行内展开并展示脱敏写入差异，Codex 与 Claude 列表行为一致。
- 本机网关页按「主机面—接线排—仪表带」重构：监听状态、p50/p95 耗时、上游协议分布与最近请求一屏可读，监听失败时整体转警示并内嵌恢复动作。
- 供应商编辑、运行参数、用量查询、导入供应商与扩展发现等二级页共用全页编辑框架：固定返回与动作栏、二级页期间草稿保活，扩展发现从弹窗改为全页工作面。
- 托盘浮层重构为单行供应商项：名称与缓存的用量余额内联显示并限定面板高度，不再展开用量详情或虚构数值。
- Codex 第三方档案行获得与 Claude 一致的行内动作（编辑、测试连通性、配置用量、删除），展开状态由应用设置持久化。
- 配置审阅收为单一模块并支持原地编辑：仅允许修改界面未拥有的字段，真实配置无法安全解析时转为自动修复入口。
- 主导航与页面术语更名：「供应商」→「供应商切换」、「客户端通用配置」→「客户端配置」、「会话」→「会话记录」，README 同步。
- 自动端点候选与档案基础地址相同时不再经本机网关：单一地址的档案保持直连路由，不占用网关。

### 修复

- 修复 Codex 路由卡片服务地址显示「地址无法解析」：`openai_base_url` 归类回普通字符串，脱敏与发现缓存不再整体遮蔽路由地址，卡片恢复显示上游主机。
- 修复安装引导界面品牌图仍为旧双半圆图形：自绘品牌图改为与应用图标一致的蓝紫双路径交叉符号，完成页「打开应用」改为默认勾选。

## [0.2.1] - 2026-09-17

### 新功能

- 供应商编辑器增加独立的「配置运行参数」二级页面。模型行为、输出、网页搜索、请求选项及 Codex 上下文窗口随供应商保存；返回编辑器时保留草稿，最终与连接信息在同一次「保存供应商」中提交。
- Codex 供应商运行参数增加「默认子 agent 模型」与「默认推理强度」：前者可从当前供应商获取的模型列表下拉选择，后者使用滑块；它们随供应商切换投影到 `agents.default_subagent_model` 与 `agents.default_subagent_reasoning_effort`。
- 设置新增「子 agent 运行」独立模块，集中控制启用、每会话最大并发数和中断说明；生成脱敏全文预览并经确认后才写入真实 Codex 配置。
- 供应商列表和编辑器共用「供应商测试」：可先做不带密钥的地址连通检测，再显式准备、发送和取消一次真实模型请求；支持按需获取模型、查看脱敏后的回复、状态码、耗时、请求标识和可操作的失败分类。
- 扩展工作区补齐 Skills / MCP 的多目标部署、批量检查与更新、按绑定固定 Skill 内容版本、来源浏览、接管本机安装、字段级 MCP 编辑、便携包导入导出及历史恢复入口；所有客户端写入仍先显示计划与差异并要求确认。
- Codex 官方订阅额度支持按供应商设置自动刷新间隔；仅官方 Codex 档案可配置，保存该间隔不会重新应用客户端配置。
- 顶层界面收敛为「供应商、扩展、会话、用量、设置」五个工作区；供应商页保留 Codex 与 Claude 的双连接状态卡，设置页归集偏好、备份、诊断、网关、日志和更新入口。
- 标题栏左上角产品名旁新增 `Beta` 版本标识。

### 改进

- 更新 Agent Switchboard 图标为双路径切换符号：网页 favicon、应用内品牌图、桌面端与安装包的各规格图标都从同一份 SVG 源同步生成，构建命令会先校验并刷新图标资产。
- 供应商列表将可选的用量或账户摘要移动到固定信息轨下方，名称、模型、服务地址与操作保持稳定扫描顺序；窄窗口下的信息密度和操作可达性更清晰。
- README 改用当前源码和隔离演示档案生成的实际界面截图，覆盖供应商、客户端通用配置与切换预览；文档明确说明所有截图数据均为虚构沙箱内容。
- 供应商参数与客户端偏好明确分离：每个 `ProviderProfile`、草稿和落盘文件都携带完整的供应商参数；通知、历史、审批、沙箱和全局指令继续按客户端共享。各自的「自动」只移除自己拥有的配置键。
- 默认子 agent 模型和推理强度从全局直接写入路径移至供应商归属；全局模块不再读取或覆盖这两项，避免非 OpenAI 上游经 Codex harness 接入时残留前一供应商的模型标识。
- 当前启用供应商的连接或运行参数改动统一走「准备 → 脱敏预览 → 确认提交」事务：提交前重新核对档案、活动身份和客户端文件哈希；任何阶段失败都会恢复已经改变的档案、客户端配置和网关状态。
- Codex 统一使用 `openai` provider 标识并保留官方登录：官方订阅直连，第三方 `Responses`、Chat Completions 和 Anthropic Messages 请求经受限的本机网关转发；网关同时处理 HTTP/SSE、WebSocket、多轮上下文和 Codex 历史压缩。
- 网关端口保持为唯一的持久化状态。修改端口时会先占用候选端口、预览两端客户端地址、确认后原子切换监听器；未确认、失败或中断都会释放或恢复对应状态，不自动换号。
- 档案、客户端偏好、云端快照和本地扩展库的升级路径收敛为一次性、可验证的当前契约迁移；迁移不完整、字段混合或外部改动会保留原数据并明确阻断，而不是运行时兼容两套结构。
- 模型选择器、供应商连接地址、运行参数目录、诊断和官网配置展示均从同一后端目录与类型化 IPC 生成，减少前端手写字段与错误的跨模块归属。
- 双连接状态卡、供应商编辑、扩展弹窗和窄窗口布局完善了深浅主题、键盘焦点、减少动态效果和跨工作区草稿保留；供应商页面不再因列表选择或未保存草稿误报当前连接。

### 修复

- 补齐客户端当前配置读取的内容修订与语法状态契约，配置审阅和自动修复能够基于同一份后端事实判断是否可继续操作。
- 清理重复的减少动态效果媒体规则，并收紧配置编辑器中的数值归约类型，避免构建时的隐式类型漂移。
- 修复 Windows 自绘安装器向 MSBuild 传递生成版本信息时使用项目相对路径的问题，正式安装包构建现在始终传入绝对路径。
- 修复扩展 stdio 检查无条件导入 Windows 进程扩展导致 macOS / Linux 发布构建失败的问题；Windows 隐藏子进程窗口行为保持不变。
- 修复供应商保存后仍显示“待重新应用”、隐藏用量编辑器响应其他页面 Escape、保存用量后客户端选择错位，以及外部变更后仍可能使用过期候选的问题。
- 修复网关端口切换中的监听器交接、事务恢复、占用端口诊断和路由状态一致性问题；端口冲突不再阻止应用打开。
- 修复跨协议请求的认证、端点拼接、模型列表请求、最小请求模式、推理载荷与流式响应转换边界；错误、日志、预览和真实请求结果持续脱敏。
- 修复扩展库迁移、批量恢复、接管已有安装、MCP 条目恢复与来源发现中的数据保真问题；未知原生字段、项目范围和凭据槽位不会因受管操作被静默丢失。
- 更新供应商、设置和官网截图及配置装配展示，移除已无消费者的旧概览、通用设置、导航“更多”、旧探测面板和重复设置入口。

### 重要变更

- Codex 第三方档案现在要求完成文件模式的官方登录，并统一经本机网关工作；切换、回滚、端口修改和启动恢复不再写入、删除或恢复 `auth.json`。不支持旧的自定义 provider 投影、第三方直连、供应商 WebSocket 开关或认证双文件执行器。
- 默认子 agent 模型与默认推理强度不再是全局运行设置。已有 Codex 档案会在启动边界补为两个“自动”供应商参数；只存在其中一个、或升级前后参数混合的档案会被拒绝，原数据保持不变。
- 旧「概览」「通用设置」「发现」顶层页面及导航“更多”已删除。发现与导入归入供应商，客户端偏好与诊断归入设置，全局指令归入扩展；旧路由和旧组件不保留兼容入口。
- 旧版供应商文件、客户端偏好文件、快照和扩展存储只在明确升级边界转换；当前运行时不再双读、双写或猜测旧形状。更早、不完整或无法证明归属的数据会被拒绝而非静默套默认值。

## [0.1.15] - 2026-09-07

### 新功能

- Codex 默认固定为 `openai` 并保留官方登录；所有第三方协议通过本机网关，支持客户端 WebSocket、多轮续接和模型摘要压缩。
- 网关使用持久化的本机回环端口；新安装默认 `127.0.0.1:47821`，并可通过预览、确认和可恢复事务修改端口，同时同步更新受管客户端地址。
- 扩展发现返回一次扫描快照，按 Skills / MCP 标签、客户端和搜索条件展示本机结果；发现的扩展可复制到扩展库或管理现有安装。

### 改进

- 主导航整合为供应商、扩展、会话、用量、设置，默认进入供应商；导入、全局指令、额度和维护工具按任务归位，移除概览与主导航更多菜单。编辑草稿跨工作区保留，客户端偏好可保存后直接预览应用，确认前不写客户端配置。
- 原概览的 Codex / Claude 渐变星光双卡保留在供应商列表顶部，合并连接状态与诊断入口，移除重复摘要；离开列表停止动画，减少动态效果时展示静态星光。
- 发现诊断改为带稳定编码和修复资格的结构化契约；默认收起为单行摘要，信息提示不计入警告数量。摘要行本身即展开 / 收起控件，删除与之重复的独立「展开」按钮，行尾只保留「一键修复」。
- 全站文本输入框统一由全局单行 / 多行组件承载：网关端口弹层、云端备份、MCP 表单与字体、模型搜索框接入同一字段外观；提示词编辑器尺寸改由父级容器拥有。
- 网关页监听信息与端口修改说明收进同一内嵌信息面；「近 60 分钟请求」趋势在零请求时显示文字空态，有数据时坐标轴固定每 10 分钟一刻度。
- 设置页更新区在发起检查前即显示当前运行版本，版本号读取自应用进程自身。

### 修复

- 修复网关端口切换中的监听器、事务恢复和并发状态问题；端口冲突不会阻止应用打开。
- MCP 一键修复会在提交前重新确认目标槽位仍为空，且修复后同步共享文档的受管基线，避免覆盖外部新值或遗留错误诊断。

### 重要变更

- 使用 Codex 第三方前需完成文件模式的官方登录。切换、回滚、恢复和改端口不再写入 `auth.json`，不再提供第三方直连、自定义 provider 或供应商 WebSocket 开关。
- 旧 Responses 档案字段在启动边界一次性转换并保留原目录备份；旧认证联动备份禁止恢复。历史会话不迁移，旧版网关推理载荷和跨后端加密内容不保证续接；第三方使用期间需保持本应用运行。

## [0.1.14] - 2026-09-06

### 新功能

- 新增「扩展」工作区，集中管理 Skills 与 MCP：可从本机发现或导入来源，按客户端和作用域预览、确认部署，并保留可恢复的操作历史。

### 改进

- 供应商编辑保存收束为草稿预览、原位确认与统一提交；修改当前生效的运行参数时，档案、客户端配置和网关状态在同一可恢复事务中完成。
- 发布前收紧应用界面设置的持久化契约：缺失、未知或不完整字段不再静默套用旧默认值，而由显式修复入口处理。
- 删除已无运行时消费者的通用设置旧基线元数据；界面与执行器继续从同一份当前设置目录派生规则。

### 修复

- 供应商用量的定时查询改由桌面端调度线程唯一拥有：主窗口隐藏、关闭到托盘、切换页面、切换客户端标签或打开编辑器都不再中断自动刷新，托盘面板始终按配置间隔更新；查询失败保留上次读数并仍按原定间隔重试。
- 云端恢复遇到旧版或未版本化快照时，只在恢复边界按既有语义完成一次确定性升级，并立即回写为当前加密快照；本机运行时不保留旧档案路径。

## [0.1.13] - 2026-09-06

### 新功能

- 供应商档案支持 `Responses`、`Chat Completions` 与 `Anthropic Messages` 三种 API 格式；非原生组合经受限的本机回环转换网关完成路由。
- 加密云端备份现在保存三协议档案字段，并可将符合既有语义的旧快照确定性升级为当前快照。

### 改进

- 跨协议供应商的变更预览与确认弹层完整显示网关警示文本：明确说明切换后服务地址为何会被改写为本机 `127.0.0.1` 回环网关地址，且警告先于差异呈现。
- 新建或编辑供应商选择「API 格式」时，按所选协议显示接口说明；与客户端原生协议不同时同步提示切换后将经本机协议网关转换。
- 云端备份页的 Supabase 配置教程默认收起，需要时可直接展开查看完整步骤。

### 修复

- 点击未启用供应商不会再留下持续高亮；只有实际生效的档案显示「使用中」状态。

## [0.1.12] - 2026-09-05

### 新功能

- 应用检测到新版本时，可在设置页直接阅读按类别排版的更新说明。
- GitHub Release 与应用内更新说明使用同一份版本日志。

### 改进

- 增加 Codex 与 Claude Code 的完整沙箱回归验证，覆盖通用配置保存、新建供应商、切换、备份和撤回。

### 修复

- 修复从托盘切换供应商后，主窗口的“使用中”状态必须重新聚焦才能更新的问题。
- 修复 Claude Code 设置了 `CLAUDE_CONFIG_DIR` 后，配置切换和全局指令仍使用默认目录的问题。

## [0.1.11] - 2026-09-05

### 新功能

- 新增系统托盘浮层，可查看 Codex 与 Claude Code 的当前供应商并快速切换。
- Windows 安装包改用应用自绘安装引导，并保留签名自动更新载荷。

### 改进

- 提升托盘浮层在不同显示器与窗口焦点状态下的可用性。
