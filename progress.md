# Agent Switchboard 目标与进度

本文件是项目**唯一**的阶段目标文档。不得创建平行的 `GOALS.md`。`README.md` 说明产品；本文件记录每个阶段必须达成的目标、验收方式以及何时可开始下一阶段。

## 工作规则

**供应商预览只读化收尾（2026-09-12，完成）：** 接续 zcode `sess_91fa839e-bf02-4993-88dd-1f2e9955a393` 的未完实现。Codex 第三方、Codex 官方登录与 Claude 行内「变更预览」均只展示假设差异，没有「确认切换」「取消」操作，再次点击预览图标收起。两端共用 `useSwitchPreview`，只读预览与显式启用的意图绑定到各自候选；「启用」始终重新读取候选，即使同档案的只读预览尚未完成也不复用旧请求，随后打开统一的 `SwitchConfirmSheet`。取消确认保留只读差异，离开列表、撤回预览或写入失效后，迟到响应不能恢复确认。生效行补齐重新启用入口，并保持键盘可达；「保存并预览应用」只保存应用内偏好、跳回只读预览，不再自动进入切换确认。Codex 行渲染按职责移入 `CodexProviderRows`，保留官方登录、额度、编辑、删除与排序行为。README、DESIGN 及端到端预览/启用/确认消费者同步更新。

验证：相关 10 个前端测试文件 / 115 项通过，覆盖三类供应商只读预览、取消后再次预览、重新启用、精确候选哈希、设置预览及异步请求失效；`npm run typecheck` 与 `npm run build -- --logLevel warn` 通过，构建仅保留既有大 chunk 提示。三个改动的 E2E 文件 `node --check` 通过，本次文件范围的 `git diff --check` 通过。未运行原生桌面 E2E 或 Rust 测试，未改后端配置写入路径、未对真实客户端配置或凭据执行写入测试；保留工作区其他并行改动，未提交或发布。

**全局排版规则与守卫（2026-09-11，用户指令「排版规则」，未提交）：** 本轮先彻查扩展工作区与全局字阶 / 间距现状，再落地规范。诊断结论是「规范有洞，扩展界面把每个洞都踩了一遍」，因此改的是洞本身，不是逐处样式微调。

字阶补洞与封顶：`tokens.css` 原只有 5 档且**缺失 caption 档**，却有 3 处 CSS 引用不存在的 `--asb-text-caption`（`panels.css` 两处、`app-settings.css` 一处）与 1 处不存在的 `--asb-text-primary`。`font: var(--未定义)` 在计算值层面直接失效，这些文字静默继承父级字体——「看着不对但说不出哪不对」的典型来源。现补齐 `--asb-text-caption`（`400 12px/1.4`）、修正幽灵引用，并把字阶收紧为封闭五档（`title` 18 / `section` 15 / `body` 15 / `label` 13 / `caption` 12，另 `code` 为字族而非新档）；`--asb-text-signal`（72px）明确登记为**唯一展示数值**，不属于字阶。标题只有三个类并全部由 `panels.css` 唯一持有：`.asb-panel-title`、`.asb-section-title`、新增 `.asb-group-title`（卡片内字段簇，取标签档 600）。

标题泄漏修复：`root.css` 原先只重置 `body { margin: 0 }`，没有 `h1`–`h6` 重置，也没有全局认领规则，于是任何没有被类或元素选择器认领的标题都回落成浏览器默认 `1.17em` 粗体并自带外边距，静默压过字阶（`ExtensionCheckPanel` 的「连接检测」`h3` 是典型漏网项）。现全局重置标题的 `margin` 与 `font: inherit`，并收编全部 34 处无类标题；同时删除 7 条元素选择器局部设权的字体规则（`.asb-session-detail-head h3`、`.asb-official-directory-entry h4`、`.asb-request-receipt h4`、`.asb-update-notes-section h4`、`.asb-usage-history h4/h5`、`.asb-cloud-backup-guide-list h4`、`.asb-about-product h3`），标题字号不再有第二所有者。

控制高度三档封顶：扩展列表页原先自上而下堆着 40px 搜索框、32px 过滤 Tabs、30px 数量胶囊、28px 搜索清除钮与 32px 行内图标钮，四档高度、五种控件语言同屏。现收敛为标准 40（输入 / 按钮）、紧凑 32（分段 / 胶囊 / 行内图标钮）、微钮 28（仅装饰性字形钮，命中区经伪元素投影回 40）。30px 胶囊归入 32，`.asb-row-menu-item` 的 36px 提升到 40 以满足 §10 最小交互目标。

间距与尺寸守卫：把「禁止裸 px」变成可执行契约。`tokens.test.ts` 新增三项守卫——消费的 `--asb-*` 必须已定义（能直接抓住 caption 这类幽灵引用）、间距 / 尺寸属性禁止裸 px（白名单仅 tokens 所有者、图表层隔离区、`1px` 发丝线、图标选择器尺寸；组件内部固有几何须在声明处以 `--asb-<component>-<thing>` 具名）、每个标题必须带类。另加第四项：标题的字体只能由三个标题类设置，禁止元素选择器局部设权。为支撑守卫新增 `--asb-space-hairline`(2)、`--asb-space-concentric`(3)、`--asb-icon-size-xs…display`、`--asb-radius-*-inner`、`--asb-dialog-width-*`、`--asb-detail-aside-width` 等令牌，并收编全部 118 处样式表裸 px（数值除上述两处外逐一同值替换）。

扩展列表页与详情弹窗：列表上方三条工具带（数量条 / 搜索行 / 结果行）合并为**一条**——左侧视图控件（搜索 + 客户端过滤），右侧整库动作（两个客户端批量胶囊 + 全部更新），总数降为列表下方一行注脚。客户端过滤从 `tablist` 改为与会话页一致的分段单选，并提取唯一组件 `components/ClientFilter.tsx`（`SessionManager` 的内联重复实现一并删除）；`tablist` 承诺的是面板切换语义，筛选没有该语义。详情弹窗层级修复：弹窗 `h2` 与 `asb-ext-detail-head` `h3` 原同为 18px 无层级，现标题走 `.asb-dialog-title`、体内小节走 `.asb-section-title`、侧栏与卡片簇走 `.asb-group-title`。

四个全局原语：弹窗骨架（`.asb-dialog-backdrop` / `.asb-dialog` / `.asb-dialog-surface` / heading-body-footer，宽度三档 440/620/1000）提升为全应用唯一模态解剖，`asb-sheet` 的确认单与端口修改弹层一并接入，`asb-ext-modal*` / `asb-ext-dialog*` 局部骨架删除；`.asb-empty-state` 为唯一空态形态（字形 + 小节标题 + 一句说明 + 至多一动作），`.asb-empty` 重新定义为**表面已有内容时的行内注记**（取标签档，不再冒充空态）；`.asb-fact-row` 取代各地自造的键值行；`.asb-num` 承载 `tabular-nums`。

顺带修复的所有权违规：`components/session/session-content.ts` 私有一份 `clientName`，对 Claude 返回「Claude Code」，而 `lib/client-name.ts`（自称唯一所有者）返回「Claude」——同一标识符两处含义不同，会话页客户端筛选因此与全站其余控件标签不一致。现由 `lib/client-name.ts` 统一提供 `clientName`（控件短标签）与 `clientFullName`（正文产品名），会话模块改为消费后者。新增 `components/ClientFilter.tsx`；删除已无消费者的 `components/extensions/ExtensionCountBar.tsx`、`.asb-tab-logo`、`.asb-sheet*` 与 `list-filters.ts` 中的 `CLIENT_FILTER_OPTIONS`。

`DESIGN.md §5` 记录封闭字阶表与三档控件高度、§8 记录四个全局原语与客户端筛选 / 客户端名称唯一所有者、§10 改为引用三档高度。**未新增客户端、云同步、遥测、账户体系、自动代理、提供商故障转移、渲染器注入或安装器补丁；未改动任何配置写入路径；未提交或发布。**

验证：`npm run typecheck` 通过；样式守卫 `src/styles/tokens.test.ts` 9 项通过（含新增的未定义令牌、裸 px、标题带类、标题字体唯一所有者四项）；扩展页、扩展组件、会话与应用集成切片通过；前端全量 101 文件 / 653 项中 645 项通过，8 项失败经单独复验确认全部为并行负载下的 10s 超时抖动（`ProviderEditor.models` 与 `CodexProviderEditor` 两文件单独运行 22/22 通过），非本轮回归。当日终态复验（与并行会话的 Codex 编辑器、导入工作流代码合并后）：串行全量 `npm test -- --run --maxWorkers=1 --no-file-parallelism` 101 文件 / 654 项全部通过、`npm run typecheck` 通过；其间一次运行窗口内的 `draft.test.ts` 单点失败经单文件复跑（22/22）与整树重跑确认均为并行编辑快照所致，非代码回归。本轮未执行 Rust 构建或原生 E2E——改动只涉及 `src/` 前端标记与样式、`DESIGN.md` 与 `progress.md`；未读取或写入真实客户端配置、凭据或环境；未提交、打包或发布。

**供应商与全局控件视觉收敛（2026-09-09）：** 当前连接双卡改为实体内容面与 Codex / Claude 身份细边，移除渐变、粒子画布和光晕组件及其令牌、样式和测试；全局面板、控件与操作按钮圆角统一收敛，标题层级调整为 18px，新建供应商按钮改为统一行动蓝实体面。扩展页搜索框、下拉框、按钮与客户端 Tab 统一使用 40px 控件高，标准按钮最小宽 72px，Tab 内段使用 32px，不保留 72px 高度特例。

**`0.2.1` 本地 Windows 安装包（2026-09-08，完成）：** 工作区版本、锁文件、更新日志和自绘安装器统一使用 `0.2.1`。`0.2.01` 不是可用于 Cargo / 发布脚本的规范 SemVer 写法，因此以等价且可验证的 `0.2.1` 作为唯一版本；后续继续使用 `0.2.x` 序列。已生成本地 Windows 自绘安装包，产品版本为 `0.2.1`、文件版本为 `0.2.1.0`，未执行安装、提交、签名或发布。

验证：发布脚本 9 项、安装器契约检查、前端生产构建和 Windows release 构建均通过；最终安装包凭据扫描通过，SHA-256 为 `459EC3065C635F1FF883F4E23B01B429BF527326121C6ADDCCC1A6642CAB7C5E`。

**Codex 子 agent 参数归属调整（2026-09-08）：** 默认模型 `agents.default_subagent_model` 与默认推理强度 `agents.default_subagent_reasoning_effort` 已改为供应商运行参数：模型从当前供应商模型列表选择，推理强度使用滑块，切换时随档案投影，因此通过 Codex harness 接入的非 OpenAI 上游不会遗留前一个供应商的模型标识。旧档案启动时以全 Automatic 值补齐这两个字段，缺一字段或混合前后契约会拒绝升级并保留原数据。设置页的独立「子 agent 运行」资源仅直管 `agents.enabled`、`agents.max_concurrent_threads_per_session` 和 `agents.interrupt_message` 三个全局运行键；它仍通过类型化读取、脱敏全文预览、二次确认、文件哈希、锁、备份、语法验证、原子替换和恢复事务写入。角色表、角色 TOML、未知 `[agents]` 成员和 `agents.max_threads` 都不读取、不映射、不写入。

验证：`cargo test -p asb-core` 264 项、`cargo test -p asb-switch` 74 项、`cargo test -p agent-switchboard config_store::migration --lib` 22 项、隔离开发桥接的 `client_settings_then_new_providers_switch_through_real_commands` 以及前端 6 文件 / 67 项均通过；`npm run typecheck`、`npm run build` 和本模块 Rust 格式检查通过。完整 `cargo test -p agent-switchboard --lib -- --test-threads=1` 另外复现一项既有且无关的 `dev_api::provider_request_tests::requests_complete_and_cancel_through_browser_commands` 失败：测试期望 `web-argument-invalid`，当前实际返回 `web-argument-missing`；子 agent 桥接在同轮通过，未借本次改造改变该 provider-request 行为。所有本模块写入验证均使用隔离临时目录；未读取或修改真实 Codex 配置、凭据或远端服务，未提交或发布；后续本地安装包见本页最新发布记录。

**Codex 固定 openai 与保留官方登录（2026-09-08，代码与本地验收完成）：** provider 标识及对应显示统一为小写 `openai`，不增加保留登录或统一历史开关。官方直连；所有第三方 Codex 协议通过本机网关，要求先完成文件模式官方登录。切换、失败回滚、备份恢复、改端口和启动恢复均只处理配置与应用状态，不写 `auth.json`。删除旧自定义 provider 投影、认证双文件执行器、第三方直连与供应商 WebSocket 字段；仅在明确升级边界转换旧档案与快照并保留原始材料，不双读旧运行时结构，不迁移会话。

网关按档案 capability 接收客户端 HTTP/SSE 与 WebSocket，第三方仅使用 HTTP/SSE，官方凭据不转发，上游 401 隔离为网关诊断。原生 Responses 多轮上下文保留完整输出，minimal 先重建上下文再裁剪；支持旧式与 V2 模型摘要压缩、用途隔离的持久续接密钥、空闲连接关闭和请求限额。配置及应用事务绑定已确认版本与前后哈希，外改阻断，精确备份补偿；恢复旧端口备份重建当前地址，原备份不修改。

验证：Rust 核心 236、执行器 66、桌面 455 项通过，生产 Rust 检查通过；前端类型检查、生产构建通过，前端全量 579/585 后相关失败切片复验 24/24，复用未受影响结果；网站类型检查、24 项测试、生产构建与配置产物校验通过。真实 Codex CLI `0.153.4` 单轮 standard、minimal、Chat 转换 3 项及手动/自动压缩 2 项通过；压缩后重启客户端、恢复已注册动态工具、精确结果回传和认证文件字节不变均已验证。工具夹具只返回固定文本，不执行系统命令。全部写入使用隔离临时目录与假认证，未修改真实配置、凭据、会话或远端业务数据，未提交、打包或发布。目标 Codex 桌面客户端和真实第三方服务尚未做端到端验收；认证支持范围及取消延迟边界见 `docs/codex-openai-gateway-design.md`。本轮验证日志位于 `target/codex-openai-validation/`，最终压缩结果为 `cli-compaction-tests.log`。

**供应商渐变星光双卡保留（2026-09-08，历史记录，已由 2026-09-09 视觉收敛替代）：** 按用户确认的放置方案，将原 Codex / Claude 渐变与星光放在供应商标题下、客户端选择器上方，两端同时展示实际供应商、模型、服务主机名和接入方式。连接警告与诊断入口合入所属卡片；列表选择和未应用草稿不会改写卡片事实。移除单客户端 `CurrentConnectionSummary` 组件、测试入口、样式及旧单客户端 props，不保留并行展示路径。双卡只在列表挂载，编辑、参数、用量查询、导入和离开工作区时卸载；文档隐藏取消动画帧，应用与系统减少动态效果均改为静态矩阵，主题与动态设置即时生效。配色与原有粒子绘制保持既有令牌和算法，未读取或不可用配置不绘制星光。README、设计规范与截图同步更新。

验证：类型检查、前端生产构建通过（保留现存包体积提示）。双卡、导航、令牌切片 30 项通过，增加双卡独立于列表选择与动画生命周期覆盖后相关切片 21 项通过，应用集成 12 项通过，复用未改动测试结果。实际生产前端加隔离内存夹具的浏览器检查通过：1180 / 940 深浅主题、390 窄布局、长名称和模型无横向溢出、减少动态效果静态矩阵、隐藏页面和编辑页卸载、不可读配置无星光、跨页草稿与键盘操作；截图人工核对，页面无未捕获错误。差异、旧引用、冲突标记及本轮源码行数检查通过。材料保留在 `target/navigation-validation/dual-relay-*`。本轮未重新构建桌面包或运行原生 E2E，上一轮 WebView2 驱动连接阻塞仍未解决；没有真实配置写入、提交或发布。

**五工作区导航整合（2026-09-08，实现完成，原生 E2E 受阻）：** 默认进入供应商，主导航唯一保留供应商、扩展、会话、用量、设置。发现与导入归入供应商子界面；全局指令归入扩展；官方额度与公开重置信号归入用量；客户端偏好、备份恢复、配置与环境、网关、日志、关于与更新归入设置。删除旧概览、主导航更多菜单及无消费者的装饰组件，不保留旧路由；配置状态样式改为按当前职责命名。供应商、运行参数、用量查询、双客户端偏好和全局指令草稿跨页保留，隐藏页面停止轮询。修复隐藏用量编辑器响应其他工作区 Escape、保存用量后客户端选择错位，以及应用成功仍显示待重新应用的问题。保存偏好会失效旧候选并刷新状态；「保存并预览应用」重新生成完整预览，仍由显式确认触发执行器。

验证：前端全量首轮 92 文件 / 580 项中 573 项通过，7 个失败均已修复并在相关切片复验；后续导航、偏好及面板 26 项、应用外壳与导航 21 项、草稿与用量 23 项、编辑器与请求错误 9 项及样式 5 项分别通过，复用无关测试结果。`npm run typecheck`、前端生产构建及隔离桌面 release 构建通过，最终构建指纹一致。实际 `dist` 前端加内存夹具的浏览器验证覆盖 5 工作区 × 2 宽度（1180/940）× 深浅主题，20 张截图无横向溢出，键盘折叠与跨页草稿通过；README 更新为当前截图。原生 `client-settings` / `workspace-navigation` E2E 未执行到页面测试：修复测试启动器覆盖驱动 PATH 后，WebView2 会话仍报 `DevToolsActivePort file doesn't exist`。失败沙箱因系统创建的 INetCache 链接被清理保护保留，凭据清理结果和启动日志均已归档；测试驱动进程已停止。浏览器夹具验证不替代原生读写验收。新增 E2E 脚本语法、源码旧引用、冲突标记与差异检查通过。验证材料位于 `target/navigation-validation/` 与 `target/e2e-reports/20260908014923-995040cd-summary.json`；未写真实客户端配置、提交或发布。

**供应商独立参数与二级编辑（2026-09-08 完成）：** 供应商编辑页标题旁以「配置运行参数」按钮进入二级子界面；主表单和参数页共用唯一草稿，返回保留、取消一起丢弃，最终统一走供应商准备 / 提交保存。官方登录面板在子界面打开时保持挂载。`ProviderProfile` / `ProviderDraft` / `ProviderFile` 必须持有完整 `parameters`，Codex 上下文窗口沿用 `modelOptions` 并移到参数子页；目录明确区分供应商与客户端所有权，推理、输出和供应商请求参数随各档案独立保存和投影，Automatic 移除上一档案留下的对应值。通知、终端、历史、审批与沙箱等客户端偏好以及官方目录、全局指令移入「设置 → 客户端设置」。旧通用页、Common 类型 / 命令 / 读取路径及旧截图已删除；共享控件更名 `SettingsFields`，官网生成契约改为 `parameterFields`，文案、截图和直接测试同步更新。

持久化只使用档案内参数与 `configuration/client-settings/{client}.json`；当前布局以 `client-settings` 目录确定边界，当前档案缺字段不能被误认为旧版。仅在启动单实例闸门后，将直接前序布局 / schema 3 快照一次性分配并验证为 schema 4，普通读取严格拒绝旧形状，删除 authScheme 回退和更早快照转换。转换通过隔离暂存、原目录指纹、日志和目录交换恢复中断，保持已确认保存事务的身份；缺少供应商归属的显式参数（包括 false、显式 auto）拒绝转换并保留原数据，云端不会覆盖旧快照；空档案且全 Automatic 正常转换。

验证：`npm run typecheck` 与 `npm run build` 通过；前端全量 83 文件 / 502 项已执行，唯一旧备份文案断言修正后，相关 2 文件 / 15 项复验通过，其余 501 项结果复用；另二级编辑与应用集成的相关测试已通过。`cargo test -p asb-core -p asb-switch` 共 287 项通过，目录文案调整后 ownership 11 项复验通过；最终后端 `cargo test -p agent-switchboard --lib --target-dir target-global-model-settings --quiet -- --test-threads=1` 为 389 通过、4 项既有外部条件忽略。网站类型、24 项测试、构建和生成产物一致性通过。生产构建在浏览器隔离演示环境完成二级入口、两层草稿保留、取消无保存、重开恢复、客户端偏好分离及窄窗口检查，刷新 README 截图；此验证使用演示 IPC，不冒充原生桌面端到端运行。更新的原生 E2E 脚本完成语法检查，未运行桌面 E2E 或打包。修改差异、冲突标记、旧引用与本轮重构源码行数检查通过；写入验证均使用隔离临时目录。

**网关端口管理与可恢复监听（2026-09-07，完成）：** 新安装固定监听 `127.0.0.1:47821`，已有的有效持久化端口继续使用；用户可显式选择 1024–65535，端口只由 `gateway.json.port` 保存。不会自动换号、随机端口或回退到第二个端口来源。

端口修改是独立的端点投影事务：准备阶段直接绑定并持有新套接字，确认阶段在全局客户端配置写入闸门和端口事务锁内重新校验客户端文件、Codex 登录缓存及供应商路由；在维护窗口排空在途请求后，经执行器的锁、备份、语法校验、原子替换和审计边界，仅改写 Codex 的 `openai_base_url` 或 Claude Code 的 `ANTHROPIC_BASE_URL`。模型、通用设置、上游地址、API 密钥与能力令牌均不变。新监听器只有在 HTTP 客户端和请求工作线程就绪后才交接，旧监听器随后停止。端口修改历史不可作为单个客户端恢复，备份页明确要求回到网关页处理。

事务日志只保存端口、哈希、客户端归属和写入历史，不保存密钥；`prepared` 阶段中断回滚，`committed` 阶段中断前滚。回滚目标被外部修改、恢复材料无法清理或日志无效时进入「需要恢复」，不会覆盖当前文件；用户确认保留当前配置后才清理恢复记录，并将保存端口重新对齐到实际监听器。状态页独立呈现已保存端口与实际监听端口；客户端带有本机网关身份但不能和当前路由精确匹配时持续显示「需要修复」，重启后仍然有效。

验证：网关 Rust 切片、端点写入执行器、配置历史、真实命令沙箱、asb-core、asb-switch、网关页与备份页前端切片、TypeScript 类型检查、Rust 编译和改动文件格式检查均已运行；测试写入均使用隔离临时目录。

**扩展持久化迁移与当前契约收口（2026-09-07，完成，用户指令「对理解与实际实现做无兼容无过渡无技术债查漏补缺」）：** 审计扩展库的真实启动、持久化和恢复路径后，删除了隐藏的第二迁移入口：`LocalState` 是唯一 schema 门禁，`ExtensionStore` 只打开已验证根目录，不再自行迁移或 panic。一次性离线迁移改为先完整复制扩展根到暂存目录，再转换并按当前类型、内容目录摘要和实际内容树验证定义、绑定、基线、历史、项目、提交回执与 Skill 内容清单；未版本化 MCP 检查记录及其他非目标文件不会在根目录交换时遗失，非普通文件或目录明确拒绝。中断恢复只在根目录缺失且恰有一个精确目录备份时恢复它；根目录不完整、多个备份、伪备份文件或其他歧义均保留现场并阻断后续写入，绝不猜测、删除或运行时兼容旧形状。存储读取、内容索引与提交校验同步收紧为只接受当前 schema，定义同时复用核心语义校验，历史快照和回执引用也逐层校验；旧版本或目录摘要不符的记录无法穿透到计划或写入器。扩展客户端文件备份同步从误嵌套的扩展库目录收敛到唯一的 `state/backups/extensions/` 根，历史恢复继续使用其记录的精确备份路径。验证：`cargo test --workspace --all-targets --locked -- --test-threads=1` 全部通过（4 项外部条件忽略）、前端全量 65 文件 / 417 项、`npm run typecheck`、`npm run build`、`cargo fmt --all -- --check` 和 `git diff --check` 通过。未读取或写入真实客户端配置、凭据或环境变量；真实客户端隔离加载与跨平台故障验收仍是明确的发布前外部验证门禁，未以模拟兼容路径替代。

**Codex 官方订阅额度按时查询（2026-09-07 完成，用户指令「官方登录也需要加入类似三方的可设置按时查询」；同日按「无兼容无过渡无技术债」复查收敛）：** 官方登录档案获得与第三方 `usageQuery.refreshIntervalMinutes` 同语义的可设置轮询。契约：`asb-core` 供应商 `ProviderProfile` / `ProviderDraft` / `ProviderFile` 三处同步新增可选字段 `official_quota_refresh_interval_minutes`（整分钟；缺省即手动，无第二种关闭形态，0 不落盘；旧档案文件缺字段照常读取，无迁移无补写），`draft_touches_live_configuration` 将其归为应用侧元数据——仅改间隔的保存恒为 `SaveOnly`，绝不触发客户端配置重应用，网关路由指纹因只按路由消费参数计算亦不旋转能力令牌；校验唯一所有者在供应商字段校验：仅 Codex 官方档案允许设置，自定义或 Claude 官方携带即拒绝（`QuotaIntervalRequiresOfficialCodex`），有效域 1–1440（`OfficialQuotaRefreshIntervalOutOfRange`，未设置表示关闭），上界常量因同时约束两个间隔契约更名为唯一所有者 `MAX_AUTO_REFRESH_INTERVAL_MINUTES`，发现与导入的档案一律从手动开始。前端：`CodexOfficialQuotaPanel` 内新增与用量查询工作台一致的「自动刷新间隔（分钟，0 为关闭）」数字输入（blur / Enter 提交、越界或保存失败回退已存值），读取状态机仍由唯一所有者 `useAutoQuery` 按间隔轮询、收起即卸载停止；保存路径把 `saveProfileUsageQuery` 的副本逻辑收敛为通用 `saveProfilePatch`（用量查询与间隔两条调用），`onSaveQuotaInterval` 经 App→ProvidersPage→ProviderList→面板全程必填、无「未提供则静默失败」回退；编辑器草稿经 `draftFrom` 携带该字段、切换路由模式时清空；概览「Codex 官方额度重置」账号级面板保持手动刷新不变。DESIGN.md 官方额度面板事实同步更新。测试：asb-core 新增校验（作用域 + 0/1441 越界 + 1440 边界）与「间隔为纯元数据 + 档案存储往返」契约用例，config_store 新增存储往返与旧文件缺字段读取用例；前端新增面板按间隔轮询 / 间隔 0 关闭 / 有效提交 / 越界回退用例，既有精确载荷断言补齐新字段。验证：`cargo test -p asb-core`（209 通过）、`CARGO_TARGET_DIR=target-isolated cargo test -p agent-switchboard --locked --lib -- --test-threads=1`（302 通过、4 项外部条件忽略）、定向 vitest（面板 / 列表 / 页面 / 工作台）与全量前端（65 文件 / 417 项）、`npm run typecheck`、`npm run build`、改动 Rust 文件 `rustfmt --check` 与 `git diff --check` 通过；工作区级 `cargo fmt --all --check` 与个别文件既有格式偏差属并行会话未提交代码（扩展模块、adapter、gateway 签名行），本轮新增行全部合规；网关 WebSocket 用例仅在全量并发下偶发端口竞争、单线程全绿。未打包。

**扩展页面批量与版本固定收口（2026-09-07，完成）：** 一次性补齐扩展工作区剩余设计承诺，不保留旧入口。安装目标从单选 `Select` 改为 `Checkbox` 多选，一次安装计划覆盖多个客户端与作用域（后端 `targets[]` 契约原样承接，连接检测以第一个所选目标运行）；Skills 列表行首新增选择框，多选后工具条出现「检查更新」，经唯一后端命令 `check_skill_updates(definitionIds[])` 批量返回——单资源调用即批量为一份，旧单 id 签名删除，`dev_api` 桥接同步——逐项给出新提交、文件级差异（`SkillUpdateReport.changedFiles`：新增 / 修改 / 移除，由后端比较库内当前版本与来源候选）与失败原因，单项失败不阻断其余项；勾选项先经 `update_skill_definition` 逐个入库（失败逐项报告名称与原因），再合成一份多操作批量计划走同一条预览→确认→事务路径，冲突项由用户移除后重新预览剩余集合。版本固定落地 `ExtensionBinding.locked_digest` 的唯一完整链路：新命令 `set_binding_lock` 以库提交（generation + 定义修订前置校验）固定 / 解除固定，固定仅在绑定内容与库当前版本一致（`inSync`）时允许，校验器拒绝 MCP 绑定携带摘要或空摘要；`binding_file_state` 对固定绑定以锁定摘要而非定义修订判定一致性（固定目标不因库编辑显示「待应用」）；`skill_deploy_steps` 为固定绑定部署锁定版本内容；`build_redeploy` 对已启用且固定在旧版本的绑定产出零步骤警告目标（「已固定版本 …；解除固定后才会更新」），停用绑定无论是否固定与既有语义一致地静默跳过，绝不静默迁移；详情绑定行显示「已固定 <摘要12位>」徽标与固定 / 解除固定入口，固定为库记录变更、不产生客户端写入、不走计划预览，commit marker 落在 `commits/` 与所有库批次共享同一 receipt 契约，不进入待恢复列表。文档同步：`DESIGN.md` 扩展工作区新增多目标、批量更新与版本固定交互事实，`docs/skill-mcp-management-status.md` 更新为当前边界（仅余真实客户端与跨平台验收），README 扩展能力句同步。审查复核（2026-09-07）：全仓无旧入口残留（单 id 检查、单选安装目标、`asb-ext-target-select` 均已删除）；`cargo check` 零警告；本轮全部变更 Rust 文件 `rustfmt --check` 通过。验证：`npx vitest run src/pages/ExtensionsPage.test.tsx`（15 通过，含多目标安装、批量更新合成计划、固定往返）、`npm run typecheck`、`cargo test -p asb-core --locked extensions`（65 通过，含锁定摘要校验）、`cargo test -p agent-switchboard --locked --lib`（303 通过、4 忽略；含已启用固定绑定警告目标与停用固定绑定跳过两个 planner 用例）、`cargo test -p asb-switch --locked` 全量通过；真实客户端隔离加载与跨平台验收仍未交付并维持写入门禁。注意：本仓库存在并行会话同时重构扩展模块（skills 拆目录、takeover 拆分），同文件收敛以编译与测试通过为准；`src-tauri/src/commands/mod.rs` 的 EOF 空行为并行会话遗留，未越权处理。

**供应商编辑保存事务化：草稿预览、原位确认与统一提交（2026-09-06，完成）：** 一次性替换当前编辑保存流程为「草稿预览 → 原位确认 → 统一提交」。行为矩阵：新建供应商仅保存档案、不自动启用；编辑未启用供应商仅更新档案、不切换当前供应商；编辑当前启用供应商的生效参数（服务地址、密钥、模型、模型选项、上游协议、最大输出 token、路由模式）时在编辑页直接展示脱敏差异，经「确认保存并应用」一次确认后在同一事务中保存并重新应用；仅修改名称、备注、官网、用量查询直接保存资料、不改客户端配置；内容无变化不写文件、不生成备份；取消确认返回原草稿；保存并应用成功才关闭编辑页，失败保留草稿允许原位重试。后端以 `prepare_profile_save` / `commit_profile_save` 双命令替代 `update_profile` 与 `create_profile`：准备阶段校验草稿、按原档案版本绑定草稿，返回「无变化／仅保存／需要应用」分类（`asb-core::ProfileSaveKind` 与生效字段判定是唯一契约所有者），需要应用时以草稿生成候选全文与脱敏差异、不改任何文件；提交仅执行准备绑定的内容，锁内复核档案版本、通用设置版本与活动身份，托盘切换、其他窗口修改档案或客户端文件外部修改都会拒绝旧计划并触发编辑页重新准备。活动身份一律按修改前的档案与实际配置判定。编辑当前启用供应商走统一可恢复事务（不再要求先切直连）：先以 `configuration/save-journal.json`（仅含档案 id、客户端与修改前版本哈希，无明文密钥）登记，写入新档案后经既有 asb-switch 执行器在同一提交边界完成客户端配置（含 Codex `auth.json`）、网关持久状态与内存路由及配置写入记录；任一阶段失败按序恢复本次已改动内容，恢复失败报告具体状态；启动与后续写入前先处理未完成事务——按已落盘新档案确定性前滚完成应用，档案未写入则丢弃日志。网关路由指纹同步改为仅由路由实际消费参数（客户端、服务地址、密钥、上游协议、最大输出 token）计算，名称、备注、官网、用量查询等资料字段不再旋转能力令牌；不做双算法兼容，升级后旧派生路由在启动恢复时明确拒绝、要求重新应用。验收覆盖：当前／非当前供应商、资料修改、无变化、取消确认；Codex 与 Claude、直连与网关、协议互转；生效参数修改后档案与实际配置一致；预览后外部修改、托盘切换、重复提交均无法执行旧计划；各写入阶段故障与进程中断恢复不留静默部分成功；资料修改不改变网关能力令牌、重启后路由仍可恢复。前端运行相关交互测试与类型检查，后端运行相关契约、执行器与网关测试，写入验证全部使用隔离临时目录。验证：`cargo test --workspace --locked --quiet -- --test-threads=1`（桌面 277 通过、4 项外部条件测试忽略；core 185 通过；扩展执行器 63 通过）、前端全量 63 文件 / 394 项、`npm run typecheck`、`npm run build`、隔离实际 Codex 与 Claude Code CLI 网关黑盒、Windows release 构建、安装器契约和最终安装包凭据扫描均通过；未创建 Git 标签或推送远端。

**扩展工作区：主要闭环已实现，完整设计尚未全部交付（2026-09-06）：** 当前实现已覆盖带类型的扩展库、计划与恢复事务、Skill 内容版本与来源候选、双客户端 MCP 投影、项目范围、只读发现、显式连接检测及预览确认写入。本轮进一步移除了原生 MCP 导入的表单预填猜测：后端按 Codex stdio/HTTP 与 Claude stdio/HTTP/SSE/WS 严格解析，无法表达的字段或传输直接拒绝；发现结果改为匿名观察 ID，且同名“已管理”标记只匹配实际客户端目标。Skill 来源更新后会继续生成部署预览，客户端写入仍须确认。当前验证为 core 扩展专项 44 项、桌面发现专项 2 项、扩展页面 7 项、TypeScript 类型检查、桌面 cargo check 和相关 rustfmt 检查通过。真实客户端隔离加载、自定义 Claude 配置根、各平台目录故障仍未验收并应维持写入门禁。已有 MCP 的安全编辑器、Skill 自建/本地副本编辑/依赖关联/批量与导出、以及“接管已有本机安装”均未交付；详细边界见 docs/skill-mcp-management-status.md。不得再将扩展工作区标注为“完整实现”。

**`0.1.14` 发布前契约收敛与本地 Windows 安装包（2026-09-06，完成）：** 发布审查确认供应商档案、通用设置、网关路由和云端快照各自只有一份当前持久化契约；旧云端快照只在恢复边界确定性升级并立刻回写，不保留双读或双写路径。应用界面设置缺少当前字段时不再静默套用默认值，已删除没有运行时消费者的通用设置旧基线元数据；release 中仅供调试桥接使用的传输分支也已从 release 枚举排除，不再产生死代码警告。工作区版本、锁文件、更新日志与自绘安装器统一为 `0.1.14`。验证：`cargo test --workspace --locked --quiet -- --test-threads=1`（428 通过、4 项需要外部客户端或凭据的测试忽略）、前端全量 `62` 文件 / `387` 项、`npm run typecheck`、`npm run build`、Rust 格式、发布脚本 `9` 项、版本/更新日志校验及 `git diff --check` 均通过；Windows release 构建成功，最终自绘安装包产品版本为 `0.1.14`、文件版本为 `0.1.14.0`，安装包内容凭据扫描通过。本地安装包未创建 Git 标签或上传远端。

**NVIDIA Kimi K3 Chat 实供方双客户端沙箱验证（2026-09-06，完成）：** 使用用户临时提供的 NVIDIA 账户凭据、`https://integrate.api.nvidia.com/v1` 和 `moonshotai/kimi-k3`，在两个独立临时目录中创建 Chat Completions 供应商档案、执行真实切换并经每档案能力令牌保护的本机网关调用已安装的 Codex 与 Claude Code。两端均收到精确完成标记并正常退出，整条真实验证耗时 293.25 秒；没有读取或改写真实客户端配置、凭据缓存或进程环境。实测补齐了 NVIDIA 流中的 `usage: null`、带标准细节的最终用量帧和空 `choices` 尾帧；同时将当前 Codex 回放的空 `Responses reasoning.content` 设为明确的唯一可转换形状，任何非空明文推理仍在边界拒绝，推理续接只通过本机不透明载荷返回上游。验证：转换 31 项、WebSocket 上下文 6 项、实际 Codex CLI 与 Claude Code 回环黑盒各 1 项、真实 NVIDIA 双客户端忽略测试均通过；`cargo fmt --all -- --check`、`cargo test -p agent-switchboard --locked --no-run`、`git diff --check` 和源码 NVIDIA 密钥模式扫描通过。

**`0.1.13` 本地 Windows 安装包（2026-09-06，完成）：** 将本轮三协议供应商切换、本机回环协议转换网关、云端备份三协议快照升级、备份教程收放与未启用供应商行持续高亮移除汇总为 `0.1.13`。工作区版本唯一由根目录 `Cargo.toml` 拥有，锁文件和自绘安装器元数据由该版本生成；`CHANGELOG.md` 同步提供应用内与发布使用的同一份说明。已生成本地自绘 Windows 安装包，产品版本为 `0.1.13`、文件版本为 `0.1.13.0`。验证：前端全量 `62` 文件 / `384` 项、Rust 工作区 `416` 项通过（`2` 项既有外部条件测试忽略）、`npm run typecheck`、发布脚本 `9` 项、安装器契约、Rust 格式、生产前端与 Windows Release 构建、最终安装包凭据扫描和 `git diff --check` 均通过。本地测试包未作为 GitHub Release 发布。

**供应商未启用行高亮移除（2026-09-06，完成）：** 点击供应商行仍更新预览、编辑与切换流程需要的内部选择，但未启用行不再获得持续蓝调卡面、`is-selected` 类、星光或暖色粒子；当前实际生效档案仍由后端匹配结果、安静蓝调面和「使用中」文字唯一表达。同步删除仅供该行使用的 `warm` 星光变体与令牌，避免保留无消费者的视觉契约。验证：`npx vitest run src/components/ProviderList.test.tsx src/pages/ProvidersPage.test.tsx src/App.test.tsx --maxWorkers=1 --no-file-parallelism`（3 文件、63 通过）、`npm run typecheck` 与 `git diff --check` 通过；源码搜索仅剩测试对已删除类名的反向断言。按用户指令，本轮未重新运行生产构建或创建安装包。

**云端备份教程展开与收起（2026-09-06，完成）：** 备份页的「从零配置 Supabase」教程收敛为默认收起的单一 disclosure；标题行的次级按钮以 `aria-expanded` / `aria-controls` 控制内容，展开后保留原有六步与 Dashboard 链接，收起时不影响云端连接表单、初始化 SQL 或备份操作。教程仍使用同一内容面，不新增嵌套卡片或第二入口。验证：`npx vitest run src/components/CloudBackupPanel.test.tsx src/pages/BackupsPage.test.tsx --maxWorkers=1 --no-file-parallelism`（8 通过）、`npm run typecheck`、`npm run build` 与 `git diff --check` 通过。

**三协议供应商切换与本机协议转换网关（2026-09-06，完成）：** 供应商编辑器和带类型校验现在共同拥有 `Responses`、`Chat Completions`、`Anthropic Messages` 三种协议契约。协议与客户端原生协议相同才直连；其余组合只经每档案能力令牌保护的 `127.0.0.1` 本机网关。Codex 的原生 `Responses` 路由只允许 Bearer，Codex 转 Anthropic 必须显式设置正数输出上限；跨协议工具名以可逆命名空间编码保存，流式响应和 Codex WebSocket 重放同样保留该映射。不支持的跨协议字段会在网关边界明确拒绝，不再静默忽略。跨协议时 Codex 的有效 `web_search` 设为 `disabled`，保存的通用偏好不变，切回原生 Responses 后自动恢复；当前 Codex Responses 传输元数据会在预览中说明其不透传。验证：真实命令层的隔离临时目录双客户端切换、网关 HTTP/WebSocket 黑盒、实际 Codex CLI 经隔离回环网关、Rust 工作区全量（405 通过，2 项依赖外部条件的测试忽略）、前端全量（61 文件、380 项）、类型检查、生产构建和本次 Rust 文件格式检查均通过；未触碰真实客户端配置或外部供应商。

**云端备份供应商契约迁移（2026-09-06，完成）：** `config_store::snapshot` 是明文云端快照版本与迁移的唯一所有者；新上传一律写入 `schemaVersion: 3`，其中供应商只保存 API 格式与最大输出 token，认证请求头由 API 格式推导，AES-GCM 加密信封仍独立保持 `version: 1`。恢复时仅在快照边界一次性转换精确的 `schemaVersion: 2` 供应商 `authScheme` 字段或三协议前快照，并立刻重新加密覆盖同一云端记录；此后本机与云端只保留当前契约。转换完成前不会替换本机配置，未知字段和不支持版本明确拒绝。备份页说明和确认框同步说明认证请求头由 API 格式自动推导。验证：前端全量 62 文件 / 387 项、`npm run typecheck`、`npm run build`、本轮目标差异检查及 Rust 工作区全量测试均通过（各套件合计 494 通过、4 忽略）。未使用真实 Supabase 凭据或真实客户端配置。

**`0.1.12` 汇总发布（2026-09-05，发布准备完成）：** 汇总托盘切换事件驱动的主窗口快照刷新、分类更新日志的发布/应用内展示，以及双客户端沙箱审查与 Claude 自定义目录修复。工作区及 Cargo 锁文件版本统一升级为 `0.1.12`；`CHANGELOG.md` 新增对应正文，标签发布时由同一正文生成 GitHub Release 和更新清单 `notes`。本次提交按用户要求汇总全部代码，目标为 `origin/master` 与 `v0.1.12`。验证：前端全量 61 文件/375 项、Rust 工作区 376 项（1 项既有真实数据库测试忽略）、发布脚本 9 项、MSIX 3 项、生产前端构建（含类型检查）、发布说明提取、标签/版本一致性、相关 Rust 格式、暂存差异及凭据模式检查全部通过。三平台安装包构建、签名和公开 Release 由标签触发的 [打包发布工作流](https://github.com/y4Nkk/agent-switchboard/actions/workflows/package.yml) 完成；本地验证不替代该远端构建结果。

**双客户端通用配置与供应商切换沙箱审查（2026-09-05，完成）：** 确认「先保存通用配置 → 新建供应商 → 预览 → 确认切换」真实落盘。修复 `local_state.rs` 中 Claude 配置与全局指令忽略 `CLAUDE_CONFIG_DIR` 的问题，二者现在与凭据遵循同一目录语义，符合 [Claude 官方目录说明](https://code.claude.com/docs/en/claude-directory)。新增 `src-tauri/src/dev_api/sandbox_tests.rs`：Windows 独立子进程使用临时主目录、应用数据目录和随机回环端口，经现有 HTTP 桥接调用真实桌面命令，覆盖默认/自定义目录、保存与预览无客户端写入、未确认拒绝、Codex 双文件联动、Claude JSON 合并、A→B 更换端点/模型/不同虚构密钥、通用参数继承、宿主字段保留、活动身份回读、两端撤回、过期预览与外部编辑拒绝、官方路由恢复/OAuth 保留、首次创建及撤回恢复文件缺失。自定义目录测试还确认默认目录哨兵不变；凭据全部运行时随机生成，未接触真实配置或外部供应商。验证：`cargo test --workspace --locked --quiet`（376 通过、1 项既有真实 外部数据库测试忽略，含两种沙箱完整流程）、`npm test -- --maxWorkers=1 --no-file-parallelism`（61 文件、373 通过）、`npm run typecheck`、`npm run build`、本次修改的路径/沙箱测试 Rust 文件格式检查及 `git diff --check` 通过。测试宿主 GNU 链接需进程级 `RUSTFLAGS=-C link-self-contained=yes`；未改仓库工具链配置。前端构建仅有既有大 chunk 提示。黑盒边界为真实命令与文件，不代表真实供应商连通、已运行客户端热重载或 macOS/Linux 桌面验收；未打包、安装或发布。

**安装器交互与完整构建交付（2026-09-05，完成）：** 引导顶部改用 WindowChrome 原生标题命中拖动，增加可独立点击的最小化按钮，安装期间也可最小化；删除旧 MouseLeftButtonDown 拖动路径。移除构建脚本 EnginePath 参数，强制先构建当前应用及部署引擎，再嵌入自绘引导，防止同版本旧应用与新引导混装。安装器结构、最小化点击测试及前端边界 9 项通过；完整 Windows release 构建成功，内嵌引擎 SHA-256 与本轮新引擎一致。本次发布使用 `v0.1.11` 标签，包含供应商身份与托盘外框修复；原生拖动和托盘外观尚待实机验收。

**托盘与安装器双层外框（2026-09-05，代码完成，视觉验收受锁屏阻断）：** 托盘关闭 Tauri shadow 白边路径，删除 CSS 外层描边与圆角，Windows DWM 统一拥有无描边轮廓；安装器根 Border 改为铺满客户区的 Grid，去除 WPF 第二层外框。没有增加兼容窗口、透明留白或新的渲染路径。已通过托盘 Rust 7 项、前端边界及托盘 16 项测试、安装器参数和真实 WPF 窗口结构测试，自绘引导重新编译成功。打开引导验收时桌面已锁定，停止操作；安装器未执行安装，托盘新版本未实机核对，未发布。

**双客户端供应商身份识别（2026-09-05，完成）：** 将当前供应商身份与完整配置一致性分离；Codex、Claude、供应商页、概览及托盘消费同一后端 `activeProfileId`，删除从配置匹配或历史名称推断活动供应商的路径。模型与通用设置变化不取消「使用中」，端点或自定义密钥不匹配不误标，相同连接档案仅按有效最近切换记录消歧。官方标记表示路由而非 OAuth 有效性。验证：前端全量 61 文件 / 372 项通过；Rust 工作区全量通过（桌面 199、核心 132、执行器 32，另 11 项集成测试；1 项原有真实数据库测试忽略），托盘身份与真实模型投影专项通过，生产前端构建、类型检查及差异检查通过。React 审查确认无新增请求或第二份身份状态。测试未写真实客户端配置；未重新打包、安装或发布，构建保留既有大 chunk 提示。

**Windows 全自绘安装引导（2026-09-05，代码与本机构建完成，真实安装验收待完成）：** WPF 拥有可见安装界面，NSIS 只作为内嵌静默部署引擎。发布流程只收集自绘安装包及其签名，删除旧页头、侧栏位图及配置引用；保留覆盖安装、安装目录、快捷方式、静默更新及重启参数。引导负责 WebView2 前置检查，防止旧下载窗口并行出现。已通过安装参数与真实进程退出码测试、发布脚本 7 项测试、边界 8 项测试、完整 Windows 构建、实际安装包扫描及差异检查。实际打开自绘窗口，核实版本、现有目录识别与取消操作；本地生成 0.1.10 包，未覆盖安装、未发布。真实升级、卸载、缺失 WebView2 的安装以及签名更新端到端验收尚未执行。

**自定义托盘浮层（2026-09-05，代码完成，原生实机验收待完成）：** 已删除原生菜单、旧菜单格式化与 Linux AppIndicator 显式依赖，改为独立透明 WebView；统一左右键入口、隐藏/显示状态及物理坐标工作区定位。Windows/macOS 使用系统材质，Linux 使用 StatusNotifierItem；Wayland 位置由合成器管理。显示投影、缓存用量、共享外观及既有切换执行器各有唯一实现。预建隐藏窗口避免 Windows 点击回调创建 WebView 死锁；首次显示等待布局与尺寸提交，加载超时恢复主窗口，快速图标点击单次去重，关闭即退出不受隐藏窗口保活影响。验证：前端 60 文件 / 365 项通过，生产双入口构建与类型检查通过；Windows Rust 全量 197 通过 / 1 项原有真实数据库测试忽略，托盘专项 7 项通过；变更 Rust 文件格式、最终差异及旧入口引用检查通过。全局格式检查仍指出未修改的 `dev_api.rs`、`distribution.rs` 既有差异；构建保留现有大 chunk 提示。Linux 全量交叉编译因本机缺少 `x86_64-linux-gnu-gcc` 阻断，独立 zbus 协议编译检查通过，但不能代替完整平台构建。Windows 自动化未暴露任务栏入口，未执行原生浮层点击与多屏材质验收；macOS/Linux 桌面亦未实测。未执行真实供应商切换，未发布或生成新版安装包。

**供应商用量收放与摘要（2026-09-05，完成）：** 已配置查询的卡片由 `useProviderUsage` 持有查询状态，展开详情与收起模型栏摘要消费同一结果。收放不停止轮询、不增加请求、不重置间隔；间隔 0 保持首次读取及手动刷新。摘要按套餐分别显示剩余百分比与余额，无总量不猜百分比；失败保留并标明上次读数，查询配置变化后清除旧读数。卡片离开列表仍停止查询。删除详情组件内独立请求路径，比例计算收敛至 `usage-format.ts`，补齐收放计时、失败与配置变化的回归验证。 验证：前端全量 58 文件、353 项测试通过，`npm run typecheck`、`npm run build`、`git diff --check` 通过；构建保留现有大 chunk 提示。未生成新版安装包。

- 同时最多只能有一个实施阶段处于进行中。
- 仅在每项列出的验收检查都有证据后，才能将阶段标记为已完成。
- 如目标、范围或验收条件变化，必须先在此更新，再改动实现。
- 不得将会话记忆、本机代理状态或个人工作记录写入本仓库。
- 即使所有技术检查均通过，任何写入真实 `Codex` 或 `Claude Code` 文件的阶段仍需要用户明确授权。
- `外部配置` 可为独立编写的后端行为提供启发；界面由 `DESIGN.md` 独立实现，不得从参考项目迁移。

**更新签名密钥轮换（2026-09-05，完成）：** 已生成并替换 GitHub Actions 的 Tauri 更新签名私钥与口令，`0.1.9` 已内嵌与其配对的新公钥。GitHub 标签构建 [33899867961](https://github.com/y4Nkk/agent-switchboard/actions/runs/33899867961) 的 Windows、Linux、macOS Apple Silicon 与 macOS Intel 均使用该公钥验证了更新载荷签名，随后公开 `v0.1.9` Release。旧版 `0.1.6` / `0.1.8` 内嵌的已遗失旧公钥无法验证新签名，必须手动安装一次 `0.1.9`；后续直接安装版本将使用新的签名链更新。

**应用内更新失败恢复与发布签名验收（2026-09-05，完成）：** 更新检查与安装错误现在在 `src/api/client.ts` 的类型边界统一归一；签名密钥不匹配会给出可操作的 GitHub Release 手动安装提示。下载或安装失败后保留同一个已发现更新供直接重试，只有安装成功才释放更新资源，避免失败后误显示「已是最新版本」。标签发布构建在上传资产前使用应用内嵌更新公钥逐个验证已签名载荷，密钥不匹配会在公开 Release 之前阻止发布；`verify_updater_artifact` 的同源测试覆盖有效签名与变更载荷拒绝。版本更新为 `0.1.9`。验证：`npm test -- --run --maxWorkers=1 --no-file-parallelism`（58 文件、346 通过）、`npm run build`、`cargo test --locked --workspace`（197 个应用测试通过、1 个依赖真实 外部数据库的测试忽略）、`cargo test --locked --manifest-path src-tauri/Cargo.toml --example verify_updater_artifact`、`npm run test:updater-release`、发布标签校验、相关 Rust 格式检查与 `git diff --check` 通过。

**发布页恢复入口、安装器视觉与 `0.1.10` 发布（2026-09-05，完成）：** 直接安装渠道的「软件更新」新增 GitHub「更新发布页」入口，Store 包仍完全隐藏 GitHub 更新器与该入口；无法调用系统浏览器时显示可操作错误，不再静默吞掉失败。标题栏最小化、最大化/还原与关闭钮移除 Tooltip，保留 `aria-label` 可及名和原有窗口命令。Windows NSIS 的页头与侧栏位图替换为当前 Agent Switchboard 品牌素材，并保持 MUI 要求的 `150×57` / `164×314` 尺寸与 `tauri.windows.conf.json` 的唯一引用。由于包含用户可见功能与安装器资产，工作区及锁文件版本统一升为 `0.1.10`；没有第二版本来源、旧 UI 分支或兼容路径。验证：`npm test -- --run --maxWorkers=1 --no-file-parallelism`（58 文件、348 通过）、`npm run typecheck`、`npm run build`、`cargo test --workspace`（332 通过、1 忽略）、`npm run test:updater-release`（5 通过）、发布标签版本校验与 `git diff --check` 通过；[GitHub Actions 33908009850](https://github.com/y4Nkk/agent-switchboard/actions/runs/33908009850) 的 Windows、Linux、macOS Apple Silicon / Intel 全部成功，签名载荷、发布资产与 Windows Store MSIX 均已验证并上传，公开 [v0.1.10 Release](https://github.com/y4Nkk/agent-switchboard/releases/tag/v0.1.10) 已发布。

**用量图表单位与真实读数审查（2026-09-05，完成）：** `src/lib/usage-format.ts` 成为供应商用量与官方额度的通用数值/单位唯一所有者，表格、趋势标题和比例轨对同一读数保留至多两位小数；本地模型 Token 继续由 `token-format.ts` 负责。`UsageTrendChart` 现在要求调用方显式声明 `generic`、`local-token` 或 `percentage` 读数语义，供应商即使使用 `tokens` 单位也不会误用本地模型 K/M/B 格式。删除趋势标题与官方窗口的整数化路径，官方百分比趋势固定为 0–100 纵轴，通用负值不再被零基线裁切；比例轨保留实际比例文字，仅限制填充宽度。只有同日完整记录的本地 Token 分量才能聚合；同单位的独立供应商套餐不再相加，时间缺口不会伪造为 0。后端停止生成没有消费者的供应商 `usedPercent` 历史序列，仅官方额度窗口保留该指标；原始账本读数不变，无迁移、兼容或双路径。验证：`npm test -- --maxWorkers=1 --no-file-parallelism`（58 文件、346 通过）、`npm run build`、`cargo test -p agent-switchboard usage_history --lib`（9 通过）、`cargo test -p asb-core contracts --lib`（9 通过）、本次 Rust 文件 `rustfmt --edition 2021 --check --config skip_children=true` 与 `git diff --check` 通过。

**供应商新建表单客户端隔离与官方重登录入口（2026-09-05，完成）：** 新建表单在客户端下拉改变时整体重建目标客户端的空草稿，名称、站点、备注、服务地址、密钥、模型和模型选项均不跨客户端保留。每客户端仍只允许一个官方档案：当该档案已存在时，「官方登录」不再是无说明的禁用项，点击会切换到既有档案的编辑视图并使用同一 `OfficialLoginPanel` 重新登录，后端拒绝重复档案的约束不变。`DESIGN.md` 同步为当前交互契约。验证：`npx vitest run src/components/ProviderEditor.test.tsx src/pages/ProvidersPage.test.tsx --maxWorkers=1 --no-file-parallelism`（26 通过）、`npm run typecheck`、`npm run build` 与 `git diff --check` 通过。

**CNB Linux 安装包构建对齐（2026-09-04，完成）：** `.cnb.yml` 的所有分支推送现在构建 GitHub Linux job 同一份 `.deb` 与 `.AppImage`：安装相同的 Tauri/Linux 依赖和 PowerShell，覆盖仓库的 Windows Rust toolchain 固定值，执行发布清单测试、Rust/前端测试及发布资产凭据扫描，再以 `updater-release.mjs stage` 生成统一文件名并作为 CNB 提交附件上传。公共 CNB 仅提供 Linux 容器，因此 GitHub 仍是 Windows/macOS、完整签名更新清单与正式跨平台 Release 的唯一来源。验证：Prettier YAML 解析、`npx vitest run src/boundary.test.ts --maxWorkers=1 --no-file-parallelism`（6 通过）、`npm run test:updater-release`（5 通过）、`pwsh -File scripts/assert-release-secret-free.test.ps1` 与 `git diff --check` 通过。

**概览运行环境信息（2026-09-04，完成）：** 概览页底部新增只读「运行环境」区域，后端 `runtime_overview` 作为唯一事实来源，提供应用版本、构建模式、平台/架构、监听端口、响应情况与应用数据目录；浏览器开发模式由 `dev_api` 的回环主机、端口及健康检查状态码作为唯一来源，桌面壳明确显示无 TCP 端口与桌面协议响应。它不涉及 Codex / Claude Code 配置、凭据、账号或外部网络请求。端口 / 响应扩展后的验证：`cargo test --workspace`（360 通过、1 忽略）、`npx vitest run src/components/RuntimeOverviewPanel.test.tsx src/App.test.tsx --maxWorkers=1 --no-file-parallelism`（32 通过）、`npm run typecheck`、`npm run build`、本次 Rust 文件的 `rustfmt --edition 2021 --check --config skip_children=true` 和 `git diff --check` 通过；全仓 `cargo fmt --check` 仍受无关既有格式差异影响。

**供应商用户级配置模型语义收敛（2026-09-04，完成）：** 删除会误称运行中会话事实的旧模型上下文契约与文案。`App → ProvidersPage → ProviderList / ProviderEditor / PreviewInspector` 现在只传递 `ConfigFileStatus.route` 中读取的 `userConfigModel` 与 `userConfigWarnings`：命中真实配置的供应商卡显示当前用户级配置模型而非档案保存值；编辑器与预览用同一名称显示，预览在未设置模型时明确为「默认模型」；Codex 配置包含 profile 时，编辑器和预览显示后端给出的范围警告，避免把用户级配置误报为运行会话模型。无旧属性、旧文案、草稿反推或会话级伪探测路径保留。测试覆盖卡片覆盖显示、编辑器/预览范围警告、页面传递与应用快照至预览链路。验证：`npx vitest run src/App.test.tsx src/pages/ProvidersPage.test.tsx src/components/ProviderList.test.tsx src/components/ProviderEditor.test.tsx src/components/PreviewInspector.test.tsx --maxWorkers=1 --no-file-parallelism`（85 通过）、`npm test -- --run --maxWorkers=1 --no-file-parallelism`（56 文件、324 通过）、`npm run build` 通过。

**概览 Codex 重置信号时间线与版式对齐（完成，2026-09-04，取代旧的“仅读取 `events` 全局重置”说明）：** `codex_reset.rs` 仍是唯一公开 feed 契约所有者，现将 `events`、`fulfilledSchedules` 与 `manualCompletions` 的完成记录统一规范为最近确认信号，并以 `resetType` 明确分为全局重置、重置卡发放和其他完成信号；完成时间成为信号时间，避免把公告时间冒充实际完成。概览面板改为左侧“本次公开检查当日是否有重置信号”的显式结论，右侧显示最近完成信号、最近检查与下一次预计；最近 Tibo 动态留在结论区。缓存、手动刷新、失败保留旧快照、非官方来源与不代表账号额度的既有契约不变。实测 `https://www.codexrunway.com/api/status.json` 可访问，2026-09-04 的最新结果为 `banked`（重置卡发放）；此前漏显由旧解析忽略时间线完成记录导致。已验证：通过本机开发桥接调用 `check_codex_reset_status` 返回实时 `banked` 结果；`cargo test -p agent-switchboard codex_reset`（8 通过）、`npx vitest run src/components/CodexResetPanel.test.tsx src/api/client.test.ts`（2 文件、28 测试）、`npm run build`、`cargo fmt --check` 与 `git diff --check` 通过。

**Codex 重置信号单一契约收紧（完成，2026-09-04）：** `latestConfirmedSignal` 是后端缓存、IPC 和界面唯一的最近完成信号字段；旧 `latestConfirmedReset` 已从所有运行时消费者和序列化契约中移除，不提供读取、别名或迁移。旧缓存严格拒绝、保持原文件不改写，用户须手动刷新取得当前快照。验证补充：旧字段缓存拒绝用例、`cargo test -p agent-switchboard codex_reset`（8 通过）、`npx vitest run src/components/CodexResetPanel.test.tsx src/api/client.test.ts`（2 文件、29 测试）与 `npm run build` 通过。

**Windows 双渠道发布（2026-09-04，完成）：** Windows 保留 GitHub 的 NSIS 直接下载及其现有签名更新链，同时新增由 Partner Center 身份 yy1.AgentSwitchboard 驱动的 Microsoft Store MSIX。标签构建将 MSIX 作为独立 Actions 制品，绝不将未经 Store 重签名的文件上传到公开 GitHub Release；维护者手工上传该制品至 Partner Center，认证后的 Store 版本由 Store 更新。运行时根据真实进程是否具有 MSIX 包身份决定更新所有者：直接安装继续使用 GitHub，Store 安装不调用 GitHub 更新器。MSIX 清单、版本映射、MakeAppx 打包与产物凭据扫描均已实现。Windows SDK 不提供旧的 `MakeAppx validate` 子命令，故打包脚本已收敛为 `pack` 的内建语义检查加受支持的 `unpack` 回读，并确认清单、主程序、WebView2 Loader 与全部图标都存在。验证：`cargo test --workspace`（364 通过、1 忽略）、`npm test -- --run --maxWorkers=1 --no-file-parallelism`（56 文件、337 通过）、`node --test scripts/msix-release.test.mjs`（3 通过）、`npm run test:updater-release`（5 通过）及 [GitHub Actions run 33883746534](https://github.com/y4Nkk/agent-switchboard/actions/runs/33883746534) 的四个平台构建全部成功；其中 `agent-switchboard-store-windows-x64` MSIX 制品已上传。

**Supabase 云端备份连接验证（2026-09-04，完成）：** `cloud_backup.rs` 仍是远端契约唯一所有者，新增无副作用 `test_connection`：使用未保存的项目地址、publishable key、项目 Auth 邮箱与临时密码完成 `/auth/v1/token?grant_type=password` 登录，再只读查询当前 `auth.uid()` 的 `agent_switchboard_cloud_backups` 行。它不写入 Supabase、不持久化草稿、密码、access token 或 refresh token；测试成功只在当前窗口内保留项目 Auth 密码，上传或恢复成功后清空。连接失败明确区分认证、Data API/表不可用和表权限不足；真实响应字段按官方 `access_token` 读取，修复了错误的 camelCase 反序列化。备份页新增「测试连接」并将 Dashboard 身份与项目 Auth 用户明确区分，日志记录成功/失败的无凭据事件。验证：`cargo test -p agent-switchboard --lib`（195 通过、1 忽略）、`npx vitest run src/components/CloudBackupPanel.test.tsx src/pages/BackupsPage.test.tsx src/api/client.test.ts --maxWorkers=1 --no-file-parallelism`（31 通过）、`npm run typecheck` 与 `npm run build` 通过。未使用真实用户凭据执行远端测试。

**Supabase 云端备份可用性修复与从零引导（2026-09-04，完成）：** 备份页加入从零教程，提供填写项目对应的 Dashboard、Data API、SQL Editor 和 Auth Users 直达链接；初始化 SQL 可一键复制。教程改用「Authentication → Users → Add user → Create new user」并保留 Auto Confirm User，避免邮件邀请重定向到本机 `localhost`。加密快照明确包含完整供应商配置（名称、服务地址、模型、API 密钥）、通用设置和切换历史，但不直接读取或覆盖 Codex / Claude Code 原始配置文件。修复了上传和恢复记录错误使用 camelCase（`userId`、`updatedAt`）而被 Supabase Data API 以 HTTP 400 拒绝的问题；远端数据库契约现在唯一使用 `user_id` 和 `updated_at`，并有序列化/反序列化回归测试。版本更新为 `0.1.7`。验证：`cargo test -p agent-switchboard cloud_backup --lib`（8 通过）、`npx vitest run src/components/CloudBackupPanel.test.tsx src/pages/BackupsPage.test.tsx src/api/client.test.ts --maxWorkers=1 --no-file-parallelism`（33 通过）、`npm run build`、Windows NSIS 构建、安装包凭据扫描及 `git diff --check` 通过；未用真实用户凭据执行云端上传。

**Supabase 完整快照回归闭环（2026-09-04，完成）：** 审查确认快照契约只包含受本应用管理的两端供应商档案、通用设置与切换历史；不包含原始 Codex / Claude 配置、OAuth 凭据、应用界面设置、云端连接设置、缓存或日志。修正教程中已过时的「测试成功后清空项目 Auth 密码」表述：密码现在在当前窗口保留，只有上传或恢复成功后才清空。删除原先只覆盖空快照的加密用例，改为通过实际上传与恢复路径验证完整闭环：隔离源状态含 Codex 自定义与官方档案、Claude 自定义档案、端点、模型、模型选项、备注、网站、用量查询、显式通用设置与两端切换历史；上传 JSON 不含夹具 API 密钥明文，模拟远端返回后经实际恢复路径替换目标中的陈旧档案，并与源 `ConfigurationSnapshot` 完全相等。上传和恢复仍各有唯一生产路径，私有请求注入边界仅供该回归测试使用，不存在旧实现或兼容路径。版本更新为 `0.1.8`。验证：`cargo test -p agent-switchboard --lib`（196 通过、1 忽略）、`npx vitest run src/components/CloudBackupPanel.test.tsx src/pages/BackupsPage.test.tsx src/api/client.test.ts --maxWorkers=1 --no-file-parallelism`（33 通过）与 `npm run build` 通过；未使用真实用户凭据执行远端上传。

**用量布局、表格对齐与会话实现审查（2026-09-04，完成）：** 用量页的四项汇总、模型构成、日趋势与精确明细现在是通过固定间距分开的独立模块；汇总指标不再以连接式外框渲染，明细拥有独立内容面。全局 `Table` 是唯一对齐所有者，表头和单元格均由 `asb-table-cell` 左对齐；用量表已删除旧的数字右对齐覆盖。会话审查发现并修复 Linux 把整条恢复命令错误作为一个 shell 词执行的问题，macOS 工作目录现安全地逐参数引用；还修复了异步恢复完成后会错误显示在后续选中会话上的竞态。`session_manager` 已按扫描协调、JSONL 解析与跨平台恢复拆分，`SessionManager` 已按页面编排、内容规则与消息视图拆分，旧实现与别名均已删除。验证：`cargo test -p agent-switchboard --lib`（197 通过、1 忽略）、`npm test -- --maxWorkers=1 --no-file-parallelism`（56 文件、337 通过）、`npm run build`、针对会话、用量和表格的回归测试、Rust 格式及 `git diff --check` 通过。Linux 交叉检查因宿主缺少 `x86_64-linux-gnu-gcc` 无法完成 `ring` 依赖编译，未影响 Windows 交付；Linux 的命令组装由当前平台单元测试覆盖。版本为 `0.1.8`。

## 阶段图

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| 00 | 建立产品、安全与设计基线 | 已完成 |
| 01 | 在不触碰用户配置的前提下运行隔离桌面外壳 | 已完成 |
| 02 | 定义一份带类型的配置补丁契约和测试配置样本 | 已完成 |
| 03 | 安全读取并预览 `Codex` 与 `Claude Code` 配置 | 已完成 |
| 04 | 以事务且可逆的方式切换隔离测试配置 | 已完成 |
| 05 | 围绕真实预览构建 `Frosted Relay` 界面 | 已完成 |
| 06 | 以只读模式发现真实本地配置 | 已完成 |
| 07 | 提供显式的真实切换、恢复和 Windows 打包 | 进行中 |

## 00 — 文档与视觉基线

**目标：** 在任何代码或真实配置集成存在前，建立一套精简且不重叠的项目规则。

**范围**

- 创建项目目录。
- 定义产品范围、安全规则、设计系统和阶段目标。
- 定义参考边界：`外部配置` 为后端行为提供启发；`Frosted Relay` 是项目自己的界面。

**验收**

- `AGENTS.md` 将产品限制为 `Codex` 和 `Claude Code`。
- `README.md` 说明产品边界与计划架构。
- `DESIGN.md` 定义 `Frosted Relay`、`Dual Relay`、材质、字体与无障碍约束。
- 本文件定义所有未来阶段的目标和验收条件。
- 文档区分后端参考模式与原创界面工作，并禁止从任一参考项目复制界面 / 代码。

**退出条件：** 四份根目录文档均存在、交叉引用正确，且未创建或修改任何代码或真实配置。

**结果：** 于 2026-08-26 完成。

## 01 — 隔离桌面外壳

**目标：** 创建最小的 `Tauri` + `React` + `TypeScript` 应用，使其能在本地打开而不读取或写入 `Codex` 或 `Claude Code` 状态。

**范围**

- 建立工作区、前端、Rust 核心和应用外壳。
- 使用 `DESIGN.md` 中的结构实现占位外壳。
- 只添加类型检查和聚焦单元测试所需的工具。

**验收**

- 开发应用能够打开。
- 进程不扫描、读取或写入真实用户配置路径。
- 前端与 Rust 检查成功运行。
- 运行时样式令牌只有一个所有者；组件不得引入局部色板或模糊常量。

**退出条件：** 能够在没有任何提供商功能的情况下验证隔离桌面外壳。

**结果：** 于 2026-08-26 完成。工作区由 `crates/asb-core`（纯契约）+ `crates/asb-switch`（仅执行器具备写入能力）+ `src-tauri`（`Tauri 2`、`React` / `TS`）组成。`cargo check --workspace --all-targets` 通过；`tsc --noEmit` 通过；`src/styles/tokens.css` 是唯一令牌所有者，并由 `src/styles/tokens.test.ts` 约束。开发应用窗口可在桌面打开（人工验证：名为 `Agent Switchboard` 的窗口渲染了侧边栏、`Dual Relay` 和状态面板；早期只暴露应用元数据）。

## 02 — 配置契约与测试配置样本

**目标：** 在接触文件格式前，为提供商配置档、通用配置、所有权、预览和备份定义一份经过校验的带类型契约。

**范围**

- 创建带类型的 `AppKind`、`ProviderProfile`、`CommonConfigPatch`、`SwitchPreview` 和 `BackupRecord` 契约。
- 定义可观察的锁状态契约，区分空闲、占用、陈旧和不确定状态。
- 准确定义 `Codex` 与 `Claude Code` 中哪些键属于应用。
- 创建包含宿主所有字段的合成 `TOML` 与 `JSON` 测试配置样本。
- 在编写任一格式适配器前，定义纯规划、客户端适配器与切换执行器之间的边界。

**验收**

- Rust、界面绑定、持久化和测试样本消费同一份契约。
- 报告锁状态时不隐式删除或恢复锁。
- 无效的提供商和补丁形状无法通过校验。
- 测试样本不包含真实凭据或私有端点。
- 所有权测试证明未识别键始终在补丁范围外。
- 规划与适配器被证明不产生变更；只有执行器能够接收真实写入依赖。

**退出条件：** 不再有任何模块需要猜测一次切换可以变更哪些字段。

**结果：** 于 2026-08-26 完成，2026-09-03 更新 Codex 路由所有权。契约位于 `crates/asb-core/src/contracts.rs`（`AppKind`、`ProviderProfile` / `ProviderDraft`、`CommonConfigPatch` / `PatchEntry` / `PatchValue`、`SwitchPlan`、`SwitchPreview` / `KeyChange` / `ChangeKind`、`BackupRecord`、`RouteState`、`ImportProposal`）；所有权表位于 `ownership.rs`（Codex 受管顶层键为 `model_provider`、`openai_base_url` 与旧 `OpenAi` 表的一次性清理键；其他 `model_providers.*` 表归宿主所有；以及 Claude 所有的键）；锁契约位于 `lock.rs`，其中纯函数 `classify_lock` 区分空闲 / 占用 / 陈旧 / 不确定。`validate.rs` 会拒绝空名称、非 `http(s)` URL、形似密钥或非法的 Codex 环境变量名、Claude 环境变量名、宿主所有的补丁键和应用不匹配，并给出修复提示。`test_support.rs` 仅在测试构建中提供样本，包含宿主所有字段且不含凭据。规划 / 适配器在结构上不产生变更：`asb-core` 完全没有文件系统依赖；只有 `asb-switch` 接收写入能力（注入 `SwitchIo` trait）。

## 03 — 读取并预览两个客户端配置

**目标：** 从隔离测试样本和受支持的用户级 `Codex`、`Claude Code` 配置解析内容，并生成不产生变更的预览。

**范围**

- `Codex TOML` 适配器。
- `Claude Code` 用户设置 `JSON` 适配器。
- 解析器诊断和脱敏配置差异。

**验收**

- 有效与无效 `TOML` / `JSON` 测试样本均有聚焦测试。
- 预览列出每个变更键、保留键、警告和备份目标。
- 生成预览不会写入文件。
- 每份返回的预览和错误都会脱敏密钥。

**退出条件：** 用户无需执行切换即可理解提议的切换。

**结果：** 于 2026-08-26 完成。`adapter/codex.rs` 使用保留格式的 `toml_edit` 编辑（宿主注释 / 布局保留，已由测试证明）；`adapter/claude.rs` 使用启用 `preserve_order` 的 `serde_json`。公共边界 `adapter::{preview, render, validate_syntax, route_state}` 会先校验规划（子模块入口为 `pub(crate)`，因此没有路径可绕过校验）。预览用脱敏后的变更前 / 后值列出每个变更键、保留宿主键、警告和备份目标。解析是纯操作（输入 `&str`，输出 `String` —— 结构上不写入）；解析错误包含行号，且会清除形似令牌的值；脱敏同时基于键与值形状（稳定的 `••••••••` 占位符）。测试样本覆盖有效与无效 `TOML` / `JSON`、幂等渲染、宿主键保留和脱敏。

## 04 — 隔离测试配置的事务式切换

**目标：** 在提供商配置档之间安全切换，其备份和回滚行为与生产契约等价。

**范围**

- 文件加锁、冲突检测、带时间戳备份、临时写入、校验、原子替换和写后验证。
- 用户可见的锁诊断以及显式的陈旧锁恢复流程。
- 恢复紧邻的前一份备份。
- 使用内容哈希或等价的显式比较检测外部变更。

**验收**

- 提供商 A → 提供商 B → 恢复会保留每个宿主所有的测试配置字段。
- 无效临时写入会恢复前一份内容。
- 检测到外部编辑会阻止切换，并产生可用诊断。
- 占用或不确定锁会阻止切换；陈旧锁恢复会被记录且需要显式操作。
- 执行结果会报告锁状态、变更目标、警告、备份位置和恢复结果，无需由界面自行推断。
- 测试仅在隔离的临时目录中运行。

**退出条件：** 不得有任何失败模式能静默留下部分配置写入。

**结果：** 于 2026-08-26 完成。`asb-switch` 执行器实现：加锁 → 哈希检查 → 预览 → 渲染 → 再次哈希 → 备份（+ 哈希旁车文件）→ 临时写入 → 语法校验 → 原子重命名（`MOVEFILE_REPLACE_EXISTING`）→ 写后验证 → 释放；若写后验证失败，则恢复紧邻的前一份备份。锁文件（`<target>.asb-lock`）通过纯契约和真实 Windows `pid` 探测分类（`OpenProcess`；访问被拒绝 → 不确定）；占用 / 陈旧 / 不确定锁会在不删除锁的情况下阻止操作；`recover_stale_lock` 是显式操作并返回已记录条目。通过与预览时哈希进行 `SHA-256` 比较检测外部编辑。`SwitchOutcome` 报告锁状态、变更目标、警告、备份记录和恢复结果。11 项集成测试仅在 `tempfile` 目录中运行，其中包括用于临时写入失败和写后校验损坏的确定性失败注入（注入 `SwitchIo`）。

## 05 — `Frosted Relay` 工作界面

**目标：** 按 `DESIGN.md` 将预览、配置档、诊断和恢复组织成清晰的桌面工作流。

**范围**

- `Dual Relay`、提供商列表、通用设置表单、预览检查器和备份历史。
- 空状态、加载状态、校验状态、确认状态和失败状态。
- 键盘导航和减少动态效果行为。

**验收**

- 每个视觉令牌均来自唯一令牌所有者。
- `Dual Relay` 除颜色外，还通过文字和位置表示状态。
- 差异和配置文字无需背景模糊也可阅读。
- 界面评审确认标签、徽标和副标题传达真实信息，而非装饰空白区域。
- 界面测试证明组件请求带类型的预览和操作，而不构造配置文本或文件系统路径。
- 类型检查通过；聚焦界面测试覆盖主要操作和错误状态。
- 视觉评审确认没有嵌套玻璃和不必要的装饰性动画。

**退出条件：** 能够完全通过界面完成受控配置切换和恢复。

**结果：** 于 2026-08-26 完成。`Frosted Relay` 界面按 `DESIGN.md` 实现：`Dual Relay`（双轨道、圆点 + 文字 + 位置）、提供商行、通用设置叠加表单、差异检查器（接近实心、代码字体）、带确认面板的备份历史表、空 / 加载 / 错误状态、键盘焦点环和减少动态效果支持。`src/api/client.ts` 是唯一与后端通信的模块；`src/boundary.test.ts` 强制组件既不导入后端，也不构造配置文件文本。令牌所有权由测试约束。隔离测试覆盖选择、预览、确认、切换与恢复；真实用户文件写入不由自动化测试执行。

## 06 — 只读本地发现

**目标：** 识别已安装的 `Codex` 与 `Claude Code` 用户级配置，并在不改变用户状态的前提下报告其就绪情况。

**范围**

- 发现受支持的配置位置。
- 检测缺失文件、解析错误、受管理设置和不受支持的提供商形状。
- 展示只读诊断摘要和导入建议。

**验收**

- 发现不会变更文件系统或凭据。
- 人工验证确认发现后真实配置时间戳和内容均不变。
- 报告不受支持状态，而非猜测或覆盖。

**退出条件：** 用户能在充分了解安全状态后决定是否导入当前设置。

**结果：** 于 2026-08-26 完成，2026-09-03 更新。`asb-core::discovery` 是纯操作（注入读取函数和路径），将每个文件分类为缺失 / 解析错误（行号 + 已清除的消息）/ 正常，并附带受管理标记与警告（不受支持的 Codex 协议、明文 `ANTHROPIC_AUTH_TOKEN`）。Codex 自定义路由仅在 `model_provider = "openai"` 且有 `openai_base_url` 时可导入，并从同一用户目录的 API-key `auth.json` 缓存读取密钥；Claude 建议只读取模型与服务地址。`discover_local` 命令最多读取三个真实用户级文件；「发现」页面只展示路径、状态、警告与显式导入操作，不写入文件。

## 07 — 真实配置单路径与打包

**目标：** 移除运行时虚拟配置路径和预置供应商，使应用只管理本机 `Codex` 与 `Claude Code` 配置，并保留由用户确认的可逆切换与 Windows 打包。

**范围调整（2026-08-26，2026-09-03 更新）：** 不再提供两套目标选择。供应商配置档保存在应用数据目录的独立供应商文件中，首次启动为空；它们保存稳定标识、客户端、名称、模型、端点、API 密钥及档案级模型选项。Codex 切换固定写入 `model_provider = "openai"`、`openai_base_url` 与同目录 `auth.json` 的官方 API-key 缓存字段；Claude 写入 `env.ANTHROPIC_AUTH_TOKEN`。所有展示、预览、日志、错误和诊断均脱敏。通用设置与写入历史各有独立文件。所有状态、预览、切换、恢复和备份历史均指向用户级配置路径。自动化测试仍使用隔离的临时文件，不构成运行时模式。

**范围**

- 为真实用户配置写入提供显式确认流程。
- 删除运行时虚拟路径、预置供应商和相关界面选择器。
- 提供真实供应商配置档的新增、编辑、删除与只读导入入口。
- 将 Codex 环境变量名写入受管表，并在所有预览、错误和诊断中脱敏密钥。
- 备份浏览器、恢复流程、GitHub 三端直接安装包、CNB Linux 安装包，以及 Windows Microsoft Store MSIX 构建。
- 在通用设置中直接管理 Codex 全局 `AGENTS.md` 与 Claude Code 全局 `CLAUDE.md`，不建立第二份提示词存储；编辑区按内容在受限高度内自适应，达到上限后仅自身滚动。

**验收**

- 每次真实切换均在确认前展示精确差异和备份位置。
- 首次启动没有预置供应商；每个可切换供应商均来自用户新增或真实配置导入。
- 运行时不存在虚拟配置命令、非本机目标路径或相关界面文案。
- 状态、预览、切换、恢复和备份历史只访问真实用户级配置路径。
- 受控且由用户授权的切换与恢复均经过人工验证。
- 打包后安装与启动不会丢失应用自身的本地数据。
- 应用诊断、导出或软件包产物中不出现密钥。
- 提示词文档保存不暴露绝对路径，并经过版本比对、锁、隔离备份、原子替换和写后恢复验证。
- Windows 标签构建同时产出公开 GitHub Release 所需的 NSIS 与仅供 Partner Center 上传的 MSIX；后者通过 MakeAppx 验证和凭据扫描，且不进入公开 Release。
- 直接安装的 Windows 版本继续检查 GitHub 签名更新；具有 MSIX 包身份的版本明确显示由 Microsoft Store 更新且不调用 GitHub 更新器。

**退出条件：** 用户能够安全地将应用用于日常 `Codex` / `Claude Code` 提供商管理。

**当前状态（2026-08-26，2026-09-03 更新）：** 运行时已迁移为真实配置单一路径：`config_status`、预览、切换、恢复、锁和备份命令都解析用户级目标；旧的虚拟命令、路径、预置供应商和界面选择器已删除。供应商的新增、编辑、删除、只读导入和空初始存储已实现；Codex 自定义路由固定使用官方内建 `openai` provider 与 `openai_base_url`，不创建项目自定义 provider 表。

**Codex 官方订阅额度（完成，2026-09-01）：** 新增一条独立于 `UsageQuery` 的只读能力，仅服务于 Codex 官方登录档案。`asb-core` 拥有已脱敏的额度结果契约；桌面服务临时读取已有 Codex OAuth 登录态、查询服务端限额窗口，并只归一化窗口名称、已用百分比、重置时间和可恢复状态。它不接管、复制、持久化或写入登录缓存，不将 OAuth 令牌或账号标识返回渲染器，也不复用第三方档案的地址、API 密钥或脚本。供应商行使用 Frosted Relay 自有的紧凑账本与细进度轨呈现，不迁移 外部配置 视觉、文案或组件；首次显示读取一次，随后只允许手动刷新，失败时保留进程内最近一次成功结果并标明过期。未登录、失效登录、服务拒绝或网络/响应故障都有可恢复状态。验证：`cargo fmt --check` 与 `cargo test --workspace`（桌面库 120 通过、1 个既有真实 外部数据库测试忽略；核心 111、执行器 7、集成 29 均通过）、`npm test`（228 通过）、`npm run typecheck`、`npm run build` 均通过；未读取真实 OAuth 文件或执行实际额度查询，故尚未做真实账户的桌面运行时验收。

**主窗口居中与完整应用内更新（完成，2026-09-04）：** Tauri 主窗口配置保留 `center: true`，使原生窗口在创建前按当前显示器居中。旧的 GitHub Release API 查询、`check_update` 命令与「打开下载页面」路径已整体删除，更新唯一由官方 `tauri-plugin-updater` 负责：基础 `tauri.conf.json` 固定项目 Minisign 公钥和 `latest.json` HTTPS endpoint，`src-tauri/tauri.updater.conf.json` 仅在标签发布叠加 `createUpdaterArtifacts`，因此本地和分支构建不需要私钥；Rust 注册插件，默认 capability 明确授予 `updater:default`。前端 `useUpdateCheck` 是检查、更新资源、下载进度、安装和重启状态的唯一所有者：启动仅检查一次且失败静默，手动操作失败才报告；空结果显示「已是最新版本」，发现更新后用户可下载、验证、安装。Windows NSIS 由插件退出并自动重启，macOS/Linux 安装结束后调用 `restart_application`，重启失败收敛为「更新已安装，需要重启」和可重试的重启操作，绝不重复安装；已替换、已安装或卸载时均关闭原生更新资源。发布工作流以根 `Cargo.toml` 的 `[workspace.package].version` 为唯一版本源，标签必须精确等于 `v<version>`；四个构建 job 汇集 Windows NSIS、macOS 双架构 `.app.tar.gz`、Linux deb/AppImage 的签名载荷并扫描凭据，只有完整矩阵成功才创建 draft Release，先上传安装包和更新载荷、最后上传单一 `latest.json` 再公开，保证客户端只能发现完整发布。验证：`npm test -- --run` 54 文件 312 项、更新契约 43 项、`npm run test:updater-release` 4 项、`npm run typecheck`、`npm run build`、`cargo check -p agent-switchboard --lib`、`cargo fmt --all -- --check`、`git diff --check` 与发布物凭据扫描均通过。

**三端支持：Linux / macOS / Windows 构建与运行（完成，2026-09-03，用户指令「需要支持linux、macos、windows三端」）：** 目标是 GitHub 能出四组安装包：Windows NSIS（行为不变）、macOS arm64 + x64 dmg、Linux deb + AppImage（用户选定格式，不出 rpm）。**传输层契约迁移（唯一所有者 = `probe.rs`，签名零变化故 8 个消费模块零改动）**：WinHTTP 传输整体删除（约 440 行 FFI 与错误分类），换为 `reqwest 0.12 blocking` + `rustls-tls-native-roots`（OS 根证书）+ `system-proxy`（自动代理发现），错误契约收敛为 `FailureKind`（Dns/Timeout/Connect/Tls/Other）+ 现有五条中文文案原样保留；分类沿 `source()` 链关键字匹配（reqwest 无类型化 DNS/TLS 谓词），消息绝不携带 reqwest 原始文本防 URL 泄漏；超时对齐旧预算（client connect 5s、probe 每尝试 10s、携带载荷请求总 15s）；CRLF 头串经新 `header_map` 解析为 `HeaderMap`。有意差异：传输错误不再带「（WinHTTP 错误 N）」后缀；hyper 按规范将头部名小写化上线（RFC 9110 大小写不敏感），loopback 测试断言随之改小写比较，短 body 严格报错取代 WinHTTP 的截断容忍（测试自身 Content-Length 笔误已修）。**其余平台门控**：`fonts.rs` GDI 枚举 → `fontdb`（OnceLock 缓存，测试按平台拆分 Segoe UI / 非空断言）；`commands/window.rs` Win32 系统命令 → Tauri 跨平台窗口 API（关闭仍走 CloseRequested 保托盘吸收）；`session_manager.rs` 终端恢复按平台三分（Windows 原语义不变；Linux `$TERMINAL -e` → gnome-terminal/konsole/xfce4-terminal/x-terminal-emulator/alacritty/kitty 探测 + `sh -lc`；macOS osascript Terminal.app），UI 文案统一中性「在终端中恢复」；`build.rs` windres manifest 与 `WindowsAttributes` 按 `cfg(target_os)` 门控；`lib.rs` WebView2 硬件加速链 cfg(windows) + 非 Windows no-op；`windows-sys` 从 src-tauri 依赖中整体删除（已无使用者）、asb-switch 中移入 `[target.'cfg(windows)'.dependencies]`；`pids.rs` 测试按平台拆分（非 Windows 断言 Unknown）；7 处 `USERPROFILE` 收敛到新 `user_home_dir()`（USERPROFILE → HOME，文案「无法确定用户主目录」）；`rust-toolchain.toml` 保持不动，非 Windows CI 以 `RUSTUP_TOOLCHAIN=stable` 覆盖。**打包拆分**：`npx tauri icon` 生成全套图标（含 `.icns`；android/ios/Store 产物不入库），`tauri.conf.json` 平台中性化，新建 `tauri.{windows,macos,linux}.conf.json` 三份平台 overlay（Windows 契约原样搬入唯一所有者），`npm run tauri:build:<平台>` 三脚本 + `cross-env` 取代 cmd 语法 dev 脚本，`boundary.test.ts` 改为四文件分平台契约锁（BMP 尺寸断言保留）。**CI**：`windows-package.yml` → `package.yml` 四 job 矩阵（windows-latest GNU 工具链 NSIS、macos-latest arm64 dmg、macos-15-intel x64 dmg、ubuntu-22.04 deb+AppImage），三端都跑 `cargo test --workspace` + 前端测试；凭据扫描脚本参数化为四类产物（exe→7z、dmg→hdiutil、AppImage→--appimage-extract、deb→ar+tar），PEM 规则收紧为完整块匹配（schannel 断言字面量假阳性排除，实测真 PKCS8/RSA 块仍命中）；`v*` 标签由 release job 聚合发布全部安装包（`--clobber`）。**文档**：README（标语/徽章/下载表/三端构建说明）、AGENTS.md §1、DESIGN.md（§1/托盘/主题/硬件加速 Windows-only/§13 打包）对齐三端定位；`.cnb.yml` 的 Ubuntu `cargo test --workspace` 随本次从必然红转绿。已验证（Windows 本机）：`cargo check --workspace`、`cargo test --workspace` 324 项、`rustfmt --check`、`npm run typecheck`、`npm test` 290 项、`npm run tauri:build:windows` 经新 overlay 产出 NSIS、新扫描脚本对新产物通过、PEM 规则三态断言。**无法本机验证、以 CI 首跑为准**：mac/linux `cargo test` 与 dmg/deb/AppImage 构建、macos-15-intel runner 可用性、真实桌面托盘/终端恢复/WebKitGTK 渲染、扫描脚本对 dmg/AppImage/deb 的提取路径。未推送标签。

**1M 勾选内联到模型行（完成，2026-09-03）：** 按用户指令把三处 `1M` 启用勾选从模型下方独占一行改为内联在各自模型行右侧，避免换行显示：主模型的 `1M` 位于快捷下拉与「检测连通 / 获取模型」操作之间，`Claude` Sonnet / Opus 档的 `1M` 内联在各自输入框行内，`Codex` 的上下文窗口勾选同位显示并随之删除只含这一项的「模型运行参数」字段集。可见文案统一为紧凑「`1M`」，完整语义（如「主模型启用 1M 上下文」）经 `Checkbox` 新增的可选 `ariaLabel` 保留为无障碍名（原生 input `aria-label` 优先于包裹 label 文本），既有按名称定位的测试契约零改动。`base.css` 为行内 `1M` 勾选补充 `flex: none` + 垂直居中规则。同轮完成「无兼容无过渡无技术债」清理：模型上下文在 `ProviderEditor` / `PreviewInspector` 上为必选属性，同轮更新全部 26 处测试调用点。已验证：`npm run typecheck` 通过；`npx vitest run src/components/ProviderEditor.test.tsx src/components/Checkbox.test.tsx` 26 项通过；`ProvidersPage` + `App` 集成 56 项通过；未启动或打开浏览器。

**模型行布局修复与用户级配置模型展示（完成，2026-09-03，2026-09-04 语义修正）：** 按用户指令完成两项修正。①主模型行布局：旧 `.asb-model-control` 栅格（`grid-row: 1` 的按钮在自动放置时会抢占 `1fr` 轨道）导致「检测连通」占满整行、主模型输入框与快捷下拉被挤到边缘，现改为弹性换行布局——输入框 `flex: 1 1 16rem` 弹性伸缩，快捷下拉与「检测连通」「获取模型」保持固有宽度，探测结果占满下方整行；720px 媒体查询里三条栅格覆盖规则随旧布局删除（换行行为由弹性布局天然承担）。②「当前用户级配置模型」上下文：唯一数据来源是应用快照的 `ConfigFileStatus.route.model`（`App.tsx` 按当前客户端过滤后经 `ProvidersPage` 下发，绝不重新推导或读草稿值）；命中真实配置的供应商卡显示该值，编辑器在主模型输入框下方显示「当前用户级配置模型：X」，变更预览顶部新增同名键值行（空值为「默认模型」）。此值只表示已读取的用户级配置，绝不宣称为运行会话模型；`scopeWarnings` 非空时编辑器和预览显示范围警告，以明确 Codex `--profile` 等覆盖风险。已验证：`npm run typecheck`、`npm run build` 通过；`npx vitest run` 编辑器 / 预览 / 快捷下拉 / 供应商页 / 列表 / `App` 集成六文件 87 项中除一项外全部通过（唯一失败为并行会话正在进行的用量刷新契约改动 `refreshIntervalMinutes` 引起的 `ProvidersPage` 用量工作区断言，非本轮引入；`App.test` 两处断言因预览新增模型上下文行与差异 before 值同文本，已改为 `getAllByText` 并注明原因）；未启动或打开浏览器。

**快捷模型选择下拉（完成，2026-09-03）：** 按用户指令考察 `外部配置` 的模型快捷选择（浅克隆其仓库，仅作行为参考）后在供应商编辑器落地同等能力，接入范围为用户选定的主模型与 `Claude` 三档映射（Haiku / Sonnet / Opus；Fable 档不存在于本仓库契约，未引入）。契约迁移（唯一所有者 = `probe.rs`）：`/v1/models` 解析从 `Vec<String>` 升级为 `ProviderModel { id, owned_by }`（缺失 / 非字符串 / 空白 `owned_by` 归 `None`），`fetch_provider_models` 与前端 `ProviderModel { id, ownedBy }` 同轮对齐，旧 `parse_model_ids` 删除。界面：新 `ModelPicker` 组件（唯一所有者）是可搜索分组模型菜单——获取模型后主模型与三档映射输入框旁各出现字段形 chevron 按钮，点开浮层菜单按 `owned_by` 厂商分组（无归属归「其他」并固定末尾，不随用户环境排序变化）、组名小号弱化标题、按模型 `id` 与厂商名过滤、当前值右置勾选、方向键 / `Escape` / 外点关闭与字体选择器同契约，浮层与搜索框经联合选择器共享 `asb-font-*` 菜单材质（零声明重复）；菜单只负责选中，清除模型一律直接编辑输入框，旧的「（从获取列表选择）」`Radix Select` 路径、`MODEL_NONE` 哨兵与 `asb-field-actions` 样式同轮删除。已验证：`cargo test --lib` 169 项通过（含 `models_parse_in_order_and_dedupe_with_vendor` 与回环 HTTP 新结构断言，独立 `CARGO_TARGET_DIR`）、`npx vitest run src/components/ModelPicker.test.tsx src/components/ProviderEditor.test.tsx` 26 项通过（新增映射档位填充用例）、`npm test` 286/286、`npm run typecheck`、`npm run build` 通过；过期引用扫描（`parse_model_ids` / `MODEL_NONE` / `asb-field-actions`）零残留；未启动或打开浏览器。

**自有 NSIS 安装体验与 Logo 展示（完成，2026-09-02）：** 安装器继续由 Tauri 的单一 NSIS 打包路径生成，保留其已验证的当前用户安装、升级/降级、WebView2、开始菜单、桌面快捷方式、启动、卸载与静默安装语义；不复制或维护第二套安装脚本。`tauri.conf.json` 的 `bundle.windows.nsis` 是安装器品牌资源的唯一配置所有者：页头、欢迎/完成页侧栏、安装器与卸载器图标均使用 Agent Switchboard 自有资源，内置简体中文和英语而不添加语言选择页。侧栏以深流蓝实体底与蓝紫双路由线呈现，中央展示现有双半宝石 Logo；页头使用同一 Logo 和两条路由线。第三方客户端标识不进入安装器，不暗示官方认可。`src/boundary.test.ts` 锁定 NSIS 资源配置、图标复用与 BMP 资产有效性；已验证边界测试、完整前端与 Tauri/NSIS 打包，以及安装包凭据扫描。

**概览 Codex 重置信号（完成，2026-08-31）：** 概览不承载会话管理。`codex_reset.rs` 成为 `https://www.codexrunway.com/api/status.json` 的唯一后端契约所有者：一次手动读取后规范化最近确认的全局重置、`resetTimeline.nextSchedule` 公告预计与 Tibo 最近的重置相关帖子（仅放行其 `x.com/thsottiaux/status/{id}` 原帖链接）；不访问本地会话、凭据、账号额度或 X 的直接接口。最后一次成功的规范化公开结果由 `LocalState` 独立原子保存为 app-data `state/codex-reset-cache.json`，仅包含展示快照，不保存原始 payload 或凭据。概览启动只调用 `get_cached_codex_reset_status` 读取该快照并标出「本地缓存」，用户手动刷新才调用 `check_codex_reset_status`；刷新失败保留旧快照，缓存写入失败也仍展示新鲜结果并明确警告下次启动无法保留。缓存损坏响亮拒绝且不改写。当前 UI 显示来源健康提示、公开 feed 时间与非官方/非账号额度说明，并支持原帖跳转。Codex Radar 的替换数据源需先取得接口授权，未授权前不调用其完整 API。已验证：`cargo test -p agent-switchboard codex_reset`（7 过，含缓存持久化/损坏缓存用例）、`npx vitest run src/components/CodexResetPanel.test.tsx src/api/client.test.ts`（2 文件、12 测试）通过；此前 `npx vite build` 通过，`npm run build` 未完整通过的既有原因仍是本轮未触碰的 `SessionManager.tsx:395` 将 `string | null` 传给仅接受 `string` 的函数。

**会话页缓存模式（完成，2026-08-31）：** `SessionManager` 移植 `外部配置` 的查询缓存语义（其前端以 `react-query` + `staleTime: 30s` 实现，本仓库不引入该依赖，以组件内时间戳与缓存表重新实现）：激活时缓存期内零请求直接显示列表；过期后先显示缓存再后台重扫，列表不闪回「正在扫描」占位；对话内容按 `app:sessionId` 缓存同样 30 秒，重新选中最近查看的会话即时出内容；缓存至多保留最近查看的 16 条对话记录，超出时淘汰最旧条目，保证长驻托盘进程内存有上界；「刷新会话」保留强制重扫；后台刷新失败保留已显示数据并提示错误，仅首扫失败回落空态。扫描触发的判定只读扫描时间戳、不读列表 state，避免首扫失败后 state 变化立即自我重扫。修复了每次切回会话页都整页重扫的问题。已验证：`npx vitest run src/components/SessionManager.test.tsx` 14 项通过（含期内重进零重扫、过期后台重扫保列表、后台失败保数据、消息缓存零重取、上限淘汰重取）；`tsc` 对 `SessionManager.tsx` 零错误；`npx vite build` 通过；浏览器连 dev 实测切页重进零新请求、重选零新请求。上轮记录的 `SessionManager.tsx:395` 类型错误已确认消除；当前 `tsc` 全仓剩余错误来自并行进行中的 `interfaceFont` 测试夹具，与本组件无关。


**会话消息目录清洗（完成，2026-08-31）：** 查证 `外部配置` 的「会话内逐项对话定位」：其形态为消息目录（提取用户消息 → 点击滚动居中 → 高亮 2 秒），本项目会话页初版已有等价实现（`outlineItems` + `jumpToMessage`，含 `> 2` 条显示门槛与契约测试）。真正的差距是目录噪音清洗，本轮以自有契约复刻：`Codex` 以用户角色记录的非对话载荷不进入目录——`AGENTS.md` 指令转储与 `<environment_context>` 整条隐藏；`# Context from my IDE setup:` 包装提取内嵌请求段（取最后一个匹配标题的负载，避免选中文件内容里的同名标题干扰），提不出请求段整条隐藏。清洗只作用于 `Codex` 会话，`Claude Code` 不清洗；消息数组本身不重排，目录项仍指向原始索引。已验证：`npx vitest run src/components/SessionManager.test.tsx` 16 项通过（新增：Codex 注入载荷不进目录且 IDE 包装提取真实提问、Claude 同形消息仍进目录）；`tsc` 对 `SessionManager.tsx` 零错误；`npx vite build` 通过。

**供应商编辑器连通检测（完成，2026-08-31）：** 按用户指定的形态（配置页内、主模型右侧按钮栏）移植 `外部配置` 的可达性检测模式，升级既有 `probe.rs` / `ProbePanel`，旧的两态契约（`url` / `reachable` 字段、表单底部「验证端点」按钮、HEAD→GET 回退）已同轮删除。现契约为三态 `grade`（`ok` / `slow` / `unreachable`）：收到任意 `HTTP` 响应即算可达（状态码只证明端点存活），延迟超过 6 秒判「连通但较慢」，仅网络级失败判「无法连通」并按 `WinHTTP` 错误码给出失败类别（域名解析失败 / 连接超时 / 连接被拒绝 / `TLS` 握手失败 / 网络请求失败）；超时类失败自动重试一次（策略函数与网络层分离、可单测），拒连与 `DNS` 失败立即定论。探测统一 `GET` 且不读响应体、不携带密钥、不发送模型请求；结果行附常驻说明「可达不代表密钥或模型配置正确」。唯一按钮与「获取模型」并列，服务地址字段只保留输入，结果显示在这组操作下方；地址变更即重置结果。已验证：`cargo test -p agent-switchboard probe` 10 项通过（分级阈值、超时重试一次、非超时不重试、二次超时终局、错误码分类）；当前按钮位置复验：`npx vitest run src/components/ProviderEditor.test.tsx src/components/ProbePanel.test.tsx` 21 项通过，`npm run typecheck` 与 `npm run build` 通过，未启动或打开浏览器。

**供应商用量查询 V2（完成，2026-08-31；展示形态于 2026-09-01 更新）：** 供应商编辑器已移除「查询用量」按钮，主模型右侧只保留连通检测与获取模型。每张供应商卡片右侧的操作按钮区都有唯一的无文字用量图标：未配置时直达独立工作区；已配置时默认显示并读取余额、已用、总量、查询方式、时间与刷新读数，并从读数内「编辑查询」回到同一工作区。图标的操作语义仅经悬浮提示与无障碍名称表达。独立工作区自己拥有查询草稿与「保存查询」动作，保存直接更新应用档案并回到列表，不再和供应商编辑草稿耦合。`UsageQuery` 是唯一带 `kind` 的契约：`declarative` 保留现有一次 `GET` + JSON 路径方式，`script` 保存用户自编 JavaScript。脚本在受限 `QuickJS` 中只生成一次 `GET` / `POST` 请求与 JSON 提取函数，实际网络仍统一由 `WinHTTP` 执行；脚本没有文件、进程、会话或直接网络能力，错误不回显脚本或密钥。旧的精确裸声明式 `usageQuery` 只在应用自有 `profiles.json` 或云备份恢复时一次性补入 `kind: "declarative"` 并原子改写；其余旧 / 残缺形状拒绝且不改写。保存边界同时校验数据形状与脚本可加载性，桌面与开发 RPC 均运行同一查询命令。已验证：`npx vitest run src/components/UsageQueryWorkspace.test.tsx src/components/ProviderUsagePanel.test.tsx src/components/ProviderList.test.tsx src/pages/ProvidersPage.test.tsx`（22 项通过）、用量相关的 `ProviderEditor` 定向断言（2 项通过）、`npm run build` 与目标文件 `git diff --check` 通过；未启动或打开浏览器。

**供应商用量账本默认展开（完成，2026-09-01）：** 已配置 `UsageQuery` 的供应商卡片进入列表即显示并读取紧凑账本区；未配置卡片不增加占位，仍由用量图标进入独立配置工作区。账本标题为「用量 / 字段提取或脚本 / 更新时间」，右侧保留「编辑查询」和「刷新」；每额度只保留名称、余额或已用主数字和总量，只有能够由总量与已用或余额推导比例时才显示细进度轨。原三列大表格、整块灰底和点击后才读取的展开路径已删除；用量图标改为默认展开状态下的收起/再次展开控制。验证：`npx vitest run src/components/ProviderUsagePanel.test.tsx src/components/ProviderList.test.tsx src/pages/ProvidersPage.test.tsx`（23 项通过）、`npm run typecheck` 与 `npm run build` 通过；未启动桌面实例做视觉验收。

**系统托盘快速切换与缓存用量摘要（完成，2026-09-01）：** 原静态的 Codex / Claude 路由文本已删除。`tray.rs` 以真实档案和客户端状态动态构建两个原生子菜单；当前实际匹配的供应商显示为禁用勾选项，选择其他供应商先调用共享 `preview_switch`，再带预览哈希调用唯一的 `execute_switch` 事务，托盘不生成配置文本、不开新写入路径。`query_profile_usage` 成为已保存供应商的唯一受管查询入口：前端仅提交 `profileId`，后端解析查询、服务地址和密钥并将成功摘要暂存于进程内；托盘只读取该缓存，在每个已有读数的菜单项与当前供应商标题显示第一条紧凑额度，不在打开菜单时联网。更新/删除档案会使对应缓存失效，重置档案会清空缓存；任何档案变更、查询成功或切换后均刷新菜单。档案状态不可读时保留可打开主界面的恢复菜单。验证：隔离 Rust 构建目录中 `cargo test -p agent-switchboard tray --lib`（6 项通过）、`cargo fmt --check`、`npx vitest run src/api/client.test.ts src/components/ProviderUsagePanel.test.tsx src/components/ProviderList.test.tsx`（3 文件、37 项）、`npm run typecheck`、`npm run build` 与目标文件 `git diff --check` 通过；未启动桌面实例做真实托盘交互验收。

**用量图标化（完成，2026-08-31）：** 卡片用量入口已收进既有操作按钮区，视觉只显示图标，继续以 tooltip、`aria-label` 与展开态表达真实操作。已验证：`npx vitest run src/components/ProviderList.test.tsx src/pages/ProvidersPage.test.tsx`（14 项通过）、`npm run typecheck`、`npx vite build --logLevel error` 与目标文件 `git diff --check` 通过；未启动或打开浏览器。

**供应商卡点击高亮移除（完成，2026-08-31）：** 点击供应商仍保留内部选择语义，用于预览、编辑与启用流程，但不再给卡片附加 `.is-selected` 或渐变描边、蓝调表面、投影等视觉态。启用按钮的持久可见性改直接消费行控件的 `aria-selected`，避免保留仅为呈现服务的选择类；实际匹配配置的 `.is-live` 卡片及「使用中」状态保持不变。验证：`npx vitest run src/components/ProviderList.test.tsx`、`npm run typecheck` 与 `npx vite build --logLevel error`。

**供应商卡连通性测试图标（2026-08-31 实现，2026-09-01 更新）：** 供应商行右侧既有动作簇新增无文字连通性图标。首次点击直接复用 `probe_endpoint` 对该档案的服务地址做检测，检测中禁用图标，结果（结论 / HTTP 状态 / 延迟 / 时间与范围说明）在同一张卡片内展开；结果显示时图标保持激活，提示和无障碍名称改为「收起 {供应商} 连通性结果」，再次点击移除结果区域，收回后再次点击重新检测。不新增第二个网络 API、凭据路径或模型请求。编辑器与卡片共享 `useEndpointProbe` 请求生命周期和 `ProbeFeedback` 展示，卡片可见性只由本地 `probeOpen` 与探测结果共同派生，地址变更时两处均废弃旧结果。验证：`npx vitest run src/components/ProviderList.test.tsx src/components/ProbePanel.test.tsx`（25 项）、`npm run typecheck` 与目标文件 `git diff --check` 通过。

**供应商卡操作区排序（完成，2026-09-01）：** 按用户指定将右侧图标动作簇固定为「编辑 → 预览/收起预览 → 测试连通性 → 用量查询/配置用量 → 删除」；只调整 DOM 次序，五项既有条件、图标、提示和行为均不变。验证：`npx vitest run src/components/ProviderList.test.tsx`（16 项）通过。

**路由、模型映射与通用设置（2026-09-01 契约重塑，2026-09-03 更新）：** 供应商档案以显式 `routeMode` 表示 `custom` 或 `official`。自定义档案必须声明服务地址与密钥；官方入口不存储地址、密钥、模型覆盖或用量脚本，OAuth 凭据继续由 Codex / Claude 原生登录管理。Codex 自定义档案固定写回 `model_provider = "openai"` 与顶层 `openai_base_url`，并在同一事务中把 API key 写入官方 `auth.json` 的 API-key 缓存形态；不创建项目自定义 provider 表，且不覆盖其他 `model_providers.*` 表。Claude 自定义档案使用主模型、Haiku、Sonnet、Opus 与 `availableModels`；`ANTHROPIC_SMALL_FAST_MODEL` 是供应商受管清理字段，当前档案未声明时一定移除，导入时升格为 Haiku 档。启用官方 Codex 入口会移除 `openai_base_url` 并恢复 `auth.json` 的 `chatgpt` 模式，保留 OAuth tokens。每客户端的通用设置存于 `state/configuration/common/{codex,claude}.json`，字段都是完整具体值。启用或重新应用当前供应商时，执行器合成「固定通用设置 + 完整供应商声明」，清理上一供应商拥有但当前未声明的字段，并保留未知宿主字段。旧聚合文件及上一版拆分档案均只做一次确定性、原子迁移；运行时没有兼容读取。

**官方设置目录覆盖（2026-09-01 完成第一阶段）：** `asb_core::ownership::official_setting_directory` 成为 Codex / Claude Code 官方设置族的唯一覆盖图，并直接派生已有可写标量的目录行；每一族都标注为「基础参数」「独立模块」或「保留不写入」。因此页面不再把 MCP、Hooks、权限/沙箱、环境映射、模型选择器、项目/本地/受管配置、官方身份与运行态笼统藏为“未知键”：用户可见其精确路径族及当前真实边界。全局 `AGENTS.md` / `CLAUDE.md` 被明确归入官方目录中的独立模块，仍走专属文件事务；基础参数仍只写应用状态，供应商重新应用仍是唯一真实客户端配置写入路径。目录不引入任意 TOML/JSON 编辑器，也不改变项目、受管或登录资源的所有权。验收：core 目录测试、Tauri 编辑器投影测试、通用设置页目录/文档隔离测试与 TypeScript 类型检查。

**生效状态与回退（2026-09-01 收敛）：** `config_status` 报告 `MatchStatus`：匹配当前供应商投影、供应商或通用设置已变更、已恢复备份、外部修改、未托管或不可判定。恢复记录优先于同形状档案，因此不会错误点亮供应商行。通用设置保存只更新应用状态；页面据最新投影记录和哈希显示已应用、待重新应用或真实配置已被外部修改。只有供应商预览绑定当前文件哈希与候选渲染哈希，确认前任一方变化都会拒绝写入。缺失的用户级文件只会在确认后创建，撤回会恢复为“不存在”。恢复前快照拥有旁车元数据，因而也可撤回；备份浏览只接受属于当前客户端、当前本机目标且与旁车路径一致的记录。供应商投影的应用记录保存失败时，客户端文件会在同一事务中恢复；概览显示可观察写入锁，并要求确认后才清理遗留锁。

**端点验证（2026-08-26 审查收尾）：** 供应商编辑器提供手动“验证端点”：Windows 原生 WinHTTP 使用系统自动代理发送 HEAD，在 HTTP `405` / `501` 或请求错误时回退 GET，只展示可达性、HTTP 状态、延迟与检测时间，不自动选择端点。地址变更会使旧结果失效。

**验证证据（2026-08-26 审查收尾）：** `cargo fmt --all -- --check`、`cargo test --workspace`（96 项）、`cargo build --workspace`、`npm run typecheck`、`npm test`（35 项）与 `npm run build` 已通过；NSIS 安装程序已生成到 `target/release/bundle/nsis/Agent Switchboard_0.1.0_x64-setup.exe`。关键新增回归覆盖候选预览过期、首次创建后撤回为缺失、恢复后恢复前快照可用、恢复写后校验回滚、遗留锁空闲判定、旧存储迁移保真、审计日志失败警告与前端预览失效。本轮仍未对用户真实配置执行切换或恢复，安装程序也尚未安装或启动验证。

**外部配置导入供应商（2026-08-27 完成）：** 发现页提供只读扫描本机 外部数据库（`外部导入数据库`，SQLite，`mode=ro&immutable=1` 连接）并一键导入供应商档案。范围与规则：

- 仅导入 `app_type` 为 `codex` / `claude` 的供应商；其余客户端列入跳过清单并说明超出产品范围。
- 自定义供应商的 API 密钥在后端提案中保留并仅写入应用自有档案：Codex 取 `auth.OPENAI_API_KEY`，Claude 取 `env.ANTHROPIC_AUTH_TOKEN` 或 `env.ANTHROPIC_API_KEY`。官方行导入为无密钥的官方入口，不读取或复制 OAuth / 登录令牌。扫描响应是无密钥摘要，导入响应只返回数量与跳过项；两种响应、警告和日志均不含密钥值。
- Claude 映射：`env.ANTHROPIC_BASE_URL` → 服务地址（有→自定义，无→官方）；`ANTHROPIC_MODEL` → 主模型；`ANTHROPIC_DEFAULT_{OPUS,SONNET,HAIKU}_MODEL` → 三档模型映射；模型名末尾 `[1m]` 或 `[1M]` 归一为语义化 1M 开关，实际 Claude 配置始终渲染为小写 `[1m]`；无法表示的键仅以字段名列警告。
- Codex 映射：`auth` 含 OAuth 令牌或空 `OPENAI_API_KEY` → 官方登录；否则解析内嵌 TOML 的 `model_providers` 表（`wire_api` 必须为 `responses`），表内额外键（如 `http_headers`）会导致该供应商跳过并说明原因，TOML 解析失败同样跳过。
- 通用设置项不从发现文件导入；用户在通用设置页自行保存。启用的、可转换的 外部配置 查询脚本随其供应商档案导入。
- 去重：与现有档案七字段全等者标记“已存在，将跳过”；导入时重新扫描避免过期预览。
- 全程不写真实 `Codex` / `Claude Code` 配置；唯一写入目标是应用自有配置目录。

实现：映射规则唯一所有者为 `核心映射模块`（纯函数，注入行数据）；SQLite 只读访问在 桌面端只读访问模块（`rusqlite` bundled，首个 C 编译依赖，已在 windows-gnu 工具链验证）；命令 `扫描命令` / `导入命令`；界面为发现页「外部配置导入」区块，逐项复选（通用 Checkbox 组件）、批量导入并汇总结果。`LocalState::profile_exists` 与 `import_profile` 共用同一七字段判等谓词。

验证证据：`cargo fmt --all -- --check` 通过；`cargo test --workspace` 111 项全部通过（含 asb-core 映射 12 项、tempdir 假库扫描/导入集成 3 项）；`npm run typecheck` 与 `vitest` 40 项通过（含扫描预览、勾选、导入参数与结果汇总）；对本机真实库执行 `--ignored` 只读扫描测试：10 行供应商 → 7 可导入 + 3 跳过，警告全部来自固定模板、无密钥值；NSIS 包已重建并静默安装。

**供应商列表拖拽排序（2026-08-28，2026-09-01 契约更新）：** 供应商行新增行首拖拽把手，列表可拖拽重排（2026-08-28 用户指令，交互方式；界面用本系统令牌实现）。显示顺序的唯一所有者是每个客户端目录内供应商文件的 `order` 字段；`reorder_profiles` 命令按客户端校验排序清单（必须覆盖该客户端全部档案、不得重复、不得混入另一客户端）及每个文件版本后，在单个存储事务中写入新顺序；非法或陈旧清单整体拒绝、不部分写入。前端引入 `@dnd-kit`（core / sortable / utilities，React 19 兼容）：`PointerSensor` 8px 启动约束保证行点选不被吞、`KeyboardSensor` + `sortableKeyboardCoordinates` 提供键盘排序、`closestCenter` + 垂直列表策略；把手是行内唯一拖拽目标，把手按钮 ≥40px 且保留可见键盘焦点。拖拽中的行加强为行动蓝描边与路由投影，不缩放。

验证证据：`cargo fmt --all -- --check` 通过；`cargo test`（src-tauri 17 项，含重排两条：跨客户端位置保真、非法清单拒绝且不改写存储）；`npm run typecheck` 与 `npm test` 49 项通过（含把手按需渲染、键盘重排上报新顺序）。

**供应商行「启用」主按钮（2026-08-28 完成）：** 非生效供应商行的动作簇左侧新增实心行动蓝「启用」主按钮（Play 图标，复用共享 `.asb-btn-primary` 令牌样式），生效行不显示、由既有「使用中」胶囊表达状态（2026-08-28 用户指令补齐 行主按钮；行为经用户选定）。行为契约：启用按钮点击＝选中该行并拉起变更预览（与预览图标同一处理器），写入仍需显式确认——为此变更预览面板头部新增「确认切换」主按钮，复用概览页路由控件同款 `ConfirmSheet` 确认弹窗与 `runSwitch`（带 `confirmWrite`）。供应商页由此具备完整的「启用 → 看差异 → 确认切换」闭环，此前确认入口仅在概览页。安全规则不变：每次真实切换在确认前展示精确差异与备份位置，任何行内按钮都不直接写配置。现展示时机见「供应商行『启用』主按钮选中门控」条目。

验证证据：`npm run typecheck` 与 `npm test` 50 项通过（含新增：生效行无启用按钮、非生效行点击启用回调携带完整档案）。

**Supabase 加密云端备份（2026-08-31 完成，用户指令「放到备份页内」）：** 备份页在本地历史下方新增「加密云端备份」，不增加设置页入口。`src-tauri/src/cloud_backup.rs` 是远端契约唯一所有者：用户填写自己的 Supabase HTTPS 项目地址、publishable key 与登录邮箱；应用显示一次性初始化 SQL，创建 `agent_switchboard_cloud_backups`，启用 RLS，仅向 `authenticated` 授予 select/insert/update，并以 `(select auth.uid()) = user_id` 分别约束读、写入和更新。用户每次上传/恢复临时输入 Supabase 登录密码（`/auth/v1/token?grant_type=password`），不持久化登录密码、access/refresh token 或 secret/service key；`profiles.json`（供应商 API key、通用配置、切换记录）在本机以用户备份密码的 Argon2id 派生 AES-GCM 密钥加密，远端只接收密文。`state/cloud-backup.json` 仅原子保存项目地址、publishable key、邮箱。上传和恢复都需确认；恢复只替换本机档案数据，不写 Codex/Claude 真实配置，随后失效预览并刷新状态。`probe.rs` 收敛出共享 `http_request` WinHTTP POST/GET 通道；Tauri 与浏览器 debug bridge 均注册同一套带类型命令。测试：Rust `cargo test -p agent-switchboard`（隔离 target）67 过 1 忽略，覆盖加密往返、错误密码、RLS SQL、设置持久化；前端 `CloudBackupPanel`/API/App 定向 35 项全过，`rustfmt --check` 新模块通过，`git diff --check` 无问题。未作真实 Supabase 上传/恢复（本轮无用户项目凭据）。全量前端目前被并行中的 `ProviderEditor.test.tsx:60` invoke mock 类型错误和同文件 5 秒超时阻断；全 workspace Rust 测试另被并行新增 `ProviderProfile.usage_query` 未更新 `asb-switch/tests/switch_tests.rs` 的两处 fixture 阻断，均未触碰。

**备份页双 Tab（2026-08-31 完成，用户指令「备份页内双tab页实现」）：** 备份页改为「本地备份」与「加密云端备份」双 `Tab`，默认本地历史。撤回上一次切换、打开备份文件夹与历史恢复只在本地页签显示；Supabase 连接、加密上传与恢复只在云端页签显示。页签切换仅改变界面状态，不读写档案或云端数据。新增 `BackupsPage` 定向测试，连同云端面板、API 边界和应用集成测试共 36 项通过。

**本地备份内滚动（2026-08-31 完成，用户指令「本地备份实现模块内自身滚动」）：** 本地备份页签填满主工作区，历史容器 `asb-backups` 成为唯一滚动所有者；撤回、打开文件夹和上次操作说明固定在模块顶部。外层 `asb-main` 不再因备份行增长而承担该页签的滚动；空历史保持普通空态。

**撤回差异确认（2026-08-31 完成，用户指令「撤回弹窗显示差异化」）：** 撤回确认弹窗在写入前读取目标备份的受管键差异，并按撤回实际方向呈现为“当前 → 将恢复备份”。差异载入中或失败时确认按钮禁用，取消会丢弃此次只读结果且绝不写入；长差异在弹窗明细区内滚动，确认 / 取消操作固定可见。

**外部配置 查询脚本导入（完成，2026-09-01）：** 只读扫描额外读取 providers.meta.usage_script。唯一映射点 核心映射模块 把可表示的已启用 JavaScript 自定义源码一次性编译为本应用的 UsageQuery::Script；持久化和运行时没有 导入源 脚本种类或兼容分支。导入脚本仅使用档案的 baseUrl 与 apiKey，并将单项或多项套餐结果转换为 UsageSummary.readings；界面逐项显示具名套餐，绝不合并或丢弃套餐名。无源码内建模板、禁用脚本、独立查询凭据/地址、未知占位符和运行时不可加载源码均保留供应商导入并以字段名说明。脚本文本、查询凭据和提案对象始终留在后端，扫描/导入响应仅包含安全的“可导入”状态和计数。验证：cargo test -p asb-core、cargo test -p agent-switchboard --lib、cargo test -p agent-switchboard usage_query --lib、npm run typecheck、定向 vitest 通过；未对真实客户端配置写入或执行网络查询。

## 当前风险

**软件日志五档复验阻断（2026-08-31）：** 本轮完成时的日志定向 Rust 测试、应用设置测试、前端定向测试与 `cargo check` 均已通过；随后并行模型契约新增 `ClaudeModelSettings.primaryOneM / sonnetOneM / opusOneM`，但 导入映射模块 与 `crates/asb-core/src/discovery.rs:250` 尚未同步初始化，当前会阻断后续 `cargo test`；同一并行改动也使全仓 `npm run typecheck` 在 `ProviderEditor.tsx:100` 和其测试夹具报相同三字段缺失。以上文件不属于软件日志范围，本轮未修改，待其所有者收敛后再做全仓复验。

**软件自身日志五档与中文界面（2026-08-31 完成，用户指令「日志等级应该分为这么多类并汉化」）：** `runtime_log.rs` 将持久化的记录阈值与已写入事件的严重性拆为两个闭合契约：`debug / info / warn / error / silent` 是 `AppSettings.runtimeLogLevel` 的唯一阈值，`debug / info / warn / error` 才可作为 `RuntimeLogEntry.level` 进入日志表；`silent` 只停止后续写入，绝不伪装成一条日志。启动读取已持久化阈值，设置成功落盘后立即更新进程缓存，插件保持 Debug 上限而由契约本身过滤，故没有双持久化状态或旧级别回退。`settings.json` 收紧为七字段当前形态；仅恰好上一版六字段形态会原子补入 `info`，更旧或未知形态明确拒绝。日志页复用既有 `Select` 与 `useAppSettings` 的唯一保存路径，中文呈现「调试 / 信息 / 警告 / 错误 / 静默」选择器及前四级筛选；窄屏控件纵向排列。验证：`npm run typecheck`、`npx vitest run src/pages/LogsPage.test.tsx src/api/client.test.ts src/App.test.tsx src/components/AppSettingsForm.test.tsx`（50 项）、隔离 `CARGO_TARGET_DIR` 下 `cargo test -p agent-switchboard runtime_log --lib`（5 项）、`cargo test -p agent-switchboard local_state --lib`（24 项）和 `cargo check -p agent-switchboard` 全通过；按用户要求未启动桌面/浏览器、未操作真实配置或日志目录。

**软件日志 Tab（2026-09-01 收敛）：** 用户澄清要的是软件自身日志后，日志页不承载配置写入历史或主从详情；写入历史仅由配置状态和撤回操作消费。唯一后端所有者为 `src-tauri/src/runtime_log.rs`：官方 `tauri-plugin-log` 仅接受本应用的闭合 JSON 运行事件，当前文件 20 MiB、保留 4 个轮转归档、跨启动追加；记录应用启动及显式的应用设置/档案、通用设置、配置切换与恢复、锁恢复、云端备份、会话恢复、外部配置 档案导入操作，成功写 `info`，失败只写既有稳定错误码，绝不写档案名、路径、配置、密钥、请求/响应、异常消息或栈。`list_runtime_logs` 同时经 Tauri 与浏览器开发桥只读该应用日志目录，解析当前及归档文件、倒序返回最多 500 条；日志页自己加载/刷新，不再混入 `useConfigSnapshot` 的配置状态轮询，提供「全部 / 信息 / 错误」筛选及时间、级别、事件、错误码表格。新增 `open_runtime_log_dir`：后端自行解析并在必要时创建应用日志目录，再交给系统文件管理器，渲染层不接收路径；日志页提供对应的「打开日志文件夹」按钮。公开 外部配置 源码只作为“统一后端记录 → 有界文件轮转 → 前端只读”的职责参考，未复制代码或视觉。

- 真实配置切换与恢复仍需在用户选定的可恢复配置副本上逐项人工验证。
- "需要重启客户端生效"目前是静态提示；应用不检测 Codex / Claude Code 进程的启动时间。
- 除非明确将其加入新目标，`MCP` 管理仍不在这些初始阶段范围内。

**通用设置与供应商投影（2026-09-01 完成）：** 通用设置页以 `Codex` / `Claude` 双 `Tab` 编辑 `state/configuration/common/{codex,claude}.json` 的具体合法值。目录的所有权、控件、值约束和供应商缺席清理由 `asb_core::ownership::setting_specs` 唯一拥有。保存不接触客户端文件、备份、锁或写入历史；保存后保留草稿失败态，并显示已应用、待重新应用或外部修改状态。真实配置仅由供应商事务生成：完整供应商声明与固定通用设置合成，当前供应商没有声明的旧供应商字段会移除，未识别宿主字段保持不变。供应商、通用设置和写入历史各有独立文件；旧聚合数据只在首次读取时确定性迁移，成功后删除源文件，运行时不再读取。真实用户文件未被自动化测试触碰。

**通用设置单一页尾工具栏（2026-09-01 完成）：** 全量恢复、保存状态与保存操作收敛为一个页面页尾：左侧「全部恢复默认值」，右侧「状态 → 保存通用设置」。待重新应用或真实配置异常占用同一状态槽；预览维持在其后的独立分隔区。分组只保留本组恢复，页面不再存在重复页尾或跨页面跳转操作。

**开发机调试开关（2026-08-28）：** 应对「每次改动都要重新构建」的迭代成本：`tauri` 启用 `devtools` 特性，新增 `toggle_devtools` 命令并在前端监听 `F12` 切换 WebView 检查器（唯一后端入口 `src/api/client.ts::toggleDevtools`，边界测试约束不变）。运行中的 exe 可直接改 CSS / DOM 实时预览；源码级迭代用 `npm run tauri dev`（Vite 热重载，调试构建自带 devtools）。按用户指令：仅在用户明说「打包」时执行 `npm run tauri build`。验证：`cargo test --workspace`（122 项）与 `npm test`（50 项）通过；本轮未构建交付物。

**推理强度档位滑块（2026-09-01 收敛）：** `model_reasoning_effort` 是 Codex 通用设置的具体档位：`minimal / low / medium / high / xhigh`。滑块保存到通用设置文件，供应商重新应用时才投影。其所有权、合法值、控件和供应商清理由 `setting_specs` 唯一提供，档案不再携带该字段；旧档案级值仅经一次冲突安全迁移进入固定客户端通用设置文件。

**Tooltip 与复选框复刻（2026-08-28 完成）：** 从 `本项目` 复刻两个通用组件并接入。`Tooltip`：引入 `@radix-ui/react-tooltip`（悬停/聚焦触发、Portal 定位、150ms 延迟），深色墨底 + 画布白字 + 模态投影，动画随 `prefers-reduced-motion` 关闭；禁用按钮经 `span` 锚点包裹也能给出原因提示。`Checkbox`：改为参考项目的 `sr-only input + 绘制控件` 结构（`data-checked` 驱动、悬停描边、按下缩放、内嵌高光），新增 `indeterminate` 半选态（读屏 `mixed`），既有调用方 API 不变。接入点：通用设置推理强度档位值签（悬停显示将写入的确切行 / 默认档说明）与 `Dual Relay` 的「查看变更」「安全切换」按钮（禁用时悬停解释前置条件：未选档案 / 未确认预览 / 写入中）。验证：`npm run typecheck`、`npm test`（54 项）与 `cargo check` 通过；本轮未打包。

**滑块完整样式复刻（2026-08-28）：** 按用户指令把推理强度滑块升级为 `本项目` 完整复刻：胶囊轨道（墨色 9% 底 + 内嵌描边）、能量渐变填充（新令牌 `--asb-gradient-energy`：安全绿→行动蓝→Claude 紫 96°，本系统第二处获准渐变表面）、填充内 9 颗白色微光粒子（glow + 1.8s 闪烁动画、逐粒子定位与延迟）、刻度悬停放大（ease-out-back）、28px 白滑钮（悬停加深投影、按下 0.96 + 120ms 快速跟随）、原生 range 全尺寸透明叠加（webkit/moz thumb 透明占位）。`tokens.css` 新增 `--asb-motion-fast` 与 `--asb-ease-out-back`；`prefers-reduced-motion` 下全部动效静止。验证：`npm run typecheck`、`npm test`（54 项）通过；未打包。

**原生 tooltip 全量替换（2026-08-28 完成）：** 界面不再使用浏览器原生 `title` 提示，全部改为复刻的深色 `Tooltip` 组件。替换点：供应商页与通用设置页的品牌图标 Tab（`Codex` / `Claude`，补 `aria-label` 维持可及名）、新建供应商 `+` 钮（禁用态经 `span` 锚点包裹）、窗口控制三钮（最小化 / 最大化-还原（动态）/ 关闭，`side=bottom`）、供应商列表的拖拽手柄与启用 / 预览 / 编辑 / 删除五个行内钮（拖拽手柄与 dnd-kit 事件经 Radix `Slot` 合并无冲突）。`ConfirmSheet` 的 `title` 属性是对话框标题而非提示，保持不变。可及名一律由 `aria-label` 承担，提示仅作视觉补充。验证：`npm run typecheck`、`npm test`（54 项，含 dnd-kit 键盘拖拽）通过；未打包。

**候选文件全文与通用配置片段预览（2026-09-01 收敛）：** 候选文件全文只属于供应商投影预览：它展示当前通用设置和完整供应商声明合成后的真实候选内容，并保留宿主未知字段。通用设置页最底部可按需查看由后端适配器从当前草稿渲染的只读通用配置片段（Codex 为 TOML、Claude 为 JSON）；片段只含非默认的通用拥有字段，不读取或写入宿主文件、不包含供应商或宿主字段，不能伪装为客户端最终候选。

**时间显示改为本地时区（2026-08-28 完成）：** 按用户指令，全局时间显示从 `UTC` 原文改为本机本地时区渲染。唯一所有者 `src/lib/time.ts::timeLabel` 保持不变，实现改为经 `Date` 本地取值并附带真实偏移标注（`YYYY-MM-DD HH:MM:SS（UTC+8）`，非整点偏移如 `（UTC+5:30）`）。覆盖全部消费点：概览匹配态与上次切换、备份历史、探针面板、撤销/恢复提示。测试确定性：`vite.config.ts` 的 `test.env` 固定 `TZ=Asia/Shanghai`（Windows Node 已验证识别），本机（UTC+8）与 CI runner（UTC）断言一致；`App.test` 断言更新为转换后的 `16:00:00（UTC+8）`，反向锁定换算逻辑。验证：`npm run typecheck`、`npm test` 54 项通过；未打包。

**自有滚动条全量替换（2026-08-28 完成）：** 按用户指令设计本项目自己的滚动条并替换全部原生滚动条——「墨线滚动条」（`Frosted Relay` 语言）：`10px` 槽位、轨道 / 按钮 / 角块全透明（无凹槽底色），滑钮以透明描边内缩为 `4px` 墨色药丸线（`background-clip: padding-box`），拖拽命中区保持全槽宽；悬停加深、按下最强，最短 `32px` 防短内容滑钮过小；深浅模式各一套基于正文墨色的 `tint`，不引入第二强调色。契约：规则唯一所有者为 `base.css` 的 `::-webkit-scrollbar` 全局块，覆盖工作区（`.asb-main`）、配置预览（`.asb-filepreview-body`）与文本域等一切滚动表面；值由 `tokens.css` 新令牌提供（`--asb-scrollbar-gutter` / `-thumb` / `-thumb-hover` / `-thumb-active`、`--asb-radius-pill`）；刻意不使用标准 `scrollbar-width` / `scrollbar-color`（Chromium 121+ 下会禁用自定义伪元素样式，WebView2 即受影响）。设计意图登记 `DESIGN.md §6`。验证：`npm run typecheck`、`npm test` 55 项通过（含令牌所有权测试，确认无散落色值）；未打包。

**选中与生效行的渐变描边样式（2026-08-28 完成）：** 按用户指令参考 外部配置 与 本项目 两个项目，为供应商行的选中态补齐样式搭配与颜色。映射：本项目 的「accent border」渐变描边（静／动两档）→ 选中行用静态蓝→亮蓝 conic 渐变描边，生效行用同款缓慢流动（6s 一周，`prefers-reduced-motion` 时静止）；外部配置 生效卡的同色软投影 → 新令牌 `--asb-accent-shadow`（明暗两套）。两态共享安静蓝调实心表面（`--asb-row-selected`），层次为：悬停＝表面轻染、选中＝表面＋静态描边＋软投影、生效＝表面＋流动描边＋软投影＋「使用中」胶囊＋头像翻转；状态仍由文字与描边共同承载，不只靠颜色。实现为 `padding-box/border-box` 双层背景（描边即渐变），前置纯色回退声明，`conic-gradient`/`color-mix`/`@property` 不受支持时退回行动蓝实线描边；全部值由 `tokens.css` 拥有（`--asb-accent-border-paint`、`--asb-accent-shadow`、`--asb-accent-border-duration`，明暗各一套），组件零色值。设计意图登记 `DESIGN.md §8`。

验证证据：`npm run build` 通过（CSS 产物含全部新规则）；以构建产物 CSS 起`本地 HTTP` 临时页，用 Playwright 截图并经视觉模型核验三态渲染——普通行白底细边、选中行渐变描边＋浅蓝底＋蓝调投影＋启用按钮、生效行同套＋使用中胶囊，无渲染异常；`npm test` 56 项通过（类名未动，纯样式变更）；临时产物已清理。

**供应商行地址链接与明细分行（2026-08-28 完成）：** 按用户指令，行内明细从「主机名 · 模型」单行改为两行：首行是可点击的服务地址链接（仍显示主机名、行动蓝、悬停下划线），次行是模型（仅有值时渲染），无地址时首行保留路由模式文案。跳转链路：查证 wry 源码确认 WebView2 下未接管的新窗口请求一律 `SetHandled(true)` 静默拦截，纯 `<a target="_blank">` 在 Tauri 内点击无效——因此引入官方 `tauri-plugin-opener`（Rust 侧注册插件，JS 侧 `openUrl`，锚点 `preventDefault` 后走插件），能力作用域限定 `opener:allow-open-url` 仅允许 `http/https` URL，与项目权限收敛姿态一致。锚点保留 `href` 与 `title`（悬停可见完整地址），键盘焦点环复用全局 `:focus-visible`。设计意图登记 `DESIGN.md §8`。

验证证据：`npx vitest run src/components/ProviderList.test.tsx` 9 项通过（含新增：链接渲染独立成行且 `href` 为完整地址、点击调用 `openUrl` 传完整 URL，插件以 `vi.mock` 隔离）；`tsc` 对本改动零错误（仓库剩余错误来自并行会话的 `__debug3__.test.tsx` 调试残留）；Rust 侧 `cargo check -p agent-switchboard` 通过（含新插件依赖）。

**配置预览换行显示（2026-08-28 完成）：** 按用户指令消除预览模块的横向滚动：`FileContentPreview` 行结构由 `white-space: pre` 整行不折改为「行号列 + 文本列」网格——行号列固定右对齐，文本列 `pre-wrap` + `overflow-wrap: anywhere`（保留缩进与空格、超长无空格令牌如长服务地址也能折行），折行文本始终对齐行号列右侧，不再回卷到行首。差异检查器与键值面板本就 `overflow-wrap: anywhere`，无需改动。契约登记 `DESIGN.md §8`（配置预览任何情况下不得出现横向滚动）。验证：`npm run typecheck` 通过、`npm test` 60 项中 59 过（唯一失败为用户并发改动的 `BackupHistory` 缓存断言，非本轮引入）；未打包。

**通用设置完整覆盖官方目录（2026-08-28 完成）：** 按用户指令把通用设置从「3 开关 + 1 滑块」扩为完整官方目录（依据当日核对的 Codex 官方 config 参考与 Claude Code settings 参考逐键核实类型与取值）：`Codex` 13 个开关 + 8 个档位（推理强度滑块、推理摘要、回复详细度、助手个性、网页搜索、沙箱模式、批准策略、会话历史），`Claude` 15 个开关 + 2 个档位（输出风格、通知渠道），按五组 / 三组分组呈现（`common_groups` 为分组顺序唯一所有者）。契约变化：`ownership.rs` 新增 `ChoiceSpec`（键 / 标签 / 分组 / 控件 / 官方取值）与 `common_choices`；`common_toggles` 增加分组；通用补丁校验改为目录驱动（档位键必须是目录取值之一，开关键必须精确携带 applied 值，`BadReasoningEffort` 等三个旧错误合并为 `BadCommonValue`，三个取值常量迁入目录）。`model_reasoning_summary` / `model_verbosity` 循 `model_reasoning_effort` 先例从档案升格为通用设置：`CodexModelSettings` 收敛为仅上下文窗口，档案编辑器、适配器叠加与发现导入同步收敛，旧存储迁移把档案级三键（含冲突检测，冲突即停止）升格进通用补丁。命令层 `common_effort` 删除，新 `common_choices` 返回 `{ groups, choices }`（含文件真实值，超出目录的原样展示）。前端 `GeneralSettingsForm` 重写为分组渲染：复选行 + 通用滑块行 + 分段药丸（`radiogroup`、白凸起激活段 + 行动蓝描边、透明边框防布局跳动、`prefers-reduced-motion` 关闭过渡）。刻意不收录并在 `DESIGN.md` 记录边界：供应商路由 / 凭据、`MCP` / 权限 / 沙箱策略表、路径与自由文本键、宿主内部状态。`disable_response_storage` 在现行官方参考页已不见单列，但为用户指定项予以保留。验证：`cargo test --workspace`（126 项，独立 `CARGO_TARGET_DIR`）、`cargo fmt`、`npm run typecheck`、`npm test`（本轮相关 59 项中 58 过；`ProviderList` 两文件因用户并发引入未安装的 `@tauri-apps/plugin-opener` 无法收集、`BackupHistory` 缓存断言为并发改动，均非本轮引入）；未打包。

**代码预览统一组件（2026-08-28 完成）：** 按用户指令把所有代码预览模块统一为单一组件并全部接入：新 `src/components/CodePreview.tsx` 是代码预览渲染的唯一所有者，导出两个视图——`CodePreview`（文件视图：行号 + TOML/JSON 令牌着色 + 上一轮的换行行为）与 `DiffPreview`（差异视图：键 + 旧值 → 新值的分层着色，旧值弱化、箭头装饰、移除警示、新值承重，即用户当日加入的 `asb-diff-old/-arrow/-remove/-new` 样式语言）。接入点：通用设置的文件预览（原 `FileContentPreview` 删除，旧路径移除）、供应商页变更预览（`PreviewInspector` 内的差异列表）、备份历史的「查看差异」（`BackupHistory.DiffRow`）。消除的重复：`changeLine` 此前在 `PreviewInspector` 与 `BackupHistory` 逐字重复，两处差异列表标记各自维护且已分叉（单行拼接 vs 分层展开）——统一为展开式规范形态，`BackupHistory` 测试的整行正则断言随之改为分层断言；`PreviewInspector` 中用户已改的「保持不变的设置」措辞保留。新增 `CodePreview.test.tsx` 锁定两个视图的契约（行号 / 令牌类名 / 旧新值分层 / （无）与移除态）。验证：`npm run typecheck`、`npm test` 63/63 通过；未打包。

**编辑器搬运 外部配置 模块（2026-08-28 完成）：** 按用户指令考察 外部配置 参考项目（浅克隆 `github.com/farion1231/外部配置` 至临时目录，MIT，仅作行为参考）后在供应商编辑器落地两项能力，其余模块因产品边界排除（本地代理 / 请求覆盖 / UA 伪装 / OAuth 托管 / 计费倍率 / 故障转移与端点自动选择 / 整文件 settingsConfig 编辑 / 第三方中继预设清单——分别违反「不做 LLM 代理、不做故障转移、凭据不归应用管、叠加层只拥有声明字段」等既定规则）。落地项：①本地元数据字段「官网地址」（可选 `http(s)` URL）与「备注」（可选，上限 500 字）——契约 `ProviderDraft` / `ProviderProfile` 新增 `websiteUrl` / `notes`（`serde default` 兼容旧存储），校验器拒绝非 `http(s)` 官网地址与超长备注，仅存本应用、绝不写入客户端配置；外部配置导入扫描随之读取并携带其 `website_url` / `notes` 列（3.x 架构必有）。②「获取模型」：主模型字段旁新按钮经新命令 `fetch_provider_models` 以 WinHTTP `GET` 拉取供应商的 OpenAI 兼容模型列表（路径规则：基址以 `/v{N}` 结尾拼 `models`，否则拼 `/v1/models`；`data[].id` 按序去重；非 2xx / 解析失败给平实错误文案；首版不携带凭据，需鉴权的端点会以 HTTP 错误反馈，可手填模型名），成功后出现下拉选择填充主模型；列表随服务地址变更即失效。探测面板的延迟毫秒显示 外部配置 端点测速已覆盖，无需搬运。验证：`cargo test --workspace`（130 项，含 `models_path_for` 路径规则、模型 id 解析去重、官网地址 / 备注校验、外部配置 元数据携带断言）、`npm run typecheck`、`npm test` 65/65（含编辑器获取模型填充与元数据提交用例）通过；按工作流本轮未打包，临时克隆已留在系统临时目录供后续参考。

**变更预览移入行内展开（2026-08-28 完成）：** 按用户指令，眼睛开关展开的变更预览从供应商面板底部移到**所展开行卡片内部、行线正下方**。结构：`asb-row-item` 卡片改为纵向两段——`asb-row-line`（原 68px 行内容，横向布局原样迁移）＋展开区；`ProviderList` 新增 `renderPreview` 渲染插槽，展开区内容由 App 注入（「变更预览」标题＋「确认切换」主按钮＋差异检查器，所有权仍在 App），列表只负责放置，展开区仅在该行 `previewOpen` 时渲染。展开区与行线以细分隔线分界、复用卡片自身表面（玻璃内不放玻璃）。展开行同时被拖拽时拖拽变换作用于整卡（含展开区），dnd 碰撞几何不受影响。设计意图登记 `DESIGN.md §8`。

验证证据：`npx tsc --noEmit` 零错误；`npm test` 66 项通过（含新增结构测试：展开区以 `.asb-row-line + .asb-preview-inline` 相邻选择器断言挂在所展开行卡片内、且全列表仅渲染一份预览内容；既有眼睛开关与 App 集成测试全过）。

**官方登录模式彻底移除（2026-08-28 完成）：** 按用户指令把官方登录从供应商档案契约中整体删除（上一轮仅移除编辑器入口）。契约：`ProviderDraft` / `ProviderProfile` 删除 `mode` 字段——所有档案一律为自定义服务（`RouteMode` 枚举保留，唯一消费者是 `RouteState.route_mode`，即对真实配置文件「当前是否官方登录」的只读观察，概览与发现页显示不变）。同一改动内同步收紧：校验器删除官方分支与 `OfficialForbidsBaseUrl` / `OfficialForbidsEnvKey` 错误（服务地址缺失一律拒绝，两客户端一致）；两个适配器删除官方渲染臂（不再支持「切换到官方」，Claude 端点恒为声明式、Codex 管理表恒为自定义表）；发现页导入——Claude 无自定义端点的配置不再产生导入建议（`claude_import_is_supported` 收紧为「有服务地址」）；外部配置导入——官方行（Codex OAuth/官方、Claude 无端点）从「导入为官方档案」改为跳过并说明原因。存储迁移：`migrate_store` 剔除存量官方档案（含 mode 缺失且无服务地址的前代记录），首次剔除前把原始 `profiles.json` 备份为 `profiles.json.pre-official-removal.bak`，随后单向写回无 `mode` 形态；官方档案的通用配置若仍带档案级键且无宿主档案则响亮报错保留原文件（新增两条迁移测试锁定）。真实存储检查：仅 1 条官方档案（`OpenAI官方订阅`，无路由数据）、通用段仅通用键，可平滑剔除。顺带修复：`StoredProfile` 补 `notes` / `website_url` 透传（上一轮遗漏会导致元数据在重启后丢失）。前端：类型、编辑器草稿、供应商行明细（移除官方标签分支）、外部配置 扫描元信息行同步；全部夹具改为无 `mode` 形态。验证：`cargo test --workspace`（130 项）、`npm test` 66/66、`npm run typecheck` 通过；未打包。

**选中行动态渐变与按钮变色（2026-08-28 完成）：** 按用户指令修复选中行三处视觉问题并移植 本项目 全局渐变色系（参考源码 `F:\projects\本项目\web\src\styles\{tokens,accent-border,buttons}.css`，用户明确指示搬运色系）：①中心扩散移除——旧选中表面是半透明 `rgba(action,0.08)`，边框区的 conic 渐变透过卡面显形为中心射线（用户截图标记的缺陷）；移植 本项目 的做法把选中表面改为不透明 `color-mix`（亮 8% 混白、暗 16% 混深底），渐变只存在于边框环。②四色环系统替换旧三段蓝环：环停唯一所有者 `tokens.css` 新令牌——`--asb-accent-ring-{blue,violet,mauve,gold}`（`#046aff/#7f66f8/#c39cad/#ffe6b6`，0°/108°/216°/320°/360° 五停闭合），分静止档（`-stops-rest`，`color-mix` 半透明）与强调档（`-stops-active`，全色）；选中行由静态描边升级为静止档 6s 流动，生效行升级为强调档 4s 流动——强度与动效共同标记命中行；`prefers-reduced-motion` 下两档均静止；旧 `--asb-accent-border-paint` 令牌（明暗两处）删除，无残留消费点。③选中行内「启用」主按钮换装移植的按钮渐变 `--asb-gradient-button`（青→蓝→粉 88° 五停，明暗同值）＋内嵌蓝光晕（悬停叠加白光增强）——实心行动蓝在蓝调选中表面上同系相融（用户截图第二缺陷）；作用域限定 `.asb-row-item.is-selected .asb-btn-primary`，其余主按钮（确认切换、安全切换、`+` 新建）保持行动蓝。全部新色值仅落 `tokens.css`（令牌所有权测试约束），`DESIGN.md §8` 已登记设计意图与不透明表面的原因。验证：`npm run typecheck`、`npm test` 66 项、`npm run build` 通过；未打包。

**获取模型列表修复（2026-08-28 完成）：** 按用户指令修复供应商编辑器「获取模型」必然失败的问题。两层根因：①桥接参数错位——`client.ts` 以 `baseUrl` 键传参而 Rust 命令参数名为 `url`，Tauri 按名匹配，点击即在调用桥报错，根本到不了 HTTP 层（前端 mock 与 Rust 直连测试各自绕过了桥接，两层测试都没拦住）；②凭据缺失——实测用户中转站对 `/v1/models` 返回 401 `API_KEY_REQUIRED`，首版「不带凭据」设计在所有鉴权端点上必然失败。修复：参数名对齐为 `{ url, app, envKey }` 并以测试锁定；命令层新增 `app` / `env_key` 参数，在点击时从用户自有配置解析密钥——Codex 读档案 `env_key` 指向的进程环境变量，Claude 读真实 `~/.claude/settings.json` 的 `env` 块（`ANTHROPIC_AUTH_TOKEN` 优先，空值视为缺失）再回退进程环境变量；密钥只进请求头（`Authorization: Bearer` + `x-api-key` 双生态 + `anthropic-version`），绝不存储、记录或回显；401/403 文案区分「密钥被拒」与「未找到密钥（指明来源）」。解析逻辑为纯函数（注入环境与文件读取器），唯一所有者 `probe::{codex_credential, claude_credential, claude_token_from_settings, auth_headers}`。验证：`cargo test --workspace` 132 项（独立 `CARGO_TARGET_DIR`，用户 dev 实例占用主 target；含 4 条新凭据测试）、`npm test` 78/78（含参数名锁定与 Codex envKey 传递两条）、`npm run typecheck` 对本改动零错误（剩余 2 错误来自用户并行会话新建的 `mock-dev-entry.tsx` 进行中文件）；curl 实测确认端点鉴权要求；未打包。同日文案统一（用户指令）：「环境变量名」字段与全部相关错误文案改为「密钥」口径——编辑器标签「密钥环境变量名」、校验文案（`InvalidEnvKey` / `EnvKeyLooksLikeSecret` / `ClaudeEnvKeyUnsupported`，后者说明密钥保存在环境变量、不进本应用）、获取模型失败的来源提示；代码标识符 `env_key` 与存储契约不变（字段存的仍是变量名，不是密钥本身）。同日可用性补充（用户提问「密钥在哪里填写」暴露说明缺失）：编辑器「密钥环境变量名」标签悬停 Tooltip 讲清密钥值的去向——Windows 用户环境变量（与本字段同名的变量），Codex 运行时自读、本应用不保存；Claude 侧无此字段（密钥由 Claude Code 自身管理）。验证：`npm run typecheck`、`npx vitest run` 全量通过。

**下拉选择复用组件（2026-08-28 完成）：** 按用户指令复刻 本项目 的下拉选择菜单并统一接入：新 `src/components/Select.tsx` 是下拉选择的唯一所有者（`Radix Select`：字段形触发器 + 右侧弱化 chevron、Portal 玻璃菜单浮层、当前项右置勾选指示、上下滚动钮；触发器与 `asb-input` 同语言，浮层走玻璃菜单材质，`220ms` 淡入缩放进入，值全部来自令牌，`tokens.css` 新增 `--asb-glass-fallback-menu` 实心后备）。接入点：供应商编辑器「客户端」与「获取模型」后的「选择模型」两处原生 `<select>` 全量替换，行为保持（切换客户端清空模型参数；模型可经「（从获取列表选择）」清除——Radix 拒绝空串选项值，以 `MODEL_NONE` 哨兵映射回 null）；通用设置页为分段药丸 / 滑块，无下拉，不涉及。测试基建：`src/test/setup.ts` 补 jsdom 缺失的指针捕获与 `scrollIntoView` 存根。设计意图登记 `DESIGN.md §8`。验证：`npm run typecheck`、`npm test` 77/77（含新增 `Select.test.tsx` 四项：占位符与选中标签 / 选项点击回调 / 勾选指示 / 禁用不展开；`ProviderEditor` 模型选择用例改为触发器 + 选项点击路径）；未打包。

**输入框与文本域复用组件（2026-08-28 完成）：** 按用户指令复刻 本项目 的输入框（`web/src/components/ui/{input,textarea}.tsx`）并统一接入：新 `src/components/Input.tsx` 与 `Textarea.tsx` 是文本控件的唯一所有者——原生属性（`type` / `required` / `placeholder` / `rows` / `min` / 无障碍属性等）全量透传，字段外观类由组件内部持有，消费方不再自写 `className`；本项目 的 `appearance` 三态与 `controlSize` 四档按「无消费者即不实现」裁剪，仅保留本系统实际使用的代码变体（`code` 布尔 → `asb-code` 等宽）。`.asb-input` 补齐 本项目 字段契约的缺失状态（此前无任何交互态）：placeholder 弱化色、悬停行动蓝描边、`focus-visible` 焦点环（替换浏览器默认 outline，与复选框 / 选择触发器同语言）、禁用 0.45 透明 + 默认光标、120ms 描边与阴影过渡（`prefers-reduced-motion` 经令牌归零）——值全部来自 `tokens.css`。接入点：供应商编辑器全量 10 个 `<input>`（名称 / 官网地址 / 服务地址 / 主模型 / 环境变量名 / 上下文窗口 / 三档模型映射）与 2 个 `<textarea>`（备注 / 可选模型列表）；通用设置为复选框 / 滑块 / 分段药丸，无文本输入，不涉及。验证：`npm run typecheck`、`npx vitest run` Input / Textarea / ProviderEditor 三文件 15 项通过（含新增：原生属性透传可及树 / 输入回调 / 禁用不可输入 / code 变体类名；既有编辑器行为用例不受影响）；未打包。

**窗口控制改走 Win32 系统命令（2026-08-28 完成）：** 按用户指令把标题栏三钮（最小化 / 最大化-还原 / 关闭）的执行路径从 tauri 窗口抽象（`window.minimize()/maximize()/close()`）改为 Windows 原生实现，前端样式零改动。背景：三钮本就是自绘（无装饰窗口 + webview 内 SVG 按钮），此前仅点击动作经 tauri 转发 `ShowWindow`。实现：`commands.rs` 四个窗口命令统一经 `windows-sys` 走 `SendMessageW(WM_SYSCOMMAND, SC_*)`——与系统标题栏按钮同一通道（`SC_MINIMIZE` / 最大化态经 `IsZoomed` 判定后发 `SC_MAXIMIZE` 或 `SC_RESTORE` / `SC_CLOSE`，`SC_CLOSE` 经 `WM_CLOSE` 仍进 tauri 的 close-requested 流程）；`window_is_maximized` 同步改用原生 `IsZoomed`；HWND 取自 `window.hwnd()`（`windows` crate 句柄与 `windows-sys` 同为 `*mut c_void`，直接透传）。`Cargo.toml` 的 `windows-sys` 增加 `Win32_UI_WindowsAndMessaging` 特性。前端 `WindowControls` / api client / 命令签名均未动。架构定案（用户三选一拍板「维持现状」）：自绘按钮 + 系统命令通道即最终形态——真原生标题栏（顶部加系统栏、纯色不能磨砂）与 WebView2 Window Controls Overlay（理想形态但 SDK prerelease、wry 不支持 wry#1650、fork 即技术债）均被否；悬停最大化不弹 Snap Layouts 为 WebView2 平台限制，已知且接受。验证：独立 `CARGO_TARGET_DIR` 下 `cargo check` 通过（本轮窗口命令区域零警告零错误）；全量 `cargo test --workspace` 因用户并发进行中的 `fetch_models` 凭据重构（`probe.rs` / `commands.rs` 测试段编译错误）暂无法收集，非本轮引入，待该改动落地后随跑；未打包。

**供应商 API 密钥档案化（2026-08-28，2026-09-03 存储更新）：** `ProviderProfile` / `ProviderDraft` 的 `apiKey: String` 由每个客户端的独立供应商文件保存，编辑器中 Codex / Claude 均有必填密码输入「API 密钥」；空白或超过 4096 字符拒绝。切换映射：Codex 写入官方 `auth.json` 的 `auth_mode = "apikey"` / `OPENAI_API_KEY`，Claude 写入 `env.ANTHROPIC_AUTH_TOKEN`；通用设置不可写。模型获取命令只接收当前编辑器 `apiKey`。外部配置 和本机发现仅在后端导入边界读取凭据；密钥不进入 RouteState、告警或诊断。`FilePreview.content` 是展示专用脱敏副本，执行器内部原文只用于哈希、校验与原子写入；TOML/JSON 按敏感键路径遮罩。差异、错误、日志与诊断继续只见遮罩。

**变更预览接入优质打印全文（2026-08-28 完成）：** 按用户指令把优质打印模块接入供应商变更预览，替换真实配置下不可读的「保持不变的设置」扁平键列表（Codex 宿主键可达 90+ 个）。契约迁移（单一所有者 = `asb-switch::executor::FilePreview`）：`FilePreview` 新增 `content` 字段——`read_preview_for` 把候选渲染一次，`rendered_hash` 与界面展示共用同一文本；`preview_switch` 与 `preview_common` 由此都携带全文。同一改动删除旧路径：`commands.rs` 的 `CommonPreview` 包装结构与其重复的读取＋`common_render` 渲染段整体移除，`preview_common` 直接返回 `FilePreview`（`preview.target` 回填与 `preview_switch` 对齐）；前端 `CommonPreview` 接口删除。界面：`PreviewInspector` 的「保持不变的设置」键值行与顶部路径 h2 移除（路径由 `CodePreview` 文件条显示，避免重复），在差异视图与警告之后渲染 `CodePreview` 候选全文——被保留的宿主键原位可见，`320px` 限高滚动不撑爆行内展开区。安全不变性以隔离测试锁定：执行后落盘文件与 `fp.content` 逐字节相等。验证：`cargo test -p asb-switch`（3 + 16 项含新断言）、`npm run typecheck`（仅剩用户并发 `mock-dev-entry.tsx` 的既有脚本作用域错误，非本轮引入）、`npm test` 78/78（`PreviewInspector` 新增候选全文原位断言；`fetch_provider_models` 新签名断言由用户会话同步更新）；`cargo test --workspace` 暂被用户进行中的凭据改动（`claude_credential` 可见性）阻塞，与本轮无关；未打包。

**扁平列表全量清查与旧契约删除（2026-08-28 完成）：** 按用户指令复查全部列表渲染面并完成优质打印迁移收尾。清查结论：唯一把配置键平铺成顿号列表的就是上一轮已替换的变更预览「保持不变的设置」；其余列表均非键堆——概览配置状态与发现页为结构化状态行（路由 / 匹配 / 警告消息），外部配置 扫描为复选行＋单行 meta，操作警告为消息 `<ul>`，确认弹窗为单行事实，备份差异已走 `DiffPreview`，通用设置与供应商变更预览已走 `CodePreview` 全文，均维持现状。随之删除旧契约（前端已零消费）：`SwitchPreview.preserved` 字段从 Rust 契约与 TS 接口整体移除，两适配器的 `collect_preserved` 收集逻辑删除，宿主键安全性改由既有的渲染级测试承载（codex `render_preserves_host_content_and_comments`、claude `render_updates_owned_keys_and_keeps_host_blocks`），两个 preview 测试改名并删除 preserved 断言；前端 fixtures 同步。用户未跟踪的 `mock-dev-entry.tsx` 中残留的 `preserved: []` 为无类型 mock 数据，运行时无害，因该文件正被并发会话编辑而未触碰。验证：`cargo test --workspace`（132 项全过，含 asb-core 86 / asb-switch 19）、`npm test` 78/78、`cargo fmt --check` 本轮改动文件零差异（现存 diff 均为用户并发会话文件）；typecheck 仅剩 `mock-dev-entry.tsx` 两个既有脚本作用域错误（非本轮引入）；未打包。

**通用设置勾选导致界面偏移——实测定位与结构修复（2026-08-28 完成）：** 用户报告通用设置页勾选 / 取消勾选导致界面整体偏移。为避免凭读码猜测，搭了临时实测环境（`mock-dev` 入口给真实前端装 stateful invoke 桩 + 完整目录数据 + 真实延迟，Playwright + 系统 Edge 以 PerformanceObserver `layout-shift` 实测点击前后几何），定位出两类真实位移源：其一，操作横幅（错误 / 警告 / 切换结果）渲染在 `.asb-main` 滚动流顶部，其插拔把下方内容整体推移约一行横幅高度（实测 `scrollTop` 跳变 +64px，仅靠 Chromium 滚动锚定掩盖）；其二，配置预览体 `max-height` 随文件行数增长、滚动条槽位出现引发重排（边界几何实测位移分 0.059）。结构修复（无兼容层、无过渡方案）：`App.tsx` 新增 `.asb-workspace` 包裹层，三横幅移出滚动流、装入 `.asb-banner-stack` 绝对定位浮层（`pointer-events: none`、模态投影、近实心底）；`.asb-main` 补 `flex: 1; min-height: 0` 并加 `scrollbar-gutter: stable`；`.asb-filepreview-body` 由 `max-height` 改为固定 `height: 320px` 并加 `scrollbar-gutter: stable`。实测复验全部归零：成功路径（勾选 / 取消 / 分段选择 / 恢复默认，Codex 与 Claude）、错误注入、警告注入、内容恰好充满的边界视口，位移分全部 `0.00000`，错误 / 警告场景 `scrollTop` 不再跳变。脚手架（mock 入口、探针脚本、playwright-core 依赖、vite 实例）已全部删除，`package.json` 仅余用户既有依赖改动。验证：`npm run typecheck`、`npm test` 78/78 通过；未打包。

**差异视图拆分为全局 DiffView 并改红绿语义（2026-08-28 完成）：** 按用户指令把差异渲染实现为独立全局通用组件并统一红绿色设计。拆分：新 `src/components/DiffView.tsx` 是变更行差异渲染的唯一所有者（键 + 旧值 → 新值，等宽字体、近实心底）；`CodePreview.tsx` 收敛为仅文件视图（`DiffPreview` 导出删除）。红绿设计：旧值＝红字 + 浅红 tint 芯片（复用 `--asb-danger`），新值＝绿字 + 浅绿 tint 芯片（新令牌 `--asb-diff-add-text`），移除＝红色加粗「移除」芯片，箭头与文字保证不只靠颜色可读；旧值缺失「（无）」改中性弱化（新 `asb-diff-none`，无旧值即无移除、不染红）。令牌新增明暗两套（`--asb-diff-add-text` / `--asb-diff-remove-bg` / `--asb-diff-add-bg`，移除文字直接走模式自适应的 `--asb-danger`）。接入点：供应商变更预览（`PreviewInspector`）与备份历史「查看差异」（`BackupHistory`）统一改用 `DiffView`。测试：差异用例迁至 `DiffView.test.tsx` 并按红绿语义更新断言（旧值红类、新值绿类、「（无）」中性类），`CodePreview.test.tsx` 收敛为文件视图用例。验证：`npm run typecheck`（除用户 `mock-dev-entry.tsx` 既有错误外零错误）、`npm test` 78/78；Playwright 临时 harness 截图经视觉核验——浅色与深色模式下红 / 绿芯片、红色「移除」、中性「（无）」、弱化键名全部清晰可读，无对比度问题，临时产物已清理；未打包。

**选择框点击的外层蓝环移除（2026-08-28 完成）：** 用户报告所有选择框点击后被一层额外蓝色边框包裹。根因：复选框、单选、滑杆这类输入控件被鼠标点击获得焦点时，Chromium 对其原生恒定匹配 `:focus-visible`（与按钮不同），于是绘制控件（复选框盒、分段药丸、滑块白钮）每次点击都套上焦点环。契约要求键盘焦点必须可见，不能整体删除——改为按输入模式区分：`App.tsx` 新增输入模式跟踪（Tab / 空格 / 方向键按下时在根元素标记 `data-focus-source="key"`，任何指针按下即清除），`base.css` 中三条绘制控件焦点环选择器全部以 `:root[data-focus-source="key"]` 前置门控（复选框盒、分段药丸、滑块钮）。效果：鼠标点击无环；Tab / 方向键 / 空格导航时环照常显示；表单文本输入的点击焦点环不受影响（标准表单行为）。新增 `App.test` 用例锁定标记生命周期（键盘标记、指针清除）。验证：`npm run typecheck`、`npm test` 79/79 通过；未打包。

**全会话「无兼容无过渡无技术债」审计与清债（2026-08-28 完成）：** 用户确立全会话准则后，对本会话全部改动逐项审计。rg 全仓扫描确认：旧类名（`asb-effort-*`）、重复助手（`changeLine`）、前端档位常量（`EFFORT_DETENTS`）、已删契约符号（`common_effort`/`EffortState`/三个取值常量/三个旧错误变体）、实测脚手架（mock 入口、探针脚本、playwright-core）全部清零。唯一真实残留为 `local_state.rs` 的旧数据升格迁移——`StoredCodexOptions` 三个遗留字段、`LEGACY_CODEX_COMMON_KEYS` 表、`legacy_codex_line` 与升格循环、两个迁移测试：其唯一服务对象（在盘 profiles.json）已被该迁移重写，代码已成死过渡路径，整体删除；解析边界同步收紧为 `deny_unknown_fields`（含 serde tag 字段声明），旧形态存储从此响亮报错「旧 Codex 档案模型选项无法解析」而非静默吞字段。既有更早年代的迁移（模式推断、档案级键降格、弃用 Claude 键）非本会话产物，未动。验证：`cargo test --workspace`（130 项）、`cargo fmt`、`npm run typecheck`、`npm test` 84/84 通过（期间偶发失败均为用户并发编辑的瞬时态）；未打包。准则已写入长期记忆。

**备份页新增「打开备份文件夹」（2026-08-28 完成）：** 按用户指令在备份历史面板标题行新增「打开备份文件夹」按钮（与「撤回上一次切换」同入 `asb-panel-actions` 分组，常驻可点、不参与 busy 门控——只读导航动作；记录为空时按钮仍在，可直达目录）。契约：后端新命令 `open_backup_dir` 是该操作的唯一入口——解析 `LocalState::backup_dir()`（应用数据目录 `state/backups`）、按需 `create_dir_all`（空历史也能打开）、经 `tauri-plugin-opener` 的 Rust API（`OpenerExt::open_path`，签名收 `Into<String>`）唤起系统资源管理器；从 Rust 侧调用而非前端 `openPath`，故无需放宽 opener capability 的路径 scope，现有 `opener:allow-open-url` 保持仅 http/https 不变。前端 `client.ts` 新增 `openBackupDir()` 类型化封装，失败经统一 `setError` 呈现类型化错误。`DESIGN.md §8` 操作表同步登记。验证：`cargo check` 通过（仅余 `local_state.rs` `kind` 字段既有 dead_code 警告，非本轮引入；构建期间运行中的 dev 实例占用资源致 os error 32，按既定流程 `taskkill` 后编译通过）；`npx vitest run src/App.test.tsx` 12/12 全量连跑三次稳定（含新增「按钮点击即调 `open_backup_dir`」用例）；`npm run typecheck` 对本轮三个文件零错误（`ConfirmSheet.tsx` 的 key 类型错误为用户并行改动既有，非本轮引入）；未打包。

**时间展示统一组件与探针状态色（2026-08-28 完成）：** 按用户指令三件事：①端点验证结果行以颜色承载结论——可达＝安全绿、不可达＝警示红（新类 `asb-ok-text` / `asb-fail-text`，取既有令牌 `--asb-safe` / `--asb-danger`；文字「可达 · HTTP 状态 · 延迟」/「不可达」同时在场所保不只靠颜色可读）。②时间格式去掉 `（UTC+8）` 偏移后缀——`timeLabel` 只输出 `YYYY-MM-DD HH:MM:SS` 本地时区，`offsetLabel` 助手删除；`vite.config.ts` 的 `TZ=Asia/Shanghai` 测试锚定不变。③时间展示收敛为全局组件：新 `src/components/Time.tsx`（`<time dateTime>` 语义元素）是界面时间渲染的唯一所有者，`timeLabel` 降为格式化纯函数；全部消费点统一接入——概览匹配态三例（`matchLabel` 返回类型放宽为 `ReactNode`）、概览上次切换、备份页上次操作、备份历史表格与恢复确认、探针面板、撤回确认（`ConfirmSheet` 的 `details` 类型放宽为 `ReactNode[]`，键改用索引）。新 `Time.test.tsx` 两条契约：渲染无后缀本地时间 + 扫描测试锁定 `timeLabel` 在 `lib/time.ts` 与组件之外零直接消费者（复用 boundary 测试扫描模式）。验证：`npm run typecheck` 零错误、`npx vitest run` 84/84（20 文件）；未打包。

**开发入口单一化——根治 HMR 窗口闪烁（2026-08-28 完成）：** 用户报告「每次热更新都 build、窗口闪」。`tauri dev --verbose` 实测定位根因：非 `npm run build` 参与热更——Tauri Rust watcher 监听 `src-tauri/`、`crates/`，并行改动 `src-tauri\src\*.rs` 时被判定 `Rebuilding application...` 并重启原生窗口（闪烁即窗口销毁重建，是编译型宿主的固有行为，无热加载可言）。先尝试 `--no-watch` 形态，经用户权衡拍板回到 watcher 模式（A）：Rust 改动自动重建重启，前端改动走 Vite HMR 不闪。无兼容收敛：唯一开发入口 `npm run dev` = `CARGO_TARGET_DIR=target-dev` + `tauri dev`（带 watcher；`target-dev` 与并行实例的默认 `target` 隔离，规避 os error 32 文件锁）；Vite 服务收敛为内部脚本 `dev:frontend`，仅由 `beforeDevCommand` 消费、不是用户入口；本轮临时引入的 `dev:tauri` 别名与旧的独立 `vite` 型 `dev`、历史 `npm run tauri dev` 用法同轮删除，全仓 rg 零残留。`target-dev/` 入 `.gitignore`。验证：新入口真实启动成功——日志含 `Watching src-tauri / crates/*`、9 秒增量编译后 exe 自 `target-dev\debug` 运行、期间前端 HMR 更新正常送达；`npm run typecheck` 通过；未打包。

**「撤回上一次切换」按钮改红色填充（2026-08-28 完成）：** 按用户指令把备份页标题行的撤回按钮从 `asb-btn-secondary` 换为既有的 `asb-btn-danger`（`--asb-danger` 填充 + `--asb-text-on-action` 白字，明暗模式自适应，该类原唯一消费者是确认弹窗的破坏性确认钮）。`DESIGN.md §8` 操作表新增撤回行，并修正「删除提供商」行的「仅在确认流程中」表述以匹配红色现在同样出现在撤回入口的事实。验证：`npx vitest run src/App.test.tsx` 12/12（撤回流程用例按名称定位按钮、不受类名影响）；`npm run typecheck` 零错误（上一轮 `ConfirmSheet.tsx` 的用户并行错误已被其会话修复）；未打包。

**旧聚合存储迁移（2026-09-01）：** 首次启动只将旧聚合档案文件作为迁移源；目标是当前的通用、供应商与历史独立文件。迁移前验证完整旧形状，重复键或不可表示数据整体拒绝；成功时旧文件先隔离，所有新文件写入并校验完成后才删除隔离源，任何失败都还原原文件并删除部分新状态。运行时没有旧格式读取、重置入口或兼容分支。

**重置按钮不可点击修复（2026-08-31）：** 用户报告「清空旧档案并重新开始」点不了。根因：按钮位于错误横幅内，而横幅层在 2026-08-28 布局位移修复中被定为 `pointer-events: none` 纯信息浮层，按钮继承了不可点击；jsdom 不加载 CSS，故既有测试全绿而真机失效。修复：`base.css` 新增 `.asb-banner > button { pointer-events: auto }`——横幅表面与文字区域保持穿透，仅横幅内操作控件恢复指针事件，布局位移契约（横幅不拦截内容点击、几何不变）不受影响。新增 `src/styles/banner.test.ts` 扫描测试锁定该 CSS 契约（横幅层保持 `none` 且横幅按钮必须 `auto`）；`DESIGN.md §7` 同步修正横幅指针事件表述。

**Windows 托盘与应用设置（2026-08-28 完成）：** 按用户批准方案新增 Windows 系统托盘与顶层「设置」页。原生层：`src-tauri/Cargo.toml` 显式启用 Tauri `tray-icon`；`src-tauri/src/tray.rs` 是唯一 tray 所有者。`lib.rs` 对 `main` 的 `CloseRequested` 按应用设置处理：默认 `hideToTray` 会 prevent_close + hide，`exit` 放行；同一策略覆盖自绘关闭钮、Alt+F4 与任务栏关闭。当前托盘行为见「托盘交互定稿」条目。

应用偏好契约：`local_state.rs::AppSettings` 是唯一所有者，独立原子保存于 app-data `state/settings.json`，绝不进入 `asb-core` 客户端 ownership / `CommonConfigPatch` / `profiles.json`，缺失文件返回完整默认值且不创建文件；严格 `deny_unknown_fields`，未知字段响亮拒绝。顶层导航新增「设置」页；`AppSettingsForm` 管理关闭行为、主题、动态效果、置顶与硬件加速，不显示配置行伪预览。主窗口 focus 时调用现有 `refresh`，界面重取真实状态。

**会话管理（2026-08-28 完成）：** 阶段 07 增加只读会话浏览子目标，范围严格限定为 `Codex` 与 `Claude Code` 的本地 JSONL 会话记录。契约所有者为 `src-tauri/src/session_manager.rs`：扫描固定的用户目录、统一元数据、按客户端 + 会话 ID 重新定位详情；前端不会接收或传入源文件路径。界面目标为顶层「会话」Tab 的筛选列表、按需加载详情、恢复命令与工作目录复制，以及在新命令提示符窗口中实际恢复；不复制 多客户端、缓存、删除或会话 / SQLite 改写路径。

**托盘可恢复性加固（2026-08-28 完成）：** 用户要求应用顶栏/托盘“必须常驻时刻存在，不能因为报错导致整个应用丢失”。`tray.rs` 的唯一 `TRAY_READY` 状态只在 TrayIconBuilder `.build()` 成功后置位；纯决策 `keep_alive(tray_ready, settings)` 保证没有已创建 tray 时绝不隐藏窗口，`settings.json` 不可读时若 tray 存在则保活/隐藏，用户选择 `exit` 时放行。`lib.rs` 的 `CloseRequested` 和 `RunEvent::ExitRequested` 统一通过该决策；面板的「退出应用」设置一次性 `EXPLICIT_EXIT` 标记后调用 `app.exit(0)`，事件守卫只放行这条明确退出，其余系统退出请求仍隐藏到 tray。新增 Rust 纯函数测试覆盖：设置读取失败仍保活、exit 策略放行、无 tray 禁止隐藏、显式退出标记一次性消费。前端 `AppErrorBoundary` 仅包工作区、不包顶栏/窗口控制：任一业务页面渲染异常时显示「界面未能加载」恢复面，原生 tray 和窗口控制保持可用。

**设置页扩展：外观与窗口（2026-08-28 完成，2026-08-31 更新）：** 按用户选择扩展顶层「设置」页的应用级通用配置（外观、窗口与性能），仍完全独立于 Codex / Claude Code 配置。`AppSettings` 严格完整契约为 `{ closeBehavior, theme, motion, alwaysOnTop, hardwareAcceleration }`：`ThemePreference = system/light/dark`，`MotionPreference = system/reduce`，默认 `hideToTray + system + system + false + true`。为保留实际存在的上一版完整四字段业务数据，首次读取时仅将恰好为该形态的 `state/settings.json` 原子升级为当前对象并写入 `hardwareAcceleration: true`；这不是持续双读，升级成功后磁盘只保留当前契约。旧仅含关闭策略的设置文件、未知字段、非法枚举值一律明确报「应用设置格式无效」，不补字段、不静默改写。原子写入不触碰 profiles、备份或任何客户端配置路径。

即时行为：前端在完整设置成功加载/保存后将 `data-theme` 与 `data-motion` 应用到根元素；`system` 时移除属性。`tokens.css` 是唯一视觉值所有者：显式 light 屏蔽系统 dark，显式 dark 覆盖系统 light，`reduce` 将现有 press/fast/node/sheet 动效时长归零。`AppSettingsForm` 新增「外观」分段（界面主题、动态效果）、「窗口与托盘」中的「窗口始终置顶」复选框，以及「性能」中的「启用硬件加速」复选框。后端 `set_app_settings` 先调用 main window 的 `set_always_on_top`，失败不落盘，避免保存但未生效；硬件加速的写入只在下一次完整启动、WebView2 创建前由启动路径映射为浏览器参数，关闭时在 Wry 默认参数上追加 `--disable-gpu`，已有显式浏览器参数则保留并追加。启动时读取有效完整设置并恢复置顶，设置损坏时保持 tray/window 可恢复外壳并继续采用 GPU 默认行为，严格错误由设置 UI 呈现。App 保存每项时合并完整对象，失败不改变已应用的主题/动效，测试锁定完整提交与失败保持视觉状态。

验证：`cargo fmt --check`、`cargo test --workspace`（全部通过）、`npm run typecheck`、`npm test` 23 文件 98 项全部通过；临时 `target-check` 已清理；README、DESIGN.md 已同步；未打包。

**托盘交互定稿：左键主窗口 + 右键原生菜单（2026-08-28 完成）：** 用户最终选定：左键单击托盘图标恢复并聚焦主窗口，右键弹出 Windows 原生菜单；此前的独立 `tray` WebView 面板整体删除。`tray.rs` 是唯一托盘所有者：菜单为 Agent Switchboard 自有结构——「打开主界面」、两条禁用的 Claude / Codex 当前路由状态（经 `config_status` 读取真实文件，绝不作为未确认切换入口）、分隔线与显式「退出」（`app.exit(0)` 经一次性 `EXPLICIT_EXIT` 标记放行）；`show_menu_on_left_click(false)` 保证左键不弹菜单；写操作成功后经 `tray::refresh` 刷新菜单状态文本。`tauri.conf.json` 删除 `tray` 窗口声明，`lib.rs` 删除面板窗口事件分支与 `show_main_window` / `hide_current_window` / `exit_app` 命令注册；前端删除 `TrayPanel` 组件、测试与 `isTrayWindow` / `showMainWindow` / `hideCurrentWindow` / `exitApp` 封装及对应断言，`main.tsx` 恢复只渲染 `App`，`base.css` 删除面板样式块。`TRAY_READY` 可恢复性与 `keep_alive` 决策不变：无 tray 绝不隐藏窗口，设置不可读仍保活。验证：`cargo test --workspace`（139 项通过；src-tauri 32 passed、1 ignored）、`npm test`（23 文件、102 项）、`npm run typecheck`、`npm run build` 均通过；未打包。

**供应商行「启用」主按钮选中门控（2026-08-31 完成）：** 按用户指令，「启用」主按钮默认不展示，仅在该行被选中且非生效时渲染——`ProviderList.tsx` 的展示条件由 `onActivate && !active` 收紧为 `onActivate && selected && !active`。无兼容清债：①`base.css` 删除 `.asb-row-item.is-selected .asb-btn-primary` 两条规则的 `.is-selected` 前缀——该前缀属旧设计（按钮常驻全部非生效行、选中行换装渐变），按钮现仅存在于选中行，前缀恒成立即死条件；合并为行内主按钮恒为移植渐变身份，注释同步改写前提。②`DESIGN.md §8` 交互契约改为「默认不展示，仅行被选中且非生效时出现在动作簇左侧」，并删除已无对应形态的「实心行动蓝」描述。③测试按新契约重写：未选中时列表无任何启用按钮；选中非生效行显示并路由变更预览流；生效行即使选中也不显示（原「非生效行均显示」用例删除）。验证：`npm run typecheck`、`npm test` 23 文件 102 项全部通过；未打包。

**Tab 连续点击卡死：请求生命周期无债修复（2026-08-31 完成）：** 用户报告 Tab 按钮连续点击直接卡死，审查定位根因不在点击处理本身，而是三条读取链路在连续切换时叠加同步后端命令且旧回包无条件回写：通用设置 Tab 每次切换发起 `preview_common`（同步命令，整文件解析 + 渲染 + 双哈希）、会话页每次进入重挂载触发全量 JSONL 扫描、供应商预览在途请求晚到覆盖最新选择。按「无兼容无过渡无技术债」一轮收敛为统一契约：**同目标在途请求唯一化（复用），身份或内容变化即失效（丢弃回包）**。

实现要点：①`App.tsx` 引入 `requestSwitchPreview` / `requestCommonPreview`（在途 Promise 按目标缓存复用，完成自动出队）与两条失效路径——`retractPreview`（选择 / 客户端 Tab / 收起：仅撤展示，候选缓存仍有效）和 `invalidateCandidates`（切换、恢复、撤回、通用写入、档案增删改、导入、重置存储：清空全部候选缓存，因缓存 Promise 渲染的是写前文件）；全部写操作入口统一走 `invalidateCandidates`，消除散落的裸 `setPreview(null)`。②通用设置改用按客户端独立纪元（`commonEpoch`），替换全局版本号——全局号会在切换客户端时误丢另一客户端的写后回读（勾选 Codex 后切 Claude，Codex 的 toggle/choice/preview 刷新被静默丢弃）；`applyCommonLine` 写后回读三件套同纪元门控，且写前显式使该客户端预览缓存出队，杜绝复用写前候选。③`SessionManager` 改为必填 `active` 契约 + 常驻挂载（`hidden` 分节）：仅在激活时扫描、在途扫描被再次激活复用（乱纪元丢弃），从触发源消除「导航反复进出 → N 次全量扫描」的堆积；启动时不再扫描。④`WindowControls` 三命令补 `.catch`（连点不再产生未处理 rejection）；测试 mock 履约返回 Promise，组件侧不加 `Promise.resolve` 兜底。

回归测试（deferred 控制完成顺序）：通用设置 Tab 快速交替——`preview_common` 恰两次（codex 在途复用）且只显示当前客户端文件；供应商预览 A/B 乱序——B 先回显、A 晚到被丢弃不覆盖；会话未激活不扫描、激活即扫、反复激活共享同一在途扫描（`list_sessions` 恰一次）。验证：`npm run typecheck`、`npx vitest run` 23 文件 106 项全部通过；`rg` 确认兜底 shim、旧全局版本号与未门控回写路径零残留。已发出的 Tauri 同步命令无法被前端中止（平台约束），但其回包现被纪元严格丢弃；后端命令未改动。未打包。

**窗口控制改用 Windows 原生标题栏（2026-08-31 完成）：** 按用户指令（不要自己实现、用 Windows 原生、最新样式、无兼容无过渡无技术债），推翻 2026-08-28「自绘按钮 + 系统命令通道」定案：`tauri.conf.json` 删除 `decorations: false`（恢复默认 `true`），最小化 / 最大化-还原 / 关闭三钮由系统标题栏绘制（Win11 最新样式、Snap Layouts 悬停、系统菜单全套原生）。无兼容全量删除：前端 `WindowControls.tsx` 与测试、`icons.tsx` 四枚窗口图标、`client.ts` 五个封装（连同 `getCurrentWindow` 导入、`App.test.tsx` 的 `@tauri-apps/api/window` mock 与 `window_is_maximized` 分支）、`base.css` 窗口按钮样式块、顶栏三处 `data-tauri-drag-region` 与 `core:window:allow-start-dragging` 权限；Rust 侧 `window_minimize / window_toggle_maximize / window_is_maximized / window_close` 四命令与 `SendMessageW(WM_SYSCOMMAND)` / `IsZoomed` 通道、`windows-sys` 的 `Win32_UI_WindowsAndMessaging` 与 `Win32_Foundation` 特性同轮删除。新增原生标题栏与主题偏好的衔接：`apply_window_settings` 在置顶之外增加 `set_theme`（system → `None` 跟随系统，light / dark → 显式指定，失败码 `window-theme-failed` 先于落盘），顶栏降为应用内页头不再是拖拽区。关闭行为不变：系统关闭钮经 `WM_CLOSE` 仍进 `CloseRequested`，托盘吸收 / 显式退出决策照旧。再评估依据：wry WCO 仍无官方支持（wry#1650、tauri#4531），decorum 等插件按钮为 HTML 复刻而非系统绘制。验证：`cargo fmt --check`、隔离 `CARGO_TARGET_DIR` 下 `cargo test --workspace`（src-tauri 33 过 1 忽略、asb-core 86、执行器 5、切换 16 全过）、`npm run typecheck`、`npx vitest run` 22 文件 101 项全过、`rg` 确认旧契约零残留；未打包（原生标题栏外观需用户在运行实例中验收）。

**窗口控制：原生标题栏当日回退，定稿电脑管家式自绘（2026-08-31）：** 上午按用户「不要自己实现、用 Windows 原生、最新样式」指令切换 `decorations: true` 并全量删除自绘实现；用户运行后对照微软电脑管家质询按钮差异，确认互斥事实（电脑管家按钮本就是无边框窗口自绘 / WebView2 WCO，真原生标题栏无任何样式接口，wry 不支持 WCO——wry#1650 / tauri#4531 开放）后拍板「恢复电脑管家式自绘」。本轮完整还原上午删除的全部代码：`tauri.conf.json` `decorations: false`、capabilities `core:window:allow-start-dragging`、`commands.rs` 的 `main_hwnd` 与 `window_minimize` / `window_toggle_maximize` / `window_is_maximized` / `window_close` 四命令（`SendMessageW(WM_SYSCOMMAND)` + `IsZoomed` 通道）、`lib.rs` 注册、`windows-sys` 的 `Win32_UI_WindowsAndMessaging` / `Win32_Foundation` 特性、前端 `WindowControls` 组件与测试（五用例与原契约一致：窗口态最大化钮单方块图形、最大化态镜像双方块还原图标、resize 事件同步、三钮点击分发各一次、卸载停听——图标形态断言锁死 DESIGN.md「最大化钮镜像实时窗口状态」契约）、四枚窗口图标、`client.ts` 五封装（含 `getCurrentWindow` 导入）、`base.css` 窗口按钮样式块、顶栏 `data-tauri-drag-region` 三处、`App.test.tsx` 的 `@tauri-apps/api/window` mock 与 `window_is_maximized` 分支。原生实验期间临时引入的 `apply_window_settings` `set_theme`（服务于原生标题栏主题跟随）随原生标题栏一并删除，`apply_window_settings` 回到仅置顶单一职责。最终契约与 2026-08-28 定案完全一致；`DESIGN.md` 顶栏契约恢复并补记当日往返证据。验证：`cargo fmt --check`、隔离 `CARGO_TARGET_DIR` 下 `cargo test --workspace`（src-tauri 33 过 1 忽略、asb-core 86、执行器 5、切换 16，零警告）、`npm run typecheck`、`npx vitest run` 24 文件 107 项全过、`rg` 确认 `set_theme` / `window-theme-failed` 零残留；未打包（按钮外观需用户在运行实例中验收）。

**阻塞命令全面异步化与会话扫描限界读取（2026-08-31 完成，同日按「无兼容无过渡无技术债」收敛到全部命令）：** 用户报告会话扫描点击卡顿，后续要求参考 外部配置 源码复刻其实现；原临时克隆已被系统清理，重新浅克隆 `farion1231/外部配置` 至 临时参考目录（MIT，仅行为参考）。参考定案模式：命令层一律 `async fn` + `tauri::async_runtime::spawn_blocking`（阻塞工作进专用线程池，主线程与 async worker 均不占）；会话元数据只读头部有限行，不整文件解析。期间并行会话把 `commands.rs` 拆分为 `commands/` 域模块（error / window / status / common_settings / switching），拆分基于转换前快照、 interim 转换曾被其覆盖，最终在新结构上一次收敛完毕。最终契约：①`commands/error.rs` 新增唯一所有者 `blocking(task)`——`spawn_blocking` + JoinError（仅闭包 panic）统一映射 `task-interrupted`，全部阻塞命令经它执行，模块文档明示该契约。②全部 24 个涉 I/O 命令转为 `async fn`：设置两命令（`set_app_settings` 仅存储写入进池、窗口置顶留在 async 上下文）、档案 CRUD 六命令、探测 / 模型获取两网络命令、发现与导入（逻辑抽为 `discovery_report()` 供命令与导入共用）、会话三命令（`list_sessions` 由不可失败改为 `Result`，前端 `SessionManager` 既有 `scanError` 捕获承接）、外部数据库两项命令（SQLite）、通用配置六命令（含此前卡顿根因 `preview_common` 整文件解析与 `apply_common` 事务）、切换 / 备份 / 恢复 / 撤回 / 差异 / 打开目录七命令（写命令尾部 `tray::refresh` 随闭包在阻塞线程执行，muda 菜单项线程安全）、状态三命令；仅窗口四钮与 devtools 切换 5 个纯原生快速命令保持同步。③托盘改为消费同步逻辑所有者 `config_status_report`（`status.rs` 抽出，命令与托盘共用，`mod.rs` 转发随之更换）。④`session_manager.rs` 元数据提取由「整文件读入 `Vec<Value>` 再遍历」改为 `BufReader` 流式 + `METADATA_LINE_LIMIT = 32` 头部限界 + 五字段拿齐提前退出（全部字段 first-wins，提前退出语义等价；限界仅当字段迟于 32 行未齐时改变行为，标题走既有回退链，与参考项目同款取舍）；`last_active_at` 维持文件 mtime（优于参考的尾部时间戳解析，无需尾部读取）；`read_messages` 流式化，`read_jsonl` 删除；新增测试锁定限界。安全边界不采纳参考项目做法：路径仍由后端解析、renderer 只传客户端与会话 ID。验证：`cargo fmt`、`cargo test --workspace`（src-tauri 37 过 1 忽略、asb-core 86、执行器 5、切换 16 全过）、`npx vitest run` 26 文件 118 项全过；`npm run typecheck` 仅剩并行会话进行中文件 `src/app/useSwitchOperations.ts` 的未用变量错误（非本轮引入、未触碰）。未打包。

**全局通知组件：复刻 本项目 toast 系统（2026-08-31 完成）：** 用户确认项目此前没有全局通知组件（工作区横幅≠全局通知），指令照抄 本项目（uiux）。移植方式沿用 Select 复刻先例：结构与交互契约照抄，视觉映射本系统令牌。新增三文件：`src/components/use-toast.ts`（唯一契约所有者——模块级 store + `toast()` 直调，无 context；逻辑照抄：同屏上限 2、错误置顶不被挤出、按种类自动关闭 info 4.5s / success 3.2s / warning 6s / error 手动、`durationMs: 0` 单条关闭自动、悬停/聚焦/页面隐藏暂停并保留剩余时长、300ms 退场动画期、空内容与非法 duration 抛错）、`ToastStatusIcon.tsx`（四种手绘线性图标，currentColor）、`Toaster.tsx`（应用根挂载一次；关闭钮复用 `CloseIcon`；error 用 `role="alert"` 其余 `role="status"`）。`base.css` 新增 `asb-toast` 样式块：玻璃菜单材质（`--asb-glass-menu` + 菜单级模糊 + 模态投影 + 顶缘高光，`@supports` 回退挂入既有块）、图标色调映射行动蓝/安全绿/警告橙/系统红、进入 240ms / 退出 180ms 上移缩放动画（减动态效果经令牌归零）、栈视口顶部居中 `z-index: 70`。既有横幅层不动（分工记入 DESIGN.md §8）。测试 `Toaster.test.tsx` 六用例锁定：渲染与角色、按种类默认时长自动关闭、悬停暂停/离开续计、错误仅手动关闭、上限 2 且错误不被挤出、空内容拒绝；假定时器下 userEvent 挂起，改用 `fireEvent.pointerOver/Out`（React 由 over/out 合成 enter/leave）与同步 click。验证：`npx vitest run` 25 文件 114 项全过（含令牌所有权测试）、`npm run typecheck` 干净；未打包。

**全局通知接入：一次性操作反馈全面迁至 toast（2026-08-31 完成）：** 用户指令「审查代码，该接入的地方就接入显示」。审查结论与落地：①错误通道分流——`App.tsx` 新增 `reportError` 单一入口，`profile-store-unsupported` 保留横幅（持续错误 + 「清空旧档案并重新开始」承重按钮，测试锁定该交互不变），其余全部失败（约 20 处 catch：刷新、预览、保存/删除/重置、切换/恢复/撤回、锁清理、通用与应用设置、发现、外部配置、打开备份目录）改发 error toast（title「操作失败」+ 后端 message，不自动关闭）。②写操作结果——`outcome` / `operationWarnings` 两个 state 与对应横幅整体删除：切换成功发 success「已切换到「X」」（描述「{客户端}将在下次启动时读取新配置」），恢复备份 / 撤回同构（「已恢复备份」「已撤回上一次切换」）；带警告时合并为一条 warning（标题带条数，每条警告一行，为此 Toaster 副文容器 `p`→`div`）；切换/恢复/撤回后「无法刷新本机配置发现结果」的附加警告并入同一数组；通用设置写入仅在有警告时发 warning（勾选控件状态即成功反馈，不通知）。③保留原位：发现页 外部配置导入结果面板内嵌展示（含未导入明细）、SessionManager 等子组件局部错误、busy 指示器。④无债清理：`base.css` 删 `.asb-banner-warning` / `.asb-banner-list`（零消费者；`.asb-banner-ok` 仍服务导入结果保留）；`SwitchOutcome` 类型 import 删除；全部 `setOutcome` / `setOperationWarnings` 调用点（含客户端 Tab 切换清理）删除。⑤测试隔离：error toast 不自动关闭且 store 跨渲染存留，`use-toast.ts` 导出 `clearToasts()`（立即清空，测试隔离专用）挂入 `test/setup.ts` 全局 afterEach，杜绝跨用例 alert 残留；`App.test.tsx` 断言迁移——切换结果断言改「已切换到「备用网关」」toast、设置保存失败与 store-unreadable 改无名 alert、撤回测试补 toast 断言、`profile-store-unsupported` 横幅断言不变。验证：`npx vitest run` 25 文件 114 项全过、`npm run typecheck` 干净；另修复并发编辑引入的 `setLocks({ codex, claudeLock })` 属性简写键名笔误（git diff 证实 HEAD 为 `claude: claudeLock`）；未打包。

**会话管理模块内部滚动（2026-08-31 完成）：** 用户报告（附 828 条会话的实况 DOM）会话页由整页滚动查看内容，要求模块自身内部滚动——这正是 DESIGN.md §148 既定契约（「列表保留独立的固定高度滚动区，长记录在详情内滚动」）未被实现兑现：`.asb-session-items` 虽有 `overflow:auto`，但其 `flex:1` 没有定高父级（布局随内容撑高、`.asb-main` 整页滚），转录区则是固定 `max-height:560px` 与视口无关。`base.css` 会话块一轮收敛：①`.asb-panel:has(.asb-sessions)` 填满主区（flex 列 + `flex:1` + `min-height:0`，`:has()` 作用域仅此面板、其他页面面板不受影响，WebView2 ≥105 支持、仓库已有先例）；②`.asb-sessions` / `.asb-session-layout` 传递高度（`flex:1` + `min-height:0`，布局保留 `min-height:520px` 作为极矮窗口的地板，此时退回页面滚动属预期兜底）；③布局行模板 `minmax(0,1fr)` 约束面板高度，`.asb-session-items` 补 `min-height:0` 使滚动真正生效；④转录区删 `max-height:560px`，改为填充详情面板余量（`flex:1` + `min-height:0` + `overflow:auto`），固定头部与元数据保持可见；⑤窄屏堆叠（≤900px）行模板改 `auto minmax(0,1fr)`、列表上限 320px→`40vh`，并因堆叠下工具栏换行 + 列表占位导致详情仅剩约 87px、固定头部即占满，改为整个详情面板滚动（`overflow-y:auto`，转录还原为自然高度随面板滚）。实测（Tauri 桩 + 300 会话 / 60 消息）：桌面 1440×820 页面零滚动、列表 496/28170 与转录 288/9302 各自内部滚；窄屏 800×820 页面零滚动、列表 261/28170、详情面板整体滚；极矮 1440×500 亦无页面滚动。验证：`npx vitest run`（SessionManager + tokens 8 项）、`npm run typecheck` 不受 CSS 影响；未打包。

**错误通知默认关闭时长（2026-08-31 完成修订）：** 用户确认需要为错误通知设置关闭时长，`use-toast.ts` 的 `DEFAULT_TOAST_REMOVE_DELAY.error` 由 `0`（仅手动）改为 `10000`——比 warning 的 6s 更长，留足阅读后端错误信息的时间；`durationMs: 0` 仍可对单条强制手动关闭，错误置顶不被挤出的排序规则不变。同步：`reportError` 注释、`Toaster.test.tsx` 错误用例改为「10s 边界自动关闭 + 手动关闭路径」、DESIGN.md §8 自动关闭条目。

**顶栏置顶快捷钮（2026-08-31 完成）：** 按用户指令加入窗口置顶切换按钮。新组件 `src/components/PinTopButton.tsx`（无状态展示件，`active` / `disabled` / `onToggle` 三契约）+ `icons.tsx` 新增 `PinIcon` 图钉图标；位于顶栏导航项与窗口三钮之间，复用 `.asb-winbtn` 尺寸与触控目标，激活态 `is-active` 走行动蓝（`--asb-action`，级联顺序天然压过 `.asb-winbtn:hover`，不写冗余悬停规则），提示与可及名「置顶窗口 / 取消置顶」并携带 `aria-pressed`；因按钮会在 busy 期禁用，Tooltip 按既有惯例经 `span.asb-tooltip-anchor` 锚定（Chromium 抑制禁用控件指针事件，直接包裹按钮会令禁用态提示失效）。单一所有权无旁路：按钮不自建状态，镜像 `AppSettings.alwaysOnTop`，点击经 App 既有 `saveAppSettings` 完整对象合并路径（busy 门控、先 native 后落盘、失败保持原视觉），与设置页「窗口始终置顶」复选框是同一设置的两个视图；设置未加载（`appSettings === null`）时不渲染。验证：`npm run typecheck`、`npx vitest run` 26 文件 118 项全过（新增 `PinTopButton` 三用例：未置顶可点、置顶镜像 `aria-pressed`、禁用不分发；App 集成用例断言 `set_app_settings` 精确载荷 `{ settings: 完整五字段对象 }` 与切换后的按钮镜像）；Rust 侧零改动（复用既有 `set_app_settings`）。`DESIGN.md` 设置契约行登记双视图关系；未打包（顶栏外观需用户在运行实例中验收）。

**通知接入无债清理（2026-08-31 完成）：** 按「无兼容无过渡无技术债」复查通知相关全部改动并同轮删净三处残留：①`Toaster.test.tsx` 的 `flushToasts` 逐条点击式清理被 `test/setup.ts` 全局 `clearToasts()` 完全取代，删除（测试文件 afterEach 仅剩 `useRealTimers`，store 清理单一所有者为全局 setup）；②`notifyWriteOutcome` 的 `app: AppKind | null` 参数与「客户端将在下次启动时读取新配置」降级文案删除——参数收紧为必填 `AppKind`：恢复场景改用 `RestoreOutcome.preRestoreBackup.app`（命令返回值自带，`backups.find` 查表、`?? null` 兜底与依赖数组中的 `backups` 一并删除），撤回场景本就必填；③切换场景 `runSwitch` 入口守卫加 `!selectedProfile` 提前返回，「已完成切换」fallback 标题与 `selectedProfile?.app ?? null` 不可达分支删除。`rg` 复查：旧 fallback 文案、`asb-banner-warning` / `asb-banner-list`、`flushToasts`、`setOutcome` / `setOperationWarnings` 全仓零残留。验证：`npx vitest run` 26 文件 118 项全过（含同轮并入的顶栏置顶并发改动）、`npm run typecheck` 干净；未打包。

**通知栈位置定稿左上角（2026-08-31 完成）：** 用户指令通知位置改左上角，`base.css` 的 `.asb-toast-stack` 由顶部居中（`left: 50%` + `translateX(-50%)`）改为 `left: var(--asb-space-2)` 左上对齐（`top` 不变），居中定位的 transform 声明删除——不与进出场 keyframes 的 translateY 冲突；宽度上限与其余契约不变。DESIGN.md §8 栈条目同步。

**规模规则全仓审查与拆分重构（2026-08-31 完成）：** 按 AGENTS.md §6 阈值（文件 500 审查 / 800 硬线；函数 80 审查 / 120 必拆）全仓复测并实施拆分。审查基线用脚本统计非空非注释行、Rust 内联 `#[cfg(test)]` 模块不计入。**超硬线必拆项：**①`src/App.tsx` 1396 行、`App()` 1226 行 → 分解为「模型组合 + 视图组合 + 页面 + 领域钩子」四层：`app/useSwitchboardModel.ts`（唯一模型组合层，118 行，只组合钩子不含逻辑）、`App.tsx`（视图组合，151 行）、`pages/`（Overview/Providers/CommonSettings/Settings/Backups/Discovery+CcImport 六页文件，均 ≤230 行）、`app/` 九个领域钩子（useConfigSnapshot / useSwitchPreview / useCommonSettings / useAppSettings / useDiscovery / useCcImport / useSwitchOperations / useProviders / useOperationFrame）+ global-effects + notifications + AppShell + OperationConfirmSheets，`lib/client-name.ts` 为 AppKind 显示名单唯一所有者；全部 DOM 与行为逐块搬迁未改契约，期间与用户的 toast / 置顶 / 更新检查并发改动两次撞写，均以重读合并方式收敛（写失败即 mtime 拒绝，无静默覆盖）。②`src-tauri/src/commands.rs` 836 行（剔测试）→ `commands/` 域模块（error / window / status / common_settings / switching + mod.rs 薄委托），命令在定义路径注册（Tauri 宏需定义处解析），`lib.rs` invoke_handler 改全路径，tray 所需 `config_status` / `ConfigFileStatus` 经 pub(crate) 再导出保持不动。③`executor.rs` 的 `execute_locked` 141 行 → 按业务步骤拆为 `read_unchanged_current`（读取 + 外部变更拒写）/ `plan_candidate`（预览 + 渲染 + 渲染哈希拒写）/ `back_up_current`（备份 + 元数据）/ `commit_rendered`（临时写 → 校验 → 原子替换 → 写后验证 + 失败恢复），`execute_locked` 收敛为 48 行事务编排、全路径 `finish` 释放锁，阶段名与错误变体零变化；同轮把 `display.rs`（预览展示脱敏）与 `restore.rs`（公开恢复 + 备份列举）拆为独立模块，`asb_switch` crate 根再导出面不变。**审查不拆项（单一职责成立）：**`ownership.rs` 555 行为所有权常量目录 + 查询的唯一所有者、`adapter/claude.rs` 469 / `codex.rs` 为各客户端格式契约所有者、`local_state.rs` 剔测试后约 300 行，均不混第二职责，不为凑行数机械拆分。**例外条款判定（80–120 审查带）：**`fetch_models` 108 / `http_get` 90（用户侧已抽）为完整 WinHTTP 请求生命周期、嵌套闭包承载 FFI 句柄清理，跨函数拆散 unsafe 句柄所有权无业务边界；`restore` 107 为连续事务（锁 → 校验 → 恢复前快照 → 写入 → 验证）；`import_proposal` 82 为每客户端一个完整解析分支——均符合「连续事务 / 完整解析」例外画像，不动。**遗留：**`App()` 151 行超 120 函数线：纯声明式布线（8 页面块 + 弹层 + 壳），全部可命名业务单元已抽出，再拆只会产生名称空泛的转发组件（AGENTS.md §6 明禁），留待页面接线稳定后随用户并发演进再收敛。验证：`cargo test --workspace` 全过（src-tauri 42 过 1 忽略、asb-core 86、执行器 5、切换 16）、`npm run typecheck` 零错误、`npx vitest run` App 集成 20 项与 ProviderEditor 9 项单跑全过（全量并发跑时 5s 超时抖动为用户 dev 实例 + watcher 负载所致，失败集合逐次漂移、单跑稳定全绿）；复测无任何文件超 800、无函数超 120（App 除外如上）。未打包。

**CI 发布链路与首个 Release（2026-08-31 完成，用户指令「配置 github 的自动构建并实际实现」）：** 打通 tag → 自动构建 → GitHub Release 全链路，补上「检查更新」缺失的分发端。`.github/workflows/windows-package.yml`：`permissions` 升为 `contents: write`，新增末步「发布 GitHub Release」——`if: startsWith(github.ref, 'refs/tags/v')` 门控，用 runner 自带 `gh release create`（`GH_TOKEN: github.token`，非草稿非预发布，`--generate-notes` 自动变更日志）上传 `target/release/bundle/nsis/*-setup.exe`；刻意不引入第三方 release action（供应链面更小），触发器保持既有裸 `push`（分支与标签都会构建，仅标签发布）。`.gitignore` 收口构建垃圾：`target-dev/` 泛化为 `target-*/`（覆盖 check / session-check / update-check 隔离目录），新增 `.zcode/`、`.playwright-mcp/`。实际执行：发布前全量验证（`cargo test --workspace` 隔离 target 全过、`npm run typecheck`、`npx vitest run` 128/128——先前两次全量各挂 2 项 ProviderEditor 系并行构建资源挤占的 5s 超时抖动，单跑与最终干净全跑均全绿、`npm run build` 通过）；暂存 141 文件经密钥模式扫描（唯一命中为测试夹具的环境变量名）后提交 `2c4cccf` 并 push master，打注释标签 `v0.1.0`（与 `tauri.conf.json` 的 `version 0.1.0` 对齐）push 触发构建；两条运行（master + tag）执行完整测试与 NSIS 打包，tag 运行发布 Release。版本语义：已安装 0.1.0 的应用检查更新将得到「已是最新版本」；后续发版流程 = 改 `tauri.conf.json` 版本 → 提交 → 打 `vX.Y.Z` 标签推送。

**会话详情复刻 外部配置 布局与消息目录（2026-08-31 完成）：** 用户反馈会话内容查看区域太小，指令参考 外部配置 UI 尽量复刻（含「按点查看」），设计语言保持自有。研读 临时参考目录 的 `SessionManagerPage` 定案四项结构：定宽列表 + 详情占满、紧凑头部（单行元信息 + 恢复命令条）、长消息折叠、右侧用户消息目录（TOC）点击跳转。落地（结构与交互契约照抄，视觉全部走本系统令牌）：①布局比例 `minmax(260px,0.72fr)/1.28fr` → `320px minmax(0,1fr)`（1440 窗口实测详情占 1011px ≈ 76%）；②头部紧凑化——原 2×2 元数据网格（会话 ID / 最近活跃 / 工作目录 / 恢复命令）整体删除，改为单行元信息（等宽截断的会话 ID · 最近活跃时间 · 目录名按钮，`title` 悬停显完整路径、点击复制）+ 恢复命令条（等宽单行截断）；三个既有操作按钮（恢复 / 复制恢复命令 / 复制工作目录）与全部状态文案原样保留；③消息条目：超 3000 字符折叠为前 1500 + 省略号，「展开完整内容（约 Nk 字符）／收起」切换（`aria-expanded`）；悬停 / 键盘聚焦显现「复制」按钮，经全局 toast 反馈；④消息目录：提取用户消息为编号条目（两行截断预览），>2 条时渲染，点击 `scrollIntoView({block:'center'})` 瞬时定位并以行动蓝描边 + 路由投影高亮 2 秒——实测发现 smooth 动画会被高亮重渲染打断搁浅在半途，改瞬时跳转（注释记录原因）；窗口 ≥1280px 才显示目录（248px 定宽、自滚动），窄窗口不显示、窄屏堆叠模式沿用整面板滚动。明确不采纳参考项目项：删除 / 批量删除（产品边界）、`sourcePath` 展示与复制（本产品契约绝不向前端暴露源路径）、多客户端目录、FlexSearch 消息内高亮、虚拟化列表（本轮不加）。样式全部落在 `base.css` 会话块（元信息行 / 命令条 / 目录 / 折叠 / 复制 / 高亮），删除孤儿 `.asb-session-meta` 网格全套规则；对话区新增「对话记录」标题 + 计数徽章。测试：既有 6 项全过（按钮名与状态文案未动），新增 3 项锁定新契约——长消息折叠往返、目录跳转定位与高亮（含 scrollIntoView mock）、单条消息复制 toast。验证：`npx vitest run` 29 文件 133 项全过、`npm run typecheck` 干净、浏览器实测（Tauri 桩 + 48 条消息 / 含 1 条超长）：跳转 scrollTop 5813 精确居中（目标距视口顶 84px）、折叠展开 4200 字符、页面零滚动。未打包。

**设置页界面字体选择与 Noto Sans SC 默认字体（2026-08-31 完成，用户指令参考 ZiChe 的选择时预览）：** 设置页「外观」组新增「界面字体」行。契约：`AppSettings` 增第六必填字段 `interfaceFont`（默认 `"Noto Sans SC"`，`local_state.rs` 的 `DEFAULT_INTERFACE_FONT` 为唯一所有者）；校验拒绝空串、首尾空格、引号 / 反斜杠 / 控制字符与超 64 字符（`set` 前与文件读取后都验证）；唯一迁移路径 = 恰好上一版完整五字段对象原子补入默认字体，四字段旧形态归入明确拒绝（无第二步兼容）。枚举：新模块 `src-tauri/src/fonts.rs` 用 `GDI EnumFontFamiliesExW`（`DEFAULT_CHARSET`、跳过 `@` 竖排、`BTreeSet` 去重排序）枚举已安装字族，命令 `list_system_fonts`；`windows-sys` 加 `Win32_Graphics_Gdi` feature。前端：`FontPicker` 组件（交互参考 ZiChe、视觉走本系统 select 令牌）——触发器以当前字体渲染字体名，菜单含搜索框，选项 = 打包默认 + 系统字族去重，每个选项以该字体渲染「字体名 + 中文 Aa 012」，当前项右置勾选，方向键 / Home / End / Escape / 外部点击可达；选择经完整对象保存并即时生效。字体应用链：`lib/font-family.ts` 拥有字体名 → 带引号 CSS 值的转换；`tokens.css` 两文字栈前置 `var(--asb-font-user)`（令牌默认即 `Noto Sans SC`）；`useAppSettings` 在设置加载后覆写该变量（与 `data-theme` 同一应用点），代码栈不变。默认字体落地：`@fontsource/noto-sans-sc` 打包 400/600/700 三字重（OFL，`main.tsx` 引入，`unicode-range` 子集懒加载；fontsource CSS 同时引用 woff2+woff，dist 增量约 25MB）。测试：Rust 48 过 1 忽略（新增枚举 1 项、契约 3 项更新）；前端新增 `FontPicker` 8 项、`AppSettingsForm` 1 项、`client` 1 项、`App` 集成 1 项（完整 invoke 载荷 + CSS 变量断言），并修补 6 个用通用兜底 mock `get_app_settings` 的旧集成测试夹具。验证：`cargo fmt`、`cargo test --lib`（隔离 target）、`npm run typecheck`、相关 vitest 单文件全绿；`CodexResetPanel.tsx` 的 3 个类型错误为用户并发进行中代码，本轮未触碰。未打包。

**设置页固定失败态（2026-08-31 完成，用户指令「前端也需要显示固定UI」）：** 起因是用户本机 `state/settings.json` 残留 8-28 的单字段旧形态，被严格契约每次读取拒绝，设置页永远停留「加载中」且原因只在启动 toast 里闪现（该残缺文件已删除，其唯一值等于默认值）。本轮把失败态做成固定 UI：`useAppSettings` 新增 `loadError`（失败原因文本）与 `retryLoad`（重载 nonce 重新拉取，成功即清空）；`SettingsPage` 三态渲染——已加载出表单 / 失败出 `role="alert"` 警示行「设置加载失败：{原因}」+「重试」次级按钮 / 其余才显示「加载中」；全局错误通知照常，页面文字是持久来源。新增 App 集成测试 1 项（先拒后成的重试链路 + 失败时不渲染表单与加载占位）。验证：`npm run typecheck` 零错误（用户并发中的 `CodexResetPanel` 类型错误已由用户自行修复）、`npx vitest run` 全量 151 项通过。未打包。

**设置页 UI 审查与修复（2026-08-31 完成，用户指令「审查研究后无兼容无过渡无技术债修复」）：** 经临时 mock 入口（真实 App + 桩化 `__TAURI_INTERNALS__.invoke`，vite 1421）浏览器实测设置页常规 / 字体菜单开 / 失败 / 深色四态，DOM 实测定窗三缺陷并当场修净（无兼容层，harness 用毕即删，孤儿 vite 进程按命令行归属 taskkill）：①字体行触发器 216px 高——chevron `<svg>` 直接携带 `asb-select-chevron` 类，而尺寸规则 `.asb-select-chevron svg` 只作用于包裹元素内部，svg 无宽高退化 300×150；修复 = 与 `Select` 同构的包裹 span（不新增 CSS）。②四个设置分组间距实测 0px——`.asb-app-settings` 容器在 base.css 中无任何规则，且表单根与「软件更新」是两个并列根、父级 `.asb-panel` 为普通 block；修复 = 给 `.asb-app-settings` 立法（flex column + `--asb-space-4` 24px，§5 结构间距），并收敛 `SettingsPage` 为单一容器根（表单 / 失败行 / 更新组都是其直接子项），三处组间距复测 24px。③失败态裸奔在面板上——改为同款紧凑实心信息行（`role="alert"` + 标签行「设置加载失败：{原因}」+ 说明行默认行为 +「重试」次级按钮）。附带修掉字体选项行的勾选挤占：每行渲染固定宽度勾选槽位（空槽占位），样本列右缘全列对齐（实测 1148/1174 恒定）；菜单底 686px < 800px 视口不再裁切；深色模式抽查正常。视觉模型曾误报「软件更新信息行裸奔 / 按钮孤立」，DOM 实测其行表面与边框俱在，判定为 0 间距挤压下的误读，未改。复测证据：触发器 41px、组间距 [24,24,24]、失败行有 bg+border 且距更新组 24px、样本右缘全列恒定。验证：`npm run typecheck` 零错误、相关 vitest 单文件 43 项 + 全量 151 项全过；DESIGN.md §8 已同步（间距唯一所有者、固定勾选槽位、失败行结构）。未打包。

**设置页布尔行复刻为渐变开关（2026-08-31 完成，用户指令参考 本项目 渐变按钮搬运）：** 「窗口始终置顶」「启用硬件加速」由复选框改为开关。`/tmp/本项目` 参考克隆已被临时目录清理（仅剩 git 二进制索引），渐变按钮实现以本仓库 2026-08-28 的既有 1:1 移植为基准（`--asb-gradient-button` 青→蓝→粉 88° 五停 + `--asb-gradient-button-glow(-hover)` 内嵌蓝光晕 / 悬停白光增强）。新组件 `Switch`（`role="switch"`，隐藏原生 input 保留键盘读屏，结构与 `Checkbox` 同源）：44×26 药丸轨道 + 18px 白滑钮（复用 `--asb-slider-thumb-bg`）滑动 220ms；开启轨道 = 移植渐变 + 蓝光晕、悬停白光增强，关闭轨道 = 画布底 + 发丝边、悬停边框转行动蓝；按下 0.98 缩放；键盘圆环走既有 `data-focus-source="key"` 契约；减少动态效果令 motion 令牌归零自动瞬时。`AppSettingsForm` 两行换装，`Checkbox` 保留通用设置 / 发现页复选框语义不混用；测试角色断言 checkbox→switch 共 5 处，新增 `Switch.test.tsx` 4 项。浏览器实测（mock harness）：开启轨道渐变实测 `linear-gradient(88deg, rgb(82,225,255)…)`、滑钮 18px 位移、点击即时换装；悬停 box-shadow 实测白光 16px + 蓝光 14px；深色下关闭轨道为 `rgb(7,23,37)` 深流蓝（早前误报白色为手动改 data-theme 被设置效果重置的测试伪影，经真实「深色」段路径复测排除）。中途踩中令牌所有权测试（base.css 裸 `#ffffff`），改用 `--asb-slider-thumb-bg` 修复。验证：`npm run typecheck` 零错误、vitest 全量 163 项中 160 过（`ProviderEditor` 2 项单跑全绿，为既有全量负载抖动）+ 令牌测试过；harness 与截图已删净，孤儿 vite 进程按命令行归属 taskkill。未打包。

**开关行布局按操作逻辑定稿（2026-08-31 完成，用户指令「文字在左，按钮在右」）：** 开关行从「[轨道+文字] 左、说明文字右列」翻转为与分段 / 字体行同构的结构——标签与说明在左列（`asb-app-setting-copy`），`Switch` 控件在行右缘。`Switch` 组件不再绘制可见标签：可及名改经输入 `aria-label` 传入，只绘制轨道本体；命中区由组件自身的 40px 最小高度与纵向内边距承担；窄屏（<680px）开关随分段控件 `justify-self: start`。`Switch.test.tsx` 相应改为断言「可及名存在而可见文字不存在」。浏览器实测（mock harness）：两行文字列右缘 689、开关 705–749 与行右缘 765 对齐，说明文字入左列，逐行一致；harness 与孤儿 vite 进程已清理。验证：`npm run typecheck` 零错误、Switch/AppSettingsForm/App 相关测试 31 项全过。未打包。

**设置页移除全部说明性副文（2026-08-31 完成，用户指令）：** 设置行不再携带描述行——「界面主题 / 界面字体 / 动态效果 / 点击关闭按钮时 / 窗口始终置顶 / 启用硬件加速」各行只余标签与控件，`SegmentSetting` 的 `detail` 字段与全部选项描述、字体行 / 开关行 / 软件更新未检查态的说明文字一并删除（无兼容残留）。保留两处非描述性文字：软件更新**检查后**的「当前版本 · 检查于」（结果状态，仅在检查后出现）与设置读取失败态的恢复指引（错误恢复信息）。测试同步删除 3 处副文断言、未检查态改为断言无版本文字。验证：`npm run typecheck` 零错误、AppSettingsForm / UpdateSection / App / Switch 相关 36 项全过。未打包。

**浏览器真实开发入口（2026-08-31 实现完成，待浏览器验收）：** 按用户要求删除 `src/dev/web-preview.ts`、其样例数据与内存 IPC 桥；`npm run dev` 改由真实 `tauri dev` 承载后端并打开 `http://127.0.0.1:1420` 的浏览器页，debug 专属 `src-tauri/src/dev_api.rs` 仅监听 `127.0.0.1:1422`，接受 Vite 代理的 JSON RPC 并逐项派发到既有 Tauri 命令。故配置状态、通用设置、档案、切换、备份、发现、会话和应用设置均复用真实状态、解析和事务；浏览器没有第二实现或模拟数据。浏览器页标注「浏览器开发 · 本机后端」，隐藏原生窗口三钮并保留 F12；`npm run dev:desktop` 保留同一后端的可见原生壳。写入仍按现有确认、锁、备份、原子替换和验证路径执行。验证：`cargo test -p agent-switchboard dev_api` 2 项、`npm run typecheck`、`src/api/client.test.ts` 与 `src/boundary.test.ts` 共 10 项通过。浏览器实际验收暂被并行中的 `src-tauri/src/local_state.rs:107` 编译错误阻断（`'\\t'` / `'\\r'` / `'\\n'` 非法字符字面量），本轮未改动该文件。

**界面点击后材质丢失修复（2026-08-31 完成）：** 保留 `AppSettings.hardwareAcceleration` 作为仅桌面 WebView2 启动前读取的性能设置，不将其改造成浏览器页的自动降级或兼容开关。视觉运行路径改为唯一实体材质：`tokens.css` 删除全部 `--asb-blur-*`、`--asb-glass-*`、饱和度和后备令牌，新增轨道 / 菜单 / 抽屉实体面；`base.css` 删除环境伪层与所有 `filter`、`backdrop-filter`、`@supports` 后备分支，以及路由、菜单、提示、抽屉、通知的缩放 / 位移入退场。顶栏与 `DualRelay` 改消费实体轨道面；按下反馈不再缩放按钮、复选框、开关或滑块，只有表达开关真实状态的滑钮位移保留。Toast 同步移除 300ms 退出状态、计时器和渲染分支，关闭即时移除；令牌测试锁定不存在旧材质、背景采样和退出类名。`DESIGN.md` 已改为同一契约。验证：改动完成时 `npm run typecheck` 与 `Toaster`、令牌定向测试 9 项通过；随后并行工作树新增 `runtimeLogLevel` / `LogsPage` 契约未同步，导致全量构建与全量测试分别出现无关类型错误和超时，未改动这些并发文件。浏览器自动化工具在本机不可用，需在当前 Edge 开发页手动连续点击验收；不声称已完成该黑盒验证。

**浏览器开发页重复打开修复（2026-09-01 完成）：** 根因是 `src-tauri/src/lib.rs` 在每次 Tauri `setup` 都调用系统 opener；Rust / Cargo workspace 热更新会重启后端进程，因此同一 Vite `1420` 地址被反复打开。契约迁移为单一所有者：Rust `setup` 只启动真实 debug 后端并隐藏承载窗口，彻底删除开发页 opener；`src-tauri/src/dev_api.rs` 增加同源限制的无数据 `GET /health`；持久的 Vite 开发进程通过 `src/dev/vite-browser-launch.ts` 直接、安静地探测回环健康地址，返回 `204` 后只调用一次公开的 `ViteDevServer.openBrowser()`。进程内 `idle / waiting / opened + generation` 是唯一启动状态：Vite 配置重载时新 server 接管未完成的等待、旧任务退出，server 关闭则取消等待，已打开后任何重载都不再打开；不写环境 claim、不遗留轮询。Vite 与 Rust 两端均只把精确 `ASB_WEB_DEVELOPMENT=1` 视为 Web 开发，`dev:desktop` 删除该变量，既不探测也不隐藏原生窗口或打开浏览器。端口、业务 API 代理、真实命令桥和 Rust 热重载均保持唯一原路径，无包装脚本、兼容分支或新依赖。验证：`cargo fmt --all -- --check`、隔离 target 的 `cargo test -p agent-switchboard --lib` 89 过 1 忽略、隔离 target 的 `cargo check -p agent-switchboard --release`、`npm run typecheck`、`npx vitest run src/dev/vite-browser-launch.test.ts src/boundary.test.ts` 6/6、`npm run build` 通过；真实 `npm run dev` 中修改 Rust 文件后，后端 PID `31148 → 11072` 且日志新增 `appStarted`，Edge 页面数保持「另外 5 个页面」不变，证明后端确已热重启而浏览器未重复打开；验收后只终止本轮启动的开发进程，`1420` / `1422` 均已释放。

**浏览器开发回环地址收敛（2026-09-01 完成）：** `src-tauri/tauri.conf.json` 的 `build.devUrl` 是浏览器开发入口唯一所有者，规范值为 `http://127.0.0.1:1420`；Vite 从该字段解析监听 host / port 与健康探测 Origin，Rust 从 Tauri 运行时配置取得同一 Origin 并传给 debug API 做精确相等校验，浏览器启动参数、测试和文档同轮迁移，不保留第二主机名、重定向、别名或兼容接受路径。边界测试加载实际 Vite 配置，验证监听值派生自 Tauri 所有者；Rust 测试锁定 Origin 必须精确匹配；全仓源码搜索确认旧主机名零残留。验证：`npm run typecheck`、`npm run build` 通过且无配置加载警告，`npx vitest run src/dev/vite-browser-launch.test.ts src/boundary.test.ts` 7/7，隔离 target 的 `cargo test -p agent-switchboard dev_api --lib` 4/4，`cargo fmt --check --manifest-path src-tauri/Cargo.toml` 与 `git diff --check` 通过；保留用户当前开发进程并实测 `1420` 与 `1422` 均只监听 `127.0.0.1`，经 `http://127.0.0.1:1420/api/health` 代理访问返回 `204`。

**编辑器连通检测改为展开/收回（2026-09-01 完成）：** 按用户指令将供应商编辑器「主模型」按钮区的「检测连通」从「每次点击都重新探测且结果无法收起」改为开合开关：无结果时点击探测并展开；结果（含错误）显示时按钮变「收起结果」，点击收回；收回后再点击重新探测并展开（保留同地址重试能力）。可见性由 `ProbePanel` 本地 `open` 与探测结果的与运算派生，url 变更经 `useEndpointProbe` 既有的结果清空自动隐藏，无兼容分支；按钮补 `aria-expanded` / `aria-controls`（`editor-probe-feedback`），两态文案等宽避免布局抖动；供应商列表行内连通图标按钮未在指令范围内，行为不变。验证：`npx vitest run src/components/ProbePanel.test.tsx src/components/ProviderEditor.test.tsx` 22/22（含新增展开/收回/重跑、错误收回重跑、展开后换地址隐藏用例）、`npm run typecheck` 零错误。

**全局指令升为通用设置第三主 Tab（2026-09-02 完成，用户指令「全局提示词管理做第三个 tab 页，不要藏在官方设置目录底下」）：** 通用设置页主 `Tab` 由两个扩为三个——「基础参数」「官方设置目录」「全局指令」；官方设置目录恢复为纯目录视图，`GlobalPromptManager` 独占第三 Tab。客户端选择随之收敛为单一所有者：删除提示词面板内部的 `Codex` / `Claude` 文档切换按钮与面板标题（Tab 名即标题），文档编辑目标跟随页面顶部既有客户端 `Tab`；`usePromptDocuments` 删除 `promptApp` / `setPromptApp` 状态，`App.tsx` 全部提示词 prop 改由 `commonSettings.settingsApp` 派生（钩子其余 API 本就按 `app` 参数化）。后端命令、文档事务、草稿与保存路径零变化。清理 `base.css` 孤儿样式（`.asb-prompt-manager-heading` / `-title` / `-document-tabs` / `-document-tab` 及窄屏分支）。`DESIGN.md` §通用设置与 README 功能清单同轮更新。验证：`npx vitest run src/pages/CommonSettingsPage.test.tsx` 6/6（含目录 Tab 无提示词编辑器、第三 Tab 编辑、页面级客户端 Tab 切换文档的新断言）、`npm run typecheck` 零错误。未打包。

**通用设置自动意图与官方目录补全（2026-09-02 完成，用户指令「无兼容无过渡无技术债；完整核对 Codex / Claude Code，Claude 有 Ultracode 思考程度」）：** 通用设置的唯一持久化契约由裸值 `BTreeMap<String, ConfigValue>` 重塑为带标签的 `CommonSettingValue::{Automatic, Explicit { value }}`：每个目录键完整存储，`自动` 交给客户端与模型决定并在投影时移除本应用原先写入的对应行，显式值才写入；`"auto"` 仍是 Codex 推理摘要的真实显式值，绝不与自动意图混淆。删除面向界面的「不接管 / 写入值 / 移除该项」语言，布尔参数统一为「自动 / 开启 / 关闭」，档位统一为「自动 + 合法值」。前端滑块新增实时「当前推理：{档位}」；Claude `effortLevel` 只含 low / medium / high / xhigh，`ultracode` 是独立布尔项，投影为顶层 `"ultracode": true`，不会伪造为滑块档位。旧版完整裸值文件只在 `ensure_layout` 阶段经整份快照暂存、严格校验、原子目录替换一次升级；正常运行不再双读。迁移保留旧有显式语义，删除已废弃的 Codex `disable_response_storage` 与 Claude `attribution.coAuthoredBy`，并将旧 Claude `outputStyle` 值正规化为当前内置命名。`official_setting_directory` 扩展为官方完整目录：直接管理可安全表达的 Codex 模型、审批、沙箱、TUI、隐私、功能与记忆偏好及 Claude 全局模型/界面/文件偏好；数值预算、模型映射、MCP、Hooks、权限、插件、项目、本地、受管、登录、凭据与自定义输出风格均明确列为保留或独立资源，绝不扁平写入。验证：`cargo test --workspace --manifest-path src-tauri/Cargo.toml`（123 + 113 + 7 + 4 + 25 项通过，1 项真实 外部数据库测试按设计跳过）、`npm test -- --run`（44 文件 234 项通过）、`npm run build`、`git diff --check` 均通过；未打包、未提交、未推送。

**Codex 重置信号混合增强：官方额度倒计时与本地事后检测（完成，2026-09-02）：** 经查证 Codex Runway 公开 feed 的核心价值（提前公告、全局重置确认）来自 X 帖子监控加运营者人工策展，不可本地计算；按用户决策保留 feed 面板原样不动，另增官方账号数据。`asb-core` 契约新增 `CodexOfficialQuotaResetKind`（scheduled / early）与 `CodexOfficialQuotaReset`，`CodexOfficialQuota` 增 `last_reset` 字段。`codex_official_quota.rs` 归属持久化基线类型（`CodexQuotaBaseline` / `BaselineRead`，仅规范化值 + 16 位 sha256 账号短摘要）与纯检测 `apply_read`：仅观察 7 天窗口，用量下降或声明重置时间变更即判定重置，先前声明时间未到判「提前」、否则「例行」；账号摘要变更清空历史避免换号误报；`query` 改返回 (额度, 摘要) 元组并新增无档案的 `query_login` 供概览直读机器登录。LocalState 以既有原子模式新增 `state/codex-quota-baseline.json` 读写，损坏拒绝不改写；命令层共享 `record_official_reset_read`（基线损坏自愈重建、保存失败仅告警不清额度），新命令 `get_cached_codex_official_reset`（缓存读，损坏显式报错）与 `refresh_codex_official_reset`（无档案守卫），`query_codex_official_quota` 成功读取时同步记录基线；基线不随档案增删改或档案库重置失效。前端 `time.ts` 新增渲染时倒计时 `countdownLabel`（无计时器），供应商页官方额度面板「重置 <时间>」后追加倒计时；概览 feed 面板之后新增「Codex 官方额度重置」面板（挂载读缓存、手动刷新、失败保数据、仅信任 available 读数），与公开信号各自标注来源互不冒充。
验证：`cargo fmt --check`、`cargo test --workspace`（桌面库 144 通过、1 项既有真实 外部数据库测试忽略；核心 113、执行器 7、集成 4 + 25 均通过）、`npm test`（47 文件 248 项）、`npm run typecheck`、`npx vite build` 均通过；收尾清查确认 `codex_reset.rs`、`CodexResetPanel.tsx/.test.tsx`、feed 样式块与 `DESIGN.md` 原条目零改动，`query_uncached` 无残留引用。注：本轮结束时 `npm run build` 因并行进行的 `OverviewPage.tsx` 配置状态徽标重构（`statusPill`→`statusWarning` 更名未完成）暂不可用，与本次改动无关；`tsc` 全仓通过先于该并行编辑落盘，`vite build` 于其后仍通过。

**发现页扫描缓存与按钮态（2026-09-02 完成，用户指令「模块加入缓存显示，扫描一次后按钮变成刷新配置」）：** 发现页「本机配置」面板不再每次启动空白：上次成功扫描的结果持久化并在进入页面时直接显示，存在任何扫描结果（缓存或本轮）时按钮由「扫描配置」变为「刷新配置」，点击总是重新扫描。契约单一所有者：`DiscoveryReport::cached_display`（`asb-core/src/discovery.rs`）定义缓存副本规则——展示事实逐字保留、`draft.api_key` 清空，因导入始终经 `import_discovered_profile` 从实时文件重新推导草稿，缓存永不落凭据；`LocalState` 以既有原子临时文件模式读写 `state/discovery-cache.json`（`load/save/clear_discovery_cache`，损坏拒绝不改写、缺文件为首次运行常态）。`discover_local` 是唯一写入方：每次成功扫描（含切档后 `refreshDiscoveryOrAppend` 的自动刷新）原子替换缓存，写失败仅 `log::warn` 不遮蔽新结果；新命令 `discover_cached` 供启动读取；`import_discovered_profile` 成功后清缓存（导入后原快照已失真），前端 `runImport` 维持既有的清态跳转。前端 `useDiscovery` 挂载时读缓存填入同一 `discovery` 状态，缓存缺失或不可读等同「尚无上次扫描」不上报；`DiscoveryPage` 按钮文案由 `discovery` 派生。测试：asb-core `discovery` 11 项（新增缓存副本仅脱敏凭据、活体报告不变）、桌面库 `local_state` 20 项（新增缺省不建文件、持久化脱敏往返、清除幂等）全过；App 集成 26 项过（新增启动即显缓存扫描 + 刷新按钮链路，三处既有发现页用例补 `discover_cached` 空缓存桩）；`npm run typecheck`、`cargo fmt --check` 零错误。验证经隔离 `CARGO_TARGET_DIR=target-test` 执行，主 `target/` 被用户运行中的 dev 实例锁定未触碰。未打包。

**时间格式改为中国常用样式（2026-09-02 完成，用户指令「无兼容无过渡无技术债实现」）：** 唯一所有者 `src/lib/time.ts::timeLabel` 格式由 `YYYY-MM-DD HH:MM:SS` 改为 `YYYY年MM月DD日 HH：MM`（年月日中文单位、两位补零、全角冒号、分钟精度无秒），本地时区、无偏移后缀不变；全部消费模块（概览匹配态与上次切换、日志、备份页与备份历史、会话管理、探针、用量与 Codex 额度/重置面板、更新检查、撤回确认）不经改动即随单一所有者生效，`Time.test.tsx` 扫描测试继续锁定组件外零直接消费者。`DESIGN.md` 契约条目同步；`Time.test`、`App.test` 断言更新为新格式字面量（`TZ=Asia/Shanghai` 锚定不变）。验证：`npx vitest run` 48 文件 255 项全过。未打包。

**新建供应商按钮改为图标＋文字（2026-09-02 完成，用户指令）：** 供应商页标题行的圆形纯图标 `+` 按钮改为全圆角胶囊行动蓝主按钮——`PlusIcon`（20px 线性加号）与「新建供应商」文字并排（`gap: 6px`、`font-weight: 600`、内边距 `--asb-space-3`，沿用行内启用按钮的图标＋文字组合模式）。文字在场后悬浮提示、`asb-tooltip-anchor` 包裹与冗余 `aria-label` 一并删除，无障碍名称由可见文字承载；`asb-btn-plus` 由 44px 定宽圆改为自适应宽度胶囊，`Tooltip` 导入仍被客户端切换 tab 使用而保留。`DESIGN.md` 供应商页条目同步。验证：`npm run typecheck` 零错误、`npx vitest run` 48 文件 255 项全过。未打包。

**供应商标题行重布局与新建按钮定型（2026-09-02 完成，用户指令三轮收敛：「左侧 供应商+切换tab、右侧 新建按钮」→「左侧纵向同通用设置」→「新建按钮与 tab 同行对齐等高、配色复刻 Tooltip 暗色」）：** 供应商页标题行只留「供应商」标题；其下新增 `asb-tabs-bar` 一行（flex 两端对齐、`--asb-space-2` 间距）——左侧客户端切换分段胶囊、右侧「+ 新建供应商」主按钮。`.asb-btn-plus` 高度由 44px 定高改为 tab 同款 `min-height: calc(var(--asb-touch-target) - 8px)`（32px 触达式）；配色由行动蓝改为 Tooltip 同款暗色三件套（`--asb-ink` 底 / `--asb-canvas` 字 / `--asb-shadow-modal` 投影），全圆角胶囊、图标＋文字、`gap: 6px`、`600` 字重不变。中间态（横向左分组 `asb-panel-heading-main`、纵向独立 tab 行）随指令收敛即删，无残留；tab 的角色/无障碍名称不变。`DESIGN.md` 契约两条（切换行布局、按钮形态与暗色配色）与 `base.css` 注释同步。验证：`npm run typecheck` 零错误、ProvidersPage/CommonSettingsPage/App 相关测试通过。未打包。

**通用设置底部预览支持展开与收起（2026-09-02 完成，用户指令）：** 通用设置页最底部「查看通用配置预览」生成的配置片段模块可开合：代码预览唯一所有者 `CodePreview` 新增可选 `collapsible` 能力（缺省关闭，`PreviewInspector` 等既有消费方零变化）——开启后文件头（目标路径 + 行数）成为 disclosure 手柄，`aria-expanded` + V 形箭头（下 = 展开 / 右 = 收起，`prefers-reduced-motion` 时旋转动效关闭，焦点环走全局 `:focus-visible` 契约），收起仅隐藏编号代码体、头部行保留；内部非受控状态默认展开，重新生成预览或客户端 Tab 切换返回时组件重新挂载即展开。`CommonSettingsPage` 的通用设置预览传入 `collapsible`，「查看通用配置预览」按钮保持纯生成语义不变（点击总是按当前草稿重新生成，草稿一变预览即失效重建）。样式新增 `.asb-filepreview-toggle` / `.asb-filepreview-chevron`（继承文字色、悬停转行动蓝，与 `.asb-row-host` 同语言）。`DESIGN.md` 通用设置条目同步。验证：`npm run typecheck` 零错误、`npx vitest run` 48 文件 262 项全过（新增 CodePreview 开合 1 项与页面级预览开合 1 项，并锁定非折叠模式文件头无按钮）。未打包。

**预览开合收敛为单按钮（2026-09-02 完成，用户指令「按钮重复了……保留一个即可」）：** 上一轮文件头 disclosure 与「查看通用配置预览」按钮并存构成两个控件，按指令收敛为 ProbePanel 同款单按钮开合：`CodePreview` 的 `collapsible` 能力与 `.asb-filepreview-toggle` / `.asb-filepreview-chevron` 样式整体删除（组件还原为纯渲染，`PreviewInspector` 消费方不变）；`CommonSettingsPage` 的 `SettingsEditor` 以本地 `previewOpen` 派生唯一按钮三态——无片段时「查看通用配置预览」（点击渲染并展开，`aria-expanded` / `aria-controls="general-settings-preview"`）、显示中「收起通用配置预览」（点击仅收起）、收起后「展开通用配置预览」（点击只复用缓存片段，不再重复渲染请求）；草稿一变片段在钩子中即失效，按钮自然回到「查看通用配置预览」，过期的 open 标记无法单独显示内容。片段渲染本身不产生备份或写入记录的契约不变。`DESIGN.md` 通用设置条目改写为单控件契约。验证：`npm run typecheck` 零错误、`npx vitest run` 48 文件 262 项全过（页面级用例改为有状态 harness 全链路：生成→收起→展开且断言 `onPreview` 仅调用一次，`CodePreview.test` 还原为原断言）。未打包。

**用量面板按分钟自动查询（2026-09-02 完成，用户指令「脚本加入和开源 导入源 项目一样的按分钟自动查询……我们自己实现可选择时间」）：** 参考 外部配置 源码的 `autoQueryInterval` 机制（分钟粒度、0 禁用、`refetchIntervalInBackground`、仅当前激活供应商触发），按本仓契约自行实现为应用级设置。契约：`local_state.rs::AppSettings` 新增必填字段 `usageRefreshInterval`（唯一所有者，分钟，`0` 为仅手动，`0–1440` 合法；文件缺省该字段与超 24 小时上限同样明确拒绝为「应用设置格式无效」，由既有「一键修复」整体替换默认值恢复——无 serde 缺省补读、无旧文件迁移、无双读）；保存仍走 `set_app_settings` 唯一路径与完整对象校验。前端：设置页「用量查询」新分组行「自动刷新间隔」为自定义数字输入（任意 0–1440 整数分钟，`0` 为关闭；blur / Enter 提交，非法输入回退已存值不落盘，单位「分钟」走 detail 弱化字，无说明性副文遵守设置行契约），经 `onPatch` 唯一保存路径落盘；间隔经 `App → ProvidersPage → ProviderList → ProviderRow` 传入 `ProviderUsagePanel` 与 `CodexOfficialQuotaPanel` 的 `refreshIntervalMinutes`，两面板的读取状态机收敛为唯一所有者 `src/components/use-auto-query.ts::useAutoQuery`（挂载即查、档案切换重查、`setInterval` 按间隔轮询、并发读取按请求版本取舍仅最新生效），两份手抄内联实现同轮删除——此前某份拷贝加入的 in-flight 互斥守卫与 StrictMode 重挂载的失效首读互相锁死（面板永驻「读取中」且手动刷新失效、轮询 tick 被吞），用户以「无兼容无过渡无技术债实现修复」指令要求结构性收敛；回归用例「档案切换时首读未返回仍须发起第二查」锁死该路径；面板收起即卸载，轮询随之停止——与 外部配置 仅对当前激活供应商触发同范围。测试：Rust 新增「完整形态但缺 `usageRefreshInterval`」与 1441 超限两个拒绝用例、持久化契约 JSON 锁定新字段、收起集合缺省用例的夹具补入该字段、`tray.rs` 夹具补字段；前端新增 `AppSettingsForm` 间隔渲染 / 自定义提交 / 越界回退、两面板 fake-timers 轮询与 `0` 关闭用例，既有夹具补齐新必填字段。验证：`npm run typecheck` 零错误、`npx vitest run` 48 文件 266 项全过、隔离 `CARGO_TARGET_DIR` 下 `cargo test --lib` 153 项全过、`rustfmt --check` 干净（默认 target 因 dev 实例文件锁改用隔离目录）。未打包。

**官方登录接入方式（2026-09-02，2026-09-03 修订）：** 用户要求供应商「新建供应商」支持与 外部配置 一致的官方登录。调研 外部配置 源码确认其 OAuth 认证中心仅覆盖 Codex（OpenAI 设备码流程）/ GitHub Copilot / xAI，无 Claude OAuth；按用户确认，本项目扩展到双客户端：Codex 沿用 OpenAI 设备码（POST `auth.openai.com/api/accounts/deviceauth/usercode` 取 user_code → 用户在 `auth.openai.com/codex/device` 输码 → 轮询换 token），Claude 新增 OAuth PKCE + 本地回环（`claude.ai/oauth/authorize` → `127.0.0.1` 临时端口收回调 → 换 token）。官方登录流程是 OAuth 凭据的唯一写入者；后续 Codex 自定义中转切换另由同一事务更新 `auth.json` 的 API-key 字段，二者不复制或暴露 OAuth token。已同步 AGENTS.md §5、README（定位声明、能力一览、所有权表、边界清单）与 `asb-core::ownership` 官方设置目录两条目文案。风险记录：Codex 设备码端点字段名取自 既有实现（serde 宽容解析，真实登录为验收门）；Claude 订阅类型采用社区验证写法（`claude /status` 验收）；OAuth client_id 为厂商公开标识（非机密），HTTP 客户端使用本项目自有 UA。退出条件：双客户端官方登录 + 官方条目重新登录全链路测试通过。

**自动刷新间隔迁入用量查询工作台（2026-09-03 完成，用户指令「别放到设置界面啊，应该放在用量查询的编辑界面，设置界面是设置软件的配置」）：** 按内容归属把间隔从应用级设置迁为每档案的查询配置，全局字段整链删除。契约：`asb-core contracts.rs::UsageQuery` 两个变体各增 `refresh_interval_minutes`（`serde(default)`——字段加入前保存的查询读作 `0`，即其本来的纯手动行为，业务数据确定性映射而非兼容双读；共享访问器 `refresh_interval_minutes()` 收敛读取），校验唯一所有者 `validate_usage_query` 前置拒绝 >1440（`UsageQueryRefreshIntervalTooLarge`）；外部配置导入查询从手动开始，其 `autoQueryInterval` 键自然落入既有「未导入键」警告。前端：`UsageQueryWorkspace` 在两种模式共用区新增「自动刷新间隔（分钟，0 为关闭）」数字输入（blur/Enter 提交、越界回退、随查询草稿保存并经 `normalizeUsageQuery` 原样携带、切模式保留间隔），`ProviderUsagePanel` 改从 `profile.usageQuery.refreshIntervalMinutes` 读取轮询间隔；Codex 官方订阅额度面板无查询配置界面、回归「读取一次 + 仅手动刷新」原始契约。删除全局链路：Rust `AppSettings.usage_refresh_interval` 字段与校验、`client.ts AppSettings` 字段、设置页「用量查询」分组与数字输入、`App→ProvidersPage→ProviderList` 五处传参、`base.css` 专用行内样式、设置测试与夹具中的全部痕迹。测试：Rust 新增旧查询读 0/新字段往返、间隔越界拒绝（1441 拒/1440 过）；前端新增工作台「间隔随查询保存」「越界回退」用例，既有载荷断言（编辑器/页面/工作台）补齐新字段，面板轮询用例改为档案驱动，官方面板定时用例随契约删除；另将官方面板行查询正则锚定行首（`^5 小时`），消除并行倒计时文案「约 3 天 5 小时后」对行名匹配的串扰。验证：`cargo test --workspace` 全过（src-tauri 169 过 1 忽略、asb-core 115 等）、本次全部改动文件 `rustfmt --check` 干净（`official_login/claude.rs` 的格式问题属并行会话未收敛代码）、`npm run typecheck` 零错误、`npx vitest run` 50 文件 287 项全过。未打包。


**用量查询间隔契约转严格 + 一次性存储迁移（2026-09-03 完成，用户指令「无兼容无过渡无技术债实现」）：** 移除上一轮保留的 `serde(default)` 缺省读。`asb-core contracts.rs::UsageQuery` 两变体的 `refresh_interval_minutes` 改为严格必填字段（缺字段即解析失败）；存量供应商文件的确定性迁移进入既有迁移框架而非运行时路径：`config_store/migration.rs` 新增 `migrate_usage_query_interval`（`insert_missing_refresh_interval` 仅识别恰好上一版查询形态——declarative 的六键集合 / script 的两键集合且无未知键——补入 `refreshIntervalMinutes: 0` 后重序列化并经严格解析复核才原子写回；已当前契约文件跳过；其余任何形态原样放行给路由迁移或严格读取拒绝），在 `ensure_layout` 迁移序列中先于 `migrate_provider_route_mode` 与 `migrate_common_settings_semantics` 执行（两者都会重读供应商文件并要求当前契约）。曾按 AppSettings 模式先实现为 `load_client` 内的读取时升级，因与迁移框架的 `Migration` 门冲突且构成第二套机制而撤销——`load_client` 恢复纯严格读，升级唯一所有者归 migration.rs。测试：contracts 严格必填 + 往返夹具更新；providers 新增「旧文件升级一次且二次加载不再改写」「未知键形态拒绝且零写回」两用例。验证：`cargo test --workspace` 322 项全过、涉及文件 `rustfmt --check` 干净、前端无改动（`npx vitest run` 287 项维持全过）。未打包。


**设置读取失败的全局修复横幅（2026-09-03 完成，用户指令「在合适位置加入一键修复按钮」）：** 起因：严格设置契约拒绝含废弃字段的文件后，修复入口只在设置页失败行可见，而受影响的功能（供应商页用量收起等设置类操作）在其他页静默等待，用户无从发现阻塞原因。实现：`AppShell` 持久错误横幅新增第二种情形——`settingsError`（来自 `useAppSettings.loadError`）非空时于每一页工作区顶部显示「应用设置不可用：{原因}」+ 次级「一键修复」按钮，直连同一 `repair_app_settings`；与 `profile-store-unsupported` 横幅同构（role=alert + 行内恢复动作），与设置页失败行构成同一动作的两个视图（置顶双视图同款先例），修复/重试成功后随加载状态消失。无兼容路径：修复仍为整体替换默认值，本横幅只解决可发现性。测试：App 集成新增「任意页面显示横幅并一键修复后横幅消失」，既有设置页修复用例改为显式点击失败行内的按钮（页面行与横幅现为两个同名按钮）。验证：`npm run typecheck` 零错误、`npx vitest run src/App.test.tsx` 29 项全过。未打包。

**Codex 统一为官方 `openai` provider（2026-09-03 完成，用户指令）：** 复核 OpenAI 官方配置参考后，Codex 的唯一运行路径固定为 `model_provider = "openai"`；第三方中转站使用顶层 `openai_base_url`，不创建 `OpenAi` / `Openai` 或任何项目自定义 `model_providers` 表，因此不同中转站与官方服务共享同一会话 provider 分类。档案 API key 按 `codex login --with-api-key` 的官方缓存形态写入同目录 `auth.json`（`auth_mode = "apikey"`、`OPENAI_API_KEY`）；切回官方时移除 `openai_base_url`，恢复 `chatgpt` 模式并保留已有 OAuth `tokens`。旧顶层 bearer token 与旧 `[model_providers.OpenAi]` 受管键只在下一次投影中清理，未知同表字段及其他 provider 表保持宿主所有。`asb-switch` 为 `config.toml` 和 `auth.json` 执行同一确认事务：预览哈希覆盖两文件、认证备份关联配置备份、认证或状态提交失败时配置回滚，恢复/撤回时按关联备份同步恢复两文件。发现导入从 `openai_base_url` 加 API-key `auth.json` 读取。验证：`cargo test -p asb-core`（121）、`cargo test -p asb-switch`（39）通过；全工作区与前端检查见本轮最终验收。

**Codex 双文件恢复与宿主格式收敛（2026-09-04 完成）：** 独立审查发现认证候选未变化时仍必须建立关联 `auth.json` 快照，否则先切换配置再切换另一中转后恢复会造成端点与密钥错配；现已让每次 Codex 配置备份都创建关联认证快照，缺失关联快照的 Codex 恢复明确拒绝而不走单文件回退。认证适配器只替换根级 `auth_mode` / `OPENAI_API_KEY` 两个受管值，未知字段、空白、转义和宿主格式逐字节保留；补充多行缺字段插入、宿主原文保留及“认证未变化 → 另一中转 → 成对恢复”黑盒测试。验证：`cargo test --workspace`（184 通过、1 忽略）、`npm test -- --run --maxWorkers=1 --no-file-parallelism`（312 通过）、`npm run typecheck`、`npm run build`、`cargo fmt --check`、`git diff --check` 均通过。

**三端打包 CI 运行时契约修复（2026-09-03，待新 CI 验收）：** GitHub Actions `33764562281` 的 Windows NSIS 已完成，而 macOS ARM、macOS Intel 与 Linux 在测试阶段失败。两处根因均为平台契约缺口：`asb-switch::pids` 在所有非 Windows 平台无条件返回 `Unknown`，使真实的占用锁、陈旧锁与显式陈旧锁恢复在 Unix 上永远不可达；OAuth 测试 loopback 服务则按大小写敏感方式寻找 `Content-Length`，违反 HTTP 字段名大小写无关的规则并造成 Linux 轮询响应错位。修复后，`pids` 在 Unix 以 `kill(pid, 0)` 判定存活（`EPERM` = 存在、`ESRCH` = 不存在，其他系统错误才为不确定），不支持的平台明确拒绝编译；原有锁集成测试继续要求 `Held`、`Stale` 与显式恢复。测试服务的 `Content-Length` 解析改为字段名大小写无关，并新增两种拼写的定向回归测试。验证：`cargo test --workspace`、`npm test -- --run`、`npm run typecheck`、`npm run build`、`cargo fmt --check`、`git diff --check` 均通过；`rustup run stable-x86_64-pc-windows-gnu cargo check -p asb-switch --target x86_64-unknown-linux-gnu` 通过。新的 macOS/Linux 安装包构建由下一次三端 CI 验收。

**本地模型消耗统计（2026-09-03 完成，用户指令）：** 会话用量读取方式后，以本仓独立契约实现 `ModelUsageReport`：`ModelUsageRange`（今日 / 近 7 天 / 近 30 天 / 全部）、按客户端和模型分组的 fresh input、缓存读取、缓存创建、输出、总计与会话数，以及不完整数据的客户端级问题。`src-tauri/src/model_usage.rs` 是只读聚合所有者，仅从 `session_manager` 声明的 Codex 活动/归档与 Claude projects JSONL 根目录读取；不接收渲染器路径、不读取 SQLite、不写会话文件。Codex 从 `turn_context` 的模型和累计 `token_count` 取非负增量，计数回退只重置基线；Claude 按物理 JSONL 内 `message.id` 去重，优先已完成快照、再取输出较大的快照，缓存创建与缓存读取分别保留。根目录优先级与会话浏览一致，活动/归档重复会话只统计一次。后端命令 `get_model_usage_report` 经既有 blocking 边界暴露；前端独立「用量」导航页使用全局 `Table`，仅在激活时请求并以请求版本排除迟到回包。它明确显示「仅汇总本地会话记录，不表示供应商剩余额度」；供应商余额查询与 Codex 官方订阅额度保持各自原有契约，未混用。测试覆盖 Codex 累计值、Claude 流式重复、时间范围、部分损坏记录与归档重复，及页面激活、范围切换、迟到响应与 API 边界。验证：`cargo test -p agent-switchboard --lib --quiet`（180 通过、1 忽略）、`npx vitest run src/pages/UsagePage.test.tsx src/api/client.test.ts src/App.test.tsx`（55 项）、`npm test -- --run`（297 项）、`npm run typecheck`、`npm run build`、`cargo fmt --check` 与 `git diff --check` 通过；本轮 Codex 路由已收敛到正式 `auth.json` API-key 缓存契约并纳入完整工作区验证。

**用量趋势与本地快照历史（2026-09-04 完成，用户选择方案 B）：** 建立 `usage-history.json` 为供应商余额和 Codex 官方额度的唯一、无凭据本地账本：供应商按当前 `UsageQuery` 摘要隔离，档案编辑、删除或重置时清理对应历史；官方读数只在前后账号短摘要都存在且相同时追加，账号变化、基线缺失或不可读时清理旧趋势后记录，绝不混合不可比较账号的快照。只有真实成功查询才写入快照，保留最近 365 天且每条序列最多 720 点，写入和读取均过滤过期点；账本不保存 API key、端点、查询脚本、原始响应或账号标识。`ModelUsageReport` 新增本机日桶与无时间戳 token：后者保留在总计和明细但永不伪造成趋势点。前端新增自有全局 `UsageTrendChart`、`ModelUsageDistributionChart`、`UsageRatioMeter`，用量页收敛为「汇总 → 本地 token 日趋势 → 模型构成 → 明细」；供应商余额、供应商卡中的 Codex 官方额度、概览的官方额度重置面板都读取各自真实历史趋势，混合余额 / 已用套餐分别呈现而不丢弃。所有图表走 `Frosted Relay` 令牌，趋势按单位隔离、空态和错误态稳定显示、减少动态效果可用；未引入图表依赖，也未复制 本项目 资源或实现。验证：`cargo fmt --check`、`cargo test --workspace --quiet`（桌面 187 通过、1 忽略；核心 125、执行器 32）、`npm test -- --run --maxWorkers=1 --no-file-parallelism`（54 文件、314 项）、`npm run typecheck`、`npm run build` 与 `git diff --check` 均通过；生产构建仅保留现有的 Vite 500KB chunk 提示。隔离桌面进程和开发服务器正常，浏览器自动化因 Edge CDP 焦点模拟超时而未取得截图。



**用量数值单位与 K/M/B 缩写（2026-09-04 完成，用户指令「接入并实现」）：** `src/lib/token-format.ts` 成为本地 token 显示格式唯一所有者：汇总卡、日趋势与模型构成统一以 K / M / B 读取量级并明确标注 `tokens`（`225,700,972` 显示为 `225.7M tokens`）；接近量级边界时避免出现 `1,000K` 或 `1,000M`。明细维持完整千分位以供审计，四个 token 表头注明 `tokens`。共享趋势图按单位识别 token，任何声明为 `token` / `tokens` 的趋势使用同一 K/M/B 标题和轴刻度；其他单位继续沿用既有格式。图表组件层 `StatCards` 新增可选单位位，避免把单位伪装成另一个指标。验证：`npx vitest run src/lib/token-format.test.ts src/pages/UsagePage.test.tsx src/components/charts/UsageTrendChart.test.tsx src/components/charts/ModelUsageDistributionChart.test.tsx src/components/ProviderUsageTrend.test.tsx src/components/CodexOfficialQuotaPanel.test.tsx src/components/CodexOfficialResetPanel.test.tsx`（6 文件、22 项通过）、`npm run typecheck`、`npm run build` 与 `git diff --check` 通过；浏览器自动化连接本地开发预览超时，未取得新截图。

**用量图表重构（2026-09-04 完成，用户指令「模型构成的图表改成环形实现……整体的图表参考这样设计一版实现」）：** 不复制参考图的资源、文字或不存在的数据字段，而是按本仓真实 `ModelUsageReport` 重组信息层级。四项已有 token 指标收束为一条 图表组件层 账本统计带；模型构成升级为大尺寸环形图，中心显示 K/M/B 总量，右侧逐行保留模型身份、真实 token 值和占比，超过前五项仍明确归为「其他」；日趋势成为独立通栏卡，并保留合计、单位、真实读数图例、缺口不插值与悬浮读数行为。供应商余额与官方额度继续使用紧凑趋势卡，不受此页面专属展示影响。验证见本轮最终检查。

**用量页端到端快照与刷新（2026-09-04 完成，用户指令「加入一些缓存显示……按时间刷新一次与用户手动刷新实现」「前后均需要无兼容无过渡无技术债实现」）：** 查询缓存与前台轮询语义，在本仓独立建立后端唯一快照契约：严格 IPC 请求 `ModelUsageRequest { range, forceRefresh }` 替代旧的单范围参数；`ModelUsageRead` 返回报告、`cached` / `fresh` 来源、`refreshAfter` 和非敏感缓存告警。后端按范围将最近成功报告原子写入独立、无凭据的 `model-usage-cache.json`；命中时立即返回，强制或到期刷新只覆盖该范围，损坏快照会重新汇总并替换。渲染器不保留 `Map`、TTL 或兼容分支，只按后端 `refreshAfter` 在页面可见时安排刷新；手动「刷新」传入 `forceRefresh: true`，失败时保留已显示报告。验证：`cargo test -p asb-core`（126 项通过）、`cargo test -p agent-switchboard --lib`（191 项通过、1 项忽略）、`npm test -- --run src/pages/UsagePage.test.tsx src/api/client.test.ts`（2 文件、32 项通过）、`npm run typecheck`、`npm run build`、改动 Rust 文件 `rustfmt --check` 与 `git diff --check` 通过；完整前端套件在当前脏工作树中另报 `SessionManager` 缓存淘汰超时和 `ProviderList` 活动模型断言失败，均不在本轮改动文件内。

**独立官网与 Vercel 发布（2026-09-04 登记，进行中，用户指令「完整实现方案结论」）：** 新增独立 `website/` 静态介绍站（Vite + React + TypeScript，独立 `package.json` 与锁文件），与桌面应用严格分离；Vercel 导入仓库后 Root Directory 设为 `website`，`npm run build` 输出 `dist`，生产分支 `master`，不新增 GitHub Actions、不保存令牌。页面只含五个区块（SiteHeader、Hero、RelayDiagram、ProductFrame、FinalCta），采用 `Frosted Relay` 浅色单主题：真实产品截图为主角，装饰仅由既有 `website/public/abstract-desktop-product-background.webp` 抽象背景与一幅原创 SVG 双路由流程图承担；不使用徽章、指标卡、功能卡片墙、评价、FAQ、轮播、暗色主题、语言切换、路由、运行时 API、环境变量或分析追踪。契约归属：`site-content.ts` 是全站唯一可见文案与链接来源；`website` 的 `tokens.css` 是站点唯一视觉变量所有者（值对齐应用 `tokens.css` 浅色段）；`public/media/` 是站点唯一媒体来源——`docs/screenshots/` 迁入 `website/public/media/product/` 后删除旧副本，README 引用同步改为新路径，不保留双份资源。下载规则：下载唯一指向 `https://github.com/y4Nkk/agent-switchboard/releases/latest`，不按操作系统猜测安装包、不前端调用 GitHub API、不维护版本号、文件名或镜像地址；首个公开 Release 是生产上线前置条件——该条件已由 2026-09-04 发布的 v0.1.5（三平台安装包与 `latest.json`）满足，Release 前仅 Vercel Preview 部署的规则保留。概览页总览截图以隔离演示环境补拍：经 `USERPROFILE` / `CODEX_HOME` / `CLAUDE_CONFIG_DIR` 重定向的演示沙箱加 `ASB_WEB_DEVELOPMENT` 浏览器模式拍摄，只含虚构档案与 `example.com` 地址，不接受生成式假 UI、手工修图或真实用户数据。验收：`website` 可独立执行类型检查与生产构建；1440 / 768 / 390 三种宽度无横向溢出；所有下载入口有效；键盘焦点可见、减少动态效果生效；不出现营销文案、虚构数据或额外生成装饰图。

**独立官网视觉证据改为 DOM 复刻（2026-09-04 更新，用户指令「展示的模型需要新的……不需要截图，右侧什么的复刻即可；中转路线使用亚马逊的去展示」）：** 首屏右侧不再使用产品截图，改为按应用路由卡渐变令牌（`--asb-route-active-gradient-*`）复刻的 Codex / Claude 双卡：模型展示 GPT-6 Astra 与 Claude Opus 5.1，中转路线统一为 Amazon Bedrock（`bedrock-runtime.us-east-1.amazonaws.com`）；「写入前，先看变化」区块同步改为 DOM 复刻的变更预览（脱敏差异行、候选配置与文件路径）。站点不再引用任何 `.png` / `.jpg` 产品截图（测试锁定）：`website/public/media/product/` 已删除，演示截图回归 `docs/screenshots/`，README 引用同步还原；`ProductFrame` 组件删除。验收：`website` 类型检查、10 项测试与生产构建通过；1440 / 768 / 390 无横向溢出，复刻卡片与差异面板经浏览器截图人工核对。

**首屏复刻范围扩大为软件界面整体（2026-09-04 再次更新，用户指令「复刻的是软件界面内的模块显示，不单单复刻一小个模块」）：** 首屏右侧由孤立双卡升级为概览界面整体复刻：应用顶栏（品牌与九项主导航，「概览」为当前态）+「当前启用配置」面板（渐变路由双卡）+「配置状态」面板（刷新状态操作与 Codex / Claude 两张状态卡，含配置文件、当前服务、匹配状态、上次切换、写入锁五行，绿色状态胶囊）。复刻文字仍由 `site-content.ts` 单一来源驱动；新增 `--site-surface-rail` 令牌。验收：`website` 类型检查、10 项测试与生产构建通过；1440 / 390 无横向溢出，浏览器截图人工核对。

**首屏标注移除、流程图精致化与行动入口收敛（2026-09-04 三次更新，用户指令「移除演示数据标注……加入 github 图标……下载按钮保留 2 个……改为 star」）：** 移除首屏「演示数据」标注；流程图重绘——修正连接线穿入步骤框与箭头被遮挡的几何缺陷（连接线止于目标边缘外侧、箭头尖端距边缘 4px），拉大纵向节奏，中枢节点增加双色接缝（呼应应用图标切换缝）与投影层次；顶栏与首屏的 GitHub 链接、收尾「查看源码」加入 GitHub 图标；「下载版本」由三处收敛为顶栏与首屏两处，收尾主按钮改为「点亮 Star」（星形图标，指向仓库主页提醒点亮星星），测试锁定下载入口数量为 2。验收：`website` 类型检查、11 项测试与生产构建通过；1440 / 390 截图人工核对（流程图双变体、图标、按钮收敛）。

**官网配置积木主视觉与顶栏控制组（2026-09-04 完成，用户指令「通用配置组件 + 供应商专属设置 → 实际配置文件」）：** 移除旧 `RelayDiagram` 与顶栏下载按钮，首页唯一下载入口保留在首屏并继续指向 GitHub Releases。新增 `ConfigurationAssembly`：可切换 Codex / Claude Code，分别将本客户端通用设置与 Amazon Bedrock 供应商设置组合为真实目标格式——Codex 展示 `config.toml` 中的 `model_provider = "openai"`、`openai_base_url` 与通用字段；Claude Code 展示 `settings.json` 中的 `effortLevel`、`autoCompactEnabled` 与 `env` 字段。官网不实现或复制配置写入器，积木台只展示由桌面端契约约束的隔离演示配置，且不出现密钥。顶栏替换为语言菜单、可用的浅/深主题切换与 GitHub 图标；中英文页面内容均由 `site-content.ts` 唯一所有，主题与语言只影响展示，不引入路由、环境变量、运行时 API 或持久化状态。验收：`npm test`（2 文件、17 项）与 `npm run build` 在 `website/` 通过；本地浏览器已核对首屏和积木台的桌面布局。

**独立官网审查问题完整修复（2026-09-04 完成，用户指令「无兼容无过渡无技术债完整修复审查出现的问题」）：** 官网配置积木台不再手写任何客户端配置值或输出行。`asb-core::website_assembly` 使用现有纯适配器以固定、无真实凭据的演示计划渲染 Codex `config.toml` 与 Claude `settings.json`，通过既有脱敏标记替换密钥后生成 `website/src/generated/configuration-assembly.ts`；官网只读取该提交的静态产物，Vercel 无须 Rust。生成器和核心测试都严格比对生成物，`npm run verify:assembly` 可在工作树中验证；没有旧的手写配置、回退或第二渲染路径。深色路由卡字段面、字段文字和标题收敛为 `website/src/styles/tokens.css` 的语义令牌，消除近白字近白底且移除组件样式中的原始白色表面值。中英文的路由字段、无障碍名称和全部行动文案均由 locale 内容直接拥有，删除基于对象引用判断语言的旧函数。语言菜单补齐方向键、Home/End、Escape 与焦点回归；配置标签页补齐 roving tabindex 与方向键、Home/End。`website/` 已纳入 Git 索引，作为 Vercel Git 部署输入。验证：`website/npm test`（2 文件、19 项）、`website/npm run build`、`cargo test -p asb-core website_assembly --lib`、生成物 `--check`、`git diff --check` 通过；本地浏览器验证了深色主题、完整英文切换、Claude JSON 输出与凭据脱敏。

**官网概览双路卡与通用设置控件展示（2026-09-04 完成，用户指令「首屏卡片左右布局……通用设置可视化展示」）：** 首屏概览内的 Codex / Claude 路由卡改为与桌面应用一致的桌面双列；窄屏才单列。模型、服务地址与接入方式不再截断：服务地址列获得更高权重，全部文字随卡片宽度自然换行，因此不再把模拟应用拉成长列表或以省略号隐藏真实内容。积木台的通用设置不再以键值行伪装：`website_assembly` 从桌面 `setting_spec` 导出每项的控件类型和完整档位（含「自动」），网站只将其渲染为只读三段开关与多档滑块；字段、档位与实际 TOML / JSON 都仍由同一生成产物提供，没有任何前端 `key → 控件` 推断或第二配置契约。三段开关复刻桌面端的灰色实体轨道、白色选中项、行动蓝描边与投影；滑块复刻 24px 能量渐变轨道、白色 28px 拇指、填充内微粒和离散刻度，并尊重减少动态效果。Codex / Claude Code 标签页移除底部彩色装饰条，只保留清晰的选中边框。验收：`cargo test -p asb-core website_assembly`、`website/npm run verify:assembly`、`website/npm test`（2 文件、20 项）与 `website/npm run build` 全通过；本地浏览器核对首屏双列、完整路由字段、Codex TOML 与 Claude JSON 两种控件化积木台，页面正常加载且未见错误覆盖层。

**官网写入可信度证据（2026-09-04 完成，用户指令「对比软件实际实现情况 web站还需要加入什么展示吗」「无兼容无过渡无技术债实现」）：** 官网不扩展为功能墙，而是在现有两处软件复刻内补齐真正的切换边界。`asb-core::website_assembly` 现从桌面 `official_setting_directory` 读取每个客户端的 `PreserveOnly` 资源路径，生成 `preservedPaths` 静态产物；积木台在目标配置文件内展示未受管范围（Codex 的 `mcp_servers.<id>` / `hooks`、Claude 的 `permissions` / `hooks`）及“保持原样”，没有前端手写字段、第二目录或虚构配置。预览下方新增同一实体预览面语言的只读事务行，完整呈现执行器 `execute_locked` 与 `commit_rendered` 的写入顺序：创建备份 → 临时写入 → 临时回读 → 语法校验 → 最终冲突检查 → 原子替换 → 写后验证；仅在替换后的写后验证失败时恢复刚创建的备份，不新增浏览器写入路径。验收：`cargo test -p asb-core website_assembly --lib`（2 项）、`website/npm test`（2 文件、23 项）、`website/npm run verify:assembly`、`website/npm run build` 与 `git diff --check` 通过；本地浏览器确认首屏、积木台的保留边界和预览事务容器均已渲染，且无错误覆盖层。

**窗口控制三钮移除 Tooltip（2026-09-05 完成，用户指令「软件的最小化、放大缩小、关闭按钮移除Tooltip」「无兼容无过渡无技术债实现」）：** `WindowControls` 三钮（最小化 / 最大化-还原（动态）/ 关闭）不再包 `Tooltip`，按钮直接渲染，悬浮与键盘聚焦不再出现提示；`aria-label` 保留为唯一可及名（本仓契约：可及名一律由 `aria-label` 承担，图标钮无可见文字故非冗余）。`Tooltip` 组件与其 `side` 参数仍由其余 5 处调用方拥有，非死代码；`.asb-tooltip` / `.asb-tooltip-anchor` 样式同理保留。全仓无窗口钮提示的残留引用，`DESIGN.md` 标题栏契约本就未记录该提示，无需改动。验证：`npx vitest run src/components/WindowControls.test.tsx`（5 项通过）与 `npx tsc --noEmit` 零错误；未打包。

**供应商编辑器显示真实网关端口（2026-09-06 完成，用户指令「警示说明需要真实端口」）：** 跨协议供应商在编辑器中按需读取现有 `gateway_status`，直接展示 `GatewayController` 已分配且切换计划同样使用的 `baseUrl`，不再拼接或显示 `127.0.0.1:端口` 占位值。读取期间明确说明正在获取实际监听地址；读取失败时要求用户在「网关」页确认状态后再切换，不伪造地址或保留旧文案。Codex、Claude 的真实地址渲染及失败路径均有组件测试。验证：`npx vitest run src/components/ProviderEditor.test.tsx --maxWorkers=1 --no-file-parallelism`（31 项通过）、前端全量 `62` 文件 / `388` 项、`npm run typecheck`、`npm run build` 与 `git diff --check` 通过；生产构建仅保留既有的 Vite 500KB chunk 提示，未打包。

**供应商认证由协议派生（2026-09-06，完成，用户指令「选择不同协议时自动切换密钥的模式」）：** `UpstreamProtocol` 是唯一认证规则所有者：Responses 与 Chat Completions 固定以 `Authorization: Bearer <API 密钥>` 发送凭据，Anthropic Messages 固定以 `x-api-key: <API 密钥>` 发送。供应商档案、编辑草稿、IPC、模型读取、用量查询、上游网关路由、快照和云端备份均移除可编辑或持久化的认证字段；编辑器只显示当前格式推导出的说明。精确的上一版本地供应商文件与云端快照只在存储边界一次性重写为当前格式，运行时不暴露旧字段。模型读取与用量查询改为严格的嵌套请求契约，旧 `authScheme` 会在访问供应商前被拒绝。网关本机能力令牌仍固定走 Bearer，且只存在于网关投影执行态，不进入供应商档案。验证：`npm run typecheck`、受影响组件 37 项、前端全量 62 文件 / 387 项、`npm run build`、隔离 Rust 命令边界测试与 Rust 工作区全量测试均通过（各套件合计 494 通过、4 忽略）。

**批量扩展契约收尾与全仓行数拆分（2026-09-07 完成，用户指令「整合三个会话内容全部实现……完整无兼容无过渡无技术债」）：** 三条线合并收尾。(1) 修复批量多操作契约的两个遗留缺陷：v2→v3 离线迁移此前把历史文件当扁平记录处理，实际磁盘形状是 `OperationSnapshot` 包装（`record` + `completedSteps` 顶层），迁移改写 `record` 内的 v2 单资源记录为 `resources` 列表并为 `completedSteps` 回填 `resourceId`；恢复批次此前把新建绑定的基线同时列入 `baseline_deletes`，与 `binding_deletes` 的隐式基线删除相撞而被 `validate_commit` 拒绝并整批回滚，现从 `baseline_deletes` 中剔除将被删绑定。修复 Codex 条目恢复把 `mcp_servers.<key>` 完整片段再嵌套进自身（`apply_codex_entry_restore` 改为从备份片段取出该键），`PortablePayload` 字段名补齐 `rename_all_fields = "camelCase"`。(2) 接管与便携包补上命令级隔离沙箱测试：`takeover_restores_native_state_on_removal` 验证接管不改写客户端文档/目录、移除逐字恢复原生条目与目录、发现状态回到未托管；`portable_roundtrip_reports_missing_credential_slots` 验证导出不含机器路径与凭据值、MCP 导入报告 `TOKEN` 凭据槽、远程 MCP 拒绝导出。(3) 全仓 >500 行文件纯移动式拆分：`commands/extensions.rs`（6608 行）拆为 workspace/definitions/takeover/portable/sources/skills/planner(7 文件)/apply(2)/checks/support 十域壳模块；`extensions/store.rs`→store/(mod+content+records+commit+tests)、`checks.rs`→checks/(mod+stdio+http+tests)、`discovery.rs`→discovery/(mod+skills+mcp+tests)；`asb-core` 12 个文件、`asb-switch` 事务/执行器/恢复与测试族、`gateway` 域、dev_api/local_state/usage 等 src-tauri 杂项、前端 `client.ts`(按域)+`ExtensionsPage`/`ProviderEditor`/`McpEditForm` 子组件+`useExtensions` 三 hook+`base.css` 19 分区全部目录化，对外路径零兼容层。过程中以测试驱动修复拆分损伤（缺口常量按二进制字面量与调用语义复原、丢失函数按调用方契约重建），非本会话域的并行中间态按等待重试处理。验证：`cargo test -p agent-switchboard --lib -- --test-threads=1`（300 通过）、`cargo test -p asb-core`（208）、`cargo test -p asb-switch`（63）、`cargo check --workspace --all-targets` 0 错误、本域 0 警告、`npm run typecheck` + 前端全量 65 文件 / 414 项 + `npm run build` 通过；网关回环用例在全套件并行负载下偶发 5 秒读超时（隔离与单线程恒绿，端口本就由系统分配，属负载抖动非绑定冲突）。

**供应商定时用量查询移至桌面调度线程（2026-09-07 完成，用户指令「供应商的定时查询好像有点问题：更新不到托盘显示」「无兼容无过渡无技术债实现修复」）：** 根因是定时引擎寄生于主窗口 React 组件树（`useAutoQuery` 的 `setInterval` 挂在供应商卡片上）：切换客户端标签时另一客户端卡片被条件渲染卸载、打开用量工作区或编辑器时整个列表被替换、主窗口关闭到托盘后 WebView 隐藏受后台节流，三场景都会静默停摆轮询，而托盘面板只被动等待 `tray-changed` 事件，从此显示旧缓存。新契约中 `src-tauri/src/usage_query/scheduler.rs` 是定时执行的唯一所有者：常驻线程每分钟遍历档案、按 `refresh_interval_minutes` 判定到期并执行查询，成功后写缓存并广播 `tray-changed`；`usage-cache.json` 条目改为 `queryDigest` + `attemptedAt` + 可空 `summary`，失败也推进 `attemptedAt` 防止失败风暴，条目按当前查询摘要匹配且读写全程持进程级写锁；手动查询与调度器共用同一 `execute_once` 执行函数，`query_profile_usage` 变为薄壳并保留成功后托盘广播，错误码收敛为 `profile-not-found` / `usage-query-failed` 两类（旧细分码无消费者）。前端 `useProviderUsage` 删除定时轮询，只保留挂载首查、手动刷新与 `tray-changed` 后经新增只读命令 `read_profile_usage` 采纳缓存；`useAutoQuery` 继续服务 Codex 官方额度面板（其消费者是面板自身而非常驻托盘）。`refresh_interval_minutes` 的契约注释统一改为桌面调度器语义。验证：`cargo test --lib usage_cache`（10 项）、`cargo test -p asb-core`（208 项）、前端 `npx tsc --noEmit` 与全量 vitest（65 文件 / 417 项）通过；`cargo check` 在改动定稿后全绿，后续 `--lib` 全量测试被并行进行中的 `commands/extensions` 拆分（未跟踪新文件自身的编译错误）阻塞，与本改动无关。

**无兼容无过渡无技术债复审（2026-09-07 完成，用户指令「审查完成情况」）：** 在前一轮基础上清债。(1) 兼容面：删除会话遗留的 `split_extensions.cjs` 过程脚本；`extensions::store` 收紧无外部消费者的 `save_lock`/`StoredVersionSummary` 公开再导出（`save_lock` 降 `pub(super)`，记录端从 commit 子模块显式导入）。(2) 文件行数：扩展命令域再拆六处——`skills.rs`(882)→skills/(mod 465 + editor 384 + diff_tests)；`takeover.rs`(593)→takeover/(mod + resolve)；`support.rs`(580)→support/(mod 396 + tests)；`apply/restore.rs`(574)→restore/(mod + invert)；`planner/rename.rs`(698)→rename/(mod + tests)；`planner/rules.rs`(611)→rules/(mod + disable)。`dev_api/extension_sandbox_tests.rs`(991)按工作流拆为 authoring/deployment/migration/takeover/portable 五文件加共享 harness mod，子进程 `--exact` 路径串同步更新。生产 Rust 全部文件 ≤500 行（最大 `skills/mod.rs` 465）。(3) 验证：`cargo test -p agent-switchboard --lib -- --test-threads=1`（302 通过）、`asb-core` 209、`asb-switch` 63、`cargo check --workspace --all-targets` 0 错误、本域 0 警告、前端 typecheck + 65 文件 / 417 项通过。遗留（已识别、未拆，含理由）：`gateway/server/tests.rs`（1211，嵌套 mod 体+共享 harness，边界事故风险高，留待专门一轮）；测试文件 `asb-core mcp/tests.rs` 783、`dev_api/sandbox_tests.rs` 749、`cloud_backup/tests.rs` 588、`local_state/tests.rs` 525；前端 `ExtensionsPage.tsx` 830（并行会话活跃编辑中，剩余为约 30 个状态编排处理器，进一步抽取需 20+ props 无内聚收益）与 `ProviderList.tsx` 521。

**托盘头部定稿：应用 logo + 图标文字「打开主界面」钮（2026-09-07 完成，用户指令「系统托盘，右上角按钮，改为图标+文字描述的（打开主界面）按钮」「左侧的Agent Switchboard改为软件logo即可,无需存在文字」「前后均需要无兼容无过渡无技术债实现」）：** `TrayPanel` 头部契约一次改到位。左侧品牌文字删除，换为 `app-icon.png`（24×24 `object-fit: contain`，`.tray-app-logo` 归 `tray.css` 所有，不借用主窗口 `asb-topbar-icon`——DESIGN.md 约定托盘不套用主界面导航）；右上角主界面入口由纯图标钮（`variant="icon"` + 20px 箭头 + `aria-label`）改为 `variant="secondary"` 图标+可见文字「打开主界面」，16px 图标与底部「退出」同构，可及名改由可见文字承担，冗余 `aria-label` 删除；面板根 `aria-label="Agent Switchboard 托盘"` 保持为唯一可及名来源。透明底钮样式 `tray-footer-button` 因同时服务头部与底部，同名改为 `tray-ghost-button`（`tray.css` 唯一定义、头部 + 底部三处消费），旧类名全仓零残留。纯托盘 webview 内呈现层改动：Rust 侧窗口契约（360 逻辑像素宽、定位、`tray-changed`/`resize`/`tray_ready` 握手）零触碰，头部 `justify-between` 布局与面板尺寸不变。`DESIGN.md` 托盘条目同步为「头部左侧应用 logo、右侧图标+文字『打开主界面』」唯一描述，旧「标题栏可打开主界面」表述删除。验证：`npm run typecheck` 通过、`npx vitest run src/tray/TrayPanel.test.tsx`（7 项通过，含按可见文字断言「打开主界面」钮可点击）；`grep` 全仓确认 `tray-footer-button` 与旧描述零残留；未打包（logo 与按钮外观需用户在运行实例中验收）。

**扩展统一诊断契约与一键修复（2026-09-07 完成，用户指令「完整实现」审查方案）：** 按审查方案一次交付、无新旧双路径。(1) 统一诊断契约：`asb-core` 新增 `extensions/diagnostics.rs`（唯一所有者）——`ExtensionDiagnostic { id, code, client, subject, message, remediation }`，`code` 为 21 个机器码枚举（Skill 清单三态、Skill 目录/根/链接/类型异常、MCP 文档不可读/不可解析/集合类型错误/条目非对象/传输缺失/传输冲突/未知传输/未识别字段/被忽略字段、托管目录缺失/条目缺失/外部改动/不可读），`subject` 三态引用（发现行 observationId / 托管绑定 bindingId / 渲染器安全的扫描位置标签，真实路径不过 IPC），`remediation` 三态（auto/manual/info 均带原因）；`discover_extensions` 返回 `scanId`+`scannedAt`+观察行+诊断的完整快照并缓存最新扫描，`finalize_diagnostics` 按「对象＋问题类型」去重、绝不按文案合并对象；旧 `DiscoveryDiagnostic { client, message }` DTO 与观察行上的自由文本 `diagnostics` 字段整体删除（核心结构体与前端类型同步）。(2) 诊断生产者补齐：MCP 读取器返回 `ObservedMcpDocument`（集合存在但类型错误报 `McpCollectionInvalid` 而非空列表）、传输「缺失 / 冲突」分离为独立机器码；Skill 扫描三态明确区分（缺文件 / 缺 frontmatter / frontmatter 无效），解析失败进诊断字段不再借用 `description`，目录名绝不虚构清单字段；新增 `discovery/managed.rs` 作为托管绑定一致性诊断唯一生产者（Skill 缺失/部分缺失=可修复、内容修改=外部改动人工、目录不可读=人工；MCP 文档缺失/不可读=人工、可解析文档内条目缺失=可修复、哈希漂移=外部改动人工；停用绑定静默），与扫描级警告汇入同一集合。(3) 一键修复：新增 `prepare_extension_repair(scanId, diagnosticIds)`（校验扫描最新性、逐项重查 auto 资格与绑定身份、同定义多绑定合并为一个 `PlannedOperation`），Skill 修复按基线 `last_digest` 从内容库取回版本整体恢复（前置校验：现存文件必须为基线未修改子集，否则拒绝），MCP 修复按基线 `last_written_value` 原样恢复条目（前置校验：文档存在可解析、位置空闲）；`PlanOperation::Repair` 新增并更新展示标签，apply 对 Repair 跳过 `last_applied_revision` 推进（版本固定、停用意图保持），修复走既有 stage_plan→预览→apply_extension_plan→锁/备份/校验/回滚/历史恢复全链路，恢复可还原修复前状态；全不可自动修复时如实显示「0 条可自动修复」且不渲染假修复按钮。(4) 前端收敛：新增 `useDiscoverScan`（扫描快照/扫描中标记/stale 标记/修复准备/接管确认唯一所有者，替代页面分散状态；扫描失败保留上次快照并标注「结果未更新」+时间；确认修复后刷新+重扫，失败如实提示「修复已执行，验证未完成」）、`DiscoveryWarnings`（收起为一行「发现 N 条警告 · M 条可修复」+「一键修复」「展开」，摘要可省略、按钮不换行（`.asb-discovery-*` 样式），每次新扫描回到收起态，展开逐对象展示原因与处理方式，`aria-expanded`/`aria-controls`/键盘可用），DiscoverPanel 条目行仅显示紧凑警告数、点击定位对应诊断；`ExtensionsPage` 删除 4 个状态与 5 个处理器（830→776 非空非注释行），`useExtensionImport` 删除被取代的 5 个发现/接管函数；删除无消费者的 `.asb-banner-lines` 样式。(5) 顺带修复阻塞编译的两处既有 gateway 半成品机械错误（此前端口变更实现中的 `codex_auth_text` 类型误用、`controller.rs` 观察字段移动）、沙箱 harness 补 `ConfigWriteGate` 注册（gateway WIP 更新 lib.rs 时遗漏）。验证：`cargo test -p asb-core`（210）、`cargo test -p asb-switch`、`cargo test --lib extensions::discovery`（6，含三态/集合错误/托管一致性新测）、sandbox 修复链路 3 项（缺失目录修复+修订保持+过期扫描拒绝+历史还原、外部改动拒绝人工、MCP 条目逐字恢复且无关内容保留）、前端 `npm run typecheck` 零错误、`DiscoveryWarnings` 6 项收放行为测试与页面级修复流程 16 项全过；`cargo test --lib` 325 过、唯一失败为既有 gateway 端口变更用例在并行负载下的端口占用抖动（隔离复跑恒绿，与本改动无关）；浏览器实测单行布局因 dev 后端依赖桌面进程未执行，以组件测试断言+CSS（min-width:0/ellipsis/flex:0 0 auto/nowrap）佐证。

**发现页跟随当前 Tab 与动作文案契约（2026-09-07 完成，用户指令「保留扩展库与本机现有安装的区别……完整实现」）：** 在统一诊断契约之上收敛发现页的范围与操作语义。(1) 一次扫描、按 Tab 展示：快照不变，Skills/MCP Tab 各取所需，切换 Tab 复用同一扫描；客户端筛选与搜索同作用于扩展库与本机发现结果；「重新扫描」统一按钮并显示扫描时间；扫描失败保留上次结果并标注未刷新。(2) 对象身份与动作资格后端判定：新增 `commands/extensions/actions.rs`，为每条观察行计算 `actions { import {supported, inLibrary, reason}, takeover {supported, reason}, managedDefinitionId }`——Skill 以内容摘要判等（inLibrary=库中已有同摘要定义），MCP 以完整可表达的候选定义判等（重解析原文档走 import_*_server，未建模字段/传输问题则复制与管理双双禁用并给原因）；历史/托管来源只读不可管理，项目根未注册给出注册原因；`managedDefinitionId` 供「查看管理详情」直达库详情。(3) 顺带修复真实契约缺口：`takeover_observed_skill` 此前总是新建定义，与预览中「库中已有等价定义，将复用」相悖——改为按内容摘要复用等价定义，预览的 `definitionExists` 同步改为摘要判等。(4) 界面重构：发现列表列为「名称（含 MCP 传输、Skill 摘要 12 位）｜客户端｜作用域／项目（项目显示注册名）｜管理状态（已托管/只读来源/未托管）｜警告｜操作」，去掉类型列与独立摘要列；操作按矩阵渲染——未入库未托管=「复制到扩展库」+「管理现有安装」、已有等价内容=禁用「已在扩展库」+「管理现有安装」、已托管=仅「查看管理详情」（跳转库详情）、内容无效=禁用并附原因、只读来源不渲染管理按钮；提示行「管理现有安装已包含加入扩展库，无需先复制」。(5) 警告与修复跟随视图：`discovery-view.ts` 纯函数判定观察行与诊断是否属于当前 Tab/客户端/搜索——发现行诊断随行隐现、托管绑定诊断按绑定的库条目（kind/客户端/名称）过滤、位置级诊断按 `resourceKind`（核心 `ScanLocation` 新增字段）与客户端过滤、绝不被搜索隐藏；摘要「当前结果有 N 条警告，M 条可修复」+「修复这 M 项」，修复只收集当前视图的诊断 id（`prepareRepair(diagnosticIds)` 签名化），确认面板以资源名展示对象；无自动修复项时显示「没有可自动修复项，展开查看各项处理方式」。(6) 复制动作后立即重扫使「已在扩展库」即时反映；接管确认面板重写为三段式（管理对象/现有文件保持原样/解除管理时恢复原始状态）。验证：`cargo test --lib -- --test-threads=1` 332 项全过（含新增 sandbox `discovered_rows_expose_backend_judged_actions`：健康/无清单/历史目录/原生 MCP 四种行的资格矩阵、复制后 inLibrary、接管后 managedDefinitionId 指向复用定义）；前端 typecheck 零错误、68 文件 441 项全过（含 `discovery-view` 5 项与页面级「发现行/警告/修复批量跟随 Tab」用例）；`cargo check --workspace` 0 错误。

**发现警告摘要单一展开控件（2026-09-07 完成，用户指令「拓展界面，展开收回按钮模块重复，保留一个即可」）：** `DiscoveryWarnings` 收起态中，整行摘要 toggle（折叠箭头 + 计数摘要，`aria-expanded` / `aria-controls` / 键盘可用）与行尾独立「展开 / 收起」次级按钮功能完全重复，删除该按钮；摘要行本身成为唯一展开 / 收起控件，行尾只保留「一键修复」按钮或「没有可自动修复项，展开查看各项处理方式」提示。收放状态机（每次新扫描回到收起态）、stale 标注与逐对象诊断列表零改动；组件测试锁定收起与展开态都不再存在独立的「展开 / 收起」按钮。`DESIGN.md` 警告面板契约同步为「摘要行即展开入口，不再另设『展开』按钮」。验证：`npm run typecheck`、全量 vitest（68 文件 / 452 项）、`npm run build`（exit 0）与 `git diff --check` 通过；全仓 grep 确认旧「展开」按钮零残留引用，组件测试锁定收起与展开态均无独立「展开 / 收起」按钮。

**输入框全局组件统一接入与网关页布局优化（2026-09-07 完成，用户指令「输入框做全局组件……各界面统一接入」+「网关界面布局优化」）：** 一次交付、无兼容分支。(1) 契约收紧：`Input`（单行）与 `Textarea`（多行）为文本输入框唯一所有者，`Textarea` 的 `className` 透传删除（与 `Input` 的 `Omit` 对齐——此前 `GlobalPromptManager` 被迫手写完整 `asb-input asb-code asb-textarea asb-prompt-editor` 类串覆盖基础外观类，属契约漏洞）；提示词编辑器尺寸变体改由父级选择器 `.asb-prompt-editor-field .asb-textarea` 拥有（特异性 0,2,0，不再依赖 CSS 文件顺序），业务组件零外观类。(2) 统一迁移（无旧路径残留）：网关端口弹层裸 `<input>`（即无样式问题源头）、`CloudBackupPanel` 5 输入框+1 只读文本域、`McpEditForm`/`NewMcpForm` 启动参数文本域、`FontPicker`/`ModelPicker` 菜单搜索框全部改经全局组件；搜索框内嵌场景由外层 label 独占边框与焦点态，`forms.css` 关闭内嵌输入的 `padding` 与全局焦点环（`:focus-visible` 专用规则，杜绝双重描边）。剩余原生控件仅 `Checkbox`/`Switch`/`RadioOption`/`Slider` 组件内部与 `asb-seg-opt` 分段模式，不属文本输入框。(3) 网关页布局：三行监听信息与端口修改说明并入同一内嵌信息面（与统计瓦片同材质）；「近 60 分钟请求」趋势零请求时以文字空态替代空坐标轴、有数据时 X 轴固定每 10 分钟一刻度。`DESIGN.md` §8 表单节新增输入框唯一所有者契约、网关节新增布局细化条目。验证：`npm run typecheck` 零错误；全量 68 文件 452 项测试通过（`GatewayPage` 空态断言随趋势空态由 2 处更新为 3 处，属有意行为变更）。


**扩展 UI 流程重构（2026-09-07 完成）：** 对照参考实现固定提交 `389dd96cb8567f41d05ba4aaebce8a73dc524040` 的 Skills/MCP 管理和来源发现；按用户补充要求，蓝紫客户端身份、实体表面、字体、圆角和控件继续由本项目 Frosted Relay 拥有。工作区改为数量与客户端批量控制、搜索、紧凑列表，来源发现独立成页，新增/编辑/本机发现/项目/便携包/历史收进独立弹窗。移除旧添加卡片、常驻历史、内联详情和 AddMode 分支；页面状态、预览、更新、列表与各表单按职责拆分。客户端汇总和操作请求共用 deployment-state，批量启用涵盖未安装和已停用项，混合作用域显式表达，全部操作继续经同一后端预览与事务；同步锁防止忙碌渲染前重复提交。统一弹窗焦点管理，并修复下拉菜单跨弹窗挂载及 Esc 同时关闭菜单和弹窗的问题。验证：扩展/来源/Select 相关 64 项通过，typecheck 与生产构建通过；隔离浏览器 7 张截图覆盖深浅色、新建 MCP、来源加入、本机发现与窄窗口。最后一次全量前端检查 498 通过、3 项在云备份和供应商请求模块失败，本轮未修改这些模块。未写真实配置、提交、推送或切换分支。

**供应商真实请求正式接入（2026-09-07 完成，用户要求「无兼容无过渡无技术债实现」）：** 移除 ProviderRequestPreview、演示结果选择器、定时假回复、假状态码和假耗时，供应商卡片改用 ProviderRequestPanel 与独立请求生命周期。新增类型化 prepare_provider_request / execute_provider_request / cancel_provider_request，展开只读准备，显式发送后由桌面端读取同版本档案并按 Responses / Chat Completions / Anthropic Messages 直达供应商；固定短提示、1,024 tokens 输出上限、45 秒超时、64 KiB 响应上限，无重试、重定向或协议降级。只有完整、非空模型文本才判成功，实际模型缺失不补造，错误和回复中的档案密钥脱敏。取消会 abort 并等待网络任务退出；前端按请求轮次隔离迟到结果，取消失败可重试，关闭或连接字段变化清理请求。浏览器开发桥改为并发处理，避免 execute 阻塞 cancel。provider_http 是端点拼接唯一所有者，网关复用原有行为；直达预览的 URL 凭据、查询参数、片段和控制字符限制仅属于本操作准备边界，不改供应商持久化契约。结果不写客户端配置、连通性、启用状态或用量历史，README 与 DESIGN.md 已更新。验证：前端相关 8 文件共 111 项（含请求专项 30 项）通过；Rust 请求相关 23 项、端点 3 项、网关协议回归 2 项通过；npm run typecheck、npm run build、cargo check --workspace --offline 与差异检查通过。隔离子进程 HTTP 流程验证真实报文、密钥归属、取消并发、档案变化拒绝及客户端文件不变，套接字测试验证取消与超时实际断开连接；隔离浏览器核对浅色成功、深色窄窗口失败、等待与取消，控制台无错误。所有配置测试仅用临时目录与虚拟密钥，上游仅 localhost，未调用用户真实供应商、未打包安装器、未提交或推送。

**自定义 Provider 端点、Responses 能力与编辑详情收尾（2026-09-08 完成）：** 承接端点修复与供应商表单研究会话，统一由 `asb_core::endpoint` 解析完整 API 根路径，移除自动补 `/v1` 与独立 `provider_http` 拼接路径；模型发现、真实请求、网关和编辑页端点预览共用契约。自定义 Responses 档案显式保存 WebSocket 能力与标准／最小请求模式，Codex 使用独立 `agent_switchboard` Provider，关闭 WebSocket 后投影 HTTP/SSE；最小模式由网关保留输入与工具语义、省略已声明的可选扩展，对远程上下文引用与显式存储语义拒绝处理。HTTP 错误、非 UTF-8 正文、SSE 错误及解析中断保留 endpoint、状态码、request id 和有界脱敏正文；补齐 HTTP 200 后解析失败的头部脱敏及错误正文读取中断的截断标记。供应商详情按基本资料、连接、模型分区，Responses 能力、模型映射和备注按需展开，参数子页与主表单共用草稿；修正备注输入宽度，真实浏览器验证 Enter／Space 折叠后保存不丢设置。删除旧 Provider 身份与缺少能力字段的测试夹具路径，修复网关模块拆分后的绑定诊断可见性和无用导入；旧或非法 Responses 档案与快照明确拒绝，原文件和当前配置保持原样，没有新增兼容读取、自动补写或过渡逻辑。

验证：`npm run typecheck`、前端全量 92 文件／581 项、最终 UI 切片 9 项、`npm run build`、`cargo check --workspace --all-targets` 通过；Rust 工作区回归 723 项（桌面端 414、核心 245、切换执行器 64）通过，随后新增的档案／快照拒绝用例随存储切片 55 项通过。隔离 Codex CLI 0.152.1 三个场景（标准 Responses 直连、Chat 转换网关、最小 Responses 网关）全部通过，实测首个请求为 HTTP POST、没有 Upgrade，最小模式扩展字段不触达上游。浏览器验证 1200px 浅色与 390px 深色无横向溢出，Claude 新建进入参数子页后返回保留连接草稿，控制台无错误；相关 Rust 格式、文件行数和差异检查通过。测试仅使用临时目录、虚拟凭据和 localhost；未调用真实供应商、未访问真实导入来源数据库、未执行本轮无关的 Claude CLI／NVIDIA 实网测试，未构建安装器或提交推送。前端生产构建仍有现存的大包体提示。验证日志与截图保存在 `target-test/provider-*.log` 和 `target-test/provider-ui-qa/`。

**供应商测试入口统一（2026-09-08 完成）：** 按用户补充要求同时整合列表与编辑器。列表动作簇只保留供应商测试入口；编辑器在模型与 Responses 选项之后、备注之前增加连接测试分区，两处复用 `ProviderTestPanel`，模块内选择连通性测试或真实请求。移除行尾独立真实请求按钮、主模型旁旧检测按钮、`ProbePanel` 及两套旧反馈定位样式；纯探测生命周期与反馈由 `EndpointProbe` 持有。打开模块不访问供应商，选择真实请求仅准备目标，显式发送才发请求。准备契约统一为 saved/draft 两类目标，列表执行前核对档案版本；草稿仅保留当前连接的内存快照，不要求先保存，不生成临时档案，不写客户端配置。关闭、切换测试类型、切换参数页或离开编辑工作区会释放请求，连接字段变化重新准备，迟到响应不覆盖新目标。保留原有渐变连接卡片与星光。

验证：类型检查与生产构建通过；前端 8 文件 103 项测试通过；Rust 请求测试 32 项通过，含三种协议的未保存连接、无落盘、一次性执行与旧契约拒绝；隔离子进程命令桥测试通过，覆盖草稿准备、实际请求、取消、文件版本变化及客户端文件不变。实际 dist 浏览器检查浅色／深色及 390、940、1180 宽度无横向溢出，验证草稿目标、离开时取消和无隐式发送，控制台无错误。同步 DESIGN.md、README 与供应商截图；日志及测试截图位于 target/navigation-validation/provider-tests-*。为完成 Rust 验证，将切换事务现有错误字符串显式转为 String，修复编译器无法推断错误类型的问题，业务语义不变。本次上游仅本机隔离服务器，未调用真实供应商，未修改真实配置，未提交或推送；原生桌面驱动端到端测试未重跑。

**真实请求测试模型下拉选择（2026-09-08 完成）：** 真实请求面板的测试模型字段在临时填写之外，增加按需获取与下拉选择：点击「获取模型」拉取被测连接的模型列表，复用编辑器同款供应商分组搜索选择器选择要真实连通的模型。新增后端命令 `fetch_provider_request_models`，按后端持有的准备令牌 `requestId` 解析连接（草稿连接只随 `prepare_provider_request` 进入后端一次，此后所有命令仅携带令牌），由后端读取已保存档案或草稿连接的地址、密钥与 API 格式，密钥不进入渲染层调用参数、不出现在响应与日志；模型列表仅走 GET 模型端点，不生成一次性请求凭证、不消费令牌、不写档案或客户端配置，失败显示脱敏原因。面板按目标变化重置列表并丢弃迟到响应，请求或获取进行中禁用获取按钮、下拉与模型输入框。同步 README 与 DESIGN.md。

验证：`npm run typecheck` 通过；前端请求面板切片 39 项（含新增获取→选择→发送与失败保留可用两项）、模型选择器 6 项、编辑器连接测试切片通过；`cargo test --lib` 三个 lib 目标共 701 项通过、0 失败（桌面主 lib 456 通过／8 既有忽略项，核心与执行器全部通过），含隔离子进程命令桥新增的 saved／draft 双目标模型列表环节（断言 GET /v1/models、Bearer 头归属与响应不含密钥）。未调用真实供应商，未修改真实配置，未提交或推送。

**真实请求模型列表令牌边界修复（2026-09-08）：** 删除模型获取命令接收 `target` 的旧路径：草稿连接（含密钥）只由 `prepare_provider_request` 在内存中登记一次，`fetch_provider_request_models`、发送与取消均只接收不透明 `requestId`。开发桥同步拒绝旧参数形状；保存档案的模型拉取与真实发送共用同一准备令牌解析并校验文件哈希，档案变动显式拒绝，绝不静默切到新连接。前端以会话版本和同步 busy ref 丢弃目标变更后的迟到列表、阻止重复获取与发送竞态；模型获取期间冻结输入、下拉、获取及发送，通用 `ModelPicker` 在禁用时立即关闭并卸载已打开的菜单。新增草稿调用参数脱密、旧参数拒绝、档案变化无网络、打开菜单后禁用和迟到列表废弃覆盖；README 与 DESIGN.md 明确准备命令是草稿密钥进入后端的唯一 IPC 边界。

验证：前端相关 5 文件 67 项通过；`npx tsc --noEmit --noUnusedLocals false` 通过；本次 Rust 文件 `rustfmt --check` 通过。常规 `npm run typecheck` 被工作区无关的 `src/app/SettingsWorkspace.tsx` 两个未使用导入阻断；`cargo test --lib provider_request::tests` 被无关的 `config_store/migration/agent_keys.rs` 缺少 `read_optional` 与 `commands/subagent_settings.rs` 缺少 `tauri::Manager` 导入阻断，未将它们混入本功能改动。未调用真实供应商、未修改真实配置、未提交或推送。

**Codex 第三方客户端接管对齐（2026-09-10 完成代码与隔离回归）：** Codex 从通用 `ProviderProfile` 工作流拆为严格的 `CodexProviderProfile`，只保存显式 API 根、密钥、上游协议、请求模式、默认模型、目录、映射和能力；官方登录是原生直连状态，不生成官方档案。第三方始终投影 `model_provider = "openai"` 和带稳定 capability 的本机 `openai_base_url`，路由修订在 A→B 热切换时替换后续请求而不影响在途请求，HTTP、SSE、WebSocket、原生/桥接 compact、三种上游协议、模型操作与压缩解码均由同一受限网关边界处理。前端专用 Codex 页补齐保存预览确认、排序的全档案哈希提交及非法目录 JSON 的本地拒绝；通用 Claude 页面不再被当成 Codex 的兼容路径。同步 README、DESIGN 和实施设计，删除旧 capability URL 夹具、旧切换执行器自动转换及无效网站演示；旧客户端入口和旧状态仅拒绝，不会被预览或执行隐式清理。验证：`npm run typecheck`、全量 Vitest（98 文件 / 616 项）、`npm run build`、`cargo check --workspace --all-targets --target-dir .tmp\codex-validation`、`cargo test -p asb-core`（269）、`cargo test -p asb-switch`（74）和 `cargo test -p agent-switchboard --lib -- --test-threads=1`（485 通过 / 9 个外部环境测试跳过）均通过；未写入真实 Codex/Claude 配置或凭据。追加真实沙箱验收固定使用 `DeepSeek-V4-Flash-0731`：Responses 非流式请求经 ASB 网关到达此前确认的供应商根，但上游返回 `401 invalid token`，本机按契约返回 `502 upstream_authentication_failed`；其余五条真实请求未发送。三协议与 Codex Desktop 闭环仍需要有效的地址和密钥组合。

**Codex 三协议真实请求验收与推理桥接修复（2026-09-10 完成）：** 使用获授权的 DeepSeek 地址与密钥把上一节的真实验收做完，并修掉它暴露的四个网关缺陷。真实验收改为两组可重复的沙箱测试：`gateway::server::live_codex_tests::external_provider_completes_through_all_codex_protocols`（每种上游两轮对话加一条流式生成；第二轮把首轮全部输出项含不透明 `reasoning` 项按真实 Codex 客户端的做法回放为新请求输入）与新增的 `gateway::server::live_codex_cli_tests::actual_codex_cli_completes_through_every_external_protocol`（隔离 Codex CLI `0.153.4` 在每种上游下完成一次完整 `codex exec`）。两者都用临时目录、临时 `LocalState`、真实 `CodexProviderProfile`、真实 `SwitchPlan`、真实 `asb-switch` 切换事务和真实 `GatewayController`；档案密钥只存在于进程环境与档案文件，客户端配置、stdout、stderr 与响应中都不出现，`auth.json` 逐字节不变，`chatgpt_base_url` 固定到不可达 loopback 端口以免访问真实账户服务。

修复一（非流式）：`gateway/transform/response/parse.rs` 的 `parse_anthropic` 此前只认识 `redacted_thinking`，真实上游返回的 `thinking` 内容块直接导致 `502 responseParse`。修复二（流式）：`gateway/transform/stream/anthropic/` 缺少 `thinking` block 处理，且释放时机曾晚于助手文本（由新增单元测试捕获），现在该 block 的 `content_block_stop` 立即释放不透明 `reasoning` 项。修复三（请求方向）：Codex 跨协议请求携带的 `reasoning.effort` 此前被 `validate_responses_transport_metadata` 拒绝；新增 `gateway/transform/anthropic_reasoning.rs` 作为 Responses→Anthropic 推理方言的唯一所有者，按参考实现映射 `minimal|low → low`、`medium → medium`、`high → high`、`xhigh|max|ultra → max`、`none|off|disabled → thinking.type = "disabled"`，无法映射的档位仍然明确报错；`request_render/protocols.rs::render_anthropic` 同时补上规范的 `output_config.effort` 渲染，使 Chat→Anthropic 方向不再拒绝 `reasoning_effort`。修复四（多轮）：第二轮回放推理时，本机把 `asb-reasoning-v2.` 密文当成 Anthropic 的 `redacted_thinking.data` 发回上游，被以 `unknown variant` 拒绝。

修复四改变了推理续接载荷本身：`gateway/transform/reasoning.rs` 的 `asb-reasoning-v3.` 封装结构化记录（可读文本、上游签名、上游不透明块），而非只有文本。上游签名随记录封存并在同一后端回放时原样发出；上游自己的不透明块整块封存、只回放给同一后端。因此 Anthropic 上游收到的是它签发并认识的形状（`thinking` 带签名，或它自己的 `redacted_thinking`），本机密文从不离开本机；非流式与流式 Anthropic 响应都接受 `thinking` 与 `redacted_thinking`；跨后端无法阅读的不透明块进入 Chat 历史时明确报错，不再被静默丢弃。旧 `asb-reasoning-v2.` 载荷按第 14.0 节规则直接拒绝，不提供双读。

顺带清理：`crates/asb-core/src/ccswitch/tests.rs` 中遗留的 `zz_temp_probe_*` 调试用例改为断言 `apiFormat`／`api_format` 两种拼写都选出 Chat Completions 上游并保留裸域名根，删除 `eprintln!("PROBE …")` 与死绑定；`gateway/transform/request.rs` 的测试模块按既有 `#[path]` 模式移出为 `request_tests.rs`，使该文件回到 75 行的业务职责范围（原 545 行含测试，超出单文件 500 行约定）。

验证：`cargo test --workspace -- --test-threads=1`（862 通过 / 0 失败 / 10 忽略，其中桌面 lib 505 通过、asb-core 270、asb-switch 16 及其余集成套件）、`npm run typecheck`、`cargo fmt --all -- --check` 通过；两组真实测试在有效密钥下连续多轮稳定通过（网关组含三种上游 × 两轮 + 流式，CLI 组三种上游各一次完整生成）。未写用户真实配置、凭据、会话或远端业务数据；未提交、未推送。Codex Desktop 闭环仍未验收，CLI 结论不替代 Desktop 结论。

**Codex 三协议冻结验收与 0.2.1 本地打包（2026-09-10 收尾）：** 按用户确认的范围收尾——跳过 Codex Desktop（本机未安装：安装目录、卸载注册表项、开始菜单快捷方式均无），以「网关 + Codex CLI 三协议完成」为交付口径。地址固定为 Responses/Chat 的 API 根 `https://api.deepseek.com/v1` 与 Anthropic 服务根 `https://api.deepseek.com/anthropic`；未添加任何裸域名自动补 `/v1` 的行为。真实验收连续执行三轮，每轮检查退出码后才进入下一轮：三轮全部 `2 passed; 0 failed`（75.95s / 63.97s / 63.34s），整体 exit 0，日志 `target/codex-openai-validation/live-three-rounds-20260910-162442.log`。最终回归：`cargo test --workspace`（862/0/10）、`npm test -- --run --maxWorkers=1 --no-file-parallelism`（98 文件 / 617 项全过）、`npm run build`、`npm run typecheck`、`cargo fmt --all -- --check`、`git diff --check` 均 exit 0。

关于前端并行模式下的失败：默认并行执行曾出现 5 项 `Test timed out in 10000ms`，全部落在 `src/components/ProviderEditor*.test.tsx`（该四个文件最后修改于 2026-09-08，不属于本轮改动）；同批文件单线程隔离复跑 30/30 通过，串行全量 617/617 通过，判定为并行负载下的超时抖动，未用无关修改掩盖，也未改动任何前端文件。差异审查（只查生产代码）：`set_path("/v1")` 仅存在于导入规范化，端点层无版本推断；`asb-reasoning-v2.` 无接受路径与第二解密器；`experimental_bearer_token`／`[model_providers.*]` 只在导入读取侧与反断言测试中；`restore_validation.rs` 显式拒绝 `model_providers` 下的 `openai`／`agent_switchboard`／`OpenAi` 备份；「自动切回官方」「回退直连」零命中。

打包：`npm run tauri:build:windows` 全流程 exit 0，产出 `target/release/bundle/installer/Agent Switchboard_0.2.1_x64-setup.exe`（54.11 MB，SHA-256 `54EFB8F89806B03390D3089826B58043D8155C66728DA02B2635F5A3BCE5ABC2`，ProductVersion/FileVersion 均经独立读取校验为 0.2.1），另有便携主程序 `target/release/agent-switchboard.exe` 与同名 NSIS 引擎。该包为未签名本地构建（未设置 `TAURI_SIGNING_PRIVATE_KEY`，无 `.sig`），不可用于 GitHub Release 更新链路。

**Codex 供应商编辑器对齐 Claude 界面（2026-09-11 完成）：** 按用户指令把 Codex 新建/编辑从三块手写 JSON 文本域改为与 Claude 完全一致的结构化编辑视图，同时零改动保留新契约（`CodexProviderFile` 严格格式、create 直写 / prepare→saveAndApply 预览确认→commit 语义、官方登录为页面级原生状态、第三方恒走本机网关、能力声明不得由 `/models` 推断）。新建 `codex-provider-editor/`：`CodexProviderEditor` 镜像 Claude 编辑器分区（基本资料 / 连接配置 / 模型 / Responses 能力 / 供应商能力 / 连接测试 / 备注）与二级运行参数页（迁入 codex 子 agent 组）；模型目录为结构化行编辑（标识、上下文窗口、输出上限、逐项能力勾选、推理档位），「获取模型」按已声明能力生成可编辑行（能力只被收窄、绝不反向推断），模型映射为成对行，能力声明用勾选组 + Chat 推理参数结构化控件；前端镜像后端 `validate` 以中文问题清单阻止无效保存。共享叶子组件（端点字段、密钥、备注、Responses 选项、连接测试、`useProviderConnection`、`useProviderParameters`）改为窄 props 由两编辑器复用，`fetchModels` 返回拉取结果供目录生成。`ProviderEditorSession` 变为 claude/codex 判别联合：Codex 保存经 `useProviders.saveCodexProfile` 分流，活跃档案的 `saveAndApply` 复用全局确认单；编辑器内切换客户端以会话替换交接，两契约草稿绝不互转；`CodexProvidersPage` 改为常挂载模式（跨页、导入页保留草稿），列表与切换预览不动。死路径清理：通用编辑器中 codex 专属分支（`OutputLimitField`、`CodexContextSettings`、codex modelOptions、客户端就地换草稿）删除，对应测试迁移或改写为 claude / Codex 编辑器用例；DESIGN.md 编辑视图章节同步改为结构化表单描述。

验证：`npm run typecheck`、编辑器相关 12 文件 89 项（ProviderEditor 全套、ProvidersPage、CodexProviderEditor、draft 单测 17 项 + 组件 11 项）、`src/App.providers.test.tsx` 7 项（结构化交互改写：新建全流程、空目录阻止、活跃档案 prepare→确认→commit、排序、官方切换、迟到预览）均通过；全量串行 `npm test -- --run --maxWorkers=1 --no-file-parallelism` 100 文件 / 639 项全过（最终代码上另复跑 `src/App` 全部 9 文件 64 项通过），`npm run build`（tsc + vite）exit 0。Rust 侧零改动（契约、命令、网关不变），未跑 cargo。

**开发环境白屏修复与 target 构建产物清理（2026-09-11 完成）：** `npm run dev` 白屏的根因是 Vite 开发服务器的 chokidar 监视器扫到了 Rust 工作区的 `target/`（140GB、195,224 个文件；Vite 默认只忽略 `node_modules` 与 `.git`），监视事件风暴把 dev server 拖死——端口接受 TCP 连接但请求永不响应，CPU 与内存被原生线程吃满。修复：`vite.config.ts` 的 `server.watch.ignored` 显式排除 `**/target/**`、`**/target-dev/**`、`**/.tmp*/**`、`**/node_modules/**`，并把 `vite` 从 8.x 固定回 `^7.3.6`（`@vitejs/plugin-react` `^5.2.0`）——8.3.0 的 rolldown/oxc 依赖优化器在本机即使排除监视后仍卡死在依赖扫描，7.3.6 的 esbuild 优化器冷启动后毫秒级响应；`.gitignore` 补充 `.tmp-*/`、`.apple-build/`、`.audit-files.txt`。按用户指令清理 `target/`：删除全部可再生构建产物（debug 121G、release 13G、codex-quota-fix 3.2G、e2e-desktop 2.0G、tray-linux-api-check 341M、e2e-runs 旧沙箱 215M、e2e-driver-build 180M、x86_64-unknown-linux-gnu 86M、parameter-ui-check、e2e-cli-help、e2e-tools、tmp、顶层构建日志与 `.rustc_info.json`，及 `.tmp-cpuprof` 剖析产物），保留 `codex-openai-validation`、`navigation-validation`、`e2e-reports` 三个验证日志目录与 `CACHEDIR.TAG`，140GB → 5.0MB。同日追加深层清理：历史验证在项目根、`src-tauri/` 与 `.tmp/` 累积的 60 个一次性 cargo 输出目录（根 33 个 `target-*` 约 91G、`.tmp` 内 cargo/gateway/codex 输出约 83G、`src-tauri` 下 `.tmp` 与 6 个 `target-*` 约 22G）全部删除，保留 `.tmp` 内的参考克隆与小型评审材料及运行中的 `target/`，项目总占用 193G → 5.5G；`server.watch.ignored` 同步扩展为 `**/target-*/**` 通配，防止未来新建一次性 target 目录时复发监视风暴。

验证：隔离实例（vite 7.3.6 + watch 排除）`/`、`/src/main.tsx`、`/src/styles/boardui.css`、`react.js` 预依赖全部 HTTP 200 且 2.5–13ms；`npm run typecheck`、codex-provider-editor 切片 2 文件 29 项、`npm run build` 均通过；清理后真实 `npm run dev` 全量重编译 4m30s 完成，`appStarted`、后端 1422 健康检查 204、1420 端口页面与模块请求毫秒级 200、`/api/health` 经代理 204（3.5ms）；二次深层清理与 watch 规则热重启后复测 1420/1422 仍全部正常。未改任何业务代码，未提交或推送。

**界面布局统一：小节标题令牌、页头右簇与筛选唯一形态、网关与托盘去 Tailwind 化（2026-09-11 完成）：** 按用户指令「无兼容无过渡无技术债」完整落地布局一致性诊断的全部修复。新契约：`--asb-text-section`（`600 15px`）是页标题与正文之间唯一的小节标题档位，唯一类 `.asb-section-title`（panels.css）——设置分组（原 `asb-toggle-group-title`）、供应商/Codex 编辑器分区与测试/用量/订阅额度面板（原按 `.asb-provider-section h3` 等元素选择器设权，四处规则已删）、网关小节、变更预览、扩展确认单资源段全部收敛；设置页分类面板标题由 `h2.asb-panel-title` 降为该 `h3`，设置页唯一 `h2` 是侧栏「设置」。页头右簇唯一容器 `.asb-panel-actions`：`asb-session-toolbar` 与 `asb-runtime-log-controls` 连同其响应式规则删除，页面特有尺寸经 `.asb-sessions .asb-panel-actions` 作用域补充；日志页头副标题按 §2 删除测试移除。单选筛选唯一形态 `asb-segments` radiogroup（`RadioOption`）：日志级别、导入来源、Skill 来源类型从 `asb-tabs`+`TabButton` 切换，`TabButton` 回归 `Tabs` 内部件。第二视觉语言清除：网关四文件（GatewayPage/Primitives/PortChangeSheet/Telemetry）全部改用 Frosted Relay 令牌，新 `styles/base/gateway.css` 唯一持有样式，趋势图表卡拆分为 `components/charts/GatewayTrendChart.tsx` 保留 `.bui-scope` 豁免；托盘 TrayPanel 的 9 处 boardui 工具类改为 `tray.css` + 令牌（根节点 `bui-scope` 移除）；boardui vendor 组件（StatCards/Chip/Tooltip）从 `components/base`、`components/application` 迁入 `components/charts/` 隔离；`tokens.test.ts` 新增守卫——业务 TSX 禁止消费 `text-body-*`、`bg-background-*`、`rounded-2xl` 等工具类，第二语言只允许存在于 `components/charts/**`。DESIGN.md §5/§7/§8 同步：小节令牌、右簇/筛选唯一形态、网关 `bg-background-secondary-default` 措辞改为 `--asb-content-muted` 并记录迁移、托盘样式唯一所有者、图表层隔离与守卫。

验证：`npm run typecheck` 通过；契约切片（tokens/LogsPage/ProviderImportPage/SessionManager/TrayPanel/App.discovery/SkillSourceBrowser 及五个编辑器套件）全过；全量串行 `npm test -- --run --maxWorkers=1 --no-file-parallelism` 101 文件 / 650 用例全部通过。测试按新契约同步改写（tab 查询 → radio），无兼容断言。附带修复一处预先存在的环境问题：`boundary.test.ts` 在全局 jsdom 环境下经 `loadConfigFromFile` 加载 esbuild 触发 realm 不变量错误（HEAD 上同样失败），以 `@vitest-environment node` pragma 修复，`src/test/setup.ts` 的 jsdom 专属桩加 `typeof` 守卫以兼容 node 环境套件。并行全量模式下的 ProviderEditor 超时抖动仍属既有已知问题（见 2026-09-10 记录），未改动。未提交、未推送。

**Codex 导入改为种子补全式（2026-09-11 完成）：** 用户实测从真实来源导入 Any/AIHub 全部被「缺少 modelCatalog」的校验整行拒绝，并指出导入不应要求修改别人软件的数据。根因：接管重构把自造扩展格式（`settings_config.modelCatalog` 严格形状 + `meta.codexCapabilities` + meta 三键白名单 + TOML 表字段白名单）当成无损导入前提，而真实来源（3.20.2）从不写这些字段（其真实 modelCatalog 形状不同且可选、meta 含 20+ 键），导入对真实数据完全失效。按用户「无兼容无过渡无技术债」指令重写为补全式并删除全部旧路径：

- `map_codex` 产出 `CodexImportSeed`（`ccswitch/row.rs` 新类型，含脱敏 Debug）：只含源里真实存在的事实——名称、端点（规范化）、密钥（缺失降级为空密钥+警告，编辑器阻止空密钥保存，不再跳过）、上游协议（meta.apiFormat 覆盖 wire_api）、TOML 主模型（仍必填）、真实形状 modelCatalog 种子条目（model/contextWindow/推理档位/inputModalities→images；重复去重+警告；主模型不在目录则补一行）、运行参数、备注、官网、用量脚本。跳过仅剩：无法解析、官方路由（官方登录不是档案）、端点非法、缺主模型、wire_api/apiFormat 不受支持；meta 未消费键、未支持表字段（env_key 等）、目录未支持字段（displayName/supportsParallelToolCalls/baseInstructions）一律「未导入: 字段名」警告。`codex/catalog.rs` 与扩展形状解析整体删除。
- 命令面：`import_ccswitch_profiles` 收窄改名 `import_ccswitch_claude_profiles`（Claude 专用；codex key 在任何写入前硬错误，混合批次整体失败不部分写入）；新增 `prepare_ccswitch_codex_seed(key)` 按行返回完整种子（含密钥），与 `prepare_provider_request` 同为显式单行密钥边界；扫描响应保持无密钥/无草稿（隐私断言测试保持）。存储层删除 `import_codex_provider`/`codex_provider_exists`/`codex_provider_will_receive_usage_query`，新增按端点+上游的 `codex_route_exists` 作扫描提示（不阻断补全）。dev_api 桥同步。
- 前端：`ProviderEditorSession` 的 codex 变体重构为判别联合 `CodexEditorSource = record | seed | blank`；`useProviders.openCodexEditorWithSeed` 打开种子会话；导入表 Codex 行无复选框、逐行「补全导入」按钮（自动全选与批量导入只统计 Claude 行）；`codexDraftFromSeed` 以 `DEFAULT_CODEX_CAPABILITIES` 与扩展后的 `catalogEntryFromModel`（支持 contextWindow/images/推理档位入参，「获取模型」与种子共用同一默认值所有者）预填草稿，种子参数即播种可直接保存；种子会话顶部展示未导入字段警告条；保存走既有 `create_codex_profile` 全量校验。夹具改为真实来源形状。
- 顺带修复：`src/boundary.test.ts` 加载 Vite 配置的断言在 vite 7 下因串行共享 worker 的 esbuild realm 污染而失败（vite 8 用 rolldown 不触发；node pragma 只解决了 jsdom 环境问题，未解决 worker 内先后文件的残留污染），改为在干净子进程中探测配置，语义不变。

验证：`cargo test -p asb-core` 275 通过（ccswitch 切片真实形状夹具：无目录→种子、真实目录→种子条目+警告、meta 多键→警告、无密钥→空密钥种子+警告、官方→仍跳过、env_key→警告）；`cargo test -p agent-switchboard --lib` 505 通过/10 忽略（扫描无密钥断言保持、种子命令返回密钥、codex key 批量硬错误、混合批次零写入、路由判重）；`cargo test --workspace -- --test-threads=1` 867 通过 0 失败、`cargo fmt --all -- --check` 通过；`npm run typecheck`、全量串行 vitest 101 文件 650 项全过、`npm run build` exit 0。README/DESIGN/实施设计文档（§14.7）同步为补全式契约。未写用户真实配置，未提交或推送。

**Codex 推理档位值域对齐真实 Codex（2026-09-11 完成）：** 用户实测补全式导入后 Any/AIHub 仍被「键 model_reasoning_effort 的值必须是 minimal、low、medium、high、xhigh 之一」整行拒绝——只读检查真实 cc-switch.db 证实 Any 行 `model_reasoning_effort = "ultra"`、AIHub 行 `"max"`（其 `[desktop] enabled-reasoning-efforts` 亦列出 low/medium/high/xhigh/ultra/persistent/max），官方 issue、OpenAI 推理文档与 Codex SDK 模型矩阵均确认 `max`/`ultra` 是现行 Codex 按模型开放的真实档位，`none`（gpt-5.5 官方建议的低延迟档）与 `minimal`（标准最低档）同属真实值域，本应用网关的 Chat/Anthropic 推理方言此前也已接受全部现代档位，唯独设置选择表与目录档位枚举落后。按「无兼容无过渡无技术债」收口为**单一值域** `none/minimal/low/medium/high/xhigh/max/ultra`：`ownership/choices.rs` 新增文档注释的 `CODEX_REASONING_EFFORT_OPTIONS` 唯一持有该域，`model_reasoning_effort`、`agents.default_subagent_reasoning_effort` 与 `plan_mode_reasoning_effort`（原内联重复列表删除）三个键共用；`contracts::CodexReasoningLevel` 枚举补 `Minimal`（此前真实目录携带 minimal 会被警告丢弃）与 `Max`/`Ultra`（目录种子、条目校验、`model_catalog_json` 投影同一值域）；前端 `CodexReasoningLevel` 类型、编辑器 `REASONING_LEVELS`/标签（无/极低/低/中/高/超高/最高/超极高）同步；`website/src/generated/configuration-assembly.ts` 经既有 bin 重新生成。`persistent` 经核实是 OpenAI 内部测试中、无发布计划的 Codex 桌面「持续模式」实验（WIRED 报道），仅出现在 desktop 选择器列表、无任何官方来源承认为 `model_reasoning_effort` 合法值，不纳入；若未来真实行携带它，导入按既有纪律对该键明确报错。

验证：`cargo test -p asb-core` 277 通过（回归改写为覆盖 none/minimal/max/ultra 全部进入种子参数且不再拒绝整行；目录 reasoningLevels 全梯子 minimal→ultra 可解析；原以 minimal 充当非法值的夹具改为合法+非法混合）；`cargo test --manifest-path src-tauri/Cargo.toml --lib` 505 通过/10 忽略、`cargo fmt --all -- --check` 通过；`npm run typecheck` 通过；vitest 切片（codex-provider-editor 套件、SettingsFields、App.discovery）4 文件 41 项全过。未写用户真实配置，未提交或推送。

**目录行官方参数表（2026-09-11 完成）：** 用户指出导入预填的上下文窗口/输出上限（128,000 / 8,192 占位值）应为官方实际参数。`codex-provider-editor/draft.ts` 新增唯一所有者 `OFFICIAL_MODEL_LIMITS`——按官方模型 id 引用 OpenAI 模型页发布值（gpt-6-astra、gpt-5.6-sol/terra/luna 均为 1,050,000 上下文 / 128,000 输出上限，2026-09 经 OpenAI 文档与 OpenRouter 双源核实）。取值优先级：源里明示的事实（导入来源目录 contextWindow）> 官方表 > 通用占位默认；输出上限无来源通道，官方表之外仍用占位值；表外模型保持占位默认待用户填写，不推断。官方 1.05M 与 Codex 客户端有效上限（社区报告 Sol 272K、Terra/Luna 872K）是两回事，编辑器字段语义是模型上下文窗口，取官方值。仅前端改动，「获取模型」与导入种子两条生成路径同时受益。

验证：`npm run typecheck` 通过；codex-provider-editor 套件 2 文件 38 项全过（新增 4 项：官方 id 预填官方值、源事实压过官方表、种子官方默认模型取官方值且可保存）。未提交或推送。

**目录行限频可留空（2026-09-11 完成，取代上一条「表外模型预填占位默认」行为）：** 用户要求上下文窗口/输出上限支持留空（留空即用默认），按「无兼容无过渡无技术债」收口：编辑器目录行类型改为 `EditableCodexCatalogEntry`（两限频 `number | null`，null = 用默认），仅存在于编辑器内部；`prepareCodexDraft` 保存时经唯一解析函数 `defaultModelLimits(id)`（官方表 → 通用默认 128,000/8,192）实体化为具体正数，线契约（`CodexCatalogEntry` u64 与后端正数校验）零改动。生成路径不再伪造数值：官方 id 预填官方发布值（引用事实），其余模型（获取模型/导入种子/手动行）两限频预填空，输入框占位显示将生效的默认值（「默认 128,000」）；输入非正整数同样归一为空=默认，原「必须大于零」前端校验分支因结构性不可达而删除（后端仍为权威），输入框 `required` 移除，`mergeFetchedCatalog`/`emptyCatalogEntry` 签名同步可空类型。

验证：`npm run typecheck` 0 错误；codex-provider-editor 套件 2 文件 41 项全过（改写断言：未知模型与种子默认行限频为空；新增：落盘时未知模型实体化 128,000/8,192、官方模型实体化官方值、组件级留空+占位提示+保存默认值全流程）；App.discovery/App.providers/SettingsFields 13 项全过。纯前端改动，未提交或推送。

**双端供应商列表界面完全统一（2026-09-11 完成）：** 按用户指令「codex、claude 的供应商列表界面的 ui 要完全一致」收口。此前 Claude 列表走成熟行组件（`asb-rows` 拖拽行 + `DualRelay` 路由卡 + tabs 工具栏），Codex 列表是手写无样式卡片（上移/下移按钮、底部预览）。抽取三个唯一所有者（新文件 `src/components/ProviderWorkspaceShell.tsx`）：`ProviderWorkspaceShell`（面板骨架：「供应商」页标题 + 偏好设置/切换历史右簇 + `DualRelay` + 客户端选择与 导入/新建 工具栏，Codex 专属「官方登录」按钮经 `extraActions` 置于 导入 与 新建供应商 之间）、`SortableProviderRows`（`asb-rows` 容器 + dnd 拖拽 + 共享空态）、`ProviderRowShell`（行壳：拖拽手柄、头像、名称与元信息、启用主操作、使用中徽章、`secondaryAction` 槽、图标操作簇、行内展开）。`ProviderList` 与 `ProvidersPage` 重构为组合壳（Claude 侧行 DOM 逐字节不变，其测试零改动通过）；`CodexProvidersPage` 整体重写为同一 UI：拖拽排序（替换上移/下移，仍走 `reorder_codex_profiles`）、启用主按钮/使用中徽章、图标簇（编辑/预览眼睛/更多菜单删除）、预览在行内展开且补齐「变更预览」标题与用户级模型注记、元信息行显示 主模型 · 官网主机（官网链接经 opener 打开）、页级 `provider-editor.css` 冗余导入删除。删除流程统一：`useProviders` 的待删状态改为判别联合 `PendingProviderRemoval = claude profile | codex record`，Codex 删除从「立即执行的红色按钮」改为与 Claude 相同的 更多→删除→全局确认单（`OperationConfirmSheets` 一张单两端复用），活跃 Codex 档案不提供删除入口（保持原守卫），`closeEditorFor` 因职责并入 `runDelete` 的 codex 分支而删除。区域 `aria-label` 保留 `Codex 供应商` 以区分双端视图（可见 UI 相同，辅助技术可辨）。

验证：`npm run typecheck` 0 错误；全量串行 `npm test -- --run --maxWorkers=1 --no-file-parallelism` 101 文件 661 项全部通过（`ProviderList`/`ProvidersPage` 套件零改动通过证明 Claude 侧 DOM 等价；App.providers 的 Codex 流程按新 UI 改写——启用/图标钮/键盘拖拽排序——并新增「Codex 删除走共享确认单」回归；App/App.discovery 18 项通过）。DESIGN.md 供应商节同步唯一形态。纯前端改动，未提交或推送。

**恢复「新建 Codex 供应商」的官方登录选项（2026-09-11 完成）：** 用户指出 Codex 专属编辑器重构把「新建时可选官方登录」的能力弄丢了（重构前官方登录与第三方同属通用档案，新建时可选）。查证结论：后端契约当时是半成品——`validate_route_fields` 至今允许 `app=Codex + routeMode=Official` 草稿、`official_quota_refresh_interval_minutes` 字段只允许出现在 codex official 档案上、按 id 的 `preview_switch`/`execute_switch` 完整支持官方档案切换、官方登录设备码命令完整支持 codex——唯独存储层对 `app==Codex` 一刀切拒绝、状态归属不回退、前端入口消失。按「补完契约而非发明新概念」收口：

- **契约定位**：通用 `ProviderProfile` 存储唯一持有客户端**官方登录档案**（claude / codex 各至多一条，沿用既有 `officialTakenApps` 语义）；Codex **第三方**供应商唯一属于严格 `CodexProviderFile`（仍绝不编码官方登录）。
- **后端**：`config_store/providers/` 的守卫收窄为「`app==Codex && route_mode==Custom` 拒绝」（消息改为「Codex 第三方供应商必须使用专用档案格式」），`load.rs` 新增 `provider_dir`：Claude 档案留在 `providers/claude/`，Codex 官方档案落在 `providers/codex/official/` 子目录（严格存储的 read_dir 按扩展名跳过目录项，两种格式不混放），`list_providers`/`locate`/`find_provider`/删除均覆盖官方记录；配置快照升 **v7**（新增 `codex_official` 列表，`stage_and_verify` 写入 official 子目录，新增 `validate_codex_official_files` 校验同客户端至多一条且必须是官方路由）；`commands/status/report.rs` 的 Codex 分支在专用匹配器返回 None 时回退到既有 `active_profile_id`，官方路由因此把「使用中」归属到存储的官方档案；**删除瞬态双路径**——`preview_codex_official_switch`/`execute_codex_official_switch` 命令、`build_codex_official_plan`、`api/providers.ts` 两个封装、dev_api 分发与注册全部删除，官方切换唯一走 `preview_switch`/`execute_switch`(官方档案 id)。
- **前端**：`CodexEditorSource` 增 `{kind:"official"; record}`；新建 Codex 编辑器的「基本资料」新增「接入方式」单选（第三方服务 / 官方登录），切换即替换会话（两份契约不互转）；新组件 `CodexOfficialProviderForm` 持有通用 `ProviderDraft`（名称/官网/备注/订阅额度间隔 + `OfficialLoginPanel(app="codex")`），经 `saveCodexOfficialProfile` 走与 Claude 相同的 prepare/commit 事务；列表顶部固定一条不可排序的官方行（启用/使用中/重新登录/订阅额度/编辑/删除，复用 `ProviderRowShell` 与 `SortableProviderRows` 新增的 `leading` 槽）；工具栏「官方登录」按钮删除（与官方行重复，且 Claude 侧无此按钮）；`PendingProviderRemoval` 泛化为 `generic(claude 或 codex 官方) | codexThirdParty`，`PendingSave` 增 `app` 字段；快照新增 `codexOfficialRecords` 桶。
- **文档**：README/DESIGN 中「官方登录不是档案」收窄为「不是**第三方**档案，官方登录档案唯一存于通用存储」，`contracts/codex.rs` 与 `plan.rs` 注释同步。

验证：`cargo test -p asb-core` 277 通过、`cargo fmt --all -- --check` 通过；`cargo test --manifest-path src-tauri/Cargo.toml --lib` **508 通过/10 忽略**（新增 3 条：官方记录在通用存储建改删读往返且严格存储仍为空、`route_mode` 非 official 的 codex 草稿仍被拒、官方路由把活跃状态归属到存储档案；改写 dev_api 沙箱全流程为「建官方档案→preview_switch→execute_switch」并补挂 `ProfileSavePreparations`）；`npm run typecheck` 0 错误；全量串行 vitest **101 文件 664 项全部通过**（新增「从官方行预览切回官方路由」与「新建供应商→官方登录→保存建档案」两条流程回归）；`npm run build` exit 0。未写用户真实配置，未提交或推送。

**存储级错误全路径保码（2026-09-11 完成）：** 用户报告另一台装了旧版数据的机器在导入时弹出「配置存储格式无效或来自已不受支持的旧版本」toast 但**没有修复按钮**。查证结论：修复入口其实存在（`AppShell` 错误横幅的「清空旧档案并重新开始」按钮），但只在错误携带 `profile-store-unsupported` 类型码时渲染；读取类命令经 `store_error()` 保码，而**全部写入/查找/导入类命令把 `ProfileStoreError` 在存储层内部 `to_string()` 拍平**，再包进命令专属码（如 `ccswitch-import-failed`、`codex-profile-create-failed`、`profile-not-found`），前端 `useOperationFrame` 只认 `profile-store-unsupported`，其余一律降级为 10 秒 toast——旧布局机器上任何写入操作失败后按钮永远不出现。按「无兼容无过渡无技术债」收口为单一契约：**任何命令不得字符串化存储级错误**。存储层新增 `StoreOperationError { Store(ProfileStoreError), Invalid(String) }`（含 `From<ProfileStoreError>`/`From<String>`/`From<&str>` 与 Display），全部变更/查找方法（`create/update/delete/reorder/import_provider`、codex 四方法、`save_client_settings`、`record_config_write`、`remove_config_write_if_last`、`find_provider(_record)`、`load/overwrite_provider_file`、`write_provider_file`、codex `write`）从 `Result<_, String>` 迁到该类型，store 级错误全程类型化穿透；命令层新增唯一映射 `error::operation_error(code, error)`——store 级保 `store_error` 稳定码、校验级用命令自身码，替换 profiles/discovery/client_settings/quota/usage_history/query/switching(plan、profile_save、codex_profile_save)/provider_request/ccswitch_source(import)/usage_query::scheduler 全部丢码封装点；恢复/回滚闭包（transaction、profile_rollback、recovery、gateway routing、port_change、backups、cloud_backup）保持既有 String 恢复语义契约，在边界显式 `to_string()`。切换保存事务里 `classify`/`apply` 对 `list_codex_providers`/`find_codex_provider_file` 的「拍平为 not-found」同步纠正为 `store_error`。存量错误消息断言随类型迁移（`StoreOperationError::Invalid` 匹配）。

验证：`cargo test --manifest-path src-tauri/Cargo.toml --lib` **511 通过/10 忽略**（新增契约测试：旧布局标记下 create/import/find/save_client_settings/record_config_write/create_codex_provider 全部返回 `Store(Unsupported)` 类型化错误而非拍平消息；`operation_errors_keep_the_store_code_and_own_the_command_code` 验证命令映射两侧）；`cargo check --all-targets` 与 `cargo fmt --all -- --check` 通过。前端零改动（横幅通道既有），端到端链路核对：导入失败 → `onError` → `reportError` → 持久横幅 + 重置按钮。未写用户真实配置，未提交或推送。

**供应商行悬浮抽搐修复（2026-09-11 完成）：** 用户报告鼠标悬浮供应商列表行时界面抽搐。根因：「启用 / 重新登录」行级按钮（`.asb-row-activate`）的悬浮显现靠 `max-inline-size: 0 → 128px` + `padding-inline: 0 → space-3` 的**瞬时塌缩/展开**（transition 只声明了 opacity 与 visibility，宽度是跳变的），每次悬浮/移出右侧动作簇整体横移 100+px、「使用中」徽章位移、行中文本重新截断，鼠标扫过列表逐行弹跳。按 2026-09-01「不做展开/弹出动作」指令的本来意图收口为与 `.asb-iconcluster` 相同的零布局位移显现：按钮常驻占位（`flex: none` + 常量 `padding-inline: var(--asb-space-3)`，删除 `max-inline-size`/`overflow: hidden`/`min-width: 0`），悬浮只切换 opacity/visibility/pointer-events——静态外观不变（静止时按钮不可见、不可点），悬浮淡入不再移动任何元素。Claude 与 Codex 两侧共用同一 `.asb-row-activate`，修复天然双端一致；`ProviderList.test.tsx` 的显现契约测试改为钉死零位移（基础规则必须含 `flex: none` 与常量 padding、基础与悬浮规则均不得出现 `max-inline-size`、悬浮规则不得改 padding）。

验证：`npm run typecheck` 0 错误；`ProviderList` 套件 31 项全过（含改写后的显现契约测试）；`App.providers`/`App`/`App.navigation` 切片 32 项全过。纯 CSS 契约改动，未提交或推送。

**导入界面一键化 + 官方登录可导入 + 外来客户端不可见（2026-09-11 完成，取代同日早先的「种子补全式导入」）：** 用户指令三点——①存在不一致的 Codex 行不再逐个手动「补全导入」，底部「导入所选」一键支持；②扫描结果不再显示 gemini / claude-desktop / grok 等外来客户端；③Codex 官方登录行从「无法导入」变为可导入并进入 Codex 供应商模块。按「无兼容无过渡无技术债」收口为单一导入契约：

- **一键批量（后端补全唯一所有者）**：`CodexImportSeed::completion_draft()`（asb-core `ccswitch/codex.rs`）把源端事实实体化为严格 `CodexProviderDraft`——声明默认能力 `contracts::DEFAULT_CODEX_CAPABILITIES`、目录行按「源明示值 > 官方发布限速 > 通用默认」解析（新所有者 `contracts::{official_model_limits, default_model_limits, CODEX_REASONING_LADDER}`，前端 draft.ts 镜像同步注释互指）、推理档位收窄到源明示阶梯否则全梯子、空密钥整行拒绝；产物经与编辑器保存相同的 `CodexProviderFile::validate`。`import_ccswitch_claude_profiles` 成为唯一批量入口：Claude 与 Codex 官方行→通用存储判重导入，Codex 第三方行→路由判重（端点+上游）后 `create_codex_provider`，空密钥/补全失败入 notImported 明细。**删除整条种子链路**：`prepare_ccswitch_codex_seed` 命令与注册、dev_api 桥、`CodexCcSwitchSeed`/`CodexCcSwitchCatalogSeed` IPC 类型、前端 `codexDraftFromSeed`/`openCodexEditorWithSeed`/`CodexEditorSource` seed 变体/编辑器种子警告条/逐行补全按钮——源密钥从此**任何响应都不携带**（此前种子命令是唯一密钥过 IPC 边界）。
- **官方登录导入**：`map_codex` 识别 `model_provider=openai 且无 openai_base_url` 的行→`CcSwitchProviderDraft::CodexOfficial(ProviderDraft)`（名称「Codex 官方登录」，参数取源 config，凭据不导入），通用存储守卫收窄为「Codex 仅拒绝非官方」（`import_provider`/`provider_exists`/`provider_will_receive_usage_query` 同步），既有官方唯一性（至多一条）成为天然判重。
- **外来客户端不可见**：`scan_at` 过滤 skipped 中 app_type ∉ {claude, codex} 的行——扫描响应只含可行动行，不再是「无法导入」提示墙；原始 skip 事实仍留在导入再解析路径。
- **前端**：导入表全部行统一复选框（existing 禁用）、扫描自动全选所有非 existing 行、「导入所选 N 项」计数含 Codex；编辑器仍可手动新建第三方与官方档案（「获取模型」路径不受影响）。

验证：`cargo test -p asb-core` **279 通过**（新增：官方行映射为官方记录且 OAuth 残留不泄漏、completion_draft 限速/阶梯/默认能力实体化并通过严格校验、空密钥拒绝；改写：官方行不再 skip）；`cargo test --manifest-path src-tauri/Cargo.toml --lib` **512 通过**（新增：混合批次一键导入双存储、同路由二次导入 skipped_existing、官方行导入通用存储与唯一性、扫描响应外来客户端不可见且官方行为可行动行）；`cargo fmt --all -- --check` 通过；`npm run typecheck` 0 错误；全量串行 vitest **101 文件 657 项全部通过**（改写 App.discovery 两条为一键流：三行全选 + 密钥集合断言、导入页 Codex 工作区复选框断言；删除种子预填测试）。README/DESIGN 导入节同步。未写用户真实配置，未提交或推送。

**Codex 用量脚本富集导入（2026-09-11 完成，补齐一键导入的用量脚本契约）：** 用户指令「用量脚本也需要支持导入」。查证：Codex 新建路径已随档携带源端用量脚本（`completion_draft` 复制 `usage_query`，`CodexProviderFile::validate` 同步校验），缺口在**路由已存在**的行——直接 `skipped_existing`，源端用量脚本被丢弃，且扫描项 `usage_script_updates_existing` 恒 false（代码注释自认「无批量富集路径」），而 Claude 行一直有「路由相同 + 本地缺脚本 → 补充」的富集语义。按「无兼容无过渡无技术债」以 Claude 边界为镜像补齐：严格存储新增 `codex_route_has_usage_query`（扫描谓词）与 `enrich_codex_route_usage_query`（找端点+上游匹配的已存档、无脚本才写入，经同一校验原子写边界，返回是否富集）；`import_at` 的 Codex 分支改为「路由存在时：源行有脚本且本地缺 → 富集并计入 imported 与用量脚本计数，否则真重复跳过」；扫描项三态与 Claude 对齐（`existing` = 路由存在且无脚本可补或本地已有脚本，`usage_script_updates_existing` = 路由存在 + 源有脚本 + 本地缺）。前端零改动（「将补充用量查询脚本」提示与行选中已按字段渲染）。

验证：`cargo test --manifest-path src-tauri/Cargo.toml --lib` **514 通过**（新增：同路由两行——无脚本行建档、带脚本行富集且档数不变、补后变真重复跳过；一键新建路径持久化源端用量脚本）；`cargo fmt --all -- --check` 通过。纯后端增量，未提交或推送。

**内建用量模板双端可导入（2026-09-11 完成）：** 用户指令「不只是 codex、Claude 的也是」——只读检查真实来源数据库证实：用户的 Claude 行用量脚本全部是**空 `code` 的内建模板**（Zhipu GLM `token_plan`、DeepSeek `balance`；QwenCloud 已禁用；AIHub `general` 带源码本就可导入），Codex 侧 OpenAI Official 是 `official_subscription`（官方行，脚本本就不跟随），此前这类空码模板一律「内建模板没有可导入的源码」整行拒收——Claude 与 Codex 同困。按「无兼容无过渡无技术债」收口：`map_usage_query` 的空码分支改为模板分发——`general` 与 `balance`（DeepSeek `user/balance`、StepFun `v1/accounts`、SiliconFlow cn/com `v1/user/info`、OpenRouter `credits`、Novita `v3/user/balance`，按档案地址在查询时选择、request 阶段闭包捕获供应商）与 `token_plan` 的智谱个人/团队（`{origin}/api/monitor/usage/quota/limit`，裸密钥认证、origin 按 bigmodel.cn/api.z.ai 派生、TOKENS/CREDIT_LIMIT 条目按 `unit` 归入 5 小时/每周窗口、百分比投影为百分制读数）在导入时合成为**原生 request/extract 程序**（`ccswitch/templates/*.js`，`include_str!` 嵌入，vendored 自钉定基线 f3b18df 的 UsageScriptModal DEFAULT_SCRIPTS / services/balance.rs / services/coding_plan.rs，只用档案自身 baseUrl/apiKey 输入）；`newapi`（站点独立凭据 accessToken/userId）、`github_copilot`（GitHub 登录凭据）、`official_subscription`（官方订阅额度由官方登录档案原生支持）与未纳入的 Coding Plan 供应商（kimi 等，逐家点名）给出行级精确未导入理由，不再是无声墙。修得一处运行时事实：`extract` 的运行时入参只含 body+status，balance 脚本的地址分支改为 request 阶段捕获。`codingPlanProvider` 在被消费时不再误报未导入键；自动刷新间隔维持既有「导入即手动」决策（autoQueryInterval 仍入未导入键警告）。

验证：`cargo test -p asb-core` **281 通过**（改写模板断言为合成结果 + 新增 token_plan 智谱合成/kimi 精确跳过/缺 codingPlanProvider、newapi/copilot/official_subscription 三类精确理由）；`cargo test -manifest-path src-tauri/Cargo.toml --lib` **517 通过**（新增：Claude balance 模板行与 Codex 智谱 token_plan 行一键导入后档案携带原生脚本——存储写入即持久化校验通过的证明；引擎级测试用 rquickjs 真实驱动合成脚本的 request/extract：URL/认证头/多币种读数全对）；`cargo fmt --all -- --check` 通过。真实数据库行全部有了归宿：DeepSeek/Zhipu/AIHub 可导入、QwenCloud 禁用跳过、OpenAI Official 走官方行。未提交或推送。

**token_plan 全供应商收口（2026-09-11 完成，清掉上一条留下的「未纳入」债）：** 用户指令「无兼容无过渡无技术债实现修复」。上一条只纳入了智谱个人版，kimi/minimax/zenmux/volcengine/opencode_go 仍是「未纳入模板导入」跳过，且 `zhipu_team` 被错误映射到个人版脚本。按钉定参考基线（`f3b18df` services/coding_plan.rs）补全 token_plan 分发：**五家新脚本**——Kimi（固定端点 `/coding/v1/usages`，limits/usage 双窗口绝对 limit/remaining 数值）、MiniMax（按档案地址选 minimaxi.com/minimax.io 域，`coding_plan/remains` 的 general 模型条目、周窗口仅 status=1 时激活、剩余百分比反转为已用）、ZenMux（端点即档案自身地址，美元值 used/max 优先、百分比回退）、OpenCode Go（固定端点 `/zen/go/v1/usage` 三窗口滚动/周/月，不可解析窗口跳过不整体失败）、智谱团队（个人端点 + `?type=2` + `bigmodel-organization`/`bigmodel-project` 头，**组织/项目 ID 从源端 meta 烘焙进合成脚本**并经 `javascript_string` 转义，缺任一给精确理由跳过）；`volcengine` 依赖控制面 AccessKey ID/Secret（非推理 API 密钥），与 newapi/github_copilot 同类——**不可表达即精确点名跳过**，不伪造。消费的模板键（codingPlanProvider、teamOrganizationId/teamProjectId）不再误报未导入。全部脚本只用档案自身 baseUrl/apiKey 输入。

验证：`cargo test -p asb-core` **281 通过**（token_plan 测试改写为：四家数据面供应商合成端点标记、团队版烘焙+转义+键消费、团队缺配置精确跳过、volcengine AccessKey 理由、未知供应商点名）；`cargo test --manifest-path src-tauri/Cargo.toml --lib` **518 通过**（新增 Kimi 引擎级测试：rquickjs 真实驱动 request/extract，URL/认证头/双窗口绝对数值与百分比投影全对）；`cargo fmt --all -- --check` 通过。未提交或推送。

**重置逃生门收口（2026-09-11 完成）：** 用户报障「清空旧档案并重新开始」永远失败并复现同错。根因有二：①`reset_profile_store` 在删除前先走 `ensure_profile_save_recovered`，其入口 `pending_profile_save()` 第一步即 `ensure_layout()`，被旧版布局标记（本机为 `state/configuration/common/`）判 `Unsupported` 直接中止——逃生门被它要清除的状态锁死；②`ConfigStore::reset()` 只删 `configuration/` 树与 `profiles.json`，不删 `state/configuration-upgrade.json`（`ensure_layout` 四个拒绝条件之一），该标记残留时重置后依然全拒绝。按「无兼容无过渡无技术债」收口：重置命令的恢复前置改为**尽力而为**（恢复仍会执行以尽可能对齐真实客户端文件，但任何恢复失败只记 warn 不得阻止已确认的清空；网关活动路由守卫保留阻断，事务备份仍在 `state/backups` 供事后手动恢复）——依据是 `ProfileStoreError` 的既有契约「重置是类型化读取器所拒绝状态的恢复路径」，故重置必须在该类状态下无条件可用；`reset()` 补删迁移 journal，使 `ensure_layout` 四个拒绝条件（`profiles.json`、`configuration/common`、`configuration-upgrade.json`、`client-settings` 非目录）全部可被一次重置清除。其余命令的恢复门不变。

验证：`cargo test --manifest-path src-tauri/Cargo.toml --lib` **519 通过**（新增 `reset_clears_every_layout_rejection_marker`：四标记齐备时 `ensure_layout` 拒绝、一次 reset 后干净）；`cargo fmt --all -- --check` 通过。命令层改动为两行控制流（门改尽力而为），无 AppHandle 测试基建故未加命令级用例。本机旧状态将在新构建中经同一按钮清除。未提交或推送。

**供应商行删除直出（2026-09-11 完成）：** 用户指令「把里面的删除按钮拿出来，不做更多操作的子选择栏按钮了」。供应商行尾动作簇的「更多操作」三点菜单只有「删除」一项，按「无兼容无过渡无技术债」整条拆除：删除成为动作簇第五个直接图标按钮（TrashIcon、Tooltip 与可及名「删除 ××」、点击即进共享删除确认弹窗，与编辑/预览/测试/用量同构），`ProviderMoreActions` 组件及其开合/Esc/外点关闭逻辑、`.asb-row-more`/`.asb-row-menu`/`.asb-row-menu-item` 样式全部删除（`--asb-surface-menu` 令牌他处仍在用故保留）。三个使用方同步：Claude 列表 `ProviderList`、Codex 页官方登录行与第三方行（后者维持仅非生效行可删的既有语义）、e2e `requestDelete` 助手改为直接点删除按钮。DESIGN.md §动作簇条目同步为「删除五个内联线性图标」。

验证：`npm run typecheck` 0 错误；全量 vitest **101 文件 656 项全部通过**（菜单开合/Esc 两测删除，新增「动作簇直接触发删除」并改写顺序断言为「…用量，then delete」；App 删除流程改单步点击）。e2e 桌面套件未运行（需真实桌面环境）。未提交或推送。

**登录阻断横幅前置到供应商列表（2026-09-11 完成）：** 用户指令「需要更明显的提示显示在供应商列表界面」——此前第三方启用被登录前置拦下时只有一次性 toast（即「Codex 当前处于 API-key 登录模式」报障场景），列表页毫无预示。新增后端只读命令 `codex_login_blocker`（commands/official_login.rs）：返回「当前第三方 Codex 切换被什么阻断」的文案或 None——文案直接复用 `observation::CodexLoginState::require()` 的失败文本，横幅与失败 toast 同源、永不漂移（单一文本所有者仍是 observation.rs，命令只是搬运）。前端 Codex 工作区在「存在第三方供应商且阻断存在」时于客户端页签下方渲染常驻警示横幅（`asb-banner-warning` + `role="status"`，新样式 `asb-provider-login-notice`），附「完成官方登录」按钮原地展开 `OfficialLoginPanel`（同一组件同一状态机），登录完成自动重查并消失；官方行「重新登录」完成也经 `onReloginFinished` 回调刷新。横幅只陈述事实，预览/编辑/删除等操作不受影响；命令失败静默降级为无横幅。共享夹具补 `codex_login_blocker: null`。

验证：`cargo test --manifest-path src-tauri/Cargo.toml --lib` **519 通过**、`cargo fmt --all -- --check` 通过；`npm run typecheck` 0 错误；全量 vitest **101 文件 657 项全部通过**（新增「登录阻断时列表页常驻警示横幅并可原地展开登录面板」）。未提交或推送。

**登录阻断横幅收口到统一快照（2026-09-12 完成）：** 用户指令「无兼容无过渡无技术债实现」。审计横幅首版发现两处债：①页面级 `useEffect` 自建了第二条刷新生命周期——外部 `codex login` 后横幅陈旧（不随窗口聚焦/托盘事件更新）；②命令失败被 `catch(() => null)` 静默吞掉。收口：`codexLoginBlocker` 读取并入 `useConfigSnapshot.refresh()` 的 `Promise.all`（挂载、窗口聚焦、托盘事件、每次操作后四条刷新路径统一生效），快照暴露 `loginBlocker` 经 `ProvidersWorkspace` 下传为页面 prop——页面零自有请求逻辑，登录完成/官方行重登完成都走既有 `onRefresh` 通道；读取失败按快照错误统一上报，静默分支删除。

验证：`npm run typecheck` 0 错误；全量 vitest **101 文件 657 项全部通过**（横幅测试补「外部登录后窗口聚焦即消失」断言；`useConfigSnapshot` 测试文件补 official-login 模块 mock——未 mock 时真实 invoke 拒绝导致 refresh 整体失败，正是并入统一快照后错误不再被吞的证明）。后端无改动（519 通过维持）。未提交或推送。

**登录连续性收口：登录完成官方行自动出现（2026-09-12 完成）：** 用户指令「方向正确的，无兼容无过渡无技术债实现」。报障：横幅登录完成后官方行不出现——官方行是存储里的档案记录而非登录状态推导，此前只有编辑器/导入两个显式创建入口，连续路径断成三截。收口：①canonical 官方草稿唯一所有者落地 asb-core `contracts::codex_official_draft(parameters)`（名称「Codex 官方登录」、无端点凭据目录），ccswitch 官方行导入删除私有副本改为消费同一构造函数；②存储层 `ConfigStore::ensure_codex_official_record()` 幂等 find-or-create——缺档时以全 Automatic 默认参数创建 canonical 档案，已存在（含用户重命名/自定义的）原样返回 `(record, created)`；③命令 `ensure_codex_official_record`（恢复门 + `operation_error` 映射 + 仅真创建时记 `ProfileCreated` 日志与托盘刷新，幂等空转不进历史）；④横幅登录面板 `onFinished(completed)` → `run(ensure + onRefresh)`——「完成登录 → 官方行出现 → 可启用」一条路径，编辑器与导入两条显式入口不变（编辑器臂仍走用户显式保存，不自动建档以免与显式创建语义冲突）。

验证：`cargo test -p asb-core` **281 通过**（导入官方行映射回归）；`cargo test --manifest-path src-tauri/Cargo.toml --lib` **520 通过**（新增：ensure 创建 canonical 档案且参数为全 Automatic 默认、二次 ensure 同版本不重建、用户重命名后原样保留）；`cargo fmt --all -- --check` 通过；`npm run typecheck` 0 错误；全量 vitest **101 文件 657 项通过**（横幅测试扩为完整连续性：展开登录 → 真实 3 秒轮询完成 → `ensure_codex_official_record` 被调 → 聚焦后横幅消失；夹具补 official_login_start/poll 与 ensure 桩）。未提交或推送。

**扩展工作区对齐 cc-switch：写入即时执行（2026-09-12 完成）：** 用户指令「skill 与 mcp 界面模块简化流程，向 cc-switch 对齐 UIUX、默认显示与启用实现，无兼容无过渡无技术债重新一轮重构」。先通读 cc-switch（farion1231/cc-switch）源码：其 MCP/Skills 面板为统一平铺列表＋每客户端图标开关一键直写 live 配置（MCP 写 `~/.claude.json`/`config.toml`，Skills 经 SSOT 目录 symlink），新建默认启用全部客户端，仅删除/卸载保留一句话确认。本项目布局已对齐（单工具带＋ClientToggleGroup＋计数胶囊），差距在流程：每次开关都走 prepare→ExtensionPlanSheet→确认→apply 三步。按新契约重构：**`useExtensionApplies` 成为唯一扩展写入所有者**——prepare 与 apply 在同一互斥运行内背靠背执行（忙碌渲染前的同步操作锁保留），结果 toast 报告、事务历史可查可恢复；仅三类操作写入前停下确认：①计划任一目标 `writesSensitiveConnectionData`（后端 redact 判定，仅凭据值/带凭据地址写入触发）→ ExtensionPlanSheet（「确认<操作>（写入敏感数据）」+脱敏 diff+「确认写入」）；②删除仍有安装的定义 → 一句话确认「删除并撤销部署」，前端组合「同一管线批量移除绑定 + 删除已无绑定定义」（后端 `delete_definition_if_unbound` 契约不变）；③停用 Claude 项目共享 Skill → 范围单（「执行停用」选定后立即执行；`needsDisableScope` 收紧为 `sharedSettings == null` 才拦，杜绝范围确认死循环）。**默认部署对齐**：新建 MCP 表单默认勾选全部支持客户端、按钮改「保存并部署」、stdio 提供可选预设（fetch/memory/time）；来源浏览候选入库后立即部署到适用客户端（hostScoped 收窄）；本机发现「复制到扩展库」仍只入库（同客户端原生安装已在位，统一由「管理现有安装」接管，不制造冲突部署）。**旧路径全删**：`useExtensionPlans.ts`、`useExtensionLibrary` 的 preparePlan/applyPlan/prepareRestore、`useSkillLibrary`/`applyMcpEdit` 各自拼装计划的分支（改为返回 `needsDeploy` 由编辑器走统一管线）、`ExtensionRemoveSheet` 的「先去管理安装」引导（改一句话确认）、常驻预览弹窗与「生成停用预览」文案。Skill 更新（逐项/全部/编辑/版本恢复）、MCP 编辑、修复、历史恢复全部改走即时管线。e2e 改写：toggle 等待 aria-pressed 翻转或敏感确认单；外部冲突拒绝测试迁移到敏感确认等待窗口（唯一可插入窗口，同一执行器拒绝路径）；取消部署用例随取消能力一并移除。DESIGN.md 扩展契约（173 行总则＋详情删除/目标意图/即时执行管线/新建导入默认部署/更新/版本固定/历史恢复/一键修复条目）与 README 扩展三段同步改写，无旧文案残留。

验证：`npm run typecheck` 0 错误；扩展相关 11 个测试文件 58 项全部通过（改写：批量/混合/范围停用断言 prepare+apply 直达；敏感写入断言确认单出现且确认前不落盘；删除有绑定定义断言「移除绑定→apply→delete_extension」链；技能更新三项断言即时部署；MCP 编辑重命名断言即时重部署；修复断言无弹窗直达 apply+复扫）。全量 vitest 与桌面 e2e 未在本轮运行（e2e 需真实桌面环境）。未提交或推送。

**三页头部两行制收口（2026-09-12 完成）：** 用户指令按页给出行布局——扩展：标题独占顶行，第二行 Skills/MCP/全局指令页签＋从本机发现/发现/更多操作，第三行搜索＋客户端页签（数量标签并入选项内显示）；会话：标题独占顶行，第二行搜索＋客户端页签（新加 Codex/Claude 图标）＋刷新；用量：标题独占顶行，第二行分类页签；模块内模型消耗标题独占一行，第二行快照文本＋范围页签＋刷新。实现：新布局原语 `.asb-panel-subheading`（panels.css 唯一所有者）承载「标题行下的控件行」，三页共用；`ClientFilter` 增可选 `counts` 渲染选项内数量徽标（会话页同步开图标）；扩展数量胶囊行与「全部更新」按钮删除——批量部署开关与全部更新以菜单项并入「更多扩展操作」（deployment-state 计算不变，整库语义不变），页脚注脚同步指向新位置；孤儿样式 `.asb-ext-count-*` 全删（与详情弹窗 `asb-ext-clienttoggle` 共享的状态着色选择器保留改写）；`asb-ext-toolbar-actions`、会话页 `asb-panel-actions` 作用域规则随结构删除。DESIGN.md 头部契约（184/185）、扩展节（页签与入口/列表/目标与意图/更新）、用量节同步。

验证：`npm run typecheck` 0 错误；全量 vitest **101 文件 657 项全部通过**（6 个断言数量胶囊/全部更新按钮的测试改写为菜单项路径并保留原行为断言——批量启停含搜索外条目、mixed 状态、不支持客户端时禁用；ClientFilter 可及名含计数后相关查询同步）；浏览器实测三页截图符合规格。后端零改动。未提交或推送。

**供应商行按钮紧凑档（2026-09-12 完成）：** 用户指令「单供应商栏的按钮可以缩小一些」。启用主钮、重新登录次级钮与图标簇从标准档 40px 收敛到紧凑档 `--asb-control-height-compact`（32px），作用域 `.asb-row-line`（展开面板在行线之外，不受影响）；这是 §5 档位表「紧凑档：行内图标钮」既有契约的落地，不引入第四档高度。拖拽手柄（unstyled 无按钮几何）与头像不变。DESIGN.md 供应商列表动作簇条目补档位说明。

验证：全量 vitest **101 文件 657 项通过**（纯 CSS 改动）；浏览器样式级联实测——注入 `.asb-row-line` 临时元素读取计算样式，primary/secondary/icon 均为 32px。发现并修正一处作用域不一致（图标宽高初版误挂在 `.asb-row-item`，统一到 `.asb-row-line`）。未提交或推送。

**行内按钮间隔对齐参考设计（2026-09-12 完成）：** 用户指令「按钮间隔参考如图设计」（附 cc-switch 截图）。图标簇 `.asb-iconcluster` 的 `gap: 0`（按钮贴死）改为 `--asb-space-2`（12px）——与 `.asb-row-line` 既有行内间隔同值，形成启用钮—图标簇—图标间均匀的 12px 节奏；cc-switch 参考图中相邻图标中心距≈44px，对应 32px 紧凑钮＋12px 间隔。DESIGN.md 动作簇条目补间隔说明。

验证：浏览器样式级联实测 gap 12px、图标钮 32px；全量 vitest 657 项维持通过（纯 CSS 间隔改动）。未提交或推送。


**会话消息目录常驻（2026-09-12 完成）：** 用户指令「查看会话详细时，不要因为窗口过小导致消息目录隐藏起来，常驻实现」＋「无兼容无过渡无技术债实现」。根因：`.asb-session-toc` 默认 `display:none`、仅 `≥1280px` 显示，而主窗口 `minWidth: 940`、默认 1180px——默认尺寸下目录从不出现。终态：目录列成为详情正文常驻栏，**任何窗口宽度单一行为**——固定 248px（`--asb-session-toc-width` 具名声明，满足样式守卫）、无显示断点、无宽度分档（首版的 ≤1099px 收窄档已按无过渡指令删除）；`<900px` 堆叠模式同为可见，随详情页整体滚动。目录渲染条件维持「用户消息目录项 > 2」（内容条件，与窗口无关）不变；React 组件零改动，纯 CSS 收口。DESIGN.md 会话管理条目同步「常驻栏、列宽不随窗口分档」契约。

验证：`npm run typecheck` 通过；SessionManager 切片 17 项（含消息目录导航与 Codex 清洗用例）与样式守卫 `tokens.test.ts` 9 项（含裸 px 禁令）通过——jsdom 不应用媒体查询，宽度无关，旧测试即覆盖新行为的 DOM 结构。最小窗口 940px 下正文列 ≈320px，`pre` 带 `overflow-wrap: anywhere` 自动换行不横滚，经样式核对确认；未做浏览器级联实测（纯 CSS 声明改动，无运行时分支）。

**供应商行信息栏去点击化＋按钮承载选择（2026-09-12 完成）：** 用户指令「模型栏移除鼠标可点击的实现，只有按钮可点击，点击后模型栏仍然高亮」。行身份栏（头像/名称/模型与地址元信息）从可点击的 `role="option"` 按钮改为纯展示 `div`——不再有 hover 指针与选中语义；选择改由动作按钮承载：双端全部行内按钮（启用/预览/编辑/测试/用量/删除/重新登录/订阅额度）点击即选中本行，选中高亮（`is-selected` 安静状态面）持续保留。列表语义随之降档：`ul role="listbox"`→`role="list"`，行上 `aria-selected` 移除（选择成为表现态，由类承载）。`ProviderRowShell` 的 `onSelect` prop 删除——页面各自在按钮处理器中显式选中（Claude 走既有 `onSelect` 回调链，Codex 走本地 `selectedId`，官方行新增 `onSelect` prop）。附带语义：点击行内按钮会收回打开中的预览（与原行点击行为一致）。

验证：`npm run typecheck` 0 错误；全量 vitest **102 文件 665 项全部通过**——改写「行点击选中」为「信息栏惰性＋按钮选中」、aria-selected 断言改 is-selected 类断言、「动作不选中」契约反转为「动作即选中」（ProviderRequestPanel 的 select 断言同步）；浏览器开发数据受限，采用样式级联+测试验证。注意：工作区存在另一条并行工作线的未提交改动（扩展即时执行管线、WorkspaceHeader 等），本轮 4 个失败测试中 3 个为本变更所致并已修复，1 个（ProvidersPage 文档位置断言）在并行改动中途的运行中出现过、复跑稳定通过。未提交或推送。

**页头结构组件化收口（2026-09-12 完成）：** 用户指令按「标题独占第一行，导航/页面操作第二行，列表筛选第三行」标准全局审查并组件化。新增唯一结构所有者 `src/components/WorkspaceHeader.tsx`：`WorkspaceHeader`（顶层页面与独立子工作区，h2 页标题）与 `ModuleHeader`（嵌入模块，统一 h3 `.asb-section-title`）两个入口，固定三行语法（title 行仅返回钮＋标题；primary 行左导航/上下文＋右 `.asb-panel-actions` 操作；secondary 行局部视图控件），`primary` 缺失时 `secondary` 自动升为第二行不产生空工具行。接入：扩展（`ExtensionToolbar` 持有全部三行，`ExtensionLibraryPanel` 重复筛选行删除；搜索＋客户端过滤在窄宽度前的拉伸回归经浏览器实测修复——`.asb-header-secondary .asb-ext-toolbar` 补 `flex: 1 1 auto`）、会话（两行）、用量＋模型消耗模块（两行）、供应商（页头第二行＝客户端切换＋偏好设置/切换历史/导入/新建供应商，双连接卡移到页头之后，`asb-tabs-bar`/`asb-provider-toolbar` 删除）、诊断/备份/日志（设置内容区子页，h3 模块标题＋页签行/控件行）、导入供应商与用量查询（`WorkspaceHeader`，用量查询的供应商名作为 primary 行上下文与页签同行）、设置分类面板、配置状态、运行环境、本机协议网关、额度与重置、CC Switch/本机配置导入（`ModuleHeader`）。孤儿布局类与死规则全删（`.asb-ext-nav`、`.asb-sessions-toolbar`、`.asb-tabs-bar`、`.asb-provider-toolbar`、`.asb-model-usage-subheading`、gap 容器内 `.asb-panel-heading` 零边距规则、≤600px 会话死规则），响应式规则改挂新头部类。`.asb-panel-heading`/`.asb-panel-subheading` 成为组件内部行实现，供应商编辑器与行内变更预览沿用既有头部（编辑器 h2 持有焦点 ref 契约，变更预览按钮位于展开区头部为本节既有契约）。DESIGN.md 页面框架唯一契约（184）改为组件所有者表述，供应商（48/50/172/236/237）同步。

验证：`npm run typecheck` 0 错误；全量 vitest **102 文件 665 项全部通过**（新增 `WorkspaceHeader.test.tsx` 8 项锁定三行结构/h2-h3 层级/id 接线/空行抑制；`ProvidersPage` 导航测试随新页头序改断言「客户端选择器在双连接卡之前」）；浏览器 vite 实测 1280/900/640 三档——供应商页头一行放下客户端切换＋全部操作、640px 动作区右对齐换行，扩展页三行且 640px 入口簇落 `1fr 1fr` 网格，会话两行，用量与模型消耗各两行，日志控件行在 640px 自然换行（≤600px 纵向堆叠规则保留），设置内容区仅侧栏一个 h2。注：本条验证完成后，另一并行会话正在改写供应商行内切换预览流（ProvidersPage `onRequestSwitch` 契约、预览头按钮、CodexProvidersPage 行选择），其半程改动暂使工作区 typecheck 报 3 处其文件内错误、预览头相关 2 项测试未过——均不涉及本轮页头改动文件。未提交或推送。


**会话删除（2026-09-12 完成）：** 用户指令「无兼容无过渡无技术债实现会话的删除功能」，前置对比了 cc-switch 会话模块（其 `delete_session`/`delete_sessions` 为硬删除＋不可恢复确认单＋批量模式；`sourcePath` 进前端契约、后端只做根目录 containment 与 ID 复核；终端恢复接受 renderer 任意命令并注释为已接受风险）。本实现按 cc-switch 语义、按自身架构收口：**删除目标是（客户端, 会话 ID）**——后端 `resolve_session` 自扫描解析路径（路径不出 IPC 边界，解析天然含「文件在批准根目录内＋解析出的 ID 与请求一致」保障），随后硬删除记录文件（无回收站无备份，与切换执行器的配置写事务不同类：会话记录不属于档案存储，不进切换历史）；Claude 记录的同名附属目录随删（对齐 cc-switch，Codex 无此形态不处理）；`sessionDeleted` 记入运行日志（`observe` 包装，成功/失败各记），前端详情动作簇新增「删除会话」危险钮 → 共享 `ConfirmSheet`（标题/会话 ID/不可恢复警示）→ 成功后本地收口（列表剔除、选中与 30 秒转录缓存条目清除、toast 报告），失败保留一切并 toast 错误。批量删除未做：本会话列表无多选模型，单删是本次指令范围。命令 `delete_session`（`session-delete-failed` 错误码）经 dev 桥接同步暴露。DESIGN.md 会话管理行操作条目改写（原「不提供删除会话」契约废止）。e2e `sessions-usage-discovery` 新增删除用例（自建一次性会话文件，不触碰共享 fixture，末位执行不影响用量断言）。

验证：`cargo test --lib session_manager` 9 项通过（新增：删除文件＋Claude 附属目录随删＋无关兄弟文件保留、缺失记录报错）；`npm run typecheck` 通过；`SessionManager` 切片 20 项（新增：确认单流＋列表/缓存/选中收口、取消保留、失败保留并报错）与 `client.test` 30 项（受控命令断言含 `delete_session`）全部通过。两点如实说明：①本轮 cargo 曾因并行会话的 `cargo run` 持锁失败（os error 32），经用户指示终止该进程后重跑通过；②e2e 删除用例已编写并通过语法检查，但本轮未执行真实桌面 e2e（需打包应用与 WebDriver 环境）。

**扩展工作区对齐 cc-switch：数量条与列表结构复刻（2026-09-12 完成）：** 用户指令「skill、mcp 的 UI/UX/功能继续对齐，现在看着一点都不像 cc-switch 的复刻实现」。此前一轮把批量部署收进「更多」菜单、计数并入客户端过滤选项，恰好藏掉了 cc-switch 最具辨识度的元素。按 AppCountBar 复刻重建：**`ExtensionCountBar` 唯一所有者**——内容列自上而下固定为「数量条 → 搜索行 → 单容器紧凑列表」，与 cc-switch 面板同构。数量条左侧「Skills/MCP · N」总数字徽章，右侧每客户端一枚可点击计数胶囊（logo＋名称＋启用数，三态：未启用/部分虚线/全部品牌色底），胶囊即整库批量部署开关，单击经即时执行管线生效；Skills 可更新时末尾出现「全部更新（N）」。**移除客户端过滤**（cc-switch 无此控件；行内开关承载客户端状态、搜索负责查找），`ClientFilter` 的 counts 扩展与 `.asb-client-filter-count` 样式随之删除；「更多」菜单不再持有部署类动作。搜索行从页头副行移入内容列（`ExtensionSearch`，与发现弹窗共用）。行内开关启用态补齐 Claude 品牌色底（此前仅 Codex 有着色）。**NewMcpForm 功能对齐**：新增「填写方式」分段切换「结构化表单 / 粘贴 JSON」——粘贴模式接受整份 mcpServers 文档（首条目、键为服务名）、单个命名条目或裸服务对象，解析回填结构化字段后走同一保存与凭据路径，sse/ws 传输明确拒绝；预设从 3 个扩到 6 个（＋filesystem / sequential-thinking / context7）。DESIGN.md 扩展工作区「页签与入口/数量条与列表/目标与意图/新建与导入/更新」五条契约同步改写。

验证：`npx tsc --noEmit` 0 错误；扩展相关 8 文件 38 项（含新增 NewMcpForm.test 4 项：预设填充、mcpServers 文档解析回填、裸对象与 sse 拒绝、按客户端保存部署）通过；全量串行 vitest 102 文件 672 项全部通过。桌面 e2e 未运行（需真实桌面环境）。未提交或推送。

**Claude 功能对齐第一阶段（2026-09-12，整体目标未完成）：** 保留 Claude 页面布局、样式和现有控件，以及供应商参数 / 客户端偏好的可视化单一所有权。独立认证已贯通导入、编辑、切换、模型获取、请求测试、用量与网关；补齐 Fable / 子 agent / 显示名与 1M 模型数据并在切换时清理旧覆盖。补齐工具错误与图片结果、缓存标记、thinking / effort、Chat 流式空增量和并行工具，以及 Responses 原生推理的 JSON / SSE 两轮回放；本机信封不发给上游。现有网关重试可保留损坏状态诊断副本并重建状态，随后仍经原执行器重新应用。用量转换统一输入总量和缓存计数，不将缓存重复累计。

验证：工作区相关前端 12 文件 / 134 项通过。临时验证副本中 152 项 Rust 专项、4 项真实 Claude Code 2.1.259 本机上游测试通过（完整内置工具声明，只授权 Read，覆盖原生 / Chat / Responses、失败纠正与图片）。副本逐文件核对 Claude 涉及源码一致，仅隔离其他 Codex 任务留下的无实现 live 模块声明，不作为真实工作区整包构建通过证明。真实工作区目前因缺少 codex/live.rs 无法完整 Rust 构建，且扩展来源页接口尚未接齐导致全量 typecheck 失败。未提交、打包、发布或使用真实配置/凭据。完整功能清单和剩余验收见 docs/claude-ccswitch-parity.md；接管热切换、故障转移、托管认证、完整预设和请求历史等仍待实现，未缩减或标记整体完成。


**Claude 本地后端续作（2026-09-13，总体仍未完成）：** 保留既有 Claude 界面与可视化通用配置；新增业务放在独立的 Claude 账号、Prompt、预设、网关与请求模块。托管账号（Copilot、用于 Claude 的 ChatGPT OAuth、xAI OAuth）具备设备登录、取消、重登、默认/指定账号、刷新、模型和订阅查询；请求时凭据贯通已有模型获取/测试请求与网关，不写原生 Codex/Claude 登录缓存。修复取消后的迟到授权保存、刷新成功但保存失败时的新凭据丢失，以及准备后换默认账号的问题。

离线 90 个 Claude 预设保留功能数据与来源许可，87 个可准备为有效档案；Gemini Native 与两个 Bedrock 预设明确不可用。补齐模型查询地址、Haiku 1M 保真与克隆。Claude Prompt 库实现 CRUD/排序、预览/启停、备份、双修订校验、中断恢复及 CC Switch 只读导入，所有 CLAUDE.md 写入仍交给执行器。22 个新接口同时接通桌面命令与开发接口；页面入口尚未全部接齐，不能以接口存在宣称完整功能对齐。

验证：真实工作区 asb-core 327 项通过；asb-switch 全套 76 项通过；Claude 后端专项 122 项通过（默认跳过的 5 个 CLI 测试另行全部执行并通过）；前端相关 10 文件 145 项通过；typecheck、生产前端构建与 Rust 非测试构建检查通过。真实 CLI 用例使用固定 2.1.259、隔离 HOME/配置和仅本机上游，覆盖首轮、完整工具声明、错误纠正、图片与原生推理回放，并校验已知模型方言的 effort。旧首轮测试改用相同隔离启动器，修复 Anthropic 客户端 beta 查询提示被转发到 OpenAI 协议的问题；供应商自己配置的查询参数保留。

全库后端：905 通过 / 1 失败 / 14 默认跳过。失败为 Codex 原生接管恢复/改端口用例，恢复时遇到 API-key 登录模式并返回 codex-official-login-required；单独运行也失败，未改其认证/恢复业务或弱化断言。定向最终 diff/空白与代码长度检查完成，样式目录相对本轮起点无变化；没有使用真实配置/凭据做写入测试，没有提交或发布。完整剩余项见 docs/claude-ccswitch-parity.md，接口和文件所有权见 docs/claude-local-backend.md。
